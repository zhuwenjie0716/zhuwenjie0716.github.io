<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>数据检索与索引</title>
    <link href="/2026/06/12/%E6%95%B0%E6%8D%AE%E6%A3%80%E7%B4%A2%E4%B8%8E%E7%B4%A2%E5%BC%95/"/>
    <url>/2026/06/12/%E6%95%B0%E6%8D%AE%E6%A3%80%E7%B4%A2%E4%B8%8E%E7%B4%A2%E5%BC%95/</url>
    
    <content type="html"><![CDATA[<h1 id="数据检索与索引"><a href="#数据检索与索引" class="headerlink" title="数据检索与索引"></a>数据检索与索引</h1><blockquote><p>在最基础的层面上，数据库需要做两件事：当你给它一些数据时，它应该存储这些数据；当你之后再询问时，它应该把数据返回给你。</p></blockquote><p>如果真的要说数据的存储与检索，就不得不提一下磁盘的结构，磁盘有柱面、盘面、扇区的概念，用（柱面号、盘面号、扇区号）可以定位任意一个 “磁盘块”，读取磁盘块需要移动磁头和旋转扇区，所以一次查询读取磁盘块的次数（IO次数）对性能有很大影响。</p><p>假设一个磁盘块 512B、一条记录是 128B（唯一标识占 8B），那么 100 条记录需要放在 25 个磁盘块上，访问一条记录平均需要读取 12 个磁盘块。这样的性能实在是太差了，那么有什么办法可以优化呢？</p><p>案例提到每条记录的唯一标识占 8B，假如我们可以用 8B 标识一个物理地址（例如柱面+盘面+扇区+偏移量），那么 16B 可以标识一条记录对应的位置，一个磁盘块可以放 32 个这样的结构，那么 100 条记录需要 4 个磁盘块来存放。此时访问数据我们可以读取这4个磁盘块，进而定位出记录的具体位置，也就是平均的 2 次索引 IO + 1 次的数据 IO。通过多使用一部分空间，访问数据的速率提高了数倍，另外一般来讲这个标识是按照顺序排列的，这样可以支持范围查询，也可以快速定位数据是否存在（否则需要访问全部的磁盘块才知道数据是否存在）。</p><p>索引可以用来快速定位数据库中的数据，多叉树和哈希是实现索引的常见方案。</p><h2 id="索引的实现选型"><a href="#索引的实现选型" class="headerlink" title="索引的实现选型"></a>索引的实现选型</h2><p><a href="https://zhuwenjie0716.github.io/2026/05/08/Redis%E9%95%BF%E4%BB%80%E4%B9%88%E6%A0%B7/">Redis</a> 就是利用哈希来实现索引，快速地定位到 key 所在的位置。Redis 中定义了一个数组，当 key 到来时对 key 执行哈希函数，并对数组长度取余（取余实现上是位运算）得到 index，接着在数组[index] 的位置查看是否有当前 key，解决哈希冲突采用的是链表法。哈希索引实现简单，对于单个 key 的查询，能以 O(1) 的速度找到对应的数据。</p><p>数据结构布隆过滤器也是通过哈希来实现的，它的工作原理是这样的：有一个比较长的 bit 数组 bits，当 key 来时，对 key 计算多个哈希值，假设哈希值对数组长度取余后是（0,9,1），如果 bit[0]、bit[9]、bit[1] 的值均为 1，那么该 key 就可能存在；反之只要这三个元素的值有一个是 0，这个 key 就肯定不存在。布隆过滤器常用来缓解缓存穿透的问题。</p><p>哈希索引的缺点就是不支持范围查询和模糊查询，Mysql 数据库采用树结构来实现索引，树有非常多的形态，搜索树、平衡树、红黑树、B树、B+树…….</p><p>一般的二叉搜索树的建立，以首节点为根节点，后续的数据比当前数据小，则成为当前节点的左子树，否则成为右子树，对于序列（34，22，89，5，23，77，91）来说，创建出的二分查找树如下图：</p><p><img src="/img/image-20251030161952133.png" alt="image-20251030161952133"></p><p>但是当数据比较极端，例如持续递增的一组序列，生成的二叉搜索树则会退化为链表，如下图：</p><p><img src="/img/image-20251030162102480.png" alt="image-20251030162102480"></p><p>为了避免极端数据情形导致查询恶化的情况，人们提出来<strong>平衡二叉搜索树（AVL树），它增加了每个节点左右子树高度差不能超过1的约束</strong>。</p><p>在平衡二叉搜索树中查找一个数据的时间复杂度近似为 O(log2n)，在数据量比较大的时候，由于每个节点只有2个子树，因此层高和查询耗时也会显著增加。</p><p>随后人们又发现如果不用二叉树，而是平衡 M（M&gt;2）叉树的情况下，以上问题可以得到较好的解决，M 的值在 Mysql 中约为 1200，当层高是 4 的时候最多已经可容纳 17 亿节点了。</p><h2 id="B-树与-B-树"><a href="#B-树与-B-树" class="headerlink" title="B 树与 B+ 树"></a>B 树与 B+ 树</h2><p>B树的英文是 Balance Tree，也就是平衡的多路搜索树，用来实现文件系统和某些数据库系统的索引，其结构如下图所示：</p><p><img src="/img/image-20251030163135258.png" alt="image-20251030163135258"></p><p>有个需要注意的地方：</p><ul><li>非叶子节点出现的关键字在子节点中一般不再出现（如果有多条关键字相同的数据，当然叶子节点上还可能会出现）。</li></ul><p>这也是 B 树与 B+ 树的重要区别之一，由于这样的实现，B 树的节点除了包含关键字，还包含关键字对应的行数据（在 B+ 树中，如果是主键索引，主键和行数据待在一起，如果是非主键索引，那么数据就是主键ID）。</p><p>B 树其实对于单个查询和范围查找已经支持的比较好了，不过单个查询的耗时不稳定，如果这个关键字在上层，耗时就短；如果随着时间推移关键字移动到了下层，查询时间就长；另外范围查找需要对树进行中序遍历，会根据上层节点去查询右子树。</p><p>B+ 树针对上述场景做了优化，B+ 树把所有的数据都存放在了叶子节点，非叶子节点只存关键字，而且这个关键字一定是所指向子节点的最小值或最大值；B+ 树上所有的叶子节点间使用双向指针连接，在范围查找时可以从任意节点开始往 前&#x2F;后 遍历。其结构如下图所示：</p><p><img src="/img/image-20251030164045360.png" alt="image-20251030164045360"></p><p>由于非叶子节点只存储关键字而不存储数据，因此 B+ 树比 B 树的非叶子节点能存下更多的关键字，同时由于数据都在叶子节点上，因此查询效率比较稳定。</p><p>如果要查询的字段不在非主键索引中，即使查询使用该索引，也需要在查到对应的主键后走主键索引拿到查询的字段值，这也被称为<code>回表</code>，有的时候走非主键索引查询速度比主键索引还快，原因可能是查询的字段刚好就是这个索引字段，此时不用回表。</p><h2 id="B-树索引上的节点到底是什么"><a href="#B-树索引上的节点到底是什么" class="headerlink" title="B+ 树索引上的节点到底是什么"></a>B+ 树索引上的节点到底是什么</h2><p>数据库中的数据是一行一行的，但是数据库的读取是按页为单位的，否则每次 I&#x2F;O 操作指只能处理一行数据，效率太低了。在数据库中，<strong>无论是操作一行还是多行，都是将这些行所在的页进行加载。也就是说，数据库管理存储空间的基本单位是页。</strong></p><p>一个 B+ 树的基本结构如下图所示，其实其中的每一个叶节点就是一个数据页。</p><p><img src="/img/image-20251030165709587.png" alt="image-20251030165709587"></p><p>数据页的大小可以使用如下命令查找，Mysql 中默认 16KB</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">show</span> variables <span class="hljs-keyword">like</span> <span class="hljs-string">&#x27;%innodb_page_size%&#x27;</span>;<br></code></pre></td></tr></table></figure><p>16KB 能存储上千条数据，如果仅依靠数据之间的链表指针查询仍然需要O(N)的复杂度，Mysql 在数据页的设计中利用槽(slot)进一步优化了这个过程。</p><p>Mysql 的页结构如下图所示，其中用户记录部分占了页结构的主要空间。</p><p><img src="/img/image-20251030170258574.png" alt="image-20251030170258574"></p><p>文件头中会有两个指针 FIL_PAGE_PREV 和 FIL_PAGE_NEXT 用来指向上一个数据页和下一个数据页，链表的结构使得数据页之间不需要是物理上的连续。</p><p>数据页中的页目录部分，就是业内记录的索引，流程如下：</p><ol><li>将所有的记录分成几个组，这些记录包括最小记录和最大记录，但不包括标记为“已删除”的记录。</li><li>第 1 组，也就是最小记录所在的分组只有 1 个记录；最后一组，就是最大记录所在的分组，会有 1-8 条记录；其余的组记录数量在 4-8 条之间。这样做的好处是，除了第 1 组（最小记录所在组）以外，其余组的记录数会尽量平分。</li><li>在每个组中最后一条记录的头信息中会存储该组一共有多少条记录，作为 n_owned 字段。</li><li>页目录用来存储每组最后一条记录的地址偏移量，这些地址偏移量会按照先后顺序存储起来，每组的地址偏移量也被称之为槽（slot），每个槽相当于指针指向了不同组的最后一个记录。如下图所示：</li></ol><p><img src="/img/image-20251030170634890.png" alt="image-20251030170634890"></p><p>所以 Mysql 查询的全流程是：从 B+ 树的根开始，逐层检索，直到找到对应的叶子结点（即数据页），将数据页加载到内存，再借助页目录内的槽（slot）进行二分查找到一个记录分组，最后在分组中链表遍历查找记录。</p>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Mysql</tag>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Mysql的日志体系</title>
    <link href="/2026/06/11/Mysql%E7%9A%84%E6%97%A5%E5%BF%97%E4%BD%93%E7%B3%BB/"/>
    <url>/2026/06/11/Mysql%E7%9A%84%E6%97%A5%E5%BF%97%E4%BD%93%E7%B3%BB/</url>
    
    <content type="html"><![CDATA[<h1 id="Mysql的日志体系"><a href="#Mysql的日志体系" class="headerlink" title="Mysql的日志体系"></a>Mysql的日志体系</h1><p>提起 redo log、binlog、undo log，使用 Mysql 的人一般都能说上一些内容，诸如：两阶段提交，事务的隔离性，但可能会对 WAL（Write-Ahead Log 预写日志） 本身有些陌生。</p><p>Mysql 中的索引数据（主键索引包含数据）按照 B+ 树存储，当一个更新的 sql 语句需要改动数十万行数据，不妨思考一下该如何实现？</p><p>最简单的方案是：首先根据更新语句的索引找到需要更新的条目，更新磁盘上对应的数据，更新完成后 sql 返回执行成功的结果。</p><p>那这个方案有什么问题：</p><ul><li>如果在更新数据到一半时机器突然崩溃，那么数据库的数据将可能呈现不一致的状态（有些数据更新了，有些没有更新）；</li><li>应用服务等待数据库更新所有数据后返回执行结果，性能会很差。</li></ul><p>WAL 一方面可以提高性能，另一方面也提高了数据库的可靠性。</p><h2 id="write-ahead-log"><a href="#write-ahead-log" class="headerlink" title="write-ahead log"></a>write-ahead log</h2><p>WAL 预写日志的思想是：在保存修改数据前先记录日志，一方面这样的日志可以循环顺序写，相比多次的随机 IO 会有显著的性能提升（尤其是在机械硬盘上），另一方面请求可以快速返回，因为只需要记录日志并刷新到磁盘即可，数据可以还未真正更新到磁盘。</p><p>举个例子</p><p>这是一个用户表（id、name、email）的 B 树结构：</p><p><img src="/img/image-20260610172341503.png" alt="image-20260610172341503"></p><p>在初始时所有的数据都没有修改过，write-ahead log 为空，此时来了一个更新请求：<code>update email = d2@l.e where id = 38</code>，通过索引很快定位到了数据所在的页，对数据进行修改：</p><p><img src="/img/image-20260610172609744.png" alt="image-20260610172609744"></p><p>同时在 write-ahead log 记录能标识本次修改的信息（下方只是示例，真实情况不是这么记录的），例如：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;op&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;update&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;id&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;38&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;changed&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;email -&gt; d2.@le&quot;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><p><strong>提高性能</strong>：该请求修改了内存中的数据（具体 IO 次数跟涉及到修改的索引数，数据是否本来就在内存中都有关系），并在 write-ahead log 上追加了更新信息，很快就能执行完成并返回。</p><p><strong>崩溃恢复</strong>：也叫 <strong>crash-safe</strong>。当数据库在更新内存时崩溃，由于没有刷入磁盘，所以没有问题；当数据库更新完内存，记录了 wal 后崩溃，重启时数据库可以检查 wal，判断数据是否已成功修改，进而执行操作保证数据的一致性。</p><p>如果 wal 是一个单纯的只追加的日志文件，那么它会无限膨胀。事实上当脏页从内存刷新到磁盘后，相关的内容已经可以从 wal 中移除了，所以 wal 通常被实现为一个循环写的文件，并有<strong>检查点（check point）</strong>的概念，写指针到检查点的位置即为可以复用的空间。如果某次更新 wal 剩余空间不够了，则会推动 check point 的前进。</p><h2 id="Mysql-中的-update-执行流程"><a href="#Mysql-中的-update-执行流程" class="headerlink" title="Mysql 中的 update 执行流程"></a>Mysql 中的 update 执行流程</h2><p>首先，数据库很少有直接操作磁盘的情况，都会先把数据读到内存，接着更新内存中的数据，再根据情况选择写回磁盘，这可以平衡 cpu 与磁盘间的处理速度，所以影响数据库性能的一个因素有缓冲池的大小。</p><p>那么更新语句在 Mysql 中的执行流程（权限验证、词法、语法分析、生成执行计划等前置步骤）是什么呢？</p><ol><li>将磁盘中的对应的数据读入到内存缓冲区中；</li><li>记录旧值对应的 undo-log；</li><li>更新内存缓存区中的数据为新值；</li><li>写入 redo-log（数据中包括 XID），此时为 prepare 状态；</li><li>写 binlog（数据中包括同一个 XID，为了对应）；</li><li>事务提交，追加状态为 commit 的 redo-log；</li></ol><p>上述的描述中，数据还在内存中，只是被修改了，这时的数据页被称为<strong>脏页</strong>，它会在后台以某种频率或由于内存需要被其它页面替换而写回到磁盘。</p><p>这个流程里有两个很容易产生疑问的问题： 首先看下二者的区别：</p><ul><li>redo-log 是 Innodb 引擎独有的，binlog 是 MySQL 的 Server 层实现的，所有引擎都可以使用；</li><li>二者记录的内容也不相同，redo-log 记录了在某个数据页上做了什么修改，而 binlog 可以通过调整格式记录不同形式的内容（例如记录原始的 sql 语句）；</li><li>redo log 是循环写的，空间固定；binlog 写到一定大小后会切换到下一个，并不会覆盖以前的日志。</li></ul><p>所以 redo-log 才是后来者，<strong>redo-log 的出现提供了 crash-safe 的能力</strong>。</p><p>为什么还要有 binlog？</p><p>一方面 redo-log 是 Innodb 引擎独有的，而 Mysql 是可以选择其它引擎的；另一方面 redo-log 是近似扇形缓冲区的结构，不保留之前的历史记录，没有归档的功能。此外，Mysql 的主从同步集群也是可以用主库的 binlog 来做同步的。</p><p>为什么要采用两阶段提交？</p><p>其实分布式事务中也有两阶段提交的思想。因为一次事务既需要写 redo-log，又需要些 binlog，为了保证二者状态的一致性因此采用了两阶段提交，即先写 redo-log 并置为 prepare 状态，再写 binlog，再修改 redo-log 为 commit 状态。</p><p>以把 “c 从 0 改为 1” 为例，如果不采用该方案：</p><ul><li>先写 binlog 再写 redo-log：如果在 binlog 写完之后 crash，由于 redo log 还没写，崩溃恢复以后这个事务无效，所以这一行 c 的值是 0。但是 binlog 里面已经记录了“把 c 从 0 改成 1”这个日志，假如拿 binlog 做了主从同步、亦或者 canal 订阅 binlog 会导致数据不一致或引发错误逻辑，利用 binlog 恢复数据时也会出问题；</li><li>先写 redo-log 再写 binlog：如果 redo-log 写成功之后 crash，那么崩溃恢复时 redo-log 可以恢复内存中的数据是 1，但 binlog 中没有这个日志，主从同步或利用 binlog 恢复数据时也会有问题（还是原始值0）。</li></ul><p>如果在 redo-log 为 prepare，binlog 成功记录后 crash，重启恢复时 Mysql 检测到数据完整会提交这个事务。</p><p>上面多次提到了利用 binlog 恢复数据，binlog 记录的日志格式有三种：</p><ul><li><strong>STATEMENT</strong> 格式，记录 sql 语句，但 sql 中可能包含 Now()、Random() 等字符，不安全；</li><li><strong>ROW</strong> 格式，记录数据行变更前后的值，最精准，但日志量会很大，更新多少记录就会有多少相关的日志；</li><li><strong>MIXED 格式</strong>：混合模式，MySQL 会判断 sql 是否安全选择使用 STATEMENT 还是 ROW 格式，但判断未必完全正确。</li></ul><p><strong>从 MySQL 8.0 开始，默认的 binlog 格式已从 STATEMENT 改为 ROW</strong>，毕竟大多数情况下，数据的一致性是更加重要的。</p><h2 id="undo-log-与事务"><a href="#undo-log-与事务" class="headerlink" title="undo-log 与事务"></a>undo-log 与事务</h2><p>update 语句也涉及到了 undo-log，在读已提交的隔离级别下，A 事务修改完成还未提交的时候，B 事务是读不到 A 事务修改的值的，这就利用了 undo-log。</p><p>事务具有四个特性：ACID（Atomicity、Consistency、Isolation、Durability，即原子性、一致性、隔离性、持久性）。</p><p>同时数据库有四个隔离级别：读未提交（Read UnCommited）、读已提交（Read Commited）、可重复读（Repeatable Read）和串行化（Serializability）。</p><p><img src="/img/image-20251029151605166.png" alt="image-20251029151605166"></p><p>广义的不可重复读指的是两次相同查询读取的数据不一样，这个不一样既包括数据值不一样，也包括数据量不一样。在这里，不可重复读仅指数据值不一样，即A事务开始时读取的值，即使B事务修改并且成功提交了，A事务再读还是原始值。幻读则指的是数据量不一样。</p><p>注意：【可重复读】的隔离级别在某些条件下是可以解决幻读的，涉及到 Mysql 中的锁机制~</p><p>以下图为案例分析四种隔离级别下出现的情况：</p><p><img src="/img/image-20251029152009184.png" alt="image-20251029152009184"></p><ul><li>读未提交：A 事务中的 V1、V2、V3 分别是 2、2、 2，原因是该级别下其他事务只要修改即使不提交也能被当前事务看到；</li><li>读已提交：A 事务中的 V1、V2、V3 分别是 1、2、 2，原因是在查询得到 V2 时事务 B 已经提交；</li><li>可重复读：A 事务中的 V1、V2、V3 分别是 1、1、 2，原因是事务 A 在查询 V2 时值虽然已经被事务 B 修改，但根据该级别的规则（规则见下文），还是得到 1；</li><li>串行化：事务 B 执行“将 1 改成 2”的时候，会被锁住。直到事务 A 提交后，事务 B 才可以继续执行。所以从 A 的角度看， V1、V2 值是 1，V3 的值是 2。</li></ul><h3 id="如何实现事务隔离"><a href="#如何实现事务隔离" class="headerlink" title="如何实现事务隔离"></a>如何实现事务隔离</h3><p>可重复读也被叫做快照读，含义是事务开始的一瞬间就像生成了一个快照一样，此后的操作都是基于这个快照，因此得到的结果一致；但是随便想想也知道，如果事务真的是在开始时对数据库内容生成了快照文件，那么磁盘的使用不可计量。</p><p>Mysql 是基于 MVCC （Multiversion Concurrency Control，多版本并发控制）和 undo log。</p><p>简单来说数据库中的一条记录是有多个版本的，而且每一条记录都有以下隐藏字段</p><ul><li>db_row_id 隐藏的行 ID，用来生成默认聚集索引。如果我们创建数据表的时候没有指定聚集索引，这时 InnoDB 就会用这个隐藏 ID 来创建聚集索引。</li><li>db_trx_id 修改记录为当前版本的事务ID，所谓的事务ID是每次进行事务操作时生成的，唯一且递增。</li><li>db_roll_ptr 回滚指针，执行该条记录对应的 undo log 信息。</li></ul><p><img src="/img/image-20251029153240053.png" alt="image-20251029153240053"></p><p>我们可以认为数据库在进行事务操作的开始生成了一个快照（视图，ReadView），里面有以下几个重要的属性：</p><ul><li>trx_ids，系统当前正在活跃的事务 ID 集合（未提交事务ID集合）。</li><li>low_limit_id，活跃的事务中最大的事务 ID，即下图的高水位。</li><li>up_limit_id，活跃的事务中最小的事务 ID，即下图的低水位。</li><li>creator_trx_id，创建这个 Read View 的事务 ID。</li></ul><p><img src="/img/image-20251029153503395.png" alt="image-20251029153503395"></p><p>现在当前事务想要进行操作了，这时候它扫描到一条记录，这条记录是否应该展示呢？应该遵循以下的判断规则：</p><ol><li>如果落在绿色部分，表示这个版本是已提交的事务或者是当前事务自己生成的，这个数据是可见的；</li><li>如果落在红色部分，表示这个版本是由将来启动的事务生成的，是肯定不可见的；</li><li>如果落在黄色部分，那就包括两种情况：<br>a. 若 row_trx_id 在数组中，表示这个版本是由还没提交的事务生成的，不可见；<br>b. 若 row_trx_id 不在数组中，表示这个版本是已经提交了的事务生成的，可见。</li></ol><p>不过虽然判断某记录的当前版本对于当前事务不可见，还是需要回滚指针结合 undo log 判断历史版本是否可见。</p><p>那么读已提交和可重复读的区别是什么呢？</p><p><strong>读已提交的 ReadView 是每次执行一条 SQL 时就生成，而可重复读是在事务一开始就生成，事务期间不变。</strong></p><p>最后整理几个与本篇内容相关的配置项，可通过 <code>show variables like &#39;[配置项]&#39;</code> 查看：</p><ul><li>innodb_buffer_pool_size：设置 innodb buffer pool 的大小，也就是文中提到的缓冲池大小；</li><li>innodb_flush_log_at_trx_commit：配置 redo-log 日志的持久化频率，设置为 1 每次事务的 redo-log 都会落盘；</li><li>sync_binlog：配置 binlog 日志的持久化频率，设置为 1 每次事务的 binlog 都会落盘；</li></ul><p>后两个配置项都推荐设置为 1，这可以提高数据库的数据一致性，虽然说是每次事务都会落盘，但会采用组提交的机制，多个事务的 redo-log 一起刷盘，以减少磁盘 IO 操作，提高吞吐量。</p><p>其实与缓冲池相关的配置有很多，可以通过<code>show variables like &#39;innodb_buffer_pool%&#39;</code>查看，分享一个看到过的调优案例：<a href="https://www.cnblogs.com/innocenter/p/14948857.html">Mysql 优化之 innodb_buffer_pool_size 篇 </a>。</p>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Mysql</tag>
      
      <tag>数据库</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>坎坷的 PaddlePaddle 旧版本安装</title>
    <link href="/2026/06/10/%E5%9D%8E%E5%9D%B7%E7%9A%84%20PaddlePaddle%20%E6%97%A7%E7%89%88%E6%9C%AC%E5%AE%9E%E8%B7%B5/"/>
    <url>/2026/06/10/%E5%9D%8E%E5%9D%B7%E7%9A%84%20PaddlePaddle%20%E6%97%A7%E7%89%88%E6%9C%AC%E5%AE%9E%E8%B7%B5/</url>
    
    <content type="html"><![CDATA[<h1 id="坎坷的-PaddlePaddle-旧版本安装"><a href="#坎坷的-PaddlePaddle-旧版本安装" class="headerlink" title="坎坷的 PaddlePaddle 旧版本安装"></a>坎坷的 PaddlePaddle 旧版本安装</h1><p>近期接手了一个老项目，Python3.7、PaddlePaddle2.4.2、Paddlets1.1.0。</p><p>Python3.7 姑且不提，<a href="https://github.com/PaddlePaddle/PaddleTS/releases">Paddlets1.1.0</a> 的最新版本是 2022 年底的，距今已经 3.5 年没更新了；PaddlePaddle2.4.2 常见的镜像源如: <a href="https://mirrors.aliyun.com/pypi/simple/paddlepaddle/">阿里云镜像源</a>、<a href="https://pypi.tuna.tsinghua.edu.cn/simple">清华镜像源</a> 均已无对应版本，<a href="https://www.paddlepaddle.org.cn/install/old#old-version-anchor-29-%E7%8E%AF%E5%A2%83%E6%94%AF%E6%8C%81">Paddle官网</a> 提供的安装方法 <code>python -m pip install paddlepaddle==2.4.2 -i https://pypi.tuna.tsinghua.edu.cn/simple</code> 实测发现也已无对应版本，所以都会安装失败。<a href="https://mirror.baidu.com/pypi/simple">百度镜像源</a> 更是访问就报 403。</p><h2 id="虚拟环境"><a href="#虚拟环境" class="headerlink" title="虚拟环境"></a>虚拟环境</h2><p>第一次正式接手生产环境的 Python 项目，所以还是先补充下基本知识。</p><p>如果接触过 Docker 之类的容器技术，对虚拟环境肯定不陌生，简单来说就是为了避免资源（例如依赖的包，某项目依赖 1.1 版本，其他项目依赖 1.3 版本）冲突，通过某种技术对项目运行环境进行隔离，每一个项目运行的环境就是一个虚拟环境。Docker 采用的是逻辑虚拟化，而 Vmware 之类的虚拟机是真正的虚拟化技术。</p><p>Pyhon 中常用的虚拟环境有 venv 和 conda。</p><h3 id="venv"><a href="#venv" class="headerlink" title="venv"></a>venv</h3><p>venv 在使用 pycharm 时很常用：</p><p><img src="/img/image-20260610113637840.png" alt="image-20260610113637840"></p><p>同时如果在项目中打开终端（Terminal），在前面会有类似 (.venv) 的字样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">(.venv) &gt;<br></code></pre></td></tr></table></figure><p>venv 需要一个真实的 python 解释器，解释一下就是 venv 中虽然也有 python 可执行文件，但这个可执行文件链接到了物理机上一个真实的解释器，就像应用程序的快捷方式一样，虽然在桌面也能打开，但删除了快捷方式不会影响真实文件，创建快捷方式的前提也是真实文件存在。假如我想创建 python3.7 的环境那么我就真的电脑上要存在一个 python3.7 的可执行文件（用可执行文件而不是 exe 的原因是，linux、macos 上不是 exe）。</p><h3 id="Conda"><a href="#Conda" class="headerlink" title="Conda"></a>Conda</h3><p>国内镜像下载地址：<a href="https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/">清华大学 minniconda 下载源</a></p><p>根据自己的环境选择合适的安装包，安装完成后在终端输入 <code>conda env list </code> 查看环境列表，如果提示 conda 命令找不到，需要把 condabin 目录添加到环境变量里，添加环境变量就是为了在任意位置都能够找到对应的可执行文件。</p><p>conda 像 python 的 pip 一样，也可以配置镜像源，对应的配置文件为用户目录下（windows: C盘当前用户目录，linux&#x2F;mac 是 ~&#x2F;）的 .condarc：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs bash">show_channel_urls: <span class="hljs-literal">true</span><br>channel_priority: flexible<br>channels:<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/Paddle/<br>default_channels:<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/<br>  - https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/Paddle/<br>custom_channels:<br>  conda-forge: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  msys2: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  bioconda: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  menpo: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  pytorch: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  pytorch-lts: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  simpleitk: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br>  deepmodeling: https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud<br></code></pre></td></tr></table></figure><p><strong>常用指令</strong></p><p>创建新的虚拟环境</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda create -n [env_name] python=[x.x]<br></code></pre></td></tr></table></figure><p>例如我想创建一个 python3.7 的名字为 python_3.7 的虚拟环境: <code>conda create -n python_3.7 python=3.7</code> </p><p>删除虚拟环境</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda remove -n [env_name] --all<br></code></pre></td></tr></table></figure><p>激活虚拟环境</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda activate [env_name]<br></code></pre></td></tr></table></figure><p>当激活虚拟环境后，终端会在最前方展示虚拟环境的名字，提醒用户此时所处的环境：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ conda activate python_3.7<br><br>$ (python_3.7) &gt;<br></code></pre></td></tr></table></figure><p>退出虚拟环境</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda activate [env_name]<br></code></pre></td></tr></table></figure><p>如果在虚拟环境外想要操作某个虚拟环境内的包，可以使用 -n 指定虚拟环境。</p><p>查看指定虚拟环境下已安装的包</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda list -n [env_name]<br></code></pre></td></tr></table></figure><p>指定虚拟环境安装某个包</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda install -n [env_name] [package]<br></code></pre></td></tr></table></figure><p>删除指定虚拟环境下某个包</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda remove -n [env_name] [package]<br></code></pre></td></tr></table></figure><p>更新指定虚拟环境下某个包</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda update -n [env_name] [package]<br></code></pre></td></tr></table></figure><p>如果已经执行了 conda activate 激活了某个虚拟环境，上述指令去掉 -n 即可，例如根据 requirement.txt 安装包：<code>conda install --file requirements.txt </code>。</p><p>离线安装也很常用，一个原因是并非所有的包都能在所配置的镜像源中找到，另一个原因是在内网环境中很多时候配置私有仓库的成本会比较大。</p><p>conda 离线安装的文件格式为 .tar.bz2，指令为：<code>conda install filename.tar.bz2</code>。但请注意，离线安装时 conda 不会处理依赖问题，如果安装的包无法工作，可能缺少需要手动解决的依赖项。就像 paddlepaddle 依赖 paddle_bfloat，离线安装时要先按照 paddle_bfloat 再安装 paddlepaddle。</p><p>有的时候即使配置了镜像源，conda install 依然很慢，原因是在下载依赖项时 conda 需要识别依赖冲突，解析环境信息，Mamba 是兼容 Conda 的更快替代工具，特别适合处理复杂依赖：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda install mamba<br></code></pre></td></tr></table></figure><p>在虚拟环境安装完成后，可以使用 mamba 来安装包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">mamba install [package]<br></code></pre></td></tr></table></figure><h2 id="安装-PaddlePaddle"><a href="#安装-PaddlePaddle" class="headerlink" title="安装 PaddlePaddle"></a>安装 PaddlePaddle</h2><p>回到正题，那么怎么安装 PaddlePaddle2.4.2 呢？以下为 windows 安装 PaddlePaddle 的解决方案：</p><p>注意安装时使用的 pip 环境为当前所在的 venv 环境或 conda 环境！</p><p><strong>venv环境</strong></p><ol><li>安装 paddle_bfloat, PaddlePaddle 依赖这个,一定要先装这个<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pip install paddle_bfloat-0.1.7-cp37-cp37m-win_amd64.whl<br></code></pre></td></tr></table></figure></li><li>安装 PaddlePaddle<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pip install paddlepaddle-2.4.2-cp37-cp37m-win_amd64.whl<br></code></pre></td></tr></table></figure><strong>conda环境</strong></li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">conda install paddlepaddle-2.4.2-py37_cpu_windows.tar.bz2<br></code></pre></td></tr></table></figure><p>其实就是去网上找对应的安装包进行离线安装，主流的镜像网站均已很难找到对应安装包。</p><p>conda 环境的可以尝试在这里找：<a href="https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/Paddle/">https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/Paddle/</a></p><p>venv 环境的可以尝试在这里找：</p><p><a href="https://dashboard.stablebuild.com/pypi-deleted-packages/pkg/paddlepaddle/2.4.2">https://dashboard.stablebuild.com/pypi-deleted-packages/pkg/paddlepaddle/2.4.2</a></p><p><a href="https://pypi.org/project/paddle-bfloat/#files">https://pypi.org/project/paddle-bfloat/#files</a></p><p>Macos、Linux 同理。</p>]]></content>
    
    
    <categories>
      
      <category>Agent与模型</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Python</tag>
      
      <tag>PaddlePaddle</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之QuickList篇</title>
    <link href="/2026/05/28/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8BQuickList%E7%AF%87/"/>
    <url>/2026/05/28/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8BQuickList%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之QuickList篇"><a href="#Redis源码分析之QuickList篇" class="headerlink" title="Redis源码分析之QuickList篇"></a>Redis源码分析之QuickList篇</h1><p>quicklist 是 Redis 对外暴露的 list 数据结构的内部实现，经常被当作队列或栈使用，我们可以从常用的一些 api 上先思考一下它的结构</p><p>最常用的就是 lpush、lpop、rpush、rpop，同时它也支持 lindex 查询某元素在 list 中的索引，linsert 在指定元素旁边插入新元素。</p><p>从头、尾节点的 push、pop 来看，这就是双向链表最优秀的设计，提供头尾节点的 O(1) 复杂度操作，但普通的链表每个节点都需要一个前驱和后向指针，空间利用率低；且在链表元素过多时查询缓慢，ziplist 让每一个 entry 都存了上一个 entry 和自己的长度，用来实现双向遍历，节省了一些空间，但缺点是增加、修改元素时可能会带来更大的内存拷贝。</p><p>有没有办法进行一个时间和空间的权衡呢？quicklist 就是这样设计的：</p><p><code>A doubly linked list of ziplists</code> 翻译为一个 ziplist 的双向链表。</p><p>那么每个 ziplist 存多少数据合适呢？比如我有 20 个元素，可以 4 个 ziplist，每个 ziplist 里 5 个元素；也可以 5 个 ziplist，每个 ziplist 里 4 个元素：</p><ul><li>ziplist 的元素越多，进行操作时候需要内存拷贝的空间就越多，如果整个 quicklist 只有一个 ziplist 节点，那么其实就退化成了一个 ziplist 了；</li><li>ziplist 的元素越少，就需要更多的节点，空间利用率低，更容易产生内存碎片，如果每个 ziplist 只有一个元素，quicklist 就退化成一个普通的双向链表了。</li></ul><p>redis 提供了一个这样的配置项：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-built_in">list</span>-max-ziplist-size <span class="hljs-comment">// 默认-2</span><br></code></pre></td></tr></table></figure><p>当取正值的时候，表示按照数据项个数来限定每个 quicklist 节点上的 ziplist 长度。比如，当这个参数配置成 5的时候，表示每个 quicklist 节点的 ziplist 最多包含 5 个数据项。</p><p>当取负值的时候，表示按照占用字节数来限定每个 quicklist 节点上的 ziplist 长度。这时，它只能取 -1 到 -5 这五个值，每个值含义如下：</p><ul><li>-5: 每个 quicklist 节点上的 ziplist 大小不能超过 64Kb；</li><li>-4: 每个 quicklist 节点上的 ziplist 大小不能超过 32Kb；</li><li>-3: 每个 quicklist 节点上的 ziplist 大小不能超过 16Kb；</li><li>-2: 每个 quicklist 节点上的 ziplist 大小不能超过 8Kb（Redis 默认值）；</li><li>-1: 每个 quicklist 节点上的 ziplist 大小不能超过 4Kb。</li></ul><h2 id="quicklist"><a href="#quicklist" class="headerlink" title="quicklist"></a>quicklist</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklist</span> &#123;</span><br>    quicklistNode *head;<br>    quicklistNode *tail;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> count;        <span class="hljs-comment">/* total count of all entries in all ziplists */</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> len;           <span class="hljs-comment">/* number of quicklistNodes */</span><br>    <span class="hljs-type">int</span> fill : <span class="hljs-number">16</span>;              <span class="hljs-comment">/* fill factor for individual nodes */</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> compress : <span class="hljs-number">16</span>; <span class="hljs-comment">/* depth of end nodes not to compress;0=off */</span><br>&#125; quicklist;<br></code></pre></td></tr></table></figure><p>和我们认知里的双向链表一样：</p><ul><li>head 头节点指针；</li><li>tail 尾节点指针；</li><li>count 整个 quicklist 中的元素数量；</li><li>len ziplist 的数量；</li><li>fill: 16bit，ziplist大小设置，存放<code>list-max-ziplist-size</code>参数的值；</li><li>compress: 16bit，节点压缩深度设置，存放<code>list-compress-depth</code>参数的值。</li></ul><p><code>list-max-ziplist-size</code> 的值在最上面已经解释过含义了，<code>list-compress-depth</code>是出于这样的考虑：</p><p>当列表很长的时候，最容易被访问的很可能是两端的数据，中间的数据被访问的频率比较低，所以 redis 提供了该选项选择压缩的中间数据，取值含义如下：</p><ul><li>0: 是个特殊值，表示都不压缩。这是 Redis 的默认值；</li><li>1: 表示 quicklist 两端各有1个节点不压缩，中间的节点压缩；</li><li>2: 表示 quicklist 两端各有2个节点不压缩，中间的节点压缩；</li><li>3: 表示 quicklist 两端各有3个节点不压缩，中间的节点压缩。</li></ul><p>注意，两个端点（头、尾）是不会压缩的。</p><h2 id="quicklistNode"><a href="#quicklistNode" class="headerlink" title="quicklistNode"></a>quicklistNode</h2><p>quicklistNode 是每一个 quicklist 节点的数据结构</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> *<span class="hljs-title">prev</span>;</span>     <span class="hljs-comment">// 前一个节点</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistNode</span> *<span class="hljs-title">next</span>;</span>     <span class="hljs-comment">// 下一个节点</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *zl;              <span class="hljs-comment">// 数据指针。如果当前节点的数据没有压缩，那么它指向一个 ziplist 结构</span><br>                                    <span class="hljs-comment">// 否则，它指向一个quicklistLZF 结构。</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> sz;                <span class="hljs-comment">// zl 指向的 ziplist 的总大小（包括数据项和 zlbytes 等）</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> count: <span class="hljs-number">16</span>;         <span class="hljs-comment">// ziplist 里面包含的数据项个数 2 字节</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> encoding: <span class="hljs-number">2</span>;       <span class="hljs-comment">// 目前只有 1/2 取值 分别代表没压缩和 LZF 压缩</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> container: <span class="hljs-number">2</span>;      <span class="hljs-comment">// 目前只有 2 取值，本意是取 1 代表节点直接存数据，取 2 代表使用 ziplist 存数据</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> recompress: <span class="hljs-number">1</span>;     <span class="hljs-comment">// 当我们使用 index 等命令查看到了已经被压缩的数据，需要暂时解压，再设置标记为合适的时候压缩回去</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> attempted_compress: <span class="hljs-number">1</span>; <span class="hljs-comment">// 自动化测试使用</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> extra: <span class="hljs-number">10</span>;         <span class="hljs-comment">// 拓展字段，暂时没用上</span><br>&#125; quicklistNode;<br><br><span class="hljs-comment">// 一个被压缩过的 ziplist</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklistLZF</span> &#123;</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> sz;    <span class="hljs-comment">// 压缩后的 ziplist 大小</span><br>    <span class="hljs-type">char</span> compressed[];  <span class="hljs-comment">// 柔性数组（flexible array member），存放压缩后的 ziplist 字节数组</span><br>&#125; quicklistLZF;<br></code></pre></td></tr></table></figure><p>以下是某一时刻一个 quicklist 的示意图：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-built_in">list</span>-max-ziplist-size <span class="hljs-number">3</span><br><span class="hljs-built_in">list</span>-compress-depth <span class="hljs-number">2</span><br></code></pre></td></tr></table></figure><p><img src="/img/528c254a597394fe7d15ed3fd00b53a9.png" alt="528c254a597394fe7d15ed3fd00b53a9"></p><ul><li>两端各有 2 个橙黄色节点没有被压缩，对应 list-compress-depth 为 2；</li><li>第二个节点和倒数第二个节点，都是元素数量为 3 的 ziplist，对应 list-max-ziplist-size。</li></ul><h2 id="代码分析"><a href="#代码分析" class="headerlink" title="代码分析"></a>代码分析</h2><ol><li>创建函数</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs c">quicklist *<span class="hljs-title function_">quicklistNew</span><span class="hljs-params">(<span class="hljs-type">int</span> fill, <span class="hljs-type">int</span> compress)</span> &#123;<br>    quicklist *quicklist = quicklistCreate();<br>    <span class="hljs-comment">// 将两个 option 设置到 quicklist 中</span><br>    quicklistSetOptions(quicklist, fill, compress);<br>    <span class="hljs-keyword">return</span> quicklist;<br>&#125;<br><br>quicklist *<span class="hljs-title function_">quicklistCreate</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span> &#123;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">quicklist</span> *<span class="hljs-title">quicklist</span>;</span><br>    quicklist = zmalloc(<span class="hljs-keyword">sizeof</span>(*quicklist));<br>    <span class="hljs-comment">// 初始化时 头尾节点都为 null</span><br>    quicklist-&gt;head = quicklist-&gt;tail = <span class="hljs-literal">NULL</span>;<br>    quicklist-&gt;len = <span class="hljs-number">0</span>;<br>    quicklist-&gt;count = <span class="hljs-number">0</span>;<br>    quicklist-&gt;compress = <span class="hljs-number">0</span>;<span class="hljs-comment">// 压缩深度</span><br>    quicklist-&gt;fill = <span class="hljs-number">-2</span>;<span class="hljs-comment">// 单个 ziplist 最多 8 字节</span><br>    <span class="hljs-keyword">return</span> quicklist;<br>&#125;<br></code></pre></td></tr></table></figure><p>创建函数其实很简单，只是按照默认值初始化，并把配置项的值放入到 quicklist 对应的 compress、fill 字段上</p><p>其他操作复杂的原因在于，比如插入操作过程中要判断是否达到了配置的值，达到的话需要创建新的 node 节点（包括 ziplist ）；删除操作如果 ziplist 为空了，那么对应的头部或尾部节点也要删除，还可能涉及到里面节点的解压缩问题……</p><ol start="2"><li>在指定节点插入（lpush、rpush 最终都会调用到这个函数）</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 在 quicklist 的指定位置插入元素，根据 after 判断在前还是在后</span><br>REDIS_STATIC <span class="hljs-type">void</span> __quicklistInsertNode(quicklist *quicklist,<br>                                        quicklistNode *old_node,<br>                                        quicklistNode *new_node, <span class="hljs-type">int</span> after) &#123;<br>    <span class="hljs-keyword">if</span> (after) &#123;<br>        new_node-&gt;prev = old_node;<br>        <span class="hljs-keyword">if</span> (old_node) &#123;<br>            new_node-&gt;next = old_node-&gt;next;<br>            <span class="hljs-keyword">if</span> (old_node-&gt;next)<br>                old_node-&gt;next-&gt;prev = new_node;<br>            old_node-&gt;next = new_node;<br>        &#125;<br>        <span class="hljs-comment">// 如果插入在之后且 old_node 是尾节点,更新尾节点</span><br>        <span class="hljs-keyword">if</span> (quicklist-&gt;tail == old_node)<br>            quicklist-&gt;tail = new_node;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        new_node-&gt;next = old_node;<br>        <span class="hljs-keyword">if</span> (old_node) &#123;<br>            new_node-&gt;prev = old_node-&gt;prev;<br>            <span class="hljs-keyword">if</span> (old_node-&gt;prev)<br>                old_node-&gt;prev-&gt;next = new_node;<br>            old_node-&gt;prev = new_node;<br>        &#125;<br>        <span class="hljs-comment">// 如果插入在之前且 old_node 是头节点,更新头节点</span><br>        <span class="hljs-keyword">if</span> (quicklist-&gt;head == old_node)<br>            quicklist-&gt;head = new_node;<br>    &#125;<br>    <br>    <span class="hljs-comment">// 为空时初始化头尾指针</span><br>    <span class="hljs-keyword">if</span> (quicklist-&gt;len == <span class="hljs-number">0</span>) &#123;<br>        quicklist-&gt;head = quicklist-&gt;tail = new_node;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (old_node)<br>        quicklistCompress(quicklist, old_node);<br><br>    quicklist-&gt;len++;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="3"><li>push 操作</li></ol><p>在调用这个函数前，会根据操作是 lpush 还是 rpush 确定 where 是头还是尾</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">quicklistPush</span><span class="hljs-params">(quicklist *quicklist, <span class="hljs-type">void</span> *value, <span class="hljs-type">const</span> <span class="hljs-type">size_t</span> sz, <span class="hljs-type">int</span> where)</span> &#123;<br>    <span class="hljs-keyword">if</span> (where == QUICKLIST_HEAD) &#123;<br>        quicklistPushHead(quicklist, value, sz);<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (where == QUICKLIST_TAIL) &#123;<br>        quicklistPushTail(quicklist, value, sz);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>头插</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">quicklistPushHead</span><span class="hljs-params">(quicklist *quicklist, <span class="hljs-type">void</span> *value, <span class="hljs-type">size_t</span> sz)</span> &#123;<br>    quicklistNode *orig_head = quicklist-&gt;head;<br>    <span class="hljs-comment">// 可以直接插入</span><br>    <span class="hljs-keyword">if</span> (likely(<br>            _quicklistNodeAllowInsert(quicklist-&gt;head, quicklist-&gt;fill, sz))) &#123;<br>        <span class="hljs-comment">// 当前节点插入到头节点对应的 ziplist 的头部</span><br>        quicklist-&gt;head-&gt;zl =<br>                ziplistPush(quicklist-&gt;head-&gt;zl, value, sz, ZIPLIST_HEAD);<br>        <span class="hljs-comment">// 更新新节点指向的 ziplist 的总大小</span><br>        quicklistNodeUpdateSz(quicklist-&gt;head);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 创建新的 node 节点</span><br>        quicklistNode *node = quicklistCreateNode();<br>        <span class="hljs-comment">// 创建对应的 ziplist，并把新元素插入到头</span><br>        node-&gt;zl = ziplistPush(ziplistNew(), value, sz, ZIPLIST_HEAD);<br>        <span class="hljs-comment">// 更新新节点指向的 ziplist 的总大小</span><br>        quicklistNodeUpdateSz(node);<br>        <span class="hljs-comment">// 把当前节点插入到原头节点的前面</span><br>        _quicklistInsertNodeBefore(quicklist, quicklist-&gt;head, node);<br>    &#125;<br>    <span class="hljs-comment">// 总数据项 + 1</span><br>    quicklist-&gt;count++;<br>    <span class="hljs-comment">// 头节点的里的元素数量 + 1</span><br>    quicklist-&gt;head-&gt;count++;<br>    <span class="hljs-keyword">return</span> (orig_head != quicklist-&gt;head);<br>&#125;<br><br>REDIS_STATIC <span class="hljs-type">void</span> _quicklistInsertNodeBefore(quicklist *quicklist,<br>                                             quicklistNode *old_node,<br>                                             quicklistNode *new_node) &#123;<br>    __quicklistInsertNode(quicklist, old_node, new_node, <span class="hljs-number">0</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>尾插</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> <span class="hljs-title function_">quicklistPushTail</span><span class="hljs-params">(quicklist *quicklist, <span class="hljs-type">void</span> *value, <span class="hljs-type">size_t</span> sz)</span> &#123;<br>    quicklistNode *orig_tail = quicklist-&gt;tail;<br>    <span class="hljs-comment">// 是否可以直接插入</span><br>    <span class="hljs-keyword">if</span> (likely(<br>            _quicklistNodeAllowInsert(quicklist-&gt;tail, quicklist-&gt;fill, sz))) &#123;<br>        quicklist-&gt;tail-&gt;zl =<br>                ziplistPush(quicklist-&gt;tail-&gt;zl, value, sz, ZIPLIST_TAIL);<br>        quicklistNodeUpdateSz(quicklist-&gt;tail);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        quicklistNode *node = quicklistCreateNode();<br>        node-&gt;zl = ziplistPush(ziplistNew(), value, sz, ZIPLIST_TAIL);<br><br>        quicklistNodeUpdateSz(node);<br>        <span class="hljs-comment">// 把当前节点插入到原尾节点的后面</span><br>        _quicklistInsertNodeAfter(quicklist, quicklist-&gt;tail, node);<br>    &#125;<br>    <span class="hljs-comment">// 更新当前 quicklist 中的元素总数和尾节点对应 ziplist 中的元素数量</span><br>    quicklist-&gt;count++;<br>    quicklist-&gt;tail-&gt;count++;<br>    <span class="hljs-keyword">return</span> (orig_tail != quicklist-&gt;tail);<br>&#125;<br><br>REDIS_STATIC <span class="hljs-type">void</span> _quicklistInsertNodeAfter(quicklist *quicklist,<br>                                            quicklistNode *old_node,<br>                                            quicklistNode *new_node) &#123;<br>    __quicklistInsertNode(quicklist, old_node, new_node, <span class="hljs-number">1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>lpush 和 rpush 的逻辑几乎是一样的，而对其进行压缩，是在 <code>__quicklistInsertNode</code> 函数的 <code>quicklistCompress</code> 函数中……</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之intset篇</title>
    <link href="/2026/05/27/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bintset%E7%AF%87/"/>
    <url>/2026/05/27/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bintset%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之intset篇"><a href="#Redis源码分析之intset篇" class="headerlink" title="Redis源码分析之intset篇"></a>Redis源码分析之intset篇</h1><p>intset 是用于实现集合 (set) 这种对外的数据结构。它包含的元素无序，且不能重复。当插入的元素都是整形，底层使用 intset 存储，否则使用 dict。</p><p><code>intset</code> 结构体定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// intset 结构体</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">intset</span> &#123;</span><br>    <span class="hljs-type">uint32_t</span> encoding;      <span class="hljs-comment">// 数据编码，表示 intset 中的每个数据元素用几个字节（2、4、8）来存储，超出上限就需要转 dict 存储</span><br>    <span class="hljs-type">uint32_t</span> length;        <span class="hljs-comment">// 表示 intset 中的元素个数</span><br>    <span class="hljs-type">int8_t</span> contents[];      <span class="hljs-comment">// 柔性数组 总长度为  encoding * length</span><br>&#125; intset;<br></code></pre></td></tr></table></figure><p>各字段含义如下：</p><ul><li><code>encoding</code> 数据编码，表示intset中的每个数据元素用几个字节来存储。</li></ul><p>可选值：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">define</span> INTSET_ENC_INT16 (sizeof(int16_t))<span class="hljs-comment">// 2字节</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> INTSET_ENC_INT32 (sizeof(int32_t))<span class="hljs-comment">// 4字节</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> INTSET_ENC_INT64 (sizeof(int64_t))<span class="hljs-comment">// 8字节</span></span><br></code></pre></td></tr></table></figure><ul><li><code>length</code> intset 中的元素个数；</li><li><code>contents</code> 柔性数组，存放真实数据，总大小为 encoding * length。</li></ul><p>需要注意的是，intset 会随着数据的添加而改变它的数据编码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">6379</span>&gt; sadd myset <span class="hljs-number">1</span> <span class="hljs-number">2</span> <span class="hljs-number">3</span><br>(integer) <span class="hljs-number">3</span><br><span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">6379</span>&gt; memory usage <span class="hljs-title function_">myset</span><br><span class="hljs-params">(integer)</span> 61<br>127.0.0.1:6379&gt; sadd myset 4 5<br><span class="hljs-params">(integer)</span> 2<br>127.0.0.1:6379&gt; memory usage <span class="hljs-title function_">myset</span><br><span class="hljs-params">(integer)</span> 65<br>127.0.0.1:6379&gt; sadd myset 36666<br><span class="hljs-params">(integer)</span> 1<br>127.0.0.1:6379&gt; memory usage <span class="hljs-title function_">myset</span><br><span class="hljs-params">(integer)</span> 79<br></code></pre></td></tr></table></figure><p>我们来分析一下：</p><p>添加 3 个小元素后，myset 占用 61 字节，再添加两个小元素，myset 占用 65 字节，说明 1 个小元素占用 2字节，与我们的分析一致。同时也可以得出除了元素所占的空间，结构体本身占用 65 - 5*2 &#x3D; 55 字节。</p><p>接着我们添加了 36666 元素，超出了 2 字节的表示范围，因此变为了4字节编码，共有（1、2、3、4、5、36666）6 个元素，占用 24 字节。</p><p>结构体本身 55 字节 + 元素 24 字节 &#x3D; 79 字节</p><p>与输出结果一致。</p><p>了解了数据结构后，分析两个主要函数（插入和查找）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs c">intset *<span class="hljs-title function_">intsetAdd</span><span class="hljs-params">(intset *is, <span class="hljs-type">int64_t</span> value, <span class="hljs-type">uint8_t</span> *success)</span> &#123;<br>    <span class="hljs-comment">// 获取要插入 value 的编码类型</span><br>    <span class="hljs-type">uint8_t</span> valenc = _intsetValueEncoding(value);<br>    <span class="hljs-type">uint32_t</span> pos;<br>    <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">1</span>;<br><br>    <span class="hljs-comment">// 如果比当前的编码类型大，需要进行升级</span><br>    <span class="hljs-keyword">if</span> (valenc &gt; intrev32ifbe(is-&gt;encoding)) &#123;<br>        <span class="hljs-keyword">return</span> intsetUpgradeAndAdd(is, value);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 如果当前 value 已经存在</span><br>        <span class="hljs-keyword">if</span> (intsetSearch(is, value, &amp;pos)) &#123;<br>            <span class="hljs-keyword">if</span> (success) *success = <span class="hljs-number">0</span>;<br>            <span class="hljs-keyword">return</span> is;<br>        &#125;<br><br>        <span class="hljs-comment">// 内存扩充以容纳新的元素</span><br>        is = intsetResize(is, intrev32ifbe(is-&gt;length) + <span class="hljs-number">1</span>);<br>        <span class="hljs-comment">// 将待插入位置后面的元素统一向后移动 1 个位置</span><br>        <span class="hljs-keyword">if</span> (pos &lt; intrev32ifbe(is-&gt;length)) intsetMoveTail(is, pos, pos + <span class="hljs-number">1</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 把 value 插入到指定 pos</span><br>    _intsetSet(is, pos, value);<br><br>    <span class="hljs-comment">// length + 1</span><br>    is-&gt;length = intrev32ifbe(intrev32ifbe(is-&gt;length) + <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">return</span> is;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>获取要插入数据的编码类型，与当前编码类型比较：<ul><li>比当前编码类型大，进行升级存储，同时由于是新元素造成的存储升级，所以很轻松知道要插入元素的位置；</li><li>比当前编码类型小，传入 value 进行查找：<ul><li>能找到，直接返回；</li><li>找不到，返回要插入的位置，首先进行内存扩充以容纳新元素，再把要插入位置的后面元素集体后移1个位置，最后把当前 value 插入到指定位置，整体长度 + 1。</li></ul></li></ul></li></ul><p>所以这个插入方法时间复杂度为 O(N)。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 在指定的 intset 中查找指定的元素 value，如果找到，则返回 1 并且将参数 pos 指向找到的元素位置</span><br><span class="hljs-comment">// 如果没找到，则返回 0 并且将参数 pos 指向能插入该元素的位置。</span><br><span class="hljs-type">static</span> <span class="hljs-type">uint8_t</span> <span class="hljs-title function_">intsetSearch</span><span class="hljs-params">(intset *is, <span class="hljs-type">int64_t</span> value, <span class="hljs-type">uint32_t</span> *pos)</span> &#123;<br>        <span class="hljs-comment">// intset 里的数据是按小端（little endian）模式存储的，因此在大端（big endian）机器上运行时，这里的 intrev32ifbe 会做相应的转换</span><br>    <span class="hljs-type">int</span> min = <span class="hljs-number">0</span>, max = intrev32ifbe(is-&gt;length) - <span class="hljs-number">1</span>, mid = <span class="hljs-number">-1</span>;<br>    <br>    <span class="hljs-type">int64_t</span> cur = <span class="hljs-number">-1</span>;<br><br>    <span class="hljs-comment">// 如果 intset 为空,直接返回 0 和要插入的位置 0</span><br>    <span class="hljs-keyword">if</span> (intrev32ifbe(is-&gt;length) == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">if</span> (pos) *pos = <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 如果要插入的 value &gt; 最大值 或 &lt; 最小值,那么要插入的位置就确定了</span><br>        <span class="hljs-keyword">if</span> (value &gt; _intsetGet(is, max)) &#123;<br>            <span class="hljs-keyword">if</span> (pos) *pos = intrev32ifbe(is-&gt;length);<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value &lt; _intsetGet(is, <span class="hljs-number">0</span>)) &#123;<br>            <span class="hljs-keyword">if</span> (pos) *pos = <span class="hljs-number">0</span>;<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 二分查找要插入的位置</span><br>    <span class="hljs-keyword">while</span> (max &gt;= min) &#123;<br>        mid = ((<span class="hljs-type">unsigned</span> <span class="hljs-type">int</span>) min + (<span class="hljs-type">unsigned</span> <span class="hljs-type">int</span>) max) &gt;&gt; <span class="hljs-number">1</span>;<br>        cur = _intsetGet(is, mid);<br>        <span class="hljs-keyword">if</span> (value &gt; cur) &#123;<br>            min = mid + <span class="hljs-number">1</span>;<br>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (value &lt; cur) &#123;<br>            max = mid - <span class="hljs-number">1</span>;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 如果找到的值就是要插入的值，返回1，并把 pos 指向该位置</span><br>    <span class="hljs-keyword">if</span> (value == cur) &#123;<br>        <span class="hljs-keyword">if</span> (pos) *pos = mid;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-comment">// 返回 0，并把 pos 指向该插入的位置</span><br>        <span class="hljs-keyword">if</span> (pos) *pos = min;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>查找时首先对特殊情况进行处理（intset 为空、比最小值小，比最大值大）</li><li>接着进行二分查找<ul><li>如果最后找到的值和要插入的值相同，修改 pos 为当前位置，返回 1；</li><li>如果不同，说明需要插入，pos 指向要插入的位置，返回 0。</li></ul></li></ul><p>查找时间复杂度为 O(log N)。</p><p>Intset 和 ziplist 相比，都有时间换空间的意思，但也有一些不同：</p><ul><li>intset 中每个元素都是定长的，ziplist 元素长度可以不同；</li><li>intset 只能存储整数，而 ziplist 任意二进制串都可以存储；</li><li>intset 中的元素是有序的，查找时可以进行二分查找，而 ziplist 只能遍历。</li></ul><p>另外，intset 在以下情况发生时会转变为 dict</p><ul><li>添加了字符串，intset 无法表示</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">6379</span>&gt; sadd myset <span class="hljs-number">1</span> <span class="hljs-number">2</span> <span class="hljs-number">3</span><br>(integer) <span class="hljs-number">3</span><br><span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">6379</span>&gt; object encoding myset<br><span class="hljs-string">&quot;intset&quot;</span><br><span class="hljs-number">127.0</span>.<span class="hljs-number">0.1</span>:<span class="hljs-number">6379</span>&gt; sadd myset <span class="hljs-title function_">zwj</span><br><span class="hljs-params">(integer)</span> 1<br>127.0.0.1:6379&gt; object encoding myset<br>&quot;hashtable<br></code></pre></td></tr></table></figure><ul><li>添加了一个数字，但它超出了能表示的范围。intset 能够表达的最大的整数范围为 -2^64^~2^64^-1，因此，如果添加的数字超出了这个范围，这也会导致 intset 转成 dict</li><li>添加的集合元素个数超过了<code>set-max-intset-entries</code>配置的值（默认 <code>512</code>）的时候，此时查找效率为 O(log N)，数据量大时会比较慢，也会导致 intset 转成 dict</li></ul><p>Redis 的 set 数据结构还提供了计算交集（<code>sinter</code>）、并集（<code>sunion</code>）、差集（<code>sdiff</code>）的方法：</p><p>计算交集的过程大概可以分为三部分：</p><ol><li>检查各个集合，对于不存在的集合当做空集来处理。一旦出现空集，则不用继续计算了，最终的交集就是空集；</li><li>对各个集合按照元素个数由少到多进行排序。这个排序有利于后面计算的时候从最小的集合开始，需要处理的元素个数较少；</li><li>对排序后第一个集合（也就是最小集合）进行遍历，对于它的每一个元素，依次在后面的所有集合中进行查找。只有在所有集合中都能找到的元素，才加入到最后的结果集合中。</li></ol><blockquote><p>O(N*M) worst case where N is the cardinality of the smallest set and M is the number of sets.</p></blockquote><p>计算并集只需要遍历所有集合，将每一个元素都添加到最后的结果集合中。向集合中添加元素会自动去重。</p><blockquote><p>O(N) where N is the total number of elements in all given sets.</p></blockquote><p>计算差集（A 和 B 的差集是 A 中有 B 中没有的，A - B）</p><p>计算差集有两种可能的算法，它们的时间复杂度有所区别。</p><p>第一种算法：</p><ul><li>对第一个集合进行遍历，对于它的每一个元素，依次在后面的所有集合中进行查找。只有在所有集合中都找不到的元素，才加入到最后的结果集合中。</li></ul><p>这种算法的时间复杂度为 O(N*M)，其中N是第一个集合的元素个数，M是集合数目。</p><p>第二种算法：</p><ul><li>将第一个集合的所有元素都加入到一个中间集合中；</li><li>遍历后面所有的集合，对于碰到的每一个元素，从中间集合中删掉它；</li><li>最后中间集合剩下的元素就构成了差集。</li></ul><p>这种算法的时间复杂度为O(N)，其中N是所有集合的元素个数总和。</p><p>在计算差集的开始部分，会先分别估算一下两种算法预期的时间复杂度，然后选择复杂度低的算法来进行运算。还有两点需要注意：</p><ul><li>在一定程度上优先选择第一种算法，因为它涉及到的操作比较少，只用添加，而第二种算法要先添加再删除；</li><li>如果选择了第一种算法，那么在执行该算法之前，Redis 的实现中对于第二个集合之后的所有集合，按照元素个数<code>由多到少</code>进行了排序。这个排序有利于以更大的概率查找到元素，从而更快地结束查找。</li></ul>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>线程安全与锁优化</title>
    <link href="/2026/05/26/%E7%BA%BF%E7%A8%8B%E5%AE%89%E5%85%A8%E4%B8%8E%E9%94%81%E4%BC%98%E5%8C%96/"/>
    <url>/2026/05/26/%E7%BA%BF%E7%A8%8B%E5%AE%89%E5%85%A8%E4%B8%8E%E9%94%81%E4%BC%98%E5%8C%96/</url>
    
    <content type="html"><![CDATA[<h1 id="线程安全与锁优化"><a href="#线程安全与锁优化" class="headerlink" title="线程安全与锁优化"></a>线程安全与锁优化</h1><p>锁可以用来保证线程安全，线程安全却未必一定需要锁。</p><h2 id="线程安全"><a href="#线程安全" class="headerlink" title="线程安全"></a>线程安全</h2><p>简单来说：如果多线程访问一个对象时，无需进行额外的同步或协调操作，调用这个对象的行为都可以获得正确的结果，那就称这个对象是线程安全的。</p><p>Brian Goetz（Java 并发编程实战的作者）将 Java 语言中各种操作共享的数据分为以下五类：不可变、绝对线程安全、相对线程安全、线程兼容和线程对立。</p><h3 id="不可变"><a href="#不可变" class="headerlink" title="不可变"></a>不可变</h3><p>如果一个对象是 final 修饰的基本类型，在构造方法中初始化完成，那其外部的可见状态永远都不会改变。“不可变”带来的安全性是最直接、 最纯粹的。</p><p>在 Java 类库 API 中符合不可变要求的类型，除了 String 之外，常用的还有枚举类型及 java.lang.Number 的部分子类，如 Long 和 Double 等数值包装类型、BigInteger 和 BigDecimal 等大数据类型。</p><h3 id="绝对线程安全"><a href="#绝对线程安全" class="headerlink" title="绝对线程安全"></a>绝对线程安全</h3><p>绝对的线程安全其实是很难满足的，Java 中几乎没有真正的绝对线程安全类。</p><p>像我们所熟知的 Collections 的 synchronizedCollection() 方法包装的集合，即使所有的方法都被 synchronized 修饰，也不意味着访问时可以完全不考虑同步。</p><p>举个例子：一个被 synchronizedCollection() 包装的 ArrayList，一个线程不断的删除元素，一个线程不断的遍历元素，有几率碰到 <code>ConcurrentModificationException</code>。原因很简单，一个方法是安全的，不意味着后续的操作也安全，例如 .size() 方法返回了当前精确的元素数，但真正进行操作时可能这个位置的元素已经被删除了，synchronized 能保证每个方法本身是安全的，不能保证业务逻辑也是线程安全的。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>            <span class="hljs-comment">// 创建一个同步 List</span><br>            List&lt;Integer&gt; syncList = Collections.synchronizedList(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;());<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">100</span>; i++) &#123;<br>                syncList.add(i);<br>            &#125;<br><br>            <span class="hljs-comment">// 线程1：不断删除元素</span><br>            <span class="hljs-type">Thread</span> <span class="hljs-variable">remover</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>                <span class="hljs-type">Random</span> <span class="hljs-variable">random</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Random</span>();<br>                <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        Thread.sleep(<span class="hljs-number">10</span>);<br>                    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                        <span class="hljs-keyword">break</span>;<br>                    &#125;<br>                    <span class="hljs-keyword">if</span> (!syncList.isEmpty()) &#123;<br>                        <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> random.nextInt(syncList.size());<br>                        syncList.remove(index);<br>                        System.out.println(<span class="hljs-string">&quot;删除了索引 &quot;</span> + index);<br>                    &#125;<br>                &#125;<br>            &#125;);<br><br>            <span class="hljs-comment">// 线程2：遍历集合</span><br>            <span class="hljs-type">Thread</span> <span class="hljs-variable">iterator</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>                <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                    <span class="hljs-keyword">try</span> &#123;<br>                        Thread.sleep(<span class="hljs-number">15</span>);<br>                    &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                        <span class="hljs-keyword">break</span>;<br>                    &#125;<br>                    <span class="hljs-comment">// 危险操作：没有外部同步的迭代</span><br>                    <span class="hljs-keyword">for</span> (Integer num : syncList) &#123;<br>                        System.out.println(<span class="hljs-string">&quot;遍历到: &quot;</span> + num);<br>                    &#125;<br>                &#125;<br>            &#125;);<br><br>            remover.start();<br>            iterator.start();<br><br>            <span class="hljs-comment">// 运行3秒后停止</span><br>            Thread.sleep(<span class="hljs-number">3000</span>);<br>            remover.interrupt();<br>            iterator.interrupt();<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="相对线程安全"><a href="#相对线程安全" class="headerlink" title="相对线程安全"></a>相对线程安全</h3><p>相对线程安全就是我们通常意义上所讲的线程安全，它需要保证对这个对象单次的操作是线程安全的，我们在调用的时候不需要进行额外的保障措施，但是对于一些特定顺序的连续调用，就可能需要在调用端使用额外的同步手段来保证调用的正确性。</p><p>在 Java 语言中，大部分声称线程安全的类都属于这种类型，例如 Vector、HashTable、Collections 的 synchronizedCollection() 方法包装的集合等。</p><h3 id="线程兼容"><a href="#线程兼容" class="headerlink" title="线程兼容"></a>线程兼容</h3><p>线程兼容是指对象本身并不是线程安全的，但是可以通过在调用端正确地使用同步手段来保证对象在并发环境中可以安全地使用。我们平时说的线程不安全通常就是指这种情况。</p><p>Java 类库 API 中大部分的类都是线程兼容的，例如 ArrayList、HashMap 等。</p><h3 id="线程对立"><a href="#线程对立" class="headerlink" title="线程对立"></a>线程对立</h3><p>线程对立是指不管调用端是否采取了同步措施，都无法在多线程环境中并发使用代码。这种情况很少，而且通常是有害的，Java 中的一些方法已经被声明废弃了。</p><h2 id="实现线程安全"><a href="#实现线程安全" class="headerlink" title="实现线程安全"></a>实现线程安全</h2><p>这里并不是演示如何使用锁的代码来实现线程安全，那是并发编程的内容，这里侧重于描述虚拟机提供的同步和锁机制，可以结合 <a href="https://zhuwenjie0716.github.io/2026/04/27/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E9%94%81%E7%AF%87/">系统性能优化之锁篇</a> 一起看。</p><h3 id="互斥同步"><a href="#互斥同步" class="headerlink" title="互斥同步"></a>互斥同步</h3><p>互斥同步（Mutual Exclusion &amp; Synchronization）是一种最常见也是最主要的并发正确性保障手段。同步是指在多个线程并发访问共享数据时，保证共享数据在同一个时刻只被一条（或者是一些， 当使用信号量的时候）线程使用。而互斥是实现同步的一种手段，<strong>临界区（Critical Section）、互斥量 （Mutex）和信号量（Semaphore）都是常见的互斥实现方式</strong>。</p><p>在“互斥同步”这四个字里面，互斥是因，同步是果；互斥是方法，同步是目的。</p><p>Java 里面，最基本的互斥同步手段就是 synchronized 关键字，它会在同步块的前后分别形成 monitorenter 和monitorexit 这两个字节码指令。关于 synchronized 到底锁的是什么这里不再赘述，锁的对象是这两个字节码指令的参数。</p><p>根据《Java 虚拟机规范》的要求，在执行 monitorenter 指令时，首先要去尝试获取对象的锁。如果这个对象没被锁定，或者当前线程已经持有了那个对象的锁，就把锁的计数器的值增加一，而在执行 monitorexit 指令时会将锁计数器的值减一。一旦计数器的值为零，锁随即就被释放了。如果获取对象锁失败，那当前线程就应当被阻塞等待，直到请求锁定的对象被持有它的线程释放为止。</p><p>根据上述描述，我们可以得出如下结论：</p><ul><li>被 synchronized 修饰的同步块对同一条线程来说是可重入的；</li><li>被 synchronized 修饰的同步块在持有锁的线程执行完毕并释放锁之前，会无条件地阻塞后面其他线程的进入。</li></ul><p>那么我们可以思考一个问题，如果锁的代码块执行速度非常快，比阻塞切换线程还要快，那么这样是不是太重了？于是 synchronized 后来进行了优化，会先进行一定次数的自旋才进行阻塞。</p><p>除 synchronized 外，JDK5 还引入了一些新的并发工具，如 Lock 接口也可以用来实现互斥同步，重入锁（ReentrantLock）是 Lock 接口最常见的一种实现，它也是可重入的。引入新的工具自然要和老的工具进行比对，Lock 比 synchronized 多了一些高级特性：</p><ul><li><strong>等待可中断</strong>：线程执行到 synchronized 修饰到代码块时如果取不到锁会一致阻塞，而 Lock 可以通过 tryLock 尝试获取锁指定时间；</li><li><strong>公平锁</strong>：锁是否公平是指是否按照请求锁的顺序获得锁。synchronized 中的锁是非公平的，ReentrantLock 默认也是非公平的，但可以通过构造参数要求使用公平锁，不过公平锁一般会影响吞吐量；</li><li><strong>锁绑定多个条件：</strong>一个 ReentrantLock 对象可以同时绑定多个 Condition 对象被唤醒，而 synchronized 只能通过 wait 和 notify&#x2F;notifyall 实现一个隐含的条件。</li></ul><p>不过在使用 Lock 时要注意，Lock 是无法自动释放的，一定要在 finally 中手动释放。</p><p>即使 Lock 的功能如此强大，性能也不逊色于 synchronized（JDK6 优化之前更是远优），但依然推荐在满足条件的情况下优先使用 synchronized，原因如下：</p><ul><li>synchronized 是在 Java 语法层面的同步，足够清晰，也足够简单；</li><li>synchronized 可以由 Java 虚拟机来确保即使出现异常，锁也能被自动释放；</li><li>Java 虚拟机更容易针对 synchronized 来进行优化，因为 Java 虚拟机可以在线程和对象的元数据中记录synchronized 中锁的相关信息。</li></ul><h3 id="非阻塞同步"><a href="#非阻塞同步" class="headerlink" title="非阻塞同步"></a>非阻塞同步</h3><p>互斥同步面临的主要问题是进行线程阻塞和唤醒所带来的性能开销，因此这种同步也被称为阻塞同步（Blocking Synchronization）。阻塞同步是一种悲观的实现，因为即使不存在并发问题，也会去获取锁，执行完后释放锁。</p><p>随着硬件指令集的发展，我们有了其他选项：例如不管风险，先进行操作，如果没有其他线程争用共享数据，那操作就直接成功了；如果共享的数据的确被争用，产生了冲突，那再进行其他的补偿措施，最常用的补偿措施是不断地重试，直到出现没有竞争的共享数据为止。</p><p>相比大家都听说或用过大名鼎鼎的 CAS（compare and swap，比较并交换），这个函数需要有三个操作数，分别是内存位置（在 Java 中可以简单地理解为变量的内存地址，用 V 表示）、旧的预期值（用 A 表示）和准备设置的新值（用 B 表示）。CAS 指令执行时，当且仅当 V 符合 A 时，处理器才会用 B 更新 V 的值，否则它就不执行更新。但是，不管是否更新了 V 的值，都会返回 V 的旧值，上述的处理过程是一个原子操作，执行期间不会被其他线程中断。</p><p>JDK 中原子类的 <code>incrementAndGet</code> 就是这么实现的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">incrementAndGet</span><span class="hljs-params">()</span> &#123;<br><span class="hljs-keyword">for</span> (;;) &#123;<br><span class="hljs-type">int</span> <span class="hljs-variable">current</span> <span class="hljs-operator">=</span> get();<br><span class="hljs-type">int</span> <span class="hljs-variable">next</span> <span class="hljs-operator">=</span> current + <span class="hljs-number">1</span>;<br><span class="hljs-keyword">if</span> (compareAndSet(current, next))<br><span class="hljs-keyword">return</span> next;<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>CAS 存在一个 ABA 问题，即我在读取时是 A，但在我操作前它先被修改为 B，又被修改为 A，那么依然符合指令的执行条件。为了解决这个问题，出现了一个带有标记的原子引用类 AtomicStampedReference，它可以通过控制变量值的版本来保证 CAS 的正确性。不过大部分情况下 ABA 问题不会影响程序并发的正确性，如果需要解决 ABA 问题，改用传统的互斥同步可能会比原子类更为高效。</p><h3 id="无同步方案"><a href="#无同步方案" class="headerlink" title="无同步方案"></a>无同步方案</h3><p>如果提倡的微服务无状态化一样，如果代码块满足不依赖全局变量、存储在堆上的数据和公用的系统资源， 用到的状态量都由参数中传入，不调用非可重入的方法等条件，那它也是线程安全的。</p><p>可重入代码块的定义是：可以在代码执行的任何时刻中断它，转而去执行另外一段代码（包括递归调用它本身），而在控制权返回后，原来的程序不会出现任何错误，也不会对结果有所影响。</p><p>另外像 Thread Local Storage（线程本地存储），如果能把共享数据的可见范围限制在同一个线程之内，这样，无须同步也能保证线程之间不出现数据争用的问题。常见的使用有 ThreadLocalMap，详细了解可以参见：<a href="https://zhuwenjie0716.github.io/2026/05/04/Netty%E7%9A%84FastThreadLocal%E5%BF%AB%E5%9C%A8%E5%93%AA/">FastThreadLocal快在哪</a>。</p><h2 id="锁优化"><a href="#锁优化" class="headerlink" title="锁优化"></a>锁优化</h2><p>作为能同时保证原子性、可见性和有序性的 synchronized，自然值得被优化。</p><p>HotSpot 虚拟机开发团队在 JDK6 上花费了大量的资源去实现各种锁优化技术，如适应性自旋（Adaptive Spinning）、锁消除（Lock Elimination）、锁膨胀（Lock Coarsening）、轻量级锁（Lightweight Locking）、偏向锁（Biased Locking）等，这些技术都是为了在线程之间更高效地共享数据及解决竞争问题，从而提高程序的执行效率。</p><h3 id="自适应自旋"><a href="#自适应自旋" class="headerlink" title="自适应自旋"></a>自适应自旋</h3><p>像我们前面说的那样，有时候要同步的代码块执行时间比线程切换阻塞要短的多，那么线程没必要阻塞，只须让线程执行一个忙循环（自旋）即可，这就是所谓的自旋锁。</p><p>JDK6 中就默认开启了自旋，自旋虽然避免了线程切换的开销，但它是要占用处理器时间的，所以自旋只适合阻塞的代码执行非常快。自旋等待的时间必须有一定的限度，如果自旋超过了限定的次数仍然没有成功获得锁，就应当使用传统的方式去挂起线程。<strong>自旋次数的默认值是十次</strong>，用户也可以使用参数<code>-XX:PreBlockSpin</code>来更改。</p><p>不过无论是默认值还是用户指定的自旋次数，对整个 Java 虚拟机中所有的锁来说都是相同的。在 JDK6 中对自旋锁的优化，引入了自适应的自旋。自适应意味着自旋的时间不再是固定的了，而是由前一次在同一个锁上的自旋时间及锁的拥有者的状态来决定的。</p><p>有了自适应自旋，随着程序运行时间的增长及性能监控信息的不断完善，虚拟机对程序锁的状况预测就会越来越精准，虚拟机就会变得越来越“聪明”了。</p><h3 id="锁消除"><a href="#锁消除" class="headerlink" title="锁消除"></a>锁消除</h3><p>举个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">concatString</span><span class="hljs-params">(String s1, String s2, String s3)</span> &#123;<br><span class="hljs-type">StringBuffer</span> <span class="hljs-variable">sb</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuffer</span>();<br>sb.append(s1);<br>sb.append(s2);<br>sb.append(s3);<br><span class="hljs-keyword">return</span> sb.toString();<br>&#125;<br></code></pre></td></tr></table></figure><p>StringBuffer.append() 方法中有一个同步块，锁就是对象本身。虚拟机经过逃逸分析后会发现它的动态作用域被限制在 concatString() 方法内部。也就是 sb 的所有引用都永远不会逃逸到 concatString() 方法之外，其他线程无法访问到它，所以这里虽然有锁，但是可以被安全地消除掉（例如 StringBuilder）。</p><h3 id="锁粗化"><a href="#锁粗化" class="headerlink" title="锁粗化"></a>锁粗化</h3><p>原则上，我们在编写代码的时候，总是推荐将同步块的作用范围限制得尽量小（只在共享数据的实际作用域中才进行同步），这样是为了使得需要同步的操作数量尽可能变少，即使存在锁竞争，等待锁的线程也能尽可能快地拿到锁。</p><p>大多数情况下，这个原则都是正确的，但有时候如果在循环体内对同一个对象反复加锁和解锁，那即使没有线程竞争，频繁地进行互斥同步操作也会导致不必要的性能损耗。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> String <span class="hljs-title function_">concatString</span><span class="hljs-params">(String s1, String s2, String s3)</span> &#123;<br><span class="hljs-type">StringBuffer</span> <span class="hljs-variable">sb</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuffer</span>();<br>sb.append(s1);<br>sb.append(s2);<br>sb.append(s3);<br><span class="hljs-keyword">return</span> sb.toString();<br>&#125;<br></code></pre></td></tr></table></figure><p>像上面这段代码如果真的需要加锁，虚拟机会把加锁同步的范围扩展（粗化）到整个操作序列的外部（即第一个 append 之前到最后一个 append 之后），这样只加一次锁就可以了。</p><h3 id="偏向锁与轻量级锁"><a href="#偏向锁与轻量级锁" class="headerlink" title="偏向锁与轻量级锁"></a>偏向锁与轻量级锁</h3><p>“轻量”是相比之前锁的实现（遇到竞争就阻塞等待，涉及状态切换和线程调度），它不是为了代替重量级锁的，其初衷是在没有多线程竞争的前提下，减少传统的重量级锁使用操作系统互斥量产生的性能消耗 。</p><p><a href="https://zhuwenjie0716.github.io/2026/05/19/Java-Object/">Java Object</a> 中介绍了 Java 中对象的格式，其中有对象头（Header），Header 中存储了对象运行时数据的部分被称为 “Mark Word”，这是实现轻量级锁和偏向锁的关键。</p><p><img src="/img/image-20260525111615505.png" alt="image-20260525111615505"></p><p>偏向锁中的“偏”，就是偏心的“偏”、偏袒的“偏”。偏向锁主要用来优化<code>同一线程多次申请同一个锁</code>的竞争。可能大部分时间一个锁都是被一个线程持有和竞争。假如一个锁被线程 A 持有，后释放；接下来又被线程 A 持有、释放……如果使用 monitor，则每次都会发生用户态和内核态的切换，性能低下。</p><p>但在高并发场景下，大量线程同时竞争同一个锁资源，偏向锁会被撤销，发生 <code>stop the world</code>后，开启偏向锁会带来更大的性能开销（于是 Java15 默认禁用了偏向锁）。</p><p>锁的升级过程比较复杂，如果考虑细枝末节（例如锁的过程中还被计算了哈希值，对象头会如何变化）就更复杂了，推荐阅读《深入理解Java虚拟机》锁优化章节原文或参考：<a href="https://www.cnblogs.com/star95/p/17542850.html">浅析synchronized锁升级的原理与实现</a>。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>并发</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java内存模型与线程模型</title>
    <link href="/2026/05/25/Java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E4%B8%8E%E7%BA%BF%E7%A8%8B%E6%A8%A1%E5%9E%8B/"/>
    <url>/2026/05/25/Java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B%E4%B8%8E%E7%BA%BF%E7%A8%8B%E6%A8%A1%E5%9E%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="Java内存模型与线程模型"><a href="#Java内存模型与线程模型" class="headerlink" title="Java内存模型与线程模型"></a>Java内存模型与线程模型</h1><p>在现代的多核架构机器中，每颗 cpu 都有自己的高速缓存，它们又共享主内存的数据，因此多 cpu 并发时需要处理缓存一致性（Cache Coherence）的问题，例如多处理器都写回时以谁的为准。为了解决这个问题，各个处理器访问缓存时都会遵循一些协议，在读写时要根据协议来进行操作，这类协议有 MSI、MESI（Illinois Protocol）、MOSI、 Synapse、Firefly 及 Dragon Protocol 等。</p><p><img src="/img/image-20260524130047857.png" alt="image-20260524130047857"></p><p><strong>“内存模型”一词，可以理解为在特定的操作协议下，对特定的内存或高速缓存进行读写访问的过程抽象。</strong>Java 的内存模型与这里的内存访问操作及硬件的缓存访问操作具有高度的可类比性。</p><p>为了充分利用资源，处理器可能会对输入代码进行乱序执行（Out-Of-Order Execution）优化，举个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>i++;<br>j++;<br></code></pre></td></tr></table></figure><p>由于处理器执行 <code>int i = 0</code> 时 i 已经在寄存器或 cpu 缓存中了，直接执行 <code>i++</code> 可能会更高效，所以顺序性并不能靠代码的先后顺序来保证，Java 中即时编译器中会有指令重排序 （Instruction Reorder）优化。</p><p>此外，Java 并发中的一些内容在分布式系统中也同样存在，例如：<a href="https://zhuwenjie0716.github.io/2026/04/15/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E4%B8%AD%E7%9A%84%E8%B0%81%E5%85%88%E8%B0%81%E5%90%8E/">分布式系统中的谁先谁后</a>。</p><h2 id="Java-Memory-Model"><a href="#Java-Memory-Model" class="headerlink" title="Java Memory Model"></a>Java Memory Model</h2><p>《Java 虚拟机规范》定义了“Java 内存模型”（Java Memory Model，JMM）来屏蔽各种硬件和操作系统的内存访问差异，以实现让Java 程序在各种平台下都能达到一致的内存访问效果。经过长时间的验证和修补，直至 JDK5（实现了JSR-133）发布后，JMM 才终于成熟、完善起来了。</p><p>Java 内存模型的主要目的是定义程序中各种变量的访问规则，即关注在虚拟机中把变量值存储到内存和从内存中取出变量值这样的底层细节（这里的变量指的是实例字段、静态字段和构成数组对象的元素，但是不包括局部变量与方法参数，因为后者是线程私有的，不会存在竞争问题）。</p><p>Java 内存模型规定了所有的变量都存储在主内存（Main Memory）中。每条线程还有自己的工作内存（Working Memory），线程的工作内存中保存了被该线程使用的变量的主内存副本，线程对变量的所有操作（读取、赋值等）都必须在工作内存中进行，而不能直接读写主内存中的数据。不同的线程之间也无法直接访问对方工作内存中的变量，线程间变量值的传递均需要通过主内存来完成。</p><p><img src="/img/image-20260524131150247.png" alt="image-20260524131150247"></p><p>注意：这里的主内存、工作内存和 JVM 模型中的堆、栈、方法区不是一个层级的划分。如果两者一定要勉强对应起来，那么从变量、主内存、工作内存的定义来看，主内存主要对应于堆中的对象实例数据部分，而工作内存则对应于虚拟机栈中的部分区域。如果从物理层次上，主内存直接对应于物理硬件的内存，而为了获取更好的运行速度，虚拟机（或者是硬件、操作系统本身的优化措施）可能会让工作内存优先存储于寄存器和高速缓存中。</p><ul><li>上文中提到：线程的工作内存中保存主内存副本，例如一个很大的对象其实是不会被完整的复制一份的，只是这个对象的引用、对象中某个在线程访问到的字段是有可能被复制的；</li><li>volatile 变量依然有工作内存的拷贝，但是由于它特殊的操作顺序性规定，所以看起来如同直接在主内存中读写访问一般。</li></ul><p>JMM 中定义了 8 种操作来完成主内存与工作内存之间具体的交互：</p><ul><li>lock（锁定）：作用于主内存的变量，它把一个变量标识为一条线程独占的状态。</li><li>unlock（解锁）：作用于主内存的变量，它把一个处于锁定状态的变量释放出来，释放后的变量才可以被其他线程锁定。</li><li>read（读取）：作用于主内存的变量，它把一个变量的值从主内存传输到线程的工作内存中，以便随后的 load 动作使用。 </li><li>load（载入）：作用于工作内存的变量，它把 read 操作从主内存中得到的变量值放入工作内存的 变量副本中。</li><li>use（使用）：作用于工作内存的变量，它把工作内存中一个变量的值传递给执行引擎，每当虚拟机遇到一个需要使用变量的值的字节码指令时将会执行这个操作。</li><li>assign（赋值）：作用于工作内存的变量，它把一个从执行引擎接收的值赋给工作内存的变量， 每当虚拟机遇到一个给变量赋值的字节码指令时执行这个操作。</li><li>store（存储）：作用于工作内存的变量，它把工作内存中一个变量的值传送到主内存中，以便随后的 write 操作使用。</li><li>write（写入）：作用于主内存的变量，它把 store 操作从工作内存中得到的变量的值放入主内存的变量中。</li></ul><p>如果要把一个变量从主内存拷贝到工作内存，那就要按顺序执行 read 和 load 操作，如果要把变量从工作内存同步回主内存，就要按顺序执行 store 和 write 操作（只要求操作的顺序，不要求操作的连续，中间可以有对其它变量的操作）。</p><p>这些操作加上一些规则描述出了 Java 程序中哪些内存访问操作在并发下才是安全的，这种定义相当严谨，但也是极为烦琐，实践起来更是无比麻烦，后来被简化为 read、write、lock 和 unlock 四种操作，但基础设计并未改变。</p><p>除了进行虚拟机开发的团队外，大概没有其他开发人员会以这种方式来思考并发问题，我们后面会用一个等效原则：<code>**先行发生原则，用来确定一个操作在并发环境下是否安全的**</code>。</p><h2 id="AVO"><a href="#AVO" class="headerlink" title="AVO"></a>AVO</h2><p>AVO 只是我自己对原子性（Atomicity）、可见性（Visibility）、有序性（Ordering）的简写，非官方的缩写或表示。</p><h3 id="原子性（Atomicity）"><a href="#原子性（Atomicity）" class="headerlink" title="原子性（Atomicity）"></a>原子性（Atomicity）</h3><p>并非一气呵成，岂能无懈可击。</p><p>提起原子性我们可以很容易的想到一个问题：为什么多线程 <code>i++</code> 会有并发问题？ </p><p>原因是虽然这只是一行代码，但真正的执行需要多个步骤，如读取原始值，原始值+1，写回新值，这就不是一个原子性的操作。</p><p>开发过程中我们经常需要更大范围的一个原子性保证，此时会通过 <code>synchronized</code> 同步代码块来保证。关于 <code>synchronized</code> 的锁升级会在后续介绍，它的原子性保证是通过生成高层次的字节码指令 <code>monitorenter</code> 和 <code>monitorexit</code> 来隐式地使用 JMM 中的 lock 和 unlock 指令来实现的。</p><h3 id="可见性（Visibility）"><a href="#可见性（Visibility）" class="headerlink" title="可见性（Visibility）"></a>可见性（Visibility）</h3><p>可见性（“你的改变我看不见”）就是指当一个线程修改了共享变量的值时，其他线程能够立即得知这个修改。</p><p>Java 中的 <code>volatile</code>、<code>final</code>、<code>synchronized</code> 都能保证可见性：</p><ul><li>volatile 的特殊规则保证了新值能立即同步到主内存，以及每次使用前立即从主内存刷新；</li><li>final 代表了这个对象的引用地址不会再发生变化，final 字段在构造器中一旦被初始化完成，在其他线程中就能看见值；</li><li>synchronized 的可见性是由“对一个变量执行 unlock 操作之前，必须先把此变量同步回主内存中（执行 store、write 操作）”这条规则保证的。</li></ul><h3 id="有序性（Ordering）"><a href="#有序性（Ordering）" class="headerlink" title="有序性（Ordering）"></a>有序性（Ordering）</h3><p>有序性（“不按套路出牌”）在文章开篇其实已经举过例子了，<code>volatile</code> 和 <code>synchronized</code>  能保证有序性：</p><ul><li>volatile 关键字本身就包含了禁止指令重排序的语义；</li><li>synchronized 是由“一个变量在同一个时刻只允许一条线程对其进行 lock 操作”这条规则保证的。</li></ul><h2 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h2><p>Java 内存模型为 volatile 专门定义了一些特殊的访问规则，使其修饰的对象具备两种特性：</p><ul><li>保证此变量对所有线程的可见性；</li><li>禁止指令重排序优化；</li></ul><p>举一个大名鼎鼎的双重检测式单例的例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Singleton</span> &#123;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-keyword">static</span> Singleton instance;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">if</span> (instance == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">synchronized</span> (Singleton.class) &#123;<br>                <span class="hljs-keyword">if</span> (instance == <span class="hljs-literal">null</span>) &#123;<br>                    instance = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();<br>                &#125;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> instance;<br>    &#125;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>            Singleton.getInstance();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>由于 <code>instance = new Singleton();</code> 不是原子操作，包括分配对象空间，初始化，instance 指向对象几步，如果 instance 指向对象先被执行，那么 main 方法中取得的单例就可能还没初始化好，导致空指针或意外错误。如果查看汇编文件：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-number">0x01a3de0f</span>: mov    $<span class="hljs-number">0x3375cdb0</span>,%esi     ;...beb0cd75 <span class="hljs-number">33</span><br>                                        ;   &#123;oop(<span class="hljs-string">&#x27;Singleton&#x27;</span>)&#125;<br><span class="hljs-number">0x01a3de14</span>: mov    %eax,<span class="hljs-number">0x150</span>(%esi)     ;..<span class="hljs-number">.89865001</span> <span class="hljs-number">0000</span><br><span class="hljs-number">0x01a3de1a</span>: shr    $<span class="hljs-number">0x9</span>,%esi            ;...c1ee09<br><span class="hljs-number">0x01a3de1d</span>: movb   $<span class="hljs-number">0x0</span>,<span class="hljs-number">0x1104800</span>(%esi) ;...c6860048 <span class="hljs-number">100100</span><br><span class="hljs-number">0x01a3de24</span>: lock addl $<span class="hljs-number">0x0</span>,(%esp)       ;...f0830424 <span class="hljs-number">00</span><br>                                        ;*putstatic instance<br>                                        ; - Singleton::getInstance@<span class="hljs-number">24</span><br></code></pre></td></tr></table></figure><p>有 volatile 修饰的变量，赋值后（前面 mov%eax，0x150(%esi) 这句便是赋值操作）多执行了一个“lock addl$0x0，(%esp)”操作，这个操作的作用相当于一个内存屏障，重排序时不能把后面的指令重排序到内存屏障之前的位置。而 lock 后面操作的作用是将本处理器的缓存写入了内存，该写入动作也会引起别的处理器或者别的内核无效化（Invalidate）其缓存，从而让前面 volatile 变量的修改对其他处理器立即可见。</p><h2 id="先行发生"><a href="#先行发生" class="headerlink" title="先行发生"></a>先行发生</h2><p>如果所有的线程安全都需要靠 volatile 和 synchronized 来保证，那么 Java 程序写起来也太复杂了。Java 语言中有一 个“先行发生”（Happens-Before）的原则。这个原则非常重要，依赖这个原则，我们可以通过几条简单规则一揽子解决并发环境下两个操作之间是否可能存在冲突的所有问题，而不需要陷入 JMM 苦涩难懂的定义之中。</p><p><a href="https://zhuwenjie0716.github.io/2026/04/15/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E4%B8%AD%E7%9A%84%E8%B0%81%E5%85%88%E8%B0%81%E5%90%8E/">分布式系统中的谁先谁后</a> 中也提到了基于 <a href="https://lamport.azurewebsites.net/pubs/time-clocks.pdf">lamport-Time, Clocks, and the Ordering of Events in a Distributed System</a> 的逻辑时钟和先行发生关系，分布式和多线程确实在很多地方有相似之处。</p><p>JMM 下有一些“天然的”先行发生关系，这些先行发生关系无须任何同步器协助就已经存在，可以在编码中直接使用。如果两个操作之间的关系不在此列，并且无法从下列规则推导出来，则它们就没有顺序性保障，虚拟机可以对它们随意地进行重排序。</p><ul><li>程序次序规则（Program Order Rule）：在一个线程内，按照<strong>控制流顺序</strong>，书写在前面的操作先行发生于书写在后面的操作。注意，这里说的是控制流顺序而不是程序代码顺序，因为要考虑分支、循环等结构。 </li><li>管程锁定规则（Monitor Lock Rule）：一个 unlock 操作先行发生于后面对同一个锁的 lock 操作。这 里必须强调的是“同一个锁”，而“后面”是指时间上的先后。</li><li>volatile 变量规则（Volatile Variable Rule）：对一个 volatile 变量的写操作先行发生于后面对这个变量的读操作，这里的“后面”同样是指时间上的先后。</li><li>线程启动规则（Thread Start Rule）：Thread 对象的 start() 方法先行发生于此线程的每一个动作。</li><li>线程终止规则（Thread Termination Rule）：线程中的所有操作都先行发生于对此线程的终止检测，我们可以通过 Thread::join() 方法是否结束、Thread::isAlive() 的返回值等手段检测线程是否已经终止执行。</li><li>线程中断规则（Thread Interruption Rule）：对线程 interrupt() 方法的调用先行发生于被中断线程的代码检测到中断事件的发生，可以通过 Thread::interrupted() 方法检测到是否有中断发生。</li><li>对象终结规则（Finalizer Rule）：一个对象的初始化完成（构造函数执行结束）先行发生于它的 finalize() 方法的开始。 </li><li>传递性（Transitivity）：如果操作 A 先行发生于操作 B，操作 B 先行发生于操作 C，那就可以得出操作 A 先行发生于操作 C 的结论。</li></ul><p>注意程序次序规则中的顺序是指控制流顺序，代码顺序由于存在重排序未必满足书写在前面的操作先行发生于书写在后面的操作。</p><p>使用这些规则去判定操作间是否具备顺序性，对于读写共享变量的操作来说，就是判断线程是否安全。</p><p>示例 1：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">value</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br><br>pubilc <span class="hljs-keyword">void</span> <span class="hljs-title function_">setValue</span><span class="hljs-params">(<span class="hljs-type">int</span> value)</span>&#123;<br><span class="hljs-built_in">this</span>.value = value;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getValue</span><span class="hljs-params">()</span>&#123;<br><span class="hljs-keyword">return</span> value;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果线程A先调用了 setValue(1)，随后线程B调用了 getValue()，那么线程B拿到的值是1吗？</p><p>根据先行发生原则，由于操作不在一个线程，所以不满足程序次序规则；由于不涉及同步块，所以不满足管程锁定规则；由于变量没有 volatile 修饰，所以不满足 volatile 变量规则；后面的线程相关规则也无从谈起。所以这两个操作没有先行发生的关系，即使线程A操作在线程B操作的时间之前，线程B操作的返回值也是不确定的。</p><p>示例1可以得出结论：<strong>一个操作“时间上的先发生”不代表这个操作会是“先行发生”</strong>。</p><p>示例2：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 以下操作在同一个线程中执行</span><br><span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><p>根据程序次序规则，“int i&#x3D;1” 的操作先行发生于 “int j&#x3D;2”，但是 “int j&#x3D;2” 的代码完全可能先被处理器执行，这并不影响先行发生原则的正确性， 因为我们在这条线程之中没有办法感知到这一点。</p><p>由于这两行代码没有依赖关系，所以处理器先执行哪句都是不影响程序执行结果的，happens-before 描述了<strong>程序行为必须呈现出的效果</strong>，在单线程内，它只能保证后续在使用到 i 时它的值一定是1。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br><span class="hljs-type">int</span> <span class="hljs-variable">j</span> <span class="hljs-operator">=</span> <span class="hljs-number">2</span>;<br>System.out.println(i);   <span class="hljs-comment">// 这里会打印什么？</span><br></code></pre></td></tr></table></figure><p>无论前面的代码物理执行顺序如何，再最终打印 i 时一定会输出1，因为它依赖于 <code>int i=1</code> 且操作之间满足先行发生规则。</p><p>示例2可以得出结论：<strong>即使一个操作“先行发生”，它也未必是“时间上的先发生”</strong>。</p><p><strong>时间先后顺序与先行发生原则之间基本没有因果关系， 所以我们衡量并发安全问题的时候不要受时间顺序的干扰，一切必须以先行发生原则为准。</strong></p><h2 id="线程模型"><a href="#线程模型" class="headerlink" title="线程模型"></a>线程模型</h2><p>线程是比进程更轻量级的调度执行单位，线程的引入，可以把一个进程的资源分配和执行调度分开，各个线程既可以共享进程资源（内存地址、文件I&#x2F;O等），又可以独立调度。这里首先以一个通用的应用程序的角度来看看线程是如何实现的。</p><p>实现线程主要有三种方式：使用内核线程实现（1：1实现），使用用户线程实现（1：N 实现）， 使用用户线程加轻量级进程混合实现（N：M 实现）。</p><h3 id="内核线程实现"><a href="#内核线程实现" class="headerlink" title="内核线程实现"></a>内核线程实现</h3><p>使用内核线程实现的方式也被称为 1：1 实现。内核线程（Kernel-Level Thread，KLT）就是直接由操作系统内核（Kernel，下称内核）支持的线程，这种线程由内核来完成线程切换，内核通过操纵调度器（Scheduler）对线程进行调度，并负责将线程的任务映射到各个处理器上。</p><p>程序一般不会直接使用内核线程，而是使用内核线程的一种高级接口——轻量级进程（Light Weight Process，LWP），轻量级进程就是我们通常意义上所讲的线程，每个轻量级进程都由一个内核线程支持，这就是一对一的线程模型。</p><p><img src="/img/image-20260525101219362.png" alt="image-20260525101219362"></p><p>由于每个轻量级进程对应一个内核线程，那么即使某个线程由于系统调用被阻塞也不影响进程其他线程的继续执行。但也正是由于其是内核线程实现的，所以各种线程操作，如创建、析构及同步，都需要进行系统调用，会发生用户态到内核态的状态切换，有一定的性能损耗。</p><p>这种实现方式也导致系统能支持轻量级进程的数量是有限的，因为每一个都需要消耗一定的内核资源（如内核线程的栈空间）。</p><p>那么为什么说内核线程调度的性能损耗比较大呢？</p><p>程序是数据与代码的组合体，代码执行时还必须要有上下文数据（内存、缓存、寄存器中各个值）的支撑。当线程发起系统调用，通过中断切换进入内核态，如果系统调用需要阻塞，就会把状态修改为阻塞，放入等待队列中，处理器切换其它线程执行。</p><p>当中断发生，尤其是阻塞涉及到线程切换时，从线程A切换到线程B去执行之前，操作系统首先要把线程A的上下文数据妥善保管好，然后把寄存器、内存分页等恢复到线程B挂起时候的状态，这样线程B被重新激活后才能仿佛从来没有被挂起过。这种保护和恢复现场的工作，免不了涉及一系列数据在各种寄存器、缓存中的来回拷贝。</p><h3 id="用户线程实现"><a href="#用户线程实现" class="headerlink" title="用户线程实现"></a>用户线程实现</h3><p>使用用户线程（User Thread，UT）实现的方式被称为 1：N 实现。</p><p><img src="/img/image-20260525101722931.png" alt="image-20260525101722931"></p><p>用户线程的建立、同步、销毁和调度完全在用户态中完成，不需要内核的帮助。如果程序实现得当，这种线程不需要切换到内核态，因此操作可以是非常快速且低消耗的，也能够支持规模更大的线程数量。</p><p>用户线程的优势在于不需要系统内核支援，劣势也在于没有系统内核的支援，所有的线程操作都需要由用户程序自己去处理。线程的创建、销毁、切换和调度都是用户必须考虑的问题，而且由于操作系统只把处理器资源分配到进程，那诸如“阻塞如何处理”“、多处理器系统中如何将线程映射到其他处理器上”这类问题解决起来将会异常困难，甚至有些是不可能实现的。</p><p>Java、Ruby 等语言都曾经使 用过用户线程，最终又都放弃了使用它。但是近年来许多新的、以高并发为卖点的编程语言又普遍支持了用户线程，譬如 Golang、Erlang 等，使得用户线程的使用率有所回升。</p><h3 id="混合实现"><a href="#混合实现" class="headerlink" title="混合实现"></a>混合实现</h3><p>线程除了依赖内核线程实现和完全由用户程序自己实现之外，还有一种将内核线程与用户线程一 起使用的实现方式，被称为 N：M 实现。在这种混合实现下，既存在用户线程，也存在轻量级进程。 用户线程还是完全建立在用户空间中，因此用户线程的创建、切换、析构等操作依然廉价，并且可以支持大规模的用户线程并发。</p><p><img src="/img/image-20260525102029382.png" alt="image-20260525102029382"></p><p>用户线程还是完全建立在用户空间中，因此用户线程的创建、切换、析构等操作依然廉价，并且可以支持大规模的用户线程并发。而操作系统支持的轻量级进程则作为用户线程和内核线程之间的桥梁， 这样可以使用内核提供的线程调度功能及处理器映射，并且用户线程的系统调用要通过轻量级进程来完成，这大大降低了整个进程被完全阻塞的风险（纯用户线程中，某个线程发起了系统调用，整个进程会陷入内核态阻塞，其它的用户线程也会被暂停，混合实现中只有一个 LWP 的用户线程会受到影响）。</p><h3 id="Java-线程模型"><a href="#Java-线程模型" class="headerlink" title="Java 线程模型"></a>Java 线程模型</h3><p>Java 线程如何实现并不受《Java虚拟机规范》的约束，这是一个与具体虚拟机相关的话题。</p><p>从 JDK1.3 起，<strong>“主流”平台上的“主流”商用 Java 虚拟机的线程模型普遍都被替换为基于操作系统原生线程模型来实现</strong>，即采用 1：1 的线程模型。</p><p>以 HotSpot 为例，它的每一个 Java 线程都是直接映射到一个操作系统原生线程来实现的，而且中间没有额外的间接结构，所以HotSpot 自己是不会去干涉线程调度的（可以设置线程优先级给操作系统提供调度建议），全权交给底下的操作系统去处理，所以何时冻结或唤醒线程、该给线程分配多少处理器执行时间、该把线程安排给哪个处理器核心去执行等，都是由操作系统完成的，也都是由操作系统全权决定的。</p><p>既然在开始强调了主流，那自然也有非主流的实现。在 Solaris 平台的 HotSpot 虚拟机，由于操作系统的线程特性本来就可以同时支持 1：1（BoundThreads） 及 N：M （LWPSynchronization）的线程模型，因此 Solaris 版的 HotSpot 也对应提供了两个平台专有的虚拟机参数，即 -XX： +UseLWPSynchronization（默认值）和 -XX：+UseBoundThreads 来明确指定虚拟机使用哪种线程模型。</p><p>线程模型只对线程的并发规模和操作成本产生影响，对 Java 程序的编码和运行过程来说，这些差异都是完全透明的。</p><p>Java 中的线程执行采用<strong>抢占式模型</strong>，每个线程由系统来分配执行时间，不会有一个线程导致整个进程甚至整个系统阻塞的问题。在两 个线程同时处于 Ready 状态时，优先级越高的线程越容易被系统选择执行。但<strong>线程优先级并不是一项稳定的调节手段</strong>，因为主流虚拟机上的 Java 线程是被映射到系统的原生线程上来实现的，所以线程调度最终还是由操作系统说了算。而且由于 Java 语言的优先级层次和操作系统的优先级层次未必能完全对应，会出现多个 Java 中的优先级对应操作系统同一个调度优先级的情况。</p><p>现代 B&#x2F;S 系统中一次对外部业务请求的响应，往往需要分布在不同机器上的大量服务共同协作来实现，这种服务细分的架构在减少单个服务复杂度、增加复用性的同时，也不可避免地增加了服务的数量，缩短了留给每个服务的响应时间。1：1 的内核线程模型是如今Java 虚拟机线程实现的主流选择，但是这种映射到操作系统上的线程天然的缺陷是切换、调度成本高昂，系统能容纳的线程数量也很有限。现在在每个请求本身的执行时间变得很短、数量变得很多的前提下， 用户线程切换的开销甚至可能会接近用于计算本身的开销，这就会造成严重的浪费。</p><p>于是 Java21 正式发布了虚拟线程，这个功能在 Java19&#x2F;20 是预览功能，默认关闭，需要添加参数<code>--enable-preview</code>启用，在很多其他语言中被称为协程、纤程、绿色线程、用户态线程等。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Java</tag>
      
      <tag>Happens-Before</tag>
      
      <tag>并发</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>JVM编译体系</title>
    <link href="/2026/05/24/JVM%E7%BC%96%E8%AF%91%E4%BD%93%E7%B3%BB/"/>
    <url>/2026/05/24/JVM%E7%BC%96%E8%AF%91%E4%BD%93%E7%B3%BB/</url>
    
    <content type="html"><![CDATA[<h1 id="JVM编译体系"><a href="#JVM编译体系" class="headerlink" title="JVM编译体系"></a>JVM编译体系</h1><blockquote><p>从计算机程序出现的第一天起，对效率的追逐就是程序员天生的坚定信仰，这个过程犹如一场没有终点、永不停歇的 F1 方程式竞赛，程序员是车手，技术平台则是在赛道上飞驰的赛车。</p></blockquote><p>Java 程序到执行一般要经过两个步骤，一个步骤是指从 .java 变为 .class 文件的过程，另一个是运行期把字节码转变成本地机器码的过程。随着提前编译器（AOT，Ahead Of Time Compiler）的发展，也有直接把程序编译成与目标机器指令集相关的二进制代码的过程。</p><p>第一个步骤就是常说的“前端编译”，常见的前端编译器有：JDK 的 Javac、Eclipse JDT 中的增量式编译器（ECJ）。</p><h2 id="前端编译"><a href="#前端编译" class="headerlink" title="前端编译"></a>前端编译</h2><p>前端编译器对代码的运行效率几乎没有任何优化措施可言，哪怕是编译器真的采取了优化措施也不会产生什么实质的效果。</p><p>Java 虚拟机设计团队选择把对性能的优化全部集中到运行期的即时编译器中，这样可以让那些即使不是由 javac 编译出的 .class 文件也享受到优化所带来的红利。</p><p>如果把“优化”的定义放宽，包括开发人员进行开发时的便捷程度，那么编译器实现的“语法糖”带来的新语言特性，也算是一种优化吧。</p><p>随着 JDK 的发展，引入了越来越多的语法糖，这里介绍一下非常重要的泛型。</p><h3 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h3><p>Java 开发者几乎都会接触到泛型的使用，首先可以引入一个结论：运行时的 <code>List&lt;String&gt;</code> 和 <code>List&lt;Integer&gt;</code> 其实是同一种类型。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    List&lt;String&gt; strings = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    List&lt;Integer&gt; integers = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>    System.out.println(strings.getClass().equals(integers.getClass()));<br>&#125;<br></code></pre></td></tr></table></figure><p>Java 选择的泛型实现方式叫作“类型擦除式泛型”（Type Erasure Generics），在编译后的字节码文件中，全部泛型都被替换为原来的裸类型（Raw Type），并且在相应的地方插入了强制转型代码。</p><p>“泛型擦除”的概念可以通过下面的代码示例来理解，这是编写的 Java 代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>Map&lt;String, String&gt; map = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;String, String&gt;();<br>map.put(<span class="hljs-string">&quot;hello&quot;</span>, <span class="hljs-string">&quot;你好&quot;</span>);<br>map.put(<span class="hljs-string">&quot;how are you?&quot;</span>, <span class="hljs-string">&quot;吃了没？&quot;</span>);<br>System.out.println(map.get(<span class="hljs-string">&quot;hello&quot;</span>));<br>System.out.println(map.get(<span class="hljs-string">&quot;how are you?&quot;</span>));<br>&#125;<br></code></pre></td></tr></table></figure><p>对这段代码编译后生成的 class 文件反编译之后，生成的代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><span class="hljs-type">Map</span> <span class="hljs-variable">map</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>();<br>map.put(<span class="hljs-string">&quot;hello&quot;</span>, <span class="hljs-string">&quot;你好&quot;</span>);<br>map.put(<span class="hljs-string">&quot;how are you?&quot;</span>, <span class="hljs-string">&quot;吃了没？&quot;</span>);<br>System.out.println((String) map.get(<span class="hljs-string">&quot;hello&quot;</span>));<br>System.out.println((String) map.get(<span class="hljs-string">&quot;how are you?&quot;</span>));<br>&#125;<br></code></pre></td></tr></table></figure><p>由此我们也知道了为什么 int、long 这样的基础类型无法作为泛型，因为泛型擦除后统一为 Object，int、long 无法转型为 Object。</p><p>这样的实现方式带来了很多的缺点：</p><ul><li>这个决定后面导致了无数构造包装类和装箱、拆箱的开销，成为 Java 泛型慢的重要原因；</li><li>由于泛型擦除，会导致在运行时无法取得泛型类型；</li><li>正常的方法重载只需要参数类型不一样即可，由于类型擦除，不同泛型对象的方法会被认为是相同的方法。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 编译失败 参数 List&lt;String&gt;  类型和 List&lt;Integer&gt; 一样，被认为重复方法</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">GenericTypes</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">method</span><span class="hljs-params">(List&lt;String&gt; list)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;invoke method(List&lt;String&gt; list)&quot;</span>);<br>    &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">method</span><span class="hljs-params">(List&lt;Integer&gt; list)</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;invoke method(List&lt;Integer&gt; list)&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>Java5.0 和 C#2.0 都会语言增加了泛型的特性，不过实现的方式截然不同，在推出后被对比的结论是 Java 的泛型直到今天依然作为Java 语言不如 C# 语言好用的“铁证”被众人嘲讽。但 Java 选择这样的泛型实现，是出于当时语言现状的权衡，而不是语言先进性或者设计者水平不如 C# 之类的原因。</p><h3 id="自动装箱、拆箱"><a href="#自动装箱、拆箱" class="headerlink" title="自动装箱、拆箱"></a>自动装箱、拆箱</h3><p>就纯技术的角度而论，自动装箱、自动拆箱与遍历循环（for-each循环）这些语法糖，无论是实现复杂度上还是其中蕴含的思想上都不能和泛型相提并论，但这些在日常使用中是最多的。</p><p>以下面一段代码看这些语法糖的实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    List&lt;Integer&gt; list = Arrays.asList(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>);<br>    <span class="hljs-type">int</span> <span class="hljs-variable">sum</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i : list) &#123;<br>        sum += i;<br>    &#125;<br>    System.out.println(sum);<br>&#125;<br></code></pre></td></tr></table></figure><p>下面是代码编译后又反编译后的结果：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;    <br><span class="hljs-type">List</span> <span class="hljs-variable">list</span> <span class="hljs-operator">=</span> Arrays.asList(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Integer</span>[] &#123;<br>        Integer.valueOf(<span class="hljs-number">1</span>),<br>        Integer.valueOf(<span class="hljs-number">2</span>),<br>        Integer.valueOf(<span class="hljs-number">3</span>),<br>        Integer.valueOf(<span class="hljs-number">4</span>) &#125;);<br>    <span class="hljs-type">int</span> <span class="hljs-variable">sum</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">Iterator</span> <span class="hljs-variable">localIterator</span> <span class="hljs-operator">=</span> list.iterator(); localIterator.hasNext(); ) &#123;<br>        <span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> ((Integer)localIterator.next()).intValue();<br>        sum += i;<br>    &#125;<br>    System.out.println(sum);<br>&#125;<br></code></pre></td></tr></table></figure><p>这段代码一共包含了泛型、自动装箱、自动拆箱、遍历循环与变长参数 5 种语法糖，泛型就不必说了，</p><ul><li>自动装箱、拆箱在编译之后被转化成了对应的包装和还原方法，如本例中的 <code>Integer.valueOf()</code> 与 <code>Integer.intValue()</code> 方法;</li><li>而遍历循环则是把代码还原成了迭代器的实现，这也是为何遍历循环需要被遍历的类实现 <code>Iterable</code> 接口的原因；</li><li>变长参数，它在调用的时候变成了一个数组类型的参数，在变长参数出现之前，程序员的确也就是使用数组来完成类似功能的。</li></ul><p>这些语法糖虽然看起来很简单，但出于 <code>Integer.valueOf()</code> 方法共享常见对象的机制，以及包装类 的“&#x3D;&#x3D;”运算在不遇到算术运算的情况下不会自动拆箱，还有 equals() 方法不处理数据转型的关系， 建议在实际编码中尽量避免这样使用自动装箱与拆箱。</p><h3 id="条件编译"><a href="#条件编译" class="headerlink" title="条件编译"></a>条件编译</h3><p>条件编译是指：编译代码时，<strong>根据预设条件，选择性编译 &#x2F; 丢弃部分代码</strong>，不满足条件的代码直接不会生成字节码，运行时不存在。Java 语言当然也可以进行条件编译，方法就是使用条件为常量的 if 语句。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <span class="hljs-keyword">if</span> (<span class="hljs-literal">false</span>) &#123;<br>        System.out.println(<span class="hljs-string">&quot;这是不可能走到的路径&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在上面的这个代码示例中，不会生成相应的字节码，因为判断条件为 false，不可能进入。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <span class="hljs-keyword">while</span> (<span class="hljs-literal">false</span>) &#123;<br>        System.out.println(<span class="hljs-number">1</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果是上面这种代码，语法上是合理的，但 javac 会直接被拒绝编译。</p><p>关于前端编译在处理语法糖时做的工作，可以通过 .class 反编译后的代码进行比对，包括 try with resources、switch 支持等。</p><h2 id="后端编译"><a href="#后端编译" class="headerlink" title="后端编译"></a>后端编译</h2><p>如果把字节码看作是程序语言的一种中间表示形式（Intermediate Representation，IR）的话， 那编译器无论在何时、在何种状态下把 Class 文件转换成与本地基础设施（硬件指令集、操作系统）相关的二进制机器码，它都可以视为整个编译过程的后端。</p><h3 id="即时编译"><a href="#即时编译" class="headerlink" title="即时编译"></a>即时编译</h3><p>主流的即时编译器有：HotSpot 虚拟机的 C1、C2 编译器，Graal 编译器。</p><p>目前主流的两款商用 Java 虚拟机（HotSpot、OpenJ9）里，Java 程序最初都是通过解释器（Interpreter）进行解释执行的，当虚拟机发现某个方法或代码块的运行特别频繁，就会把这些代码认定为“热点代码”（Hot Spot Code），为了提高热点代码的执行效率，在运行时，虚拟机将会把这些代码编译成本地机器码，并以各种手段尽可能地进行代码优化，运行时完成这个任务的后端编译器被称为即时编译器。</p><p>即使编译器的作用就是：<strong>把运行频繁的代码译为本地机器码，并尽可能地进行代码优化</strong>。</p><p>主流的 Java 虚拟机中都是解释器和编译器共存的，结构如下：</p><p><img src="/img/image-20260522105153855.png" alt="image-20260522105153855"></p><p>当程序需要迅速启动和执行的时候，解释器可以首先发挥作用，省去编译的时间，立即运行。当程序启动后，随着时间的推移，编译器逐渐发挥作用，把越来越多的代码编译成本地代码，这样可以减少解释器的中间损耗，获得更高的执行效率。</p><p>图中的 Client Compiler 即为 C1 编译器，Server Compiler 为 C2 编译器，有些资料中提到过的打算替代 C2 的 Graal 编译器，目前更多是作为实验性编译器。</p><h4 id="分层编译"><a href="#分层编译" class="headerlink" title="分层编译"></a>分层编译</h4><p>如果在终端执行这样的指令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ java -version<br>java version <span class="hljs-string">&quot;11.0.14&quot;</span> 2022-01-18 LTS<br>Java(TM) SE Runtime Environment 18.9 (build 11.0.14+8-LTS-263)<br>Java HotSpot(TM) 64-Bit Server VM 18.9 (build 11.0.14+8-LTS-263, mixed mode)<br><br>$ java -Xint -version<br>java version <span class="hljs-string">&quot;11.0.14&quot;</span> 2022-01-18 LTS<br>Java(TM) SE Runtime Environment 18.9 (build 11.0.14+8-LTS-263)<br>Java HotSpot(TM) 64-Bit Server VM 18.9 (build 11.0.14+8-LTS-263, interpreted mode)<br></code></pre></td></tr></table></figure><p>注意最后的输出 mixed mode 和 interpreted mode，实际上就是指“解释加多层编译混合模式”还是“解释模式”，当然也可以调整为“编译模式”，但是解释器仍然要在编译无法进行的情况下介入执行过程。</p><p>即时编译器编译本地代码需要占用程序运行时间，通常要编译出优化程度越高的代码，所花费的时间便会越长；而且想要编译出优化程度更高的代码，解释器可能还要替编译器收集性能监控信息，这对解释执行阶段的速度也有所影响。</p><p>为了在程序启动响应速度与运行效率之间达到最佳平衡，HotSpot 虚拟机在编译子系统中加入了<strong>分层编译</strong>的功能，其<strong>核心概念就是根据编译器编译、优化的规模与耗时，划分出不同的编译层次</strong>。</p><ol><li>程序纯解释执行，并且解释器不开启性能监控功能（Profiling）。</li><li>使用客户端编译器将字节码编译为本地代码来运行，进行简单可靠的稳定优化，不开启性能监控功能。 </li><li>仍然使用客户端编译器执行，仅开启方法及回边次数统计等有限的性能监控功能。</li><li>仍然使用客户端编译器执行，开启全部性能监控，除了上层的统计信息外，还会收集如分支跳转、虚方法调用版本等全部的统计信息。</li><li>使用服务端编译器将字节码编译为本地代码，相比起客户端编译器，服务端编译器会启用更多编译耗时更长的优化，还会根据性能监控信息进行一些不可靠的激进优化。</li></ol><p>以上层次并不是固定不变的，根据不同的运行参数和版本，虚拟机可以调整分层的数量。</p><p>实施分层编译后，解释器、客户端编译器和服务端编译器就会同时工作，<strong>热点代码可能会被多次编译，用客户端编译器获取更高的编译速度，用服务端编译器来获取更好的编译质量</strong>，在解释执行的时候也无须额外承担收集性能监控信息的任务，而在服务端编译器采用高复杂度的优化算法时，客 户端编译器可先采用简单优化来为它争取更多的编译时间。</p><h4 id="触发条件"><a href="#触发条件" class="headerlink" title="触发条件"></a>触发条件</h4><p>即时编译器会优化运行频繁的代码，那什么算频繁运行呢？目前主流的热点 探测判定方式有两种，分别是： </p><ul><li>基于采样的热点探测（Sample Based Hot Spot Code Detection）。采用这种方法的虚拟机会周期性地检查各个线程的调用栈顶，如果发现某个（或某些）方法经常出现在栈顶，那这个方法就是“热点方法”。基于采样的热点探测的好处是实现简单高效，还可以很容易地获取方法调用关系（将调用堆栈展开即可），缺点是很难精确地确认一个方法的热度，容易因为受到线程阻塞或别的外界因素的影响而扰乱热点探测。</li><li>基于计数器的热点探测（Counter Based Hot Spot Code Detection）。采用这种方法的虚拟机会为每个方法（甚至是代码块）建立计数器，统计方法的执行次数，如果执行次数超过一定的阈值就认为它是“热点方法”。这种统计方法实现起来要麻烦一些，需要为每个方法建立并维护计数器，而且不能直接获取到方法的调用关系。但是它的统计结果相对来说更加精确严谨。</li></ul><p>在 HotSpot 虚拟机中使用的是第二种基于计数器的热点探测方法，为了实现热点计数，HotSpot 为每个方法准备了两类计数器：方法调用计数器（Invocation Counter）和回边计数器（Back Edge Counter，“回边”的意思 就是指在循环边界往回跳转）。当虚拟机运行参数确定的前提下，这两个计数器都有一个明确的阈值，计数器阈值一旦溢出，就会触发即时编译。</p><p>方法调用计数器阈值在客户端模式下是 1500 次，在服务端模式下是 10000 次，可以通过虚拟机参数 -XX:CompileThreshold 来人为设定。它统计的是一段时间内的方法调用的逻辑次数，如果这段时间内未达到，则会缩减热度阈值（半衰期），可以通过参数 -XX: UseCounterDecay 来关闭热度衰减，也可以调整 -XX:CounterHalfLifeTime 修改半衰期时间（秒）。</p><p>回边计数器主要统计一个方法中循环体代码执行的次数，它没有半衰期，且计算逻辑相对复杂：</p><ul><li>虚拟机运行在客户端模式下，回边计数器阈值计算公式为：方法调用计数器阈值（-XX:CompileThreshold）乘以 OSR 比率（-XX:OnStackReplacePercentage）除以100。其中-XX： OnStackReplacePercentage 默认值为 933，如果都取默认值，那客户端模式虚拟机的回边计数器的阈值为 13995。 </li><li>虚拟机运行在服务端模式下，回边计数器阈值的计算公式为：方法调用计数器阈值（-XX: CompileThreshold）乘以（OSR比率（-XX:OnStackReplacePercentage）减去解释器监控比率（-XX: InterpreterProfilePercentage）的差值）除以 100。其中 -XX：OnStackReplacePercentage 默认值为 140， XX:InterpreterProfilePercentage 默认值为 33，如果都取默认值，那服务端模式虚拟机回边计数器的阈值为 10700。</li></ul><p>当解释器遇到一条回边指令时，会先查找将要执行的代码片段是否有已经编译好的版本，如果有的话，它将会优先执行已编译的代码，否则就把回边计数器的值加一，然后判断方法调用计数器与回 边计数器值之和是否超过回边计数器的阈值。当超过阈值的时候，将会提交一个栈上替换编译请求， 并且把回边计数器的值稍微降低一些，以便继续在解释器中执行循环，等待编译器输出编译结果，流程如图：</p><p><img src="/img/image-20260522111712898.png" alt="image-20260522111712898"></p><p>验证 JIT 即时编译可以参考：<a href="https://www.cnblogs.com/mthoutai/p/19201690">JVM的即时编译JIT的介绍 - 实践</a>。</p><h3 id="提前编译"><a href="#提前编译" class="headerlink" title="提前编译"></a>提前编译</h3><p>提前编译器有：JDK 的 Jaotc。</p><p>因为 Java 的一个核心优势是平台中立性，其宣传口号是“一次编译，到处运行”，与平台相关的提前编译在理念上就是有冲突的，所以期间沉寂了很久。</p><blockquote><p>在某些领域、某些人眼里，只要能获得更好的执行性能，什么平台中立性、 字节膨胀、动态扩展，一切皆可舍弃，唯一的问题就只有“提前编译真的会是获得更高性能的银弹吗？”</p></blockquote><p>提前编译产品分为两大类：</p><ul><li>像 C、C++ 那样，在程序运行之前把程序代码编译成机器码的静态翻译工作；</li><li>把原本即时编译器在运行时要做的编译工作提前做好并保存下来，下次运行到这些代码（譬如公共库代码在被同一台机器其他 Java 进程使用）时直接把它加载进来使用。</li></ul><p>第一类直指即时编译的最大弱点，因为无论即时编译器如何优化，即时编译消耗的时间都是原本可用于程序运行的时间，消耗的运算资源都是原本可用于程序运行的资源，这个约束从未减弱，更不会消失。这样的提前编译可以充分的进行程序优化以获得更好的运行时性能，反正做镜像阶段慢一点并没有什么大影响。</p><p>第二类也可以叫动态提前编译或即时编译缓存，本质是给即时编译器做缓存加速，去改善 Java 程序的启动时间，以及需要一段时间预热后才能到达最高性能的问题，目前已被主流商用 JDK 支持。</p><p>Jaotc 提前编译器是一个基于Graal 编译器实现的新工具，目的是让用户可以针对目标机器，为应用程序进行提前编译。HotSpot 运行时可以直接加载这些编译的结果，实现加快程序启动速度，减少程序达到全速运行状态所需时间的目的。这里面确实有比较大的优化价值，试想一下，各种 Java 应用最起码会用到 Java 的标准类库，如 java.base 等模块，如果能够将这个类库提前编译好，并进行比较高质量的优化，显然能够节约不少应用运行时的编译成本。</p><p>提前编译的优点毋庸置疑，但实现起来很有难度，就算不提平台无关性、字节膨胀，编译不仅与目标机器相关，甚至还必须与虚拟机的运行时参数绑定，例如不同垃圾收集器生成的内存屏障代码，如果要提前编译就需要这些工作也平移过去，新版的 Graal AOT 已大幅弱化该限制。</p><p><strong>尽管即时编译在时间和运算资源方面的劣势是无法忽视的，但其依然有自己的优势</strong>。</p><ul><li>性能分析制导优化：即时编译会通过包括但不限于计数器的性能监控信息，集中优化和分配更好的资源（分支预测、寄存器、缓存等）。例如某个分支，只有运行时才能看出执行的偏好性；</li><li>激进预测性优化：如果性能监控信息能够支持它做出一些正确的可能性很大但无法保证绝对正确的预测判断，就已经可以大胆地按照高概率的假设进行优化，万一真的走到罕见分支上，大不了退回到低级编译器甚至解释器上去执行，并不会出现无法挽救的后果；</li><li>链接时优化：Java语言天生就是动态链接的，一个个 Class 文件在运行期被加载到虚拟机内存当中，然后在即时编译器里产生优化后的本地代码，而 C&#x2F;C++ 主程序与动态链接库的代码在它们编译时是完全独立的，两者各自编译、优化自己的代码，跨越动态链接库的方法内联几乎不存在。</li></ul><p>提前编译有它的应用场景，也有它的弱项与不足，相信未来很长一段时间内，即时编译和提前编译都会是 Java 后端编译技术的共同主角。</p><h3 id="编译优化"><a href="#编译优化" class="headerlink" title="编译优化"></a>编译优化</h3><p>[OpenJDK Wiki](<a href="https://wiki.openjdk.org/spaces/HotSpot/pages/11829287/PerformanceTacticIndex">PerformanceTacticIndex - HotSpot - OpenJDK Wiki</a>) 里展示了相对比较全面的、即时编译器中采用的优化技术列表，这里介绍几个典型的优化方面：</p><ul><li>方法内联；</li><li>逃逸分析；</li><li>公共表达式消除；</li><li>数组边界检查消除。</li></ul><p><strong>方法内联</strong>的优化行为理解起来是很简单的，不过就是把目标方法的代码原封不动地“复制”到发起调用的方法之中，避免发生真实的方法调用而已。但实现起来颇有难度，由于 Java 的多态特性，只有在运行时才知道某些方法真正的实现，编译器很难在编译时得出绝对准确的结论。</p><p>Java 虚拟机引入了一种名为类型继承关系分析（Class Hierarchy Analysis，CHA）的技术，这是整个应用程序范围内的类型分析技术，用于确定在目前已加载的类中，某个接口是否有多于一种的实现、某个类是否存在子类、某个子类是否覆盖了父类的某个虚方法等信息。</p><p><strong>逃逸分析</strong>（Escape Analysis）是目前 Java 虚拟机中比较前沿的优化技术，它与类型继承关系分析一 样，并不是直接优化代码的手段，而是为其他优化措施提供依据的分析技术。逃逸分析的基本原理是：分析对象动态作用域，是否可能从方法、线程中传递（逃逸）出去。有了逃逸分析，编译器可以做出如下优化：</p><ul><li>栈上分配（Stack Allocations）：一方面，堆内存是线程共享的，涉及分配对象没有在栈上快，另一方面，堆内存的回收需要通过 GC，而栈内存随栈帧出栈而销毁。一般来讲，完全不会逃逸的局部对象的比例还是很大的。</li><li>标量替换（Scalar Replacement）：如果把一个Java对象拆散，根据程序访问的情况，将其用到的成员变量恢复为原始类型来访问，这个过程就称为标量替换。假如逃逸分析能够证明一个对象不会被方法外部 访问，并且这个对象可以被拆散，那么程序真正执行的时候将可能不去创建这个对象，而改为直接创建它的若干个被这个方法使用的成员变量来代替。例如新建了一个对象 user，用到了 age 属性，那么可能会直接定义 int age 代替创建 user 对象使用。</li><li>同步消除（Synchronization Elimination）：线程同步本身是一个相对耗时的过程，如果逃逸分析能够确定一个变量不会逃逸出线程，无法被其他线程访问，那么这个变量的读写肯定就不会有竞争， 对这个变量实施的同步措施也就可以安全地消除掉。例如有时候的<code>synchronized</code>代码块锁。</li></ul><p><strong>公共子表达式消除</strong>是一项非常经典的、普遍应用于各种编译器的优化技术，它的含义是：如果一 个表达式之前已经被计算过了，并且从先前的计算到现在计算这个表达式涉及到的所有变量的值都没有发生变化，那么没有必要花时间再对它重新进行计算。在 IDE 中， .var 赋值时会进行提醒。</p><p><strong>数组边界检查消除</strong>（Array Bounds Checking Elimination）是即时编译器中的一项语言相关的经典优化技术。Java 中数组下标不能越界，也就是每次访问都隐含着判断 i &gt;&#x3D; 0 &amp;&amp; i &lt; array.length()，对于拥有大量数组访问的程序代码，这必定是一种性能负担。如果访问使用常量下标或访问发生在循环之中，并且使用循环变量来进行数组的访问。假如编译器通过数据流分析就可以判定循环变量的取值范围永远在区间[0，array.length) 之内，那么在循环中就可以把整个数组的上下界检查消除掉，这可以节省很多次的条件判断操作。</p><h2 id="配置参数"><a href="#配置参数" class="headerlink" title="配置参数"></a>配置参数</h2><p>JIT 编译器基础参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:+UseCompiler</code></td><td>启用（默认）</td><td>启用 JIT 编译器。禁用时完全由解释器执行。</td></tr><tr><td><code>-XX:+TieredCompilation</code></td><td>JDK 8+ 启用</td><td>启用分层编译，结合 C1（快速编译）和 C2（深度优化）。</td></tr><tr><td><code>-XX:-TieredCompilation</code></td><td>–</td><td>关闭分层编译，回退到传统的 C2 模式。</td></tr><tr><td><code>-XX:TieredStopAtLevel=&lt;n&gt;</code></td><td><code>4</code></td><td>分层编译最高级别：1&#x3D;纯C1无优化，2&#x3D;C1部分优化，3&#x3D;C1含分析，4&#x3D;C2优化。</td></tr><tr><td><code>-XX:+PrintCompilation</code></td><td>关闭</td><td>打印方法编译日志（时间戳、编译层级、方法名等）。</td></tr><tr><td><code>-XX:+CITime</code></td><td>关闭</td><td>打印 JIT 编译耗时统计。</td></tr><tr><td><code>-XX:+PrintInlining</code></td><td>关闭</td><td>打印方法内联信息，诊断内联决策。</td></tr></tbody></table><p>方法计数与触发阈值参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:CompileThreshold=&lt;N&gt;</code></td><td><code>10000</code> (C2) &#x2F; <code>1500</code> (C1)</td><td>方法被编译前所需调用次数（非分层编译模式下）。分层编译下此参数失效。</td></tr><tr><td><code>-XX:Tier2CompileThreshold=&lt;N&gt;</code></td><td><code>0</code>（自动计算）</td><td>第2层编译阈值（从解释或第1层提升）。</td></tr><tr><td><code>-XX:Tier3CompileThreshold=&lt;N&gt;</code></td><td><code>2000</code></td><td>第3层编译阈值（C1 带 profiling）。</td></tr><tr><td><code>-XX:Tier4CompileThreshold=&lt;N&gt;</code></td><td><code>4000</code></td><td>第4层编译阈值（C2 优化编译）。</td></tr><tr><td><code>-XX:OnStackReplacePercentage=&lt;N&gt;</code></td><td><code>140</code> (C2) &#x2F; <code>933</code> (C1)</td><td>控制 OSR（栈上替换）触发阈值，与循环回边次数相关。</td></tr><tr><td><code>-XX:CompileThresholdScaling=&lt;float&gt;</code></td><td><code>1.0</code></td><td>缩放所有编译阈值，例如 <code>0.5</code> 使阈值减半。</td></tr><tr><td><code>-XX:-UseCounterDecay</code></td><td>使用衰减</td><td>关闭方法调用计数器衰减，防止长期运行的方法热度降低。</td></tr><tr><td><code>-XX:CounterHalfLifeTime=&lt;seconds&gt;</code></td><td><code>30</code></td><td>计数器半衰期（秒），用于热度衰减计算。</td></tr></tbody></table><p>编译器选择（C1 &#x2F; C2 &#x2F; Graal JIT）：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:+UseC1</code></td><td>分层编译时启用</td><td>强制使用 C1 编译器（仅限客户端模式或分层编译组合）。</td></tr><tr><td><code>-XX:+UseC2</code></td><td>默认启用</td><td>强制使用 C2 编译器（服务端模式）。</td></tr><tr><td><code>-XX:+UseGraalJIT</code></td><td>关闭（实验性）</td><td>使用 Graal 编译器作为 JIT（需解锁实验性参数 <code>-XX:+UnlockExperimentalVMOptions</code>）。</td></tr><tr><td><code>-XX:+UseJVMCICompiler</code></td><td>关闭</td><td>启用 JVMCI 接口，允许 Graal 等编译器替换 C2。</td></tr><tr><td><code>-XX:+EagerJVMCI</code></td><td>关闭</td><td>提前初始化 JVMCI 编译器（减少首次编译延迟）。</td></tr></tbody></table><p>提前编译（AOT）参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:+UseAOT</code></td><td>JDK 9-14 支持</td><td>启用 AOT 编译生成的共享库（如 <code>jaotc</code> 生成的 <code>.so</code>）。</td></tr><tr><td><code>-XX:AOTLibrary=&lt;path&gt;</code></td><td>–</td><td>指定 AOT 库路径（多个用冒号分隔）。</td></tr><tr><td><code>-XX:+PrintAOT</code></td><td>关闭</td><td>打印 AOT 库加载和使用信息。</td></tr><tr><td><code>-XX:+UseAOTStrictLoading</code></td><td>关闭</td><td>当 AOT 库版本不匹配时导致 JVM 退出（而非忽略该库）。</td></tr></tbody></table><blockquote><p><strong>注意</strong>：Oracle JDK 15 起移除了内置的 AOT（<code>jaotc</code>），GraalVM Native Image 是主流替代方案，其参数完全不同（通过 <code>native-image</code> 工具及配置文件）。</p></blockquote><p>代码缓存（CodeCache）参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:ReservedCodeCacheSize=&lt;size&gt;</code></td><td>240MB (取决于 JVM 模式)</td><td>代码缓存最大容量，存放 JIT 编译后的机器码。</td></tr><tr><td><code>-XX:InitialCodeCacheSize=&lt;size&gt;</code></td><td>48MB</td><td>代码缓存初始大小。</td></tr><tr><td><code>-XX:+PrintCodeCache</code></td><td>关闭</td><td>打印代码缓存使用情况（通常在 <code>-XX:+PrintCompilation</code> 输出中）。</td></tr><tr><td><code>-XX:+UseCodeCacheFlushing</code></td><td>启用</td><td>当代码缓存满时，允许清除非热点方法。</td></tr></tbody></table><p>编译控制与内联参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:MaxInlineLevel=&lt;N&gt;</code></td><td><code>9</code></td><td>最大内联嵌套深度。</td></tr><tr><td><code>-XX:MaxInlineSize=&lt;N&gt;</code></td><td><code>35</code> 字节</td><td>可被内联的方法最大字节码大小。</td></tr><tr><td><code>-XX:FreqInlineSize=&lt;N&gt;</code></td><td><code>325</code> 字节</td><td>热点方法可被内联的最大字节码大小。</td></tr><tr><td><code>-XX:+InlineSynchronizedMethods</code></td><td>关闭</td><td>允许内联同步方法（保守行为，默认不内联）。</td></tr><tr><td><code>-XX:CompileCommand=&lt;command&gt;,&lt;method&gt;[,...]</code></td><td>–</td><td>为特定方法指定编译指令，如 <code>exclude</code>、<code>inline</code>、<code>dontinline</code>、<code>compileonly</code> 等。</td></tr><tr><td><code>-XX:+LogCompilation</code></td><td>关闭</td><td>将编译日志输出到 <code>hotspot.log</code> 文件（详细 XML 格式）。</td></tr></tbody></table><p>调试与诊断参数：</p><table><thead><tr><th>参数名称</th><th>默认值</th><th>作用说明</th></tr></thead><tbody><tr><td><code>-XX:+PrintAssembly</code></td><td>关闭</td><td>打印编译后的汇编代码（需安装 hsdis 反汇编库）。</td></tr><tr><td><code>-XX:+TraceClassResolution</code></td><td>关闭</td><td>跟踪类解析过程，辅助诊断链接问题。</td></tr><tr><td><code>-XX:+PrintInterpreter</code></td><td>关闭</td><td>打印解释器信息（调试用）。</td></tr><tr><td><code>-XX:+CITime</code></td><td>关闭</td><td>打印 JIT 编译时间概要。</td></tr><tr><td><code>-XX:+CompilationStatistics</code></td><td>关闭</td><td>打印详细的编译统计数据（仅调试版本）。</td></tr></tbody></table><p><strong>使用建议</strong>：生产环境谨慎启用调试参数，因其会产生大量日志或影响性能。调优 JIT 时推荐组合使用 <code>-XX:+PrintCompilation</code> + <code>-XX:+PrintInlining</code>，配合 <code>-XX:+LogCompilation</code> 使用 <a href="https://github.com/graalvm/jitwatch">JITWatch</a> 分析。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>Java</tag>
      
      <tag>编译</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>类加载与双亲委派机制</title>
    <link href="/2026/05/23/%E7%B1%BB%E5%8A%A0%E8%BD%BD%E4%B8%8E%E5%8F%8C%E4%BA%B2%E5%A7%94%E6%B4%BE%E6%9C%BA%E5%88%B6/"/>
    <url>/2026/05/23/%E7%B1%BB%E5%8A%A0%E8%BD%BD%E4%B8%8E%E5%8F%8C%E4%BA%B2%E5%A7%94%E6%B4%BE%E6%9C%BA%E5%88%B6/</url>
    
    <content type="html"><![CDATA[<h1 id="类加载与双亲委派机制"><a href="#类加载与双亲委派机制" class="headerlink" title="类加载与双亲委派机制"></a>类加载与双亲委派机制</h1><p>Java 在刚刚诞生之时曾经提出过一个非常著名的宣传口号“<strong>一次编写，到处运行</strong> （Write Once，Run Anywhere）。为了实现这个口号，各种不同平台的 Java 虚拟机，以及所有平台都支持一种程序存储格式—字节码（Byte Code）。</p><p>需要指出的一点是：商业企业和开源机构已经在 Java 语言之外发展出一大批运行在 Java 虚拟机之上的语言，如 Kotlin、Clojure、Groovy、JRuby、JPython、Scala 等，Java 虚拟机不只具有平台无关性，同样具有语言无关性。</p><h2 id="Class结构"><a href="#Class结构" class="headerlink" title="Class结构"></a>Class结构</h2><p>Java 虚拟机不与包括 Java 语言在内的任何程序语言绑定，它只与 “Class文件” 这种特定的二进制文件格式所关联，Class 文件中包含了 Java 虚拟机指令集、符号表以及若干其他辅助信息。</p><p>任何一个 Class 文件都对应着唯一的一个类或接口的定义信息，但是反过来说，类或接口并不一定都得定义在文件里（譬如类或接口也可以动态生成，直接送入类加载器中，动态代理）。</p><p>《Java 虚拟机规范》中定义了相应的文件结构，解读可以参见：<a href="https://javabetter.cn/jvm/class-file-jiegou.html">详解 Java 的类文件结构</a>。</p><p>即使没有看过类的结构定义，想必在自定义通信协议或处理文件时也听过“魔数”的概念。</p><p>每个 Class 文件的头 4 个字节被称为魔数（Magic Number），它的唯一作用是确定这个文件是否为一个能被虚拟机接受的 Class 文件。不仅是 Class 文件，很多文件格式标准中都有使用魔数来进行身份识 别的习惯，譬如图片格式，如 GIF 或者 JPEG 等在文件头中都存有魔数。使用魔数而不是扩展名来进行识别主要是基于安全考虑，因为文件扩展名可以随意改动。</p><p>除了魔数外，还有主版本号、次版本号、常量池、访问标志等信息。</p><h2 id="类的加载"><a href="#类的加载" class="headerlink" title="类的加载"></a>类的加载</h2><p>Java 虚拟机把描述类的数据从 Class 文件（文件未必存储在磁盘，而是一串二进制字节流，包括但不限于从网络、数据库、内存中获取）加载到内存，并对数据进行校验、转换解析和初始化，最终形成可以被虚拟机直接使用的 Java 类型，这个过程被称作虚拟机的类加载机制。</p><p>一个类型从加载到卸载，生命周期包括加载 （Loading）、验证（Verification）、准备（Preparation）、解析（Resolution）、初始化 （Initialization）、使用（Using）和卸载（Unloading）七个阶段，其中验证、准备、解析三个部分统称 为连接（Linking）：</p><p><img src="/img/image-20260521152156038.png" alt="image-20260521152156038"></p><p>每个阶段的主要工作如下：</p><blockquote><p>加载</p></blockquote><ol><li>通过类的全限定名获取二进制字节流；</li><li>将字节流中的静态存储结构转化为方法区的运行时数据结构；</li><li>在堆中生成一个 <code>java.lang.Class</code> 对象，作为方法区数据的访问入口。</li></ol><p>不过数组类比较特殊，由 JVM 直接创建，不是通过类加载器加载。其元素类型由类加载器加载。</p><blockquote><p>验证</p></blockquote><p>确保 Class 文件的字节流符合 JVM 规范，不危害安全。分为四个阶段：</p><ol><li>文件格式验证：如魔数、版本号、常量池常量类型等；</li><li>元数据验证：语义分析，如是否有父类、是否继承被禁止的类等；</li><li>字节码验证：数据流和控制流分析，确保方法体逻辑安全（如类型转换合法）；</li><li>符号引用验证：在解析阶段进行，确保符号引用能被正确解析（如类是否存在、访问权限等）。</li></ol><p>如果程序运行的全部代码已经反复使用验证过，可通过 <code>-Xverify:none</code> 关闭大部分验证以提升加载速度。</p><blockquote><p>准备</p></blockquote><ul><li>为类变量（static 变量）分配内存并设置零值（如 <code>int</code> 为0，<code>reference</code> 为 <code>null</code>）；</li><li>若变量被 <code>final</code> 修饰且有 <code>ConstantValue</code> 属性，则直接初始化为指定值。</li></ul><p>从概念上讲，static 变量所在的区域应该是方法区，但方法区只是逻辑上的概念，在 <strong>JDK8 及之后，类变量会随着 Class 对象一起存放在 Java 堆</strong>中，这时候“类变量在方法区”就完全是一种对逻辑概念的表述了。</p><blockquote><p>解析</p></blockquote><ul><li>将常量池中的符号引用替换为直接引用（如内存地址、偏移量）。</li><li>解析对象：类&#x2F;接口、字段、类方法、接口方法等；</li><li><code>invokedynamic</code> 指令的解析是动态的，延迟到运行时。</li></ul><blockquote><p>初始化</p></blockquote><ul><li>执行类构造器 <code>&lt;clinit&gt;()</code> 方法，由编译器自动收集：<ul><li>类变量的赋值操作；</li><li>静态语句块 <code>static &#123;&#125;</code>。</li></ul></li><li>特点：<ul><li>按源码顺序执行；</li><li>父类的 <code>&lt;clinit&gt;()</code> 先于子类执行；</li><li>接口的 <code>&lt;clinit&gt;()</code> 不会触发父接口初始化；</li><li>多线程环境下，JVM 保证 <code>&lt;clinit&gt;()</code> 被正确加锁同步。</li></ul></li></ul><h2 id="类加载器"><a href="#类加载器" class="headerlink" title="类加载器"></a>类加载器</h2><p>Java 虚拟机设计团队有意把类加载阶段中的“<strong>通过一个类的全限定名来获取描述该类的二进制字节流</strong>”这个动作放到虚拟机外部去实现，以便让应用程序自己决定如何去获取所需的类。实现这个动作的代码被称为“<strong>类加载器</strong>”（Class Loader）。</p><p>类在 JVM 中的唯一性由<strong>类加载器 + 类全限定名</strong>共同决定。我曾经看到过一个 <a href="https://www.cnblogs.com/zerolinck/p/15412795.html">Spring 热部署造成的类加载问题</a>，有人在项目中使用了热部署（spring-boot-devtools），在修改代码后未重新启动，代码报错，最后定位到原因是热部署加载类的类加载器和之前的类加载器不同。</p><h3 id="双亲委派"><a href="#双亲委派" class="headerlink" title="双亲委派"></a>双亲委派</h3><p>类加载遵循大名鼎鼎的“双亲委派”模型，即<strong>类加载器发现未加载某个类时，会先委托给父加载器加载，若父加载器无法加载，再由自己尝试加载</strong>。</p><p>通过这段话我们能得出结论：<strong>类加载器是有层级的</strong>。</p><p><img src="/img/image-20260521154127786.png" alt="image-20260521154127786"></p><ul><li><strong>启动类加载器（Bootstrap）</strong>：加载 <code>rt.jar</code> 等核心库，C++ 实现。</li><li><strong>扩展类加载器（Extension）</strong>：加载 <code>jre/lib/ext</code> 下的类。</li><li><strong>应用程序类加载器（Application）</strong>：加载 <code>ClassPath</code> 下的类。</li></ul><p>JDK9 开始，由于引入的模块化特性，不再有<code> ExtensionClassLoader</code>，被 <code>PlatformClassLoader</code> 所替换。</p><p>还需要注意的一点是：所谓的父加载器，不是指子加载器继承（inherit）了父加载器，而是加载器中的 <code>parent</code> 属性（composite）指向的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ClassLoader</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> ClassLoader parent;<br>&#125;    <br></code></pre></td></tr></table></figure><p>该机制的核心目的是避免类的重复加载，保证核心类（如 <code>java.lang.Object</code>）的唯一性。</p><p>不过双亲模型只是一个规范，迄今为止已经被破坏过很多次了~</p><h3 id="破坏双亲委派"><a href="#破坏双亲委派" class="headerlink" title="破坏双亲委派"></a>破坏双亲委派</h3><p>首先来看“双亲委派”模型是如何实现的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Class&lt;?&gt; loadClass(String name, <span class="hljs-type">boolean</span> resolve)<br>    <span class="hljs-keyword">throws</span> ClassNotFoundException<br>&#123;<br>    <span class="hljs-keyword">synchronized</span> (getClassLoadingLock(name)) &#123;<br>        <span class="hljs-comment">// First, check if the class has already been loaded</span><br>        Class&lt;?&gt; c = findLoadedClass(name);<br>        <span class="hljs-keyword">if</span> (c == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-type">long</span> <span class="hljs-variable">t0</span> <span class="hljs-operator">=</span> System.nanoTime();<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-keyword">if</span> (parent != <span class="hljs-literal">null</span>) &#123;<br>                    c = parent.loadClass(name, <span class="hljs-literal">false</span>);<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    c = findBootstrapClassOrNull(name);<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (ClassNotFoundException e) &#123;<br>                <span class="hljs-comment">// ClassNotFoundException thrown if class not found</span><br>                <span class="hljs-comment">// from the non-null parent class loader</span><br>            &#125;<br><br>            <span class="hljs-keyword">if</span> (c == <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-comment">// If still not found, then invoke findClass in order</span><br>                <span class="hljs-comment">// to find the class.</span><br>                <span class="hljs-type">long</span> <span class="hljs-variable">t1</span> <span class="hljs-operator">=</span> System.nanoTime();<br>                c = findClass(name);<br><br>                <span class="hljs-comment">// this is the defining class loader; record the stats</span><br>                sun.misc.PerfCounter.getParentDelegationTime().addTime(t1 - t0);<br>                sun.misc.PerfCounter.getFindClassTime().addElapsedTimeFrom(t1);<br>                sun.misc.PerfCounter.getFindClasses().increment();<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (resolve) &#123;<br>            resolveClass(c);<br>        &#125;<br>        <span class="hljs-keyword">return</span> c;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>代码第6行，检查这个类是否已经被加载过了；</li><li>代码第10-14行，如果父加载器存在，委托父加载器加载，如果父加载器都找不到会丢出 <code>ClassNotFoundException</code>；</li><li>代码第24行，调用 <code>findClass(name)</code> 抽象方法自己加载；</li><li>代码第33行，如果需要，则完成类的链接（验证、准备、解析）。</li></ul><p>一般情况下：<code>resolve</code> 参数是 false，等到真正使用时（如 <code>Class.forName</code> 可指定）才解析，以提高性能。</p><p>在 <code>findClass(name)</code> 中，会在特定目录下去寻找类文件，把.class 文件读入内存，最后调用 native 方法 <code>defineClass</code> 将字节数组转成 Class 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> Class&lt;?&gt; findClass(String cn) <span class="hljs-keyword">throws</span> ClassNotFoundException &#123;<br>    <span class="hljs-comment">// no class loading until VM is fully initialized</span><br>    <span class="hljs-keyword">if</span> (!VM.isModuleSystemInited())<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassNotFoundException</span>(cn);<br><br>    <span class="hljs-comment">// find the candidate module for this class</span><br>    <span class="hljs-type">LoadedModule</span> <span class="hljs-variable">loadedModule</span> <span class="hljs-operator">=</span> findLoadedModule(cn);<br>    Class&lt;?&gt; c = <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">if</span> (loadedModule != <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-comment">// attempt to load class in module defined to this loader</span><br>        <span class="hljs-keyword">if</span> (loadedModule.loader() == <span class="hljs-built_in">this</span>) &#123;<br>            c = findClassInModuleOrNull(loadedModule, cn);<br>        &#125;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// search class path</span><br>        <span class="hljs-keyword">if</span> (hasClassPath()) &#123;<br>            c = findClassOnClassPathOrNull(cn);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// not found</span><br>    <span class="hljs-keyword">if</span> (c == <span class="hljs-literal">null</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassNotFoundException</span>(cn);<br>    <span class="hljs-keyword">return</span> c;<br>&#125;<br></code></pre></td></tr></table></figure><p>第12行的 <code>findClassInModuleOrNull</code> 和第17行的 <code>findClassOnClassPathOrNull</code> 最后都会调用 &#96;&#96;defineClass<code> </code>。</p><p>通过分析类加载的代码，打破双亲委派机制其实很简单，我们自定义类加载器，重写 <code>loadClass(String name)</code> 方法即可。</p><h4 id="Tomcat"><a href="#Tomcat" class="headerlink" title="Tomcat"></a>Tomcat</h4><p>首先思考一下 Tomcat 为什么要打破双亲委派机制？</p><p>一方面，Tomcat 作为实现了 Servlet 规范的 Web 容器，必然有自己的核心业务类，因此需要优先加载 web 应用目录下的类，只要这个类不覆盖 JRE 核心类，这也是 Servlet 规范的要求。Tomcat 保证了自己的实现不会覆盖 JRE 核心类。</p><p>另一方面，Tomcat 是可以部署多个 Web 应用的，如果多个 Web 应用使用了同一个依赖的不同版本，双亲委派机制是无法满足的（当加载第二个版本的依赖时，会发现类的全限定名已存在，加载失败）。</p><p>按照这个逻辑，思考一下 Tomcat 该如何实现？</p><p>遇到一个类时查找自己是否加载过：</p><ul><li>如果加载过，直接返回；</li><li>如果没有加载过，委托<code> ExtensionClassLoader</code> 加载（避免覆盖核心类，<code> ExtensionClassLoader</code> 加载时自然会找更上层的类加载器）：<ul><li>如果已经加载过，直接返回；</li><li>如果没有加载过，就尝试自己加载：<ul><li>如果加载成功，返回；</li><li>如果加载失败，委托给父加载器 <code>AppClassLoader</code> 加载：<ul><li>加载成功，返回；</li><li>加载失败，丢出异常。</li></ul></li></ul></li></ul></li></ul><p>Tomcat 的这一行为把用到的非 JRE 核心类优先自己加载，加载失败再委托 <code>AppClassLoader</code> 父加载器加载，打破了传统的双亲委派机制。</p><p>父类加载器为什么会失败？父类加载器只能加载指定路径或指定包的类，因此可能失败。</p><p>这个逻辑在 Tomcat 源码的 <code>WebappClassLoader</code> 类中，Tomcat 每个 Web 应用都会有一个 <code>WebappClassLoader</code> 。</p><p>Tomcat 的设计其实没有这么简单，虽然说 <code>WebappClassLoader</code> 做到了加载每个 Web 应用自己的类，但 Web 应用也有需要共享的类（例如二者都用了 Spring5，如果一直分开加载会内存膨胀），在这种需要共享的场景就需要共同的父加载器；另外 Tomcat 本身也是也是一个 Java 程序，因此它需要加载自己的类和依赖的 JAR 包，最终 Tomcat 的类加载器视图如下：</p><p><img src="/img/image-20260521164448269.png" alt="image-20260521164448269"></p><p><code>SharedClassLoader</code> 作为 <code>WebAppClassLoader</code> 的父加载器，专门来加载 Web 应用之间共享的类；<code>CatalinaClassloader</code>专门来加载 Tomcat 自身的类；<code>CommonClassLoader</code> 加载 Tomcat 需要和 Web 应用共享的类。</p><h4 id="Thread-Context-ClassLoader"><a href="#Thread-Context-ClassLoader" class="headerlink" title="Thread Context ClassLoader"></a>Thread Context ClassLoader</h4><p>设计是美好的，需求是残酷的~</p><p>双亲委派的设计，造成的情况是：<strong>子类加载器加载的类可以看到父类加载器加载的类，但父类加载器加载的类看不到子类加载器加载的类</strong>。</p><p>基础类型之所以被称为“基础”，是因为它们总是作为被用户代码继承、调用的 API 存在，但程序设计往往没有绝对不变的完美规则，如果有基础类型又要调用回用户的代码，那该怎么办呢？</p><p>在 Spring 和 Tomcat 的多 Web 应用程序中，相同版本的 Spring 被 <code>SharedClassLoader</code> 加载，业务类被 <code>WebAppClassLoarder</code> 加载，当请求来时，Spring 需要创建业务 Bean，例如 UserService，但 UserService 是被 <code>WebAppClassLoarder </code> 加载的，Spring 是取不到的。</p><p>为了解决这样的困境，Java 的设计团队只好引入了一个设计：线程上下文类加载器 （Thread Context ClassLoader）。Tomcat 为每个 Web 应用创建一个 <code>WebAppClassLoarder</code> 类加载器，并在启动 Web 应用的线程里设置线程上下文加载器，这样 Spring 在启动时就将线程上下文加载器取出来，用来加载 Bean。</p><p>除了 Spring，JDBC、JNDI、JAXB、Java 的 SPI（Service Provider Interface）机制等都依赖线程上下文类加载器来实现“父调用子”的能力。</p><h4 id="模块化系统"><a href="#模块化系统" class="headerlink" title="模块化系统"></a>模块化系统</h4><p>JDK9 之后，Java 引入了模块化的新特性，为了支持这个特性，类加载器也进行了一些改动。</p><p>首先，是扩展类加载器（ExtensionClassLoader）被平台类加载器（PlatformClassLoader）取代。整个 JDK 都基于模块化进行构建（原来的 rt.jar 和 tools.jar 被拆分成数十个 JMOD 文件），其中的 Java 类库就已天然地满足了可扩展的需求，那自然无须再保留 \lib\ext 目录，甚至 jdk 目录下已经没有 jre 文件夹了。</p><p>当平台及应用程序类加载器收到类加载请求，在委派给父加载器加载前，要先判断该类是否能够归属到某一个系统模块中，如果可以找到这样的归属关系，就要优先委派给负责那个模块的加载器完成加载，也许这可以算是对双亲委派的新的破坏。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>Java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>增量更新与原始快照</title>
    <link href="/2026/05/22/%E5%A2%9E%E9%87%8F%E6%9B%B4%E6%96%B0%E4%B8%8E%E5%8E%9F%E5%A7%8B%E5%BF%AB%E7%85%A7/"/>
    <url>/2026/05/22/%E5%A2%9E%E9%87%8F%E6%9B%B4%E6%96%B0%E4%B8%8E%E5%8E%9F%E5%A7%8B%E5%BF%AB%E7%85%A7/</url>
    
    <content type="html"><![CDATA[<h1 id="增量更新与原始快照"><a href="#增量更新与原始快照" class="headerlink" title="增量更新与原始快照"></a>增量更新与原始快照</h1><p>Java 语言的垃圾回收一直有一个为人所诟病的点，即垃圾回收时会触发 “STW”，为了减少 “STW” 的时间，垃圾收集器也在不断改进。</p><p>在日常开发中，我们经常会通过多线程的手段缩短程序的执行时间，目前主流的垃圾收集器也使用了并发在某些阶段让 GC线程 和 用户线程 可以同时工作，以尽可能及时地对请求进行处理。</p><p>提到并发，那就一定会涉及线程安全问题。试想：如果一个扫地机器人（GC线程）处理完了一部分的区域的垃圾（部分对象），开始处理其他区域的垃圾（其他对象），但此时用户（用户线程）又向处理过的区域丢垃圾（引用未处理区域的对象或删除对象引用），那该怎么办呢？</p><p>“STW” 的过程中，如果所有用户线程暂停，对象间的引用关系就不会改变，没有这个问题。在并发情况下，是通过<strong>先记录所有标记过程中对于对象引用的修改，并发标记完成后再暂停进行处理</strong>来保证处理时的一致性。</p><h2 id="三色标记法"><a href="#三色标记法" class="headerlink" title="三色标记法"></a>三色标记法</h2><p>三色标记法是一种经典的可达性分析师算法，它将对象分为三种颜色：</p><ul><li><p>黑色：表示对象已经被垃圾收集器访问过，且这个对象的所有引用都已经扫描过。</p></li><li><p>灰色：表示对象已经被垃圾收集器访问过，但这个对象上至少存在一个引用还没有被扫描过。</p></li><li><p>白色：表示对象尚未被垃圾收集器访问过。</p></li></ul><p>以下面这个图来分析三色标记法的流程：</p><p><img src="/img/image-20260521094412379.png" alt="image-20260521094412379"></p><ol><li>在可达性分析最开始的阶段，所有的对象都是白色的；</li><li>将<code>GC Roots</code> 直接引用到的对象 挪到 【灰色集合】中；</li><li>从灰色集合中获取对象：<br>3.1. 将本对象引用到的其他对象（出边）全部挪到 【灰色集合】中；<br>3.2. 将本对象挪到 【黑色集合】里面；</li><li>重复执行 3，直到【灰色集合】为空；</li><li>此时仍然是白色的对象，即代表不可达。</li></ol><p>一般执行步骤 2 时是会暂停用户线程的，最终的图示如下：</p><p><img src="/img/image-20260521094900788.png" alt="image-20260521094900788"></p><p>现在思考下在并发标记时什么情况下会出问题？</p><ol><li>有一个对象已经扫描完成是黑色的了，但此时引用该对象的那条引用被删除了，这个对象应该被清除的，误标记为存活对象。这会导致垃圾回收不彻底，但影响不大，可以在下个周期被回收。这种对象被称为<strong>浮动垃圾（多标）</strong>。</li></ol><p><img src="/img/image-20260521100105367.png" alt="image-20260521100105367"></p><ol start="2"><li>在遍历某个灰色对象的过程中，删除了它所引用的一个对象，这个对象还被一个黑色对象引用了。如果这么进行下去会导致本应该存活的<strong>对象消失（漏标）</strong>，是不可接受的。</li></ol><p><img src="/img/image-20260521095840792.png" alt="image-20260521095840792"></p><p>Wilson 于 1994 年在理论上证明了，当且仅当以下两个条件同时满足时，会产生“对象消失”的问题，即原本应该是黑色的对象被误标为白色： </p><ul><li>赋值器插入了一条或多条从黑色对象到白色对象的新引用； </li><li>赋值器删除了全部从灰色对象到该白色对象的直接或间接引用。</li></ul><p>如果第一个条件是新增了灰色对象到白色对象的引用是否可行呢？例如在获取灰色对象引用集合和灰色对象变黑之间新增了对一个白色对象的引用。实现上这是不可以的，JVM 通过 <strong>屏障（ Barrier）</strong>维护了这个步骤的正确性。</p><p>既然对象消失必须同时满足这两个条件，那么只要打破其中一个条件就可以了，这就是接下来介绍的增量更新（Incremental Update）和原始快照（Snapshot At The Beginning， SATB）。</p><h2 id="增量更新（Incremental-Update）"><a href="#增量更新（Incremental-Update）" class="headerlink" title="增量更新（Incremental Update）"></a>增量更新（Incremental Update）</h2><p>CMS 是基于增量更新来做并发标记的，破坏的是条件一。</p><p><img src="/img/image-20260521101020730.png" alt="image-20260521101020730"></p><p>增量更新的核心逻辑是：当黑色对象插入新的指向白色对象的引用时，就将这个新加入的引用记录下来，待并发标记完成后，重新对这种新增的引用记录进行扫描。可以简化理解为，<strong>黑色对象一旦新插入了指向白色对象的引用之后，它就变回灰色对象了</strong>。</p><p>如下图所示，A 由黑色变为灰色，将在后续被重新扫描：</p><p><img src="/img/image-20260521102746758.png" alt="image-20260521102746758"></p><h2 id="原始快照（Snapshot-At-The-Beginning）"><a href="#原始快照（Snapshot-At-The-Beginning）" class="headerlink" title="原始快照（Snapshot At The Beginning）"></a>原始快照（Snapshot At The Beginning）</h2><p>G1、Shenandoah 则是用原始快照来实现的，破坏的是条件二。</p><p>当灰色对象要删除指向白色对象的引用关系时，就将这个要删除的引用记录下来，在并发扫描结束之后，再将这些记录过的引用关系中的灰色对象为根，重新扫描一次。这也可以简化理解为，在标记阶段中如果指针更新前引用的对象是白色对象，就将其涂成灰色。</p><p>如下图所示，C 由白色变为灰色，将在后续被重新扫描：</p><p><img src="/img/image-20260521103055947.png" alt="image-20260521103055947"></p><p>这里你可能有疑问，C 虽然是灰色，但根本没有黑色对象引用呀。原始快照就是这样，它只考虑灰色对象对白色对象的引用删除，只要一个对象在并发标记开始时是可达的（即存在于快照的有效中），即使随后它所有的入口引用都被删除，仍然会被本轮 GC 标记为存活。虽然这可能暂时保留垃圾，也做到了绝不漏标快照中的对象。</p><h2 id="增量更新-VS-SATB"><a href="#增量更新-VS-SATB" class="headerlink" title="增量更新 VS SATB"></a>增量更新 VS SATB</h2><p>增量更新通过写屏障拦截<strong>黑对象新增指向白对象</strong>的引用，把这个黑对象重新标为灰色，放入待扫描队列，最后 Remark（重新标记）时，只扫这些被修改过的对象。</p><p>SATB 以并发标记开始那一刻的对象图为 “快照”，写屏障拦截<strong>引用被删除</strong>（灰→白），把 旧值（被删掉的引用） 记下来，保证 “快照里活着的对象” 本轮一定不被误删，新产生的对象直接当黑色，本轮不回收。</p><p>为什么 CMS 采用增量更新而 G1 采用原始快照呢？</p><p>这个问题涉及到很多方面，包括堆内存的布局、记忆集的维护、垃圾收集器的目标。</p><p>CMS 工作的老年代是一整块连续内存，Remark 时只追踪新增引用，重新扫描的范围小、成本可控，而且浮动垃圾更少，符合 CMS 追求低内存占用的目标。代价则是 Remark 阶段要深度重扫部分对象。</p><p>G1 的堆被拆成几百个 Region，回收是 “按 Region 回收”，采用 SATB 只记录 “被删掉的引用”，不需要深度重扫整个 Region。代价是产生更多的浮动垃圾，但 G1 是分代 + 分区，浮动垃圾影响可控，换来更低、更可控的停顿是值得的。如果 G1 用增量更新，通过记忆集跨 Region 扫描代价极高、停顿不可控。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>垃圾回收</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>垃圾收集器</title>
    <link href="/2026/05/21/%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%E5%99%A8/"/>
    <url>/2026/05/21/%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%E5%99%A8/</url>
    
    <content type="html"><![CDATA[<h1 id="垃圾收集器"><a href="#垃圾收集器" class="headerlink" title="垃圾收集器"></a>垃圾收集器</h1><p>收集算法是内存回收的方法论，而垃圾收集器是内存回收的实践者。</p><p><img src="/img/image-20260128132038344.png" alt="image-20260128132038344"></p><p>JDK8 时，默认收集器组合是（Parallel Scavenge + Parallel Old），JDK 9 之后，（Serial，CMS） 以及（ParNew，Serial Old）的组合已经被废弃了，默认收集器也变成了 G1。</p><p>垃圾回收就像打扫房间一样，当你在打扫房间的时候同时又在制造垃圾，那么房间很难打扫干净，因此 Java 垃圾回收一个被人所诟病的点就是 Stow The World（stw），直译就是停止这个世界，即 Java 垃圾回收会导致某段时间内进程完全无响应，在当前越来越追求低时延的环境下，这是很多系统不愿意接受的。</p><p>除了时延，还有一个关注的方向是吞吐量，比如把房子全部打扫一遍，那么接下来很久可能都不用再打扫了，如果每次只打扫一块区域，那么确实打扫的很快，但接下来又会频繁打扫。</p><p>因此虽然随着技术的进步，收集器的综合表现（内存占用、延迟、吞吐量）在提高，但直到现在还没有最好的收集器出现，更加不存在“万能”的收集器，所以我们选择的只是对具体应用最合适的收集器。</p><h2 id="Serial"><a href="#Serial" class="headerlink" title="Serial"></a>Serial</h2><p>Serial 收集器是一个单线程工作的收集器，但它的“单线程”的意义并不仅仅是说明它只会使用一个处理器或一条收集线程去完成垃圾收集工作，更重要的是强调在<code>它进行垃圾收集时，必须暂停其他所有工作线程</code>，直到它收集结束。</p><p><img src="/img/image-20260128135320905.png" alt="image-20260128135320905"></p><p>Serial 收集器简单而高效（与其他收集器的单线程相比），对于内存资源受限的环境，它是所有收集器里额外内存消耗（Memory Footprint，为保证垃圾收集而存储信息的额外空间）最小的。在用户桌面的应用场景以及近年来流行的部分微服务应用中，分配给虚拟机管理的内存一般来说并不会特别大，Serial 收集器对运行在客户端模式下的虚拟机来说是一个很好的选择。</p><h2 id="ParNew"><a href="#ParNew" class="headerlink" title="ParNew"></a>ParNew</h2><p>ParNew 收集器实质上是 Serial 收集器的多线程并行版本，除了同时使用多条线程进行垃圾收集之外，其余的行为都与 Serial 收集器完全一致。</p><p><img src="/img/image-20260128135719380.png" alt="image-20260128135719380"></p><p>虽然如此，它却是不少运行在服务端模式下的 HotSpot 虚拟机，尤其是 JDK7 之前的遗留系统中首选的新生代收集器，其中有一个与功能、性能无关但其实很重要的原因是：除了 Serial 收集器外，目前只有它能与 CMS 收集器配合工作。<code>CMS 收集器是 HotSpot 虚拟机中第一款真正意义上支持并发的垃圾收集器，它首次实现了让垃圾收集线程与用户线程（基本上）同时工作。</code></p><p>但 JDK9 开始，G1 是一个面向全堆的收集器，不再需要其他新生代收集器的配合工作，可以理解为从此以后，ParNew 合并入CMS，成为它专门处理新生代的组成部分。</p><p>ParNew 收集器在单核心处理器的环境中绝对不会有比 Serial 收集器更好的效果，甚至由于存在线程交互的开销，该收集器在通过超线程（Hyper-Threading）技术实现的伪双核处理器环境中都不能百分之百保证超越 Serial 收集器。</p><h2 id="Parallel-Scavenge"><a href="#Parallel-Scavenge" class="headerlink" title="Parallel Scavenge"></a>Parallel Scavenge</h2><p>Parallel Scavenge 收集器的特点是它的关注点与其他收集器不同，<code>CMS 等收集器的关注点是尽可能地缩短垃圾收集时用户线程的停顿时间，而 Parallel Scavenge 收集器的目标则是达到一个可控制的吞吐量（Throughput）</code>。</p><p><img src="/img/image-20260128140235510.png" alt="image-20260128140235510"></p><p>停顿时间越短就越适合需要与用户交互或需要保证服务响应质量的程序，良好的响应速度能提升用户体验；而高吞吐量则可以最高效率地利用处理器资源，尽快完成程序的运算任务，主要适合在后台运算而不需要太多交互的分析任务。</p><p>Parallel Scavenge 收集器提供了两个参数用于精确控制吞吐量，分别是控制最大垃圾收集停顿时间 的-XX：MaxGCPauseMillis 参数以及直接设置吞吐量大小的-XX：GCTimeRatio 参数。</p><ul><li>XX：MaxGCPauseMillis 参数允许的值是一个大于 0 的毫秒数，收集器将尽力保证内存回收花费的时间不超过用户设定值。但是垃圾收集停顿时间缩短是以牺牲吞吐量和新生代空间为代价换取的，收集 500MB 的区域肯定比收集 300MB 的区域快；</li><li>XX：GCTimeRatio  参数的值则应当是一个大于 0 小于 100 的整数，也就是垃圾收集时间占总时间的比率，相当于吞吐量的倒数。譬如把此参数设置为 19，那允许的最大垃圾收集时间就占总时间的 5% （即1&#x2F;(1+19)），默认值为 99，即允许最大 1%（即1&#x2F;(1+99)）的垃圾收集时间。</li></ul><p><code>自适应调节策略也是 Parallel Scavenge 收集器区别于 ParNew 收集器的一个重要特性。</code>参数-XX：+UseAdaptiveSizePolicy 激活之后，虚拟机会根据当前系统的运行情况收集性能监控信息，动态调整这些参数以提供最合适的停顿时间或者最大的吞吐量（按照上面两个参数的配置）。</p><h2 id="Serial-Old"><a href="#Serial-Old" class="headerlink" title="Serial Old"></a>Serial Old</h2><p>Serial Old 是 Serial 收集器的老年代版本，它同样是一个单线程收集器，使用标记-整理算法。</p><p><img src="/img/image-20260128141154645.png" alt="image-20260128141154645"></p><h2 id="Parallel-Old"><a href="#Parallel-Old" class="headerlink" title="Parallel Old"></a>Parallel Old</h2><p>Parallel Old 是 Parallel Scavenge 收集器的老年代版本，支持多线程并发收集，基于标记-整理算法实现。在此之前，新生代的Parallel Scavenge 收集器一直处于相当尴尬的状态，因为老年代除了Serial Old（PS MarkSweep）收集器以外别无选择，收集效率会被拖累导致整体效率甚至不如 （ParNew+CMS）。</p><p>直到 Parallel Old 收集器出现后，“吞吐量优先”收集器终于有了比较名副其实的搭配组合，在注重吞吐量或者处理器资源较为稀缺的场合，都可以优先考虑 Parallel Scavenge 加 Parallel Old 收集器这个组合。</p><p><img src="/img/image-20260128141515822.png" alt="image-20260128141515822"></p><h2 id="CMS"><a href="#CMS" class="headerlink" title="CMS"></a>CMS</h2><p>CMS（Concurrent Mark Sweep，直译并发标记清除）收集器是一种以获取最短回收停顿时间为目标的收集器，它的运作过程相对于前面几种收集器来说要更复杂一些，整个过程分为四个步骤：</p><ul><li><p>初始标记（CMS initial mark） ；</p></li><li><p>并发标记（CMS concurrent mark）； </p></li><li><p>重新标记（CMS remark） ；</p></li><li><p>并发清除（CMS concurrent sweep）。</p></li></ul><p>其中初始标记、重新标记这两个步骤仍然需要“Stop The World”。初始标记仅仅只是标记一下 GC Roots 能直接关联到的对象，速度很快；并发标记阶段就是从 GC Roots 的直接关联对象开始遍历整个对象图的过程，这个过程耗时较长但是不需要停顿用户线程，可以与垃圾收集线程一起并发运行；而重新标记阶段则是为了修正并发标记期间，因用户程序继续运作而导致标记产生变动的那一部分对象的标记记录，这个阶段的停顿时间通常会比初始标记阶段稍长一 些，但也远比并发标记阶段的时间短；最后是并发清除阶段，清理删除掉标记阶段判断的已经死亡的对象，由于不需要移动存活对象，所以这个阶段也是可以与用户线程同时并发的。</p><p>整个过程中耗时最长的并发标记和并发清除阶段中，垃圾收集器线程都可以与用户线程一 起工作，所以从总体上来说，CMS 收集器的内存回收过程是与用户线程一起并发执行的。</p><p><img src="/img/image-20260128141847252.png" alt="image-20260128141847252"></p><p>CMS 收集器是 HotSpot 虚拟机追求低停顿的第一次成功尝试，但是它还远达不到完美的程度。</p><ul><li>CMS 在并发标记时由于与用户线程并发运行，会争抢 CPU 资源，当处理器核心数量不足四个时， CMS 对用户程序的影响就可能变得很大（并发回收线程数为（cpu 数+3）&#x2F;4，占比超过 25%）；</li><li>并发执行时用户线程会产生新的垃圾，这部分垃圾只能下次收集，也称为浮动垃圾；另外由于用户线程也在执行，需要为其预留空间，因此老年代收集要达到某个比例时就开始，但即使这样也有可能并发标记失败，进而触发 FullGC（参数 CMSInitiatingOccupancyFraction）；</li><li>由于是清除算法，所以会有内存碎片问题，在执行过若干次（数量由参数值决定）不整理空间的 Full GC 之后，下一次进入 FullGC 前会先进行碎片整理（默认值为 0，表 示每次进入 FullGC 时都进行碎片整理）。</li></ul><p>CMS 由于耗时阶段可并行，因此停顿时间较短，使用增量更新算法解决用户线程并行影响对象引用图的问题，处理器核心数少时会影响用户线程，有浮动垃圾，可能并发标记失败，有内存碎片。</p><h2 id="G1"><a href="#G1" class="headerlink" title="G1"></a>G1</h2><p>Garbage First（简称G1）收集器是垃圾收集器技术发展历史上的里程碑式的成果，它<code>开创了收集器面向局部收集的设计思路和基于 Region 的内存布局形式</code>。JDK 9 发布之日，G1 宣告取代 Parallel Scavenge 加 Parallel Old 组合，成为服务端模式下的默认垃圾收集器。</p><p>作为 CMS 收集器的替代者和继承人，设计者们希望做出一款能够建立起“停顿时间模型”（Pause Prediction Model）的收集器，停顿时间模型的意思是能够支持指定<code>在一个长度为 M 毫秒的时间片段内，消耗在垃圾收集上的时间大概率不超过 N 毫秒</code>这样的目标。</p><p>G1 可以面向堆内存任何部分来组成回收集（Collection Set，一般简称CSet）进行回收，衡量标准不再是它属于哪个分代，而是哪块内存中存放的垃圾数量最多，回收收益最大，这就是 G1 收集器的 Mixed GC 模式。虽然 G1 也仍是遵循分代收集理论设计，但它是把连续的 Java 堆划分为多个大小相等的独立区域（Region），每一个 Region 都可以根据需要，扮演新生代的 Eden 空间、Survivor 空间，或者老年代空间。收集器能够对扮演不同角色的 Region 采用不同的策略去处理，这样无论是新创建的对象还是已经存活了一段时间、熬过多次收集旧对象都能获取很好的收集效果。</p><p>Region 中还有一类特殊的 Humongous 区域，专门用来存储大对象。G1 认为只要大小超过了一个 Region 容量（G1HeapRegionSize，1MB~32MB）一半的对象即可判定为大对象，超过了整个 Region 容量的超级大对象， 将会被存放在 N 个连续的 Humongous Region 之中。</p><p>G1 还为每一个 Region 设计了两个名为 TAMS（Top at Mark Start）的指针，把 Region 中的一部分空间划分出来用于并发回收过程中的新对象分配，并发回收时新分配的对象地址都必须要在这两个指针位置以上。G1 收集器默认在这个地址以上的对象是被隐式标记过的，即默认它们是存活的，不纳入回收范围。</p><p>G1 收集器会去跟踪各个 Region 里面的垃圾堆积的“价值”大小，价值即回收所获得的空间大小以及回收所需时间的经验值，然后在后台维护一 个优先级列表，每次根据用户设定允许的收集停顿时间（使用参数-XX：MaxGCPauseMillis 指定，默认值是200 毫秒），优先处理回收价值收益最大的那些 Region，这也就是“Garbage First”名字的由来。</p><p><strong>关于并发标记阶段如何保证收集线程与用户线程互不干扰地运行，CMS 收集器采用增量更新算法实现，而 G1 收集器则是通过原始快照（SATB）算法来实现的</strong>。</p><p>G1 收集器的工作一般也分为4个阶段：</p><ul><li>初始标记（Initial Marking）：仅仅只是标记一下 GC Roots 能直接关联到的对象，并且修改 TAMS 指针的值，让下一阶段用户线程并发运行时，能正确地在可用的 Region 中分配新对象。这个阶段需要停顿线程，但耗时很短，而且是借用进行 Minor GC 的时候同步完成的，所以 G1 收集器在这个阶段实际并没有额外的停顿。 </li><li>并发标记（Concurrent Marking）：从 GC Roots 开始对堆中对象进行可达性分析，递归扫描整个堆 里的对象图，找出要回收的对象，这阶段耗时较长，但可与用户程序并发执行。当对象图扫描完成以 后，还要重新处理 SATB 记录下的在并发时有引用变动的对象。 </li><li>最终标记（Final Marking）：对用户线程做另一个短暂的暂停，用于处理并发阶段结束后仍遗留下来的最后那少量的 SATB 记录。 </li><li>筛选回收（Live Data Counting and Evacuation）：负责更新 Region 的统计数据，对各个 Region 的回收价值和成本进行排序，根据用户所期望的停顿时间来制定回收计划，可以自由选择任意多个 Region 构成回收集，然后把决定回收的那一部分 Region 的存活对象复制到空的 Region 中，再清理掉整个旧 Region 的全部空间。这里的操作涉及存活对象的移动，是必须暂停用户线程，由多条收集器线程并行完成的。</li></ul><p><img src="/img/image-20260518160234011.png" alt="image-20260518160234011"></p><p><strong>G1 收集器除了并发标记外，其余阶段也是要完全暂停用户线程的， 换言之，它并非纯粹地追求低延迟</strong>。</p><p>但是 G1 依然会面对跨 Region 引用的问题，解决方案是每个 Region 都维护有自己的记忆集，这些记忆集会记录下别的 Region 指向自己的指针，并标记这些指针分别在哪些卡页的范围之内，由于 Region 的数量远多于分代数，G1 至少要耗费大约相当于 Java 堆容量 10% 至 20% 的额外内存来维持收集器工作。</p><p>相比 CMS，G1 的优点有很多，暂且不论可以指定最大停顿时间、分 Region 的内存布局、按收益动态确定回收集这些创新性设计带来的红利，单从最传统的算法理论上看，G1 也更有发展潜力。与 CMS 的“标记-清除”算法不同，<strong>G1 从整体来看是基于“标记-整理”算法实现的收集器，但从局部（两个Region 之间）上看又是基于“标记-复制”算法实现</strong>，无论如何，这两种算法都意味着 G1 运作期间不会产生内存空间碎片，垃圾收集完成之后能提供规整的可用内存。这种特性有利于程序长时间运行，在程序为大对象分配内存时不容易因无法找到连续内存空间而提前触发下一次收集。</p><p>不过，G1 相对于 CMS 仍然不是占全方位、压倒性优势的，从它出现几年仍不能在所有应用场景中代替 CMS 就可以得知这个结论。比起 CMS，G1 的弱项也可以列举出不少，如在用户程序运行过程 中，G1 无论是为了垃圾收集产生的内存占用（Footprint）还是程序运行时的额外执行负载 （Overload）都要比 CMS 要高。</p><p>（2019年）在小内存应用上 CMS 的表现大概率仍然要会优于 G1，而在大内存应用上 G1 则大多能发挥其优势，这个优劣势的 Java 堆容量平衡点通常在 6GB 至 8GB 之间。</p><p>G1 开创了 Region 的内存布局形式，通过原始快照（SATB）算法解决用户线程并行影响对象引用图的问题，增量回收，每次优先回收性价比高的区域，内存占用和负载较高。</p><h2 id="ZGC"><a href="#ZGC" class="headerlink" title="ZGC"></a>ZGC</h2><p>ZGC 是面向低延迟设计的垃圾收集器，希望在尽可能对吞吐量影响不太大的前提下，实现在任意堆内存大小下都可以把垃圾收集的停顿时间限制在十毫秒以内。</p><p>在《深入理解Java虚拟机》的第三版，此时的 ZGC 还是不设分代的，如今早已推出了分代 ZGC。</p><p>ZGC 也采用基于 Region 的堆内存布局，但 ZGC 的 Region 具有动态性——动态创建和销毁，以及动态的区域容量大小。</p><ul><li>小型 Region（Small Region）：容量固定为 2MB，用于放置小于 256KB 的小对象。 </li><li>中型 Region（Medium Region）：容量固定为 32MB，用于放置大于等于 256KB 但小于 4MB 的对 象。 </li><li>大型 Region（Large Region）：容量不固定，可以动态变化，但必须为 2MB 的整数倍，用于放置 4MB 或以上的大对象。每个大型 Region 中只会存放一个大对象，这也预示着虽然名字叫作“大型 Region”，但它的实际容量完全有可能小于中型 Region，最小容量可低至 4MB。</li></ul><p>对象的死亡其实跟对象中所有的属性都没关系，而是由其是否在引用链上决定的。HotSpot 虚拟机的几种收集器有不同的标记实现方案，有的把标记直接记录在对象头上（如 Serial 收集器），有的把标记记录在与对象相互独立的数据结构上（如 G1、Shenandoah 使 用了一种相当于堆内存的 1&#x2F;64 大小的，称为 BitMap 的结构来记录标记信息），而 ZGC 的染色指针是最直接的、最纯粹的，它直接把标记信息记在引用对象的指针上。</p><p>ZGC 收集器一个标志性的设计是它采用的<strong>染色指针</strong>技术，在 64 位操作系统上，理论上的寻址范围是 2^64，但出于需求、性能和成本的考虑，很多架构（AMD64、Linux、Windows）并不会完全支持 64 位，例如 64 位 Linux 支持 47 位（128TB）的进程虚拟地址空间和 46 位（64TB）的物理地址空间。ZGC 的染色指针技术盯上了这剩下的 46 位指针宽度，将其高 4 位提取出来存储四个标志信息，当然这也直接导致 ZGC 能够管理的内存不可以超过 4TB（2的42次幂）。</p><p><img src="/img/image-20260519084640055.png" alt="image-20260519084640055"></p><p>使用了染色指针技术后，就不再支持压缩指针和 32 位平台了，但它带来的收益也是非常可观的。</p><ul><li>染色指针可以使得一旦某个 Region 的存活对象被移走之后，这个 Region 立即就能够被释放和重用掉，而不必等待整个堆中所有指向该 Region 的引用都被修正后才能清理。</li><li>染色指针可以大幅减少在垃圾收集过程中内存屏障的使用数量。</li><li>染色指针可以作为一种可扩展的存储结构用来记录更多与对象标记、重定位过程相关的数据，以便日后进一步提高性能。</li></ul><p>不过作为本该由操作系统管理的内存，Java 虚拟机作为一个应用进程，有能力这样自定义内存中不同位的含义吗？</p><p>操作系统中其实会使用分页管理机制将不同进程相同索引页面映射到不同的物理地址，Linux&#x2F;x86-64 平台上的 ZGC 使用了<strong>多重映射（Multi-Mapping）</strong>将多个不同的虚拟内存地址映射到同一个物理内存地址上，这是一种多对一映射，意味着 ZGC 在虚拟内存中看到的地址空间要比实际的堆内存容量来得更大。</p><p><img src="/img/image-20260519090551006.png" alt="image-20260519090551006"></p><ul><li>并发标记（Concurrent Mark）：并发标记是遍历对象图做可达性分析的阶段，也要经历短暂停顿，与 G1 不同的是，ZGC 的标记是在指针上而不是在对象上进行的，标记阶段会更新染色指针中的 Marked0、Marked1 标志位。</li><li>并发预备重分配（Concurrent Prepare for Relocate）：根据特定的查询条件统计得出本次收集过程要清理哪些 Region，将这些Region 组成重分配集（Relocation Set）。重分配集与 G1 收集器的回收集（Collection Set）还是有区别的，它并非为了收益优先的增量回收，而是每次回收都会扫描所有的 Region，用范围更大的扫描成本换取省去 G1 中记忆集的维护成本。</li><li>并发重分配（Concurrent Relocate）：重分配是 ZGC 执行过程中的核心阶段，这个过程要把重分配集中的存活对象复制到新的 Region 上，并为重分配集中的每个 Region 维护一个转发表（Forward Table），记录从旧对象到新对象的转向关系。得益于染色指针的支持，ZGC 收集器能仅从引用上就明确得知一个对象是否处于重分配集之中，如果用户线程此时并发访问了位于重分配集中的对象，这次访问将会被预置的内存屏障所截获，然后立即根据 Region 上的转发表记录将访问转发到新复制的对象 上，并同时修正更新该引用的值，使其直接指向新对象，ZGC 将这种行为称为<strong>指针的“自愈”（Self Healing）能力</strong>。这也意味着在 Region 回收时转发表不能立刻释放掉。</li><li>并发重映射（Concurrent Remap）：重映射所做的就是修正整个堆中指向重分配集中旧对象的所有引用，但是 ZGC 的并发重映射并不是一个必须要“迫切”去完成的任务，因为指针是具有自愈能力的，重映射清理这些旧引用的主要目的是为了不变慢（还有清理结束后可以释放转发表这样的附带收益）。ZGC 很巧妙地把并发重映射阶段要做的工作，合并到了下一次垃圾收集循环中的并发标记阶段里去完成，反正它们都是要遍历所有对象的，这样合并就节省了一次遍历对象图的开销。一旦所有指针都被修正之后，原来记录新旧对象关系的转发表就可以释放掉了。</li></ul><p>ZGC 的设计理念与 Azul System 公司的 PGC 和 C4 收集器一脉相承，是迄今垃圾收集器研究的最前沿成果，它做到了几乎整个收集过程都全程可并发，短暂停顿也只与 GC Roots 大小相关而与堆内存大小无关，因而同样实现了任何堆上停顿都小于十毫秒的目标。</p><p>ZGC 由于每个 Region 并未维护记忆集，甚至连分代都没有，优点是<strong>给用户线程带来的负担会小很多，相应它能承受的对象分配速率不会太高</strong>。例如 ZGC 准备要对一个很大的堆做一次完整的并发收集，假设其全过程要持续十分钟以上（ZGC 立的 Flag 是停顿时间不超过十毫秒），在这段时间里面，由于应用的对象分配速率很高，将创造大量的新对象，这些新对象很难进入当次收集的标记范 围，通常就只能全部当作存活对象来看待——尽管其中绝大部分对象都是朝生夕灭的，这就产生了大量的浮动垃圾。如果这种高速分配持续维持的话，每一次完整的并发收集周期都会很长，回收到的内存空间持续小于期间并发产生的浮动垃圾所占的空间，堆中剩余可腾挪的空间就越来越小了，唯 一的办法就是尽可能地增加堆容量大小获得更多喘息的时间。</p><p>不过现在的 ZGC 已经支持分代了，转转有关于分代 ZGC 的一篇实践文章：<a href="https://mp.weixin.qq.com/s/bDp_4EAZoaGdXLV17s4vjw">JDK21（21.0.2_13）分代ZGC在转转商列服务中的实践</a>。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>垃圾回收</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>垃圾回收方法论</title>
    <link href="/2026/05/20/%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E6%96%B9%E6%B3%95%E8%AE%BA/"/>
    <url>/2026/05/20/%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E6%96%B9%E6%B3%95%E8%AE%BA/</url>
    
    <content type="html"><![CDATA[<h1 id="垃圾回收方法论"><a href="#垃圾回收方法论" class="headerlink" title="垃圾回收方法论"></a>垃圾回收方法论</h1><p>垃圾收集（Garbage Collect）也就是常说的 GC，GC 这个概念不是 Java 独有的，在 Java 之前已经有其他语言有 GC 的理念了，1960 年诞生于麻省理工学院的 Lisp 是第一门开始使用内存动态分配和垃圾收集技术的语言。</p><p>垃圾收集需要考虑以下这些事情：</p><ul><li>回收什么区域的内存?</li><li>如何判断内存可回收?</li><li>怎么回收?</li></ul><h2 id="回收什么区域的内存"><a href="#回收什么区域的内存" class="headerlink" title="回收什么区域的内存"></a>回收什么区域的内存</h2><p>Java 的内存区域被分为程序计数器、虚拟机栈、堆、本地方法栈和方法区几个部分，其中程序计数器、虚拟机栈和本地方法栈随线程的生命周期运转，每一个栈帧中分配的内存在类结构确定下来时基本已知，所以这几个区域的内存分配和回收都具备确定性，当方法执行结束或线程结束时，内存自然就跟着释放了。</p><p>堆和方法区的内存回收则不确定性很大，一个方法执行不同的分支所需要的内存可能都不一样，只有运行期才知道程序究竟会创建多少个对象，垃圾收集器所关注的也是这部分内存该如何管理。</p><h2 id="如何判断内存可回收"><a href="#如何判断内存可回收" class="headerlink" title="如何判断内存可回收"></a>如何判断内存可回收</h2><p>判断堆内存可回收其实就是认为这片内存所代表的对象已经不会再访问到了，方法区则是主要回收废弃的常量和不再使用的类型。和堆中判断对象可回收相比，方法区判断常量不再被引用及类型不再使用则比较复杂，进行垃圾收集的“性价比”通常也比较低。</p><p>判断一个类型不再使用是很复杂的，需要满足以下条件：</p><ul><li>该类的所有实例都已回收；</li><li>该类的类加载器也被回收，实际上这点除非是精心设计的场景（OSGI、JSP），通常很难达成；</li><li>该类对应的 Class 对象没有在任何地方被引用，无法在任何地方通过反射访问该类的方法。</li></ul><p>所以我们主要关注堆内存的回收。</p><h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><p>狭义的引用就是如果 refenence 类型的数据存储的值代表另一块内存的地址，那么该 refenence 就是某个对象的引用。</p><p>在这样的定义下，对象只有引用和无引用两种，描述不了其它的情形：譬如我们希望描述一种对象，它在内存足够时就可以存活，内存不足时可以回收，于是 JDK1.2 对引用进行了扩充，将引用分为强引用（Strongly Reference）、软引用（Soft Reference）、弱引用（Weak Reference）和虚引用（Phantom Reference）4 种，这 4 种引用强度依次逐渐减弱。</p><p>强引用即我们日常中最普遍的引用复制，诸如 <code>Object a = new Object()</code> 这样，只要引用关系还在，无论任何情况下垃圾回收器都不能回收。</p><p>软引用是用来描述一些还有用，但非必须的对象。只被软引用关联着的对象，在系统将要发生内存溢出异常前，会把这些对象列进回收范围之中进行第二次回收。</p><p>弱引用也是用来描述那些非必须对象，但是它的强度比软引用更弱一些，被弱引用关联的对象只能生存到下一次垃圾收集发生为止。</p><p>虚引用也称为“幽灵引用”或者“幻影引用”，它是最弱的一种引用关系。一个对象是否有虚引用的存在，完全不会对其生存时间构成影响，也无法通过虚引用来取得一个对象实例。为一个对象设置虚引用关联的唯一目的只是为了能在这个对象被收集器回收时收到一个系统通知。</p><h3 id="引用计数法"><a href="#引用计数法" class="headerlink" title="引用计数法"></a>引用计数法</h3><p>引用计数法的逻辑非常简单，判定效率也很高，也有一些语言的应用案例，实现就是：每个对象设置一个字段存放被引用的个数，当这个数为 0 时说明对象不再被使用了。</p><p>但是引用计数法无法解决循环依赖的问题，当 A-&gt; B &amp;&amp; B-&gt;A 时 AB 的引用都至少为1，那么永远无法被回收了，即使它们不会再被访问。</p><h3 id="可达性分析法"><a href="#可达性分析法" class="headerlink" title="可达性分析法"></a>可达性分析法</h3><p>当前主流的商用语言的内存管理子系统都是通过可达性分析（Reachability Analysis）算法来判定对象是否存活的，基本思路就是通过 一系列称为“GC Roots”的根对象作为起始节点集，从这些节点开始，根据引用关系向下搜索，搜索过程所走过的路径称为“引用链”（Reference Chain），如果某个对象到 GC Roots 间没有任何引用链相连， 或者用图论的话来说就是从 GC Roots 到这个对象不可达时，则证明此对象是不可能再被使用的。</p><p><img src="/img/image-20260127090400550.png" alt="image-20260127090400550"></p><p>Java 中可固定作为 GC Roots 的对象包括以下几个部分：</p><ul><li>虚拟机栈（栈帧中的本地变量表）中引用的对象，譬如各个线程被调用的方法堆栈中使用到的参数、局部变量、临时变量等；</li><li>方法区中类静态属性引用的对象，譬如Java类的引用类型静态变量；</li><li>方法区中常量引用的对象，譬如字符串常量池（String Table）里的引用；</li><li>虚拟机内部的引用，如基本数据类型对应的 Class 对象，一些常驻的异常对象（比如 NullPointExcepiton、OutOfMemoryError）等，还有系统类加载器；</li><li>…….</li></ul><p>除了这些固定的 GC Roots 集合以外，根据用户所选用的垃圾收集器以及当前回收的内存区域不同，还可以有其他对象“临时性”地加入，共同构成完整 GC Roots 集合。例如在回收某区域时，这个区域的对象被另一区域的对象引用，那么另一区域的这个对象也需要假如到 GC Roots 中。</p><p>不可达的对象也不是立刻死亡的，当它被第一次标记时，如果重写了 finilize 方法且 finilize 方法是没有被调用，会被放到一个低优先级的调度队列中执行 <code>finilize() 方法</code>，如果在执行过程中与 GC Roots 中的对象建立了连接，那在第二次标记时它将被移出“即将回收”的集合。任何一个对象的 finalize() 方法都只会被系统自动调用一次，如果对象面临下一次回收，它的 finalize() 方法不会被再次执行。finalize() 并不能等价于 C 语言中的析构函数，由于优先级低且为了避免执行缓慢而导致内存不能回收，甚至不能保证这个方法被执行完成，<code>推荐直接忘掉</code>这个方法。</p><h2 id="怎么回收"><a href="#怎么回收" class="headerlink" title="怎么回收"></a>怎么回收</h2><p>如何判定对象消亡的角度出发，垃圾收集算法可以划分为“引用计数式垃圾收集”（Reference Counting GC）和“追踪式垃圾收集”（Tracing GC）两大类，这两类也常被称作“直接垃圾收集”和“间接垃圾收集”，主流 Java 虚拟机中都是第二类。</p><h3 id="分代收集理论"><a href="#分代收集理论" class="headerlink" title="分代收集理论"></a>分代收集理论</h3><p>Java 开发应该经常听到诸如新生代、老年代等词语，这些词语来自于分代收集理论。</p><p>分代收集理论中有两个重要假说：</p><ul><li>弱分代假说（Weak Generational Hypothesis）：绝大多数对象都是朝生夕灭的；</li><li>强分代假说（Strong Generational Hypothesis）：熬过越多次垃圾收集过程的对象就越难以消亡。</li></ul><p>这两个分代假说共同奠定了多款常用的垃圾收集器的一致的设计原则：收集器应该将 Java 堆划分出不同的区域，然后将回收对象依据其年龄（年龄即对象熬过垃圾收集过程的次数）分配到不同的区域之中存储。</p><p>堆内存分代之后，垃圾收集器才可以每次只回收其中某一个或者某些部分的区域 ——因而才有了“Minor GC”“Major GC”“Full GC”这样的回收类型的划分；也才能够针对不同的区域安排与里面存储对象存亡特征相匹配的垃圾收集算法，例如<strong>在新生代中可以关注如何保留少量的存活对象</strong>，而<strong>在老年代关注如何回收少量的死亡对象</strong>。——因而发展出了“标记-复制算法”“标记-清除算法”“标记-整理算法”等针对性的垃圾收集算法。</p><p>现在的商用 Java 虚拟机里，设计者一般至少会把 Java 堆划分为<code>新生代 （Young Generation）</code>和<code>老年代（Old Generation）</code>两个区域。垃圾收集可以分为部分收集（Partial GC）和整堆收集（Full GC）两大类，部分收集又可以分为Young GC（新生代）、Old GC（老年代） 和 Mix GC（混合收集，G1收集器）。</p><p>需要特别考虑的是，对象不是孤立的，对象之间会存在跨代引用。基于跨代引用理论：跨代引用相对于同代引用来说仅占极少数。（因为如果老年代引用了新生代对象，那么新生代对象大概率也要进入老年代，那么就消除了跨代引用），虚拟机在<code>新生代上建立一个全局的数据结构（该结构被称为“记忆集”，Remembered Set）</code>，这个结构把老年代划分成若干小块，标识出老年代的哪一块内存会存在跨代引用。此后当<code>发生Young GC时，只有包含了跨代引用的小块内存里的对象才会被加入到 GC  Roots</code> 进行扫描。虽然这种方法需要在对象改变引用关系（如将自己或者某个属性赋值）时维护记录数 据的正确性，会增加一些运行时的开销，但比起收集时扫描整个老年代来说仍然是划算的。</p><h3 id="标记-清除（其他算法的基础）"><a href="#标记-清除（其他算法的基础）" class="headerlink" title="标记-清除（其他算法的基础）"></a>标记-清除（其他算法的基础）</h3><p>最基础的垃圾收集算法是“标记-清除”（Mark-Sweep）算法，算法分为“标记”和“清除”两个阶段，标记所有待回收的对象，清除所有标记的对象，或者相反操作。</p><p>它的主要缺点有两个：第一个是执行效率不稳定，如果 Java 堆中包含大量对象，而且其中大部分是需要被回收的，这时必须进行大量标记和清除的动作，导致标记和清除两个过程的执行效率都随对象数量增长而降低；第二个是内存空间的碎片化问题，标记、清除之后会产生大量不连续的内存碎片。优点则是没有任何的空间冗余。</p><p><img src="/img/image-20260127093942105.png" alt="image-20260127093942105"></p><h3 id="标记-复制（新生代）"><a href="#标记-复制（新生代）" class="headerlink" title="标记-复制（新生代）"></a>标记-复制（新生代）</h3><p>1969 年 Fenichel 提出了一种称为“半区复制”（Semispace Copying）的垃圾收集算法，它将可用内存按容量划分为大小相等的两块，每次只使用其中的一块。当这一块的内存用完了，就将还存活着的对象复制到另外一块上面，然后再把已使用过的内存空间一次清理掉。</p><p>该算法分配内存时不用考虑空间碎片的复杂情况，只要移动堆顶指针，按顺序分配即可。这样实现简单，运行高效，不过其缺陷也显而易见，这种复制回收算法的代价是将可用内存缩小为了原来的一半。</p><p><img src="/img/image-20260127094126338.png" alt="image-20260127094126338"></p><p>现在的商用 Java 虚拟机大多都优先采用了这种收集算法去回收新生代，不过由于新生代大多数对象朝生夕死，并不需要按照 1∶1 的比例来划分新生代的内存空间。HotSpot 虚拟机的 Serial、ParNew 等新生代收集器把新生代分为一块较大的 Eden 空间和两块较小的 Survivor空间（Appel 式回收），默认空间是 8:1:1，每次分配内存只使用 Eden 和其中一块 Survivor。不过任何人都没有办法百分百保证每次回收都只有不多于10%的对象存活，因此 Appel 式回收还有一个充当罕见情况的“逃生门”的安全设计，当 Survivor 空间不足以容纳一次 Young GC 之后存活的对象时，就需要依赖其他内存区域（实际上大多就是老年代）进行分配担保（Handle Promotion）。</p><h3 id="标记-整理（老年代）"><a href="#标记-整理（老年代）" class="headerlink" title="标记-整理（老年代）"></a>标记-整理（老年代）</h3><p>标记-复制算法在对象存活率较高时就要进行较多的复制操作，效率将会降低。更关键的是，如果不想浪费 50% 的空间，就需要有额外的空间进行分配担保，以应对被使用的内存中所有对象都100%存活的极端情况，所以在老年代一般不能直接选用这种算法。</p><p>针对老年代对象的存亡特征，1974 年 Edward Lueders 提出了另外一种有针对性的“标记-整理”（Mark-Compact）算法，其中的标记过程仍然与“标记-清除”算法一样，但后续步骤不是直接对可回收对象进行清理，而是<code>让所有存活的对象都向内存空间一端移动，然后直接清理掉边界以外的内存</code>。</p><p><img src="/img/image-20260127094710039.png" alt="image-20260127094710039"></p><p>在老年代这种每次回收都有大量对象存活区域，移动存活对象并更新所有引用这些对象的地方将会是一种极为负重的操作，而且这种对象移动操作必须全程暂停用户应用程序才能进行。但如果跟标记-清除算法那样完全不考虑移动和整理存活对象的话，弥散于堆中的存活对象导致的空间碎片化问题就只能依赖更为复杂的内存分配器和内存访问器来解决。</p><p>基于以上两点，<strong>是否移动对象都存在弊端，移动则内存回收时会更复杂，不移动则内存分配时会更复杂</strong>。移动对象暂停时间较长，不移动对象停顿时间会更短，甚至可以不需要停顿，但从吞吐量上看，不移动则可能由于内存碎片提前触发下一次的 GC，长久来看依然是影响吞吐量的。</p><p>基于标记-清除算法的 CMS 收集器平时多数时间都采用标记-清除算法，暂时容忍内存碎片的存在，直到内存空间的碎片化程度已经大到影响对象分配时，会采用标记-整理算法收集一次，以获得规整的内存空间。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>垃圾回收</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Java Object</title>
    <link href="/2026/05/19/Java-Object/"/>
    <url>/2026/05/19/Java-Object/</url>
    
    <content type="html"><![CDATA[<h1 id="Java对象"><a href="#Java对象" class="headerlink" title="Java对象"></a>Java对象</h1><p>Java 是一门面向对象的编程语言，Java 程序运行过程中无时无刻都有对象被创建出来。</p><h2 id="对象的组成"><a href="#对象的组成" class="headerlink" title="对象的组成"></a>对象的组成</h2><p>在 HotSpot 虚拟机里，对象在堆内存中的存储布局可以划分为三个部分：**<code>对象头（Header）、实例数据（Instance Data）和对齐填充（Padding）</code>**。</p><p><strong>对象头中存储的是与对象自身定义的数据无关的信息，包括：对象自身运行时数据和类型指针两部分。</strong></p><p>对象运行时数据如哈希码、GC 分代年龄、锁状态标志、线程持有锁、偏向线程ID 等，这部分在 32 位虚拟机和 64 位虚拟机分别占用 32 比特 和 64 比特，官方的称呼为 “Mark Word”。<strong>Mark Word 被设计成一个动态定义的数据结构，根据对象的状态复用自己的空间。</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// Bit-format of an object header (most significant first, big endian layout below):</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//  32 bits:</span><br><span class="hljs-comment">//  --------</span><br><span class="hljs-comment">//  hash:25 ------------&gt;| age:4    biased_lock:1 lock:2 (normal object)</span><br><span class="hljs-comment">//  JavaThread*:23 epoch:2 age:4    biased_lock:1 lock:2 (biased object)</span><br><span class="hljs-comment">//  size:32 ------------------------------------------&gt;| (CMS free block)</span><br><span class="hljs-comment">//  PromotedObject*:29 ----------&gt;| promo_bits:3 -----&gt;| (CMS promoted object)</span><br></code></pre></td></tr></table></figure><p>例如在 32 位的 HotSpot 虚拟机中，对象未被同步锁锁定的状态下，Mark Word 的 32 个比特存储空间中的 25 个比特用于存储对象哈希码，4 个比特用于存储对象分代年龄，2 个比特用于存储锁标志位，1 个比特固定为 0，在其他状态（轻量级锁定、重量级锁定、GC标 记、可偏向）下对象的存储内容如表所示。</p><p><img src="/img/image-20260104165308712.png" alt="image-20260104165308712"></p><p><strong>类型指针（Klass Pointer）即对象指向它的类型元数据的指针，Java 虚拟机通过这个指针来确定该对象是哪个类的实例。</strong>不过并不是所有的虚拟机实现都需要在对象数据上保留类型指针，例如通过句柄访问对象。</p><p>除去对象头，接下来的实例数据部分是对象真正存储的有效信息，包括继承以及自定义的字段，这部分的存储顺序会受到虚拟机分配策略参数（-XX：FieldsAllocationStyle 参数）和字段在 Java 源码中定义顺序的影响。默认规则为：longs&#x2F;doubles、ints、shorts&#x2F;chars、bytes&#x2F;booleans、oops（Ordinary Object Pointers，OOPs），在满足这个前提条件的情况下，在父类中定义的变量会出现在子类之前。如果 HotSpot 虚拟机的 +XX：CompactFields 参数值为 true（默认就为true），那子类之中较窄的变量也允许插入父类变量的空隙之中，以节省出一点点空间。</p><p>对象的第三部分是对齐填充，这并不是必然存在的，也没有特别的含义，它仅仅起着占位符的作用。由于 HotSpot 虚拟机的自动内存管理系统要求对象起始地址必须是 8 字节的整数倍，换句话说就是任何对象的大小都必须是 8 字节的整数倍。对象头部分已经被精心设计成正好是8字节的倍数，因此，如果对象实例数据部分没有对齐的话，就需要通过对齐填充来补全。</p><h2 id="对象的创建"><a href="#对象的创建" class="headerlink" title="对象的创建"></a>对象的创建</h2><p>当在程序中执行一个 new 指令创建普通 Java 对象（不包括数组和 Class 对象）时，JVM 首先将去检查这个指令的参数是否能在常量池中定位到一个类的符号引用，并且检查这个符号引用代表的类是否已被加载、解析和初始化过。如果没有，那必须先执行相应的类加载过程。</p><p>在类加载检查通过后，接下来虚拟机将为新生对象分配内存，内存的大小通过上一部分中提到的方式已经确定下来了，所以分配内存就是从 Java 堆中划分出来一个这样大小的内存块。</p><p>假如堆内存非常规整，一侧全都是使用过的内存，另一侧则是空闲内存，那么只需维护一个指针，在需要分配内存时指针向空闲侧移动即可，这种方法被称为指针碰撞；而如果堆内存并不规整，那么则需要使用“空闲列表”维护当前空闲的内存空间。堆内存非常规整取决于使用的垃圾回收算法是否带有空间整理（Compact），如果只是标记清除例如 CMS，理论上（因为CMS在分配内存时会分配一个比所需内存更大的缓冲区，这样后续依然可以在这块缓冲区内使用指针碰撞）就只能使用空闲列表法，而如果是 ParNew 这类带有整理过程的收集器，则可以使用指针碰撞法。</p><p>分配内存在并发情况下不是线程安全的，可能出现正在给对象 A 分配内存，指针还没来得及修改，对象 B 又同时使用了原来的指针来分配内存的情况。一种解决方案是采用 CAS 重试直到分配成功，另一种方案则是把内存分配的动作按照线程划分在不同的空间之中进 行，即<strong>每个线程在 Java 堆中预先分配一小块内存，称为本地线程分配缓冲（Thread Local Allocation Buffer，TLAB）</strong>，哪个线程要分配内存，就在哪个线程的本地缓冲区中分配，只有本地缓冲区用完了，分配新的缓冲区时才需要锁定。</p><p>内存分配完成后，会首先对这块内存区域赋零值，并设置相应的对象头等信息，此时在虚拟机的角度一个新的对象已经产生了，但是在用户的角度，这个对象还需要执行 class 的 &lt;init&gt; 方法，按照程序员的意愿对对象进行初始化，这样一个真正可用的对象才算完全被构造出来。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">class</span> <span class="hljs-title class_">User</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">age</span> <span class="hljs-operator">=</span> <span class="hljs-number">3</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>在内存分配完成时，user 对象的 age 属性其实是 int 的零值 0，在执行完类的 init 方法后， age 的属性才是我们定义的 3。</p><h2 id="对象的访问定位"><a href="#对象的访问定位" class="headerlink" title="对象的访问定位"></a>对象的访问定位</h2><p>Java 程序会通过栈上的 reference 数据来操作堆上的具体对象，reference 的理解是如果 reference 类型的数据中存储的数值代表的是另外一块内存的起始地址，就称该 reference 数据是代表某块内存、某个对象的引用。</p><p>但是 reference 只规范了能够找到对象，而没有规定其实现方式，所以主流的实现方式其实有使用句柄和直接访问两种。</p><p>使用句柄的好处是，在垃圾回收的过程中，对象的内存位置是会变化的，这时只需要修改句柄引用的对象位置即可，缺点则是每次访问对象都多一次内存访问。</p><p><img src="/img/image-20260126112329192.png" alt="image-20260126112329192"></p><p>使用直接访问的好处是可以直接访问到对象实例数据（注意是对象实例数据，访问对象的类型数据时需要考虑如何实现），但在垃圾回收的过程中可能需要更新多处引用对象的值。</p><p><img src="/img/image-20260126112541748.png" alt="image-20260126112541748"></p><p>对 HotSpot 而言，它主要使用直接访问方式进行对象访问， <strong>现代的 HotSpot JVM 使用了分代 GC，大部分对象都是在年轻代分配并快速回收，</strong> 这些对象基本不会移动。</p><h2 id="对象大小与压缩指针"><a href="#对象大小与压缩指针" class="headerlink" title="对象大小与压缩指针"></a>对象大小与压缩指针</h2><p>在 64 位系统中，普通的对象引用通常需要 64 位（8字节）的空间，因为 4 字节只能表示 32 位的一个地址，也就是最多只能表示 4GB 的空间，但是绝大多数的应用程序都不会占用那么大的空间，即堆内存一般不会超过 32GB。</p><p>Java 的对象通过设计，大小一定是 8 的倍数，那么也就是对象所在位置的值的 2 进制最后 3 位一定是 0，既然我们知道对象的最后 3 位一定是 0，就可以不要这 3 位了，也就是之前的 0001 代表的是第 1 个字节，而现在的 0001 代表的是第 8 个字节（ 0001 &lt;&lt; 3）,那么可以表示的内存范围也是之前的 8 倍，即可以表示 32GB 的空间。</p><p>64 位 Java 虚拟机引入了压缩指针（Compressed Pointer）技术，将 Java 对象指针压缩为 32 位。这样，对象头部中的 Klass Pointer 就被压缩为 32 位，从而将对象头部的大小从 16 字节减小到 12 字节。图示来自：<a href="https://www.cnblogs.com/poemyang/p/19170159">压缩指针对对象大小的影响</a>：</p><p><img src="/img/image-20260126131547977.png" alt="image-20260126131547977"></p><p>在存储的时候，由于已经 8 字节对齐，所以我们直接 &gt;&gt; 3 存储，在读取时，我们需要用一个 long 类型接收读取值 &lt;&lt; 3 位后的结果 + heapBase（堆起始地址）。</p><p>目前压缩指针在 64 位 JVM &amp;&amp; 堆内存 ≤ 32GB 时是默认开启的~</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
      <tag>Java</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>JVM Structure</title>
    <link href="/2026/05/18/JVM-Structure/"/>
    <url>/2026/05/18/JVM-Structure/</url>
    
    <content type="html"><![CDATA[<h1 id="JVM-内存结构"><a href="#JVM-内存结构" class="headerlink" title="JVM 内存结构"></a>JVM 内存结构</h1><p>Java 虚拟机在执行 Java 程序的过程中会把它所管理的内存划分为若干个不同的数据区域。根据《Java虚拟机规范》的规定，Java 虚拟机所管理的内存将会包括以下几个运行时数据区域：</p><p><img src="/img/img/image-20251230152447993.png" alt="image-20251230152447993"></p><h2 id="程序计数器"><a href="#程序计数器" class="headerlink" title="程序计数器"></a>程序计数器</h2><p>在 Java 虚拟机的概念模型里，程序计数器存的是<code>下一条需要执行的字节码指令的地址</code>，由于 Java 的多线程是通过分配处理器执行时间的方式来实现，所以为了在切换线程时保留执行状态（执行到了哪个位置等），<code>每个线程都有自己的程序计数器</code>。</p><p>如果线程正在执行的是一个 Java 方法，这个计数器记录的是正在执行的虚拟机字节码指令的地址；如果正在执行的是本地（Native）方法，这个计数器值则应为空（Undefined）。此内存区域是唯 一一个在《Java虚拟机规范》中没有规定任何 OutOfMemoryError 情况的区域。</p><h2 id="Java-虚拟机栈"><a href="#Java-虚拟机栈" class="headerlink" title="Java 虚拟机栈"></a>Java 虚拟机栈</h2><p><code>Java 虚拟机栈也是线程私有的</code>，每个方法被执行的时候，Java 虚拟机都会同步创建一个<code>栈帧（Stack Frame）用于存储局部变量表、操作数栈、动态连接、方法出口等信息</code>。每一个方法被调用直至执行完毕的过程，就对应着一个栈帧在虚拟机栈中从入栈到出栈的过程。</p><p>局部变量表存放了编译期可知的基本数据类型（boolean、byte、char、short、int、 float、long、double）、对象引用类型和 returnAddress 类型。</p><ul><li>对象引用类型：可能是一个指向对象起始地址的引用指针，也可能是一个代表对象的句柄，总之可以通过该值关联到对象。</li><li>returnAddress 类型：执行一条字节码指令的地址。</li></ul><p>这些数据类型在局部变量表中的存储空间以局部变量槽（Slot）来表示，其中 64 位长度的 long 和 double 类型的数据会占用两个变量槽，其余的数据类型只占用一个。<code>局部变量表所需的内存空间在编译期间完成分配</code>，当进入一个方法时，这个方法需要在栈帧中分配多大的局部变量空间是完全确定的，在方法运行期间不会改变局部变量表的大小（大小指槽的个数）。</p><p>《Java虚拟机规范》中，对这个内存区域规定了两类异常状况：如果线程请求的栈深度大于虚拟机所允许的深度，将抛出StackOverflowError 异常；如果 Java 虚拟机栈容量可以动态扩展，当栈扩展时无法申请到足够的内存会抛出 OutOfMemoryError 异常（HotSpot 虚拟机的栈容量是不可以动态扩展的）。</p><h2 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a>本地方法栈</h2><p>Java 虚拟机栈是为虚拟机执行 Java 方法服务，本地方法栈则是为虚拟机执行使用到的本地（Native）方法服务。</p><p>有的 Java 虚拟机（譬如Hot-Spot虚拟机）直接就把本地方法栈和虚拟机栈合二为一。与虚拟机栈一样，本地方法栈也会在栈深度溢出或者栈扩展失败时分别抛出 StackOverflowError 和 OutOfMemoryError 异常。</p><h2 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h2><p><code>Java 堆是被所有线程共享的一块内存区域，“几乎”所有的对象实例都在这里分配内存。</code>它是垃圾收集器管理的内存区域，因此一些资料中它也被称作 “GC堆”（Garbage Collected Heap）。“几乎”的原因是随着逃逸分析的发展和标量替换等手段，对象可能通过标量替换手段变成栈上分配。</p><p>由于垃圾收集器大部分基于分代收集理论设计，所以 Java 堆中经常出现 “新生代”、“老年代”等名词，但这些区域划分仅仅是一部分垃圾收集器的共同特性或者说设计风格而已，而非某个 Java 虚拟机具体实现的固有内存布局，更不是《Java虚拟机规范》里对 Java堆的进一步细致划分。</p><p>从分配内存的角度看，所有线程共享的 Java 堆中可以划分出多个线程私有的分配缓冲区 （Thread Local Allocation Buffer，TLAB），以提升对象分配时的效率。因为堆是线程所共享的，因此在分配内存时需要考虑线程安全，如果每个线程都有一定的私有缓冲区，则只需在为线程分配私有缓冲区时加锁即可。</p><p>如果在 Java 堆中没有内存完成实例分配，并且堆也无法再扩展时（-Xmx），Java 虚拟机将会抛出 OutOfMemoryError 异常。</p><h2 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h2><p><code>方法区（Method Area）与 Java 堆一样是各个线程共享的内存区域，它用于存储已被虚拟机加载的类型信息、常量、静态变量、即时编译器编译后的代码缓存等数据。</code>虽然《Java虚拟机规范》中把方法区描述为堆的一个逻辑部分，但是它却有一个别名叫作“非堆”（Non-Heap），目的是与 Java 堆区分开来。</p><p>在 JDK8 以前，Java 程序员习惯称呼方法区为“永久代”（Permanent Generation）或将其混为一谈，但本质上这两者并不是等价的，只是因为当时的 HotSpot 虚拟机设计团队选择把收集器的分代设计扩展至方法区，或者说使用永久代来实现方法区，使得 HotSpot 的垃圾收集器能够像管理 Java 堆一样管理这部分内存，省去了为这个区域专门编写内存管理代码。但是对于其他虚拟机实现，譬如 BEA JRockit、IBM J9 等来说，是不存在永久代的概念的。</p><p>在收购了 BEA 后，为了移植 JRockit 中的优秀特性，在 JDK 6 的 时候 HotSpot 开发团队就有放弃永久代，逐步改为采用本地内存（Native Memory）来实现方法区的计划了。JDK 7 的 HotSpot，已经把原本放在永久代的字符串常量池、静态变量等移出，而到了 JDK 8，终于完全废弃了永久代的概念，改用与 JRockit、J9 一样在本地内存中实现的元空间（Meta space）来代替，把 JDK 7 中永久代还剩余的内容（主要是类型信息）全部移到元空间中。</p><p>相对而言，垃圾收集行为在这个区域的确是比较少出现的，但<code>并非数据进入了方法区就如永久代的名字一样“永久”存在了</code>。这区域的内存回收目标主要是针对常量池的回收和对类型的卸载，一般来说这个区域的回收效果比较难令人满意，尤其是类型的卸载，条件相当苛刻，但是这部分区域的回收有时又确实是必要的。</p><blockquote><p>准备阶段是正式为类中定义的变量（即静态变量，被 static 修饰的变量）分配内存并设置类变量初始值的阶段，从概念上讲，这些变量所使用的内存都应当在方法区中进行分配，但必须注意到方法区本身是一个逻辑上的区域，在 JDK7 及之前，HotSpot 使用永久代来实现方法区时，实现是完全符合这种逻辑概念的；而在 JDK8 及之后，类变量则会随着 Class 对象一起存放在 Java 堆中，这时候“类变量在 方法区”就完全是一种对逻辑概念的表述了。</p></blockquote><p><strong>在 JDK8 及之后，类变量则会随着 Class 对象一起存放在 Java 堆中。</strong></p><h3 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h3><p>运行时常量池（Runtime Constant Pool）是方法区的一部分。Class 文件中有一项信息是常量池表（Constant Pool Table），用于存放编译期生成的各种字面量与符号引用，这部分内容将在类加载后存放到方法区的运行时常量池中。</p><p>运行时常量池相对于 Class 文件常量池的另外一个重要特征是具备动态性，Java 语言并不要求常量一定只有编译期才能产生，也就是说，并非预置入 Class 文件中常量池的内容才能进入方法区运行时常 量池，<code>运行期间也可以将新的常量放入池中</code>，这种特性被开发人员利用得比较多的便是 String 类的 intern() 方法。</p><p>《Java虚拟机规范》的规定，如果方法区（包括运行时常量池）无法满足新的内存分配需求时，将抛出 OutOfMemoryError 异常。</p><h2 id="直接内存"><a href="#直接内存" class="headerlink" title="直接内存"></a>直接内存</h2><p>直接内存（Direct Memory）并不是虚拟机运行时数据区的一部分，但也会导致 OutOfMemoryError 的出现。</p><p>NIO（New Input&#x2F;Output）类引入了一种基于通道（Channel）与缓冲区（Buffer）的 I&#x2F;O 方式，它可以使用 Native 函数库<code>直接分配堆外内存</code>，然后通过一个存储在 Java 堆里面的 DirectByteBuffer 对象作为这块内存的引用进行操作。这样能在一些场景中显著提高性能，因为避免了在 Java 堆和 Native 堆中来回复制数据（Netty 中经常使用）。</p><p>注意：直接内存不再受 Java 堆内存的限制，只受本机物理内存限制（物理大小和操作系统级限制），但如果设置 -Xmx 等信息时不注意，可能会导致各个内存区域和大于物理内存，从而导致 OutOfMemoryError 。</p>]]></content>
    
    
    <categories>
      
      <category>经典图书</category>
      
      <category>深入理解Java虚拟机</category>
      
    </categories>
    
    
    <tags>
      
      <tag>JVM</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>设计模式中的经典设计原则</title>
    <link href="/2026/05/17/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%AD%E7%9A%84%E7%BB%8F%E5%85%B8%E8%AE%BE%E8%AE%A1%E5%8E%9F%E5%88%99/"/>
    <url>/2026/05/17/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E4%B8%AD%E7%9A%84%E7%BB%8F%E5%85%B8%E8%AE%BE%E8%AE%A1%E5%8E%9F%E5%88%99/</url>
    
    <content type="html"><![CDATA[<h1 id="设计模式中的经典设计原则"><a href="#设计模式中的经典设计原则" class="headerlink" title="设计模式中的经典设计原则"></a>设计模式中的经典设计原则</h1><p>设计模式中有一些经典的设计原则，包括：SOLID、KISS、YAGNI、DRY、LOD 等。</p><p>其中 SOLID 分别对应 单一职责原则、开闭原则、里式替换原则、接口隔离原则 和 依赖反转原则。</p><h2 id="单一职责原则"><a href="#单一职责原则" class="headerlink" title="单一职责原则"></a>单一职责原则</h2><p>Single Responsibility Principle：A class or module should have a single reponsibility，翻译过来就是 一个类或者模块只负责完成一个职责。</p><p>我曾经实习的公司关于 code review 就有一些规定：代码参数不能超过 5 个，函数行数不能超过 100 行，否则认为函数的功能不够单一，其实我觉得行数这个事情应该分情况，所有的错误信息都应该打印出来或者至少不能丢失，而 go 语言当时又经常出现下面的情况:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs go"><span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<br>    log.<span class="hljs-type">error</span>(<span class="hljs-string">&quot;..... failed:%v&quot;</span>, err);<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>, err;<br>&#125;<br></code></pre></td></tr></table></figure><p>导致代码行数经常介于边缘。这个原则的判断需要根据具体情况来分析，例如有一个 UserInfo 的类，类中主要属性为用户信息和地址信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserInfo</span> &#123;<br>  <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> userId;<br>  <span class="hljs-keyword">private</span> String username;<br>  <span class="hljs-keyword">private</span> String email;<br>  <span class="hljs-keyword">private</span> String telephone;<br>  <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> createTime;<br>  <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> lastLoginTime;<br>  <span class="hljs-keyword">private</span> String avatarUrl;<br>  <span class="hljs-keyword">private</span> String provinceOfAddress; <span class="hljs-comment">// 省</span><br>  <span class="hljs-keyword">private</span> String cityOfAddress; <span class="hljs-comment">// 市</span><br>  <span class="hljs-keyword">private</span> String regionOfAddress; <span class="hljs-comment">// 区 </span><br>  <span class="hljs-keyword">private</span> String detailedAddress; <span class="hljs-comment">// 详细地址</span><br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到地址信息在 UserInfo 中的比重是很高的，可以拆分出来单独维护 Address 类，结合具体情况分析：</p><ul><li>这是一个社交软件，用户信息就是有所在地省市区，如果拆分出来查询的时候反而需要再聚合，这时就可以不再拆分；</li><li>这是一个购物软件，address 可能有多个，用来选择收货地址，那么久可以拆分出来维护地址信息。</li></ul><p>因此<code>在不同的应用场景，不同的发展阶段，对同一个类的职责是否单一的判定是有可能不一样的。</code>在实际开发过程中，可以先写一个粗粒度的类，满足业务需求，随着业务的发展再进行持续的重构。</p><p>但是依然有一些原则可以用来指导判断类是否职责单一：</p><ul><li>参数、行数或属性过多；</li><li>类依赖的其他类过多；</li><li>私有方法过多；</li><li>类、函数已经很难起新的命名了；</li><li>类中大量的方法都是在操作其中的几个属性。</li></ul><p>单一职责原则希望避免设计大而全的类而导致功能耦合，以提高类的内聚性；同时，类职责单一，依赖和被依赖的其他类变少，也可以减少代码的耦合性。</p><h2 id="开闭原则"><a href="#开闭原则" class="headerlink" title="开闭原则"></a>开闭原则</h2><p>Open Closed Principle：software entities (modules, classes, functions, etc.) should be open for extension , but closed for modification。翻译一下就是：软件实体（模块、类、方法等）应该“对扩展开放、对修改关闭”。</p><p>例如要实现一个拦截器的功能，用户分为两类，普通用户和管理用户</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">preHandle</span><span class="hljs-params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    <span class="hljs-keyword">if</span> (handler.getClass().isAssignableFrom(HandlerMethod.class)) &#123;<br>        <span class="hljs-keyword">if</span> (isExclusion(handler)) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        &#125;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> request.getHeader(<span class="hljs-string">&quot;Authorization&quot;</span>);<br>        <span class="hljs-keyword">if</span> (StringUtils.isBlank(token)) &#123;<br>            token = request.getParameter(<span class="hljs-string">&quot;access_token&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">if</span> (StringUtils.isBlank(token)) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>        <span class="hljs-type">UserToken</span> <span class="hljs-variable">userFromToken</span> <span class="hljs-operator">=</span> tokenService.getUserFromToken(token);<br>        <span class="hljs-keyword">if</span> (tokenTimeOut(token, userFromToken, request)) &#123;<br>            setResponseStatus(response, HttpStatus.UNAUTHORIZED);<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>目前的规则是很简单的，只是单纯的判断 token 是否过期，随着时间的推移慢慢有了更多的需求……</p><ul><li>&#x2F;admin&#x2F;* 下的所有请求只有管理用户能访问；</li><li>新增截止日期，在截止日期之后只有管理用户能访问；</li><li>如果用户状态是……那么只允许访问 Get 请求。</li></ul><p>这样每次改动都需要直接修改拦截器的代码，代码的拓展性就不高，如果可以定义一个接口，传入当前的 token，所有的校验规则都实现这个接口并进行自己的校验，拦截器通过注入或其他方式获取所有的校验类，挨个进行校验并返回:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Rule</span> &#123;<br>    Result <span class="hljs-title function_">validate</span><span class="hljs-params">(UserToken token, Request request)</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TimeOutRule</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Rule</span> &#123;<br>    Result <span class="hljs-title function_">validate</span><span class="hljs-params">(UserToken token, Request request)</span> &#123;<br>        <br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AdminRule</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Rule</span> &#123;<br>    Result <span class="hljs-title function_">validate</span><span class="hljs-params">(UserToken token, Request request)</span> &#123;<br>        <br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-meta">@Slf4j</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AuthenticationInterceptor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">HandlerInterceptor</span> &#123;<br><br>    <span class="hljs-meta">@AutoWired</span><br>    <span class="hljs-keyword">private</span> List&lt;Rule&gt; rules;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">preHandle</span><span class="hljs-params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-keyword">if</span> (handler.getClass().isAssignableFrom(HandlerMethod.class)) &#123;<br>            <span class="hljs-keyword">if</span> (isExclusion(handler)) &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>            &#125;<br>            <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> request.getHeader(<span class="hljs-string">&quot;Authorization&quot;</span>);<br>            <span class="hljs-keyword">if</span> (StringUtils.isBlank(token)) &#123;<br>                token = request.getParameter(<span class="hljs-string">&quot;access_token&quot;</span>);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (StringUtils.isBlank(token)) &#123;<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>            &#125;<br>            <span class="hljs-keyword">for</span> (Rule rule : rules) &#123;<br>                rule.validate(UserToken token, Request request);<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>再以后新增新的告警规则时，只需要添加新的 Rule 即可，不用修改原有的代码逻辑，而如果直接修改原有的代码，一方面需要进行逻辑的修改，测试需要全面，另一方面如果随着需求的变化不再校验了不方便删除。</p><p>相应的，为了支撑这样的拓展性，代码的可读性也变差了一些，理解起来也有了一些难度。</p><p>因此，在实际开发过程中，如果函数中的逻辑并不复杂，if 语句并没有很多，那么直接在函数中修改才是比较合理的选择。</p><h2 id="里式替换"><a href="#里式替换" class="headerlink" title="里式替换"></a>里式替换</h2><p>Liskov Substitution Principle：Functions that use pointers of references to base classes must be able to use objects of derived classes without knowing it。翻译一下就是：子类对象（object of subtype&#x2F;derived class）能够替换程序（program）中父类对象（object of base&#x2F;parent class）出现的任何地方，并且保证原来程序的逻辑行为（behavior）不变及正确性不被破坏。</p><p>如果熟悉多态的话，其实会觉得这很容易理解，因为父类引用可以指向子类对象，而子类继承了父类的方法，因此即使替换代码依然可以正常运行。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ParentFly</span> &#123;<br>    <span class="hljs-keyword">public</span> Boolean <span class="hljs-title function_">fly</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SonFly</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ParentFly</span> &#123;<br><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">test</span><span class="hljs-params">(ParentFly parentFly)</span> &#123;<br>        System.out.println(parentFly.fly());<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">Test</span> <span class="hljs-variable">test</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Test</span>();<br>        test.test(<span class="hljs-keyword">new</span> <span class="hljs-title class_">SonFly</span>());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>上述例子中 SonFly 完全可以作为 ParentFly 类型的参数，并且程序不受影响</p><p>但是里式替换并不是多态，如果子类重写了父类的 fly 方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SonFly</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">ParentFly</span> &#123;<br>    <span class="hljs-keyword">public</span> Boolean <span class="hljs-title function_">fly</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在父类的逻辑里，返回 true&#x2F;false ，而子类重写了这个方法并返回 null，完全有可能导致使用该方法的位置出现空指针异常，这种有违背了 LSP 原则。</p><p>多态是面向对象编程的一大特性，也是面向对象编程语言的一种语法。它是一种代码实现的思路。而<code>里式替换是一种设计原则，是用来指导继承关系中子类该如何设计的，子类的设计要保证在替换父类的时候，不改变原有程序的逻辑以及不破坏原有程序的正确性。</code></p><p>以下这些行为都不符合里式替换的原则：</p><ul><li>子类违背父类声明要实现的功能；</li><li>子类违背父类对输入、输出、异常的约定。如找不到一个元素是返回 null，子类抛出了异常。</li></ul><h2 id="接口隔离"><a href="#接口隔离" class="headerlink" title="接口隔离"></a>接口隔离</h2><p>Interface Segregation Principle：Clients should not be forced to depend upon interfaces that they do not use。翻译一下就是：客户端不应该强迫依赖它不需要的接口。</p><p>例如现在有一个用户接口，里面包含很多功能：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">UserService</span> &#123;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">register</span><span class="hljs-params">(String cellphone, String password)</span>;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">login</span><span class="hljs-params">(String cellphone, String password)</span>;<br>  UserInfo <span class="hljs-title function_">getUserInfoById</span><span class="hljs-params">(<span class="hljs-type">long</span> id)</span>;<br>  UserInfo <span class="hljs-title function_">getUserInfoByCellphone</span><span class="hljs-params">(String cellphone)</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">UserService</span> &#123;<br>  <span class="hljs-comment">//...</span><br>&#125;<br></code></pre></td></tr></table></figure><p>现在需要一个删除功能，但是删除功能只允许被某个模块调用，如果简单的在 UserService 中新增一个删除接口，那么所有注入 UserService 的地方都可以调用，一方面缺少足够的安全性，另一方面其他模块注入的 UserService 并不需要这个功能，因此最佳实践应该是定义一个新的 Service，并让 UserServiceImpl 实现这个接口:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">UserService</span> &#123;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">register</span><span class="hljs-params">(String cellphone, String password)</span>;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">login</span><span class="hljs-params">(String cellphone, String password)</span>;<br>  UserInfo <span class="hljs-title function_">getUserInfoById</span><span class="hljs-params">(<span class="hljs-type">long</span> id)</span>;<br>  UserInfo <span class="hljs-title function_">getUserInfoByCellphone</span><span class="hljs-params">(String cellphone)</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">RestrictedUserService</span> &#123;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">deleteUserByCellphone</span><span class="hljs-params">(String cellphone)</span>;<br>  <span class="hljs-type">boolean</span> <span class="hljs-title function_">deleteUserById</span><span class="hljs-params">(<span class="hljs-type">long</span> id)</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">UserService</span>, RestrictedUserService &#123;<br>  <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如果这不是一组接口，而是一个单独的类中的方法，例如示例计算一个集合中的最大值、最小值、平均值等</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Statistics</span> &#123;<br>  <span class="hljs-keyword">private</span> Long max;<br>  <span class="hljs-keyword">private</span> Long min;<br>  <span class="hljs-keyword">private</span> Long average;<br>  <span class="hljs-keyword">private</span> Long sum;<br>  <span class="hljs-keyword">private</span> Long percentile99;<br>  <span class="hljs-keyword">private</span> Long percentile999;<br>&#125;<br><br><span class="hljs-keyword">public</span> Statistics <span class="hljs-title function_">count</span><span class="hljs-params">(Collection&lt;Long&gt; dataSet)</span> &#123;<br>  <span class="hljs-type">Statistics</span> <span class="hljs-variable">statistics</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Statistics</span>();<br>  <span class="hljs-comment">//... 省略计算逻辑...</span><br>  <span class="hljs-keyword">return</span> statistics;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果调用该方法只是为了其中的 max 和 min，那么这个方法就该拆分，获取每个值单独一个方法；而如果所有的值都需要被展示，那么这个方法就是合适的。</p><p>现在假如有三个配置类：MysqlConfig、RedisConfig、KafkaConfig，希望其中 Redis 和 Kafka 支持功能 A，Mysql、Redis 支持功能 B，一种实现方式就是定义一个全的接口，包含 A 和 B，所有的 Config 都实现这个接口；另一种方式就是定义两个接口，不同的 Config 按需实现。</p><p>第二种方式无疑是更好的，一方面它符合单一职责的设计，另一方面它不违背接口隔离，不要求强制被实现，而且具有良好的拓展性。</p><p>某种程度上讲：<code>单一职责原则和接口隔离原则有些相似，但接口隔离侧重于接口的设计，并从调用者是否只使用部分接口或接口的部分功能的角度来判断职责是否单一</code>。</p><h2 id="依赖反转"><a href="#依赖反转" class="headerlink" title="依赖反转"></a>依赖反转</h2><p>Dependency Inversion Principle，理解依赖反转首先要理解控制反转(Inversion Of Control)，即 IOC：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceTest</span> &#123;<br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">doTest</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-comment">// ... </span><br>  &#125;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<span class="hljs-comment">// 这部分逻辑可以放到框架中</span><br>    <span class="hljs-keyword">if</span> (doTest()) &#123;<br>      System.out.println(<span class="hljs-string">&quot;Test succeed.&quot;</span>);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>      System.out.println(<span class="hljs-string">&quot;Test failed.&quot;</span>);<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>上述的示例代码中，需要我们完全控制流程的流转，而利用模板设计模式可以改变这一方式，如下代码所示，如果能在JunitApplication 启动前把所有的 TestCase 实例（其实是子类，因为这是抽象类）填充到 testCases 中即可：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestCase</span> &#123;<br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">if</span> (doTest()) &#123;<br>      System.out.println(<span class="hljs-string">&quot;Test succeed.&quot;</span>);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>      System.out.println(<span class="hljs-string">&quot;Test failed.&quot;</span>);<br>    &#125;<br>  &#125;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">doTest</span><span class="hljs-params">()</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JunitApplication</span> &#123;<br>  <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> List&lt;TestCase&gt; testCases = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">register</span><span class="hljs-params">(TestCase testCase)</span> &#123;<br>    testCases.add(testCase);<br>  &#125;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <span class="hljs-keyword">for</span> (TestCase <span class="hljs-keyword">case</span>: testCases) &#123;<br>      <span class="hljs-keyword">case</span>.run();<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceTest</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">TestCase</span> &#123;<br>  <span class="hljs-meta">@Override</span><br>  <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">doTest</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-comment">// ... </span><br>  &#125;<br>&#125;<br><br><span class="hljs-comment">// 注册操作还可以通过配置的方式来实现，不需要显示调用 register()</span><br>JunitApplication.register(<span class="hljs-keyword">new</span> <span class="hljs-title class_">UserServiceTest</span>();<br></code></pre></td></tr></table></figure><p>在上面的示例中，这个抽象类就是框架提供的拓展点，我们只需要在这个拓展点上添加跟自己业务相关的代码，程序的执行就可以通过框架来控制，控制权从开发者“反转”到了框架。</p><p>事实上，实现控制反转的方法很多，除了上面的利用模板设计模式的方法之外，还可以利用大名鼎鼎的依赖注入。所以<code>控制反转并不是一种具体的实现技巧，而是一种设计思想，一般用来指导框架层面的设计</code>。</p><p>而<code>依赖注入跟控制反转恰恰相反，它是一种具体的编码技巧。概括来说就是：不通过 new() 的方式在类内部创建依赖类对象，而是将依赖的类对象在外部创建好后，通过构造函数、函数参数等方式传递（或注入）给类使用</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Notification</span> &#123;<br>  <span class="hljs-keyword">private</span> MessageSender messageSender;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-title function_">Notification</span><span class="hljs-params">(MessageSender messageSender)</span> &#123;<br>    <span class="hljs-built_in">this</span>.messageSender = messageSender;<br>  &#125;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">sendMessage</span><span class="hljs-params">(String cellphone, String message)</span> &#123;<br>    <span class="hljs-built_in">this</span>.messageSender.send(cellphone, message);<br>  &#125;<br>&#125;<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">MessageSender</span> &#123;<br>  <span class="hljs-keyword">void</span> <span class="hljs-title function_">send</span><span class="hljs-params">(String cellphone, String message)</span>;<br>&#125;<br><span class="hljs-comment">// 短信发送类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">SmsSender</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">MessageSender</span> &#123;<br>  <span class="hljs-meta">@Override</span><br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">send</span><span class="hljs-params">(String cellphone, String message)</span> &#123;<br>    <span class="hljs-comment">//....</span><br>  &#125;<br>&#125;<br><span class="hljs-comment">// 站内信发送类</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">InboxSender</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">MessageSender</span> &#123;<br>  <span class="hljs-meta">@Override</span><br>  <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">send</span><span class="hljs-params">(String cellphone, String message)</span> &#123;<br>    <span class="hljs-comment">//....</span><br>  &#125;<br>&#125;<br><span class="hljs-comment">// 使用 Notification</span><br><span class="hljs-type">MessageSender</span> <span class="hljs-variable">messageSender</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SmsSender</span>();<br><span class="hljs-type">Notification</span> <span class="hljs-variable">notification</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Notification</span>(messageSender);<br></code></pre></td></tr></table></figure><p>例如在上述示例代码的 30 行，先创建出具体的 Sender，31 行再通过构造器传入。</p><p>但是如果一个类需要很多的依赖类对象，类对象的创建和依赖注入会变得非常复杂。人工创建不但繁琐，还需要判断创建的顺序，容易出错且开发成本高，于是有了相应的框架，及“依赖注入框架”。我们只需要通过框架提高的拓展点，配置一下所有要创建的类对象和相互的依赖关系，就可以由框架来实现自动创建对象，管理对象的生命周期，依赖注入等功能。</p><p>现成的依赖注入框架有很多，比如 Google Guice、Java Spring、Pico Container、Butterfly Container 等。不过，如果你熟悉 Java Spring 框架，你可能会说，Spring 框架自己声称是<strong>控制反转容器</strong>（Inversion Of Control Container），控制反转是一个宽泛的概念，而依赖注入框架则更具体一些。</p><p>而依赖反转原则：High-level modules shouldn’t depend on low-level modules. Both modules should depend on abstractions. In addition, abstractions shouldn’t depend on details. Details depend on abstractions。翻译一下大概意思就是：高层模块（high-level modules）不要依赖低层模块（low-level）。高层模块和低层模块应该通过抽象（abstractions）来互相依赖。除此之外，抽象（abstractions）不要依赖具体实现细节（details），具体实现细节（details）依赖抽象（abstractions）。</p><p>Tomcat 是运行 Java Web 应用程序的容器。我们编写的 Web 应用程序代码只需要部署在 Tomcat 容器下，便可以被 Tomcat 容器调用执行。按照之前的划分原则，Tomcat 就是高层模块，我们编写的 Web 应用程序代码就是低层模块。Tomcat 和应用程序代码之间并没有直接的依赖关系，两者都依赖同一个“抽象”，也就是 Sevlet 规范。Servlet 规范不依赖具体的 Tomcat 容器和应用程序的实现细节，而 Tomcat 容器和应用程序依赖 Servlet 规范。</p><h2 id="KISS-AND-YAGNI"><a href="#KISS-AND-YAGNI" class="headerlink" title="KISS AND YAGNI"></a>KISS AND YAGNI</h2><p>KISS 即 Keep It Simple and Stupid，翻译一下就是保持简单。</p><p>有些公司会禁止使用多层 stream、lambda 表达式，原因就是这些虽然缩短了代码行数，但不容易被理解，尤其是stream 中嵌套 stream。因此并不是代码行数越少就越简单。包括正则表达式，使用多个简单易理解的正则会比使用一个大而全的正则更加易于理解，也更加简单和易于维护。</p><p>但对于一些复杂的问题，如 KMP 匹配子串，或者是一些对性能要求很高的场景，这类本身具有逻辑复杂、实现难度大的场景，使用复杂的方法来解决，也并不违背 KISS 原则。</p><p>YAGNI 原则的英文全称是：You Ain’t Gonna Need It。直译就是：你不会需要它。</p><p>在软件开发的过程中，一定不要过度设计，虽然要预留拓展点，但没必要提前编写这部分的代码。例如 Java 开发为了避免缺少依赖，提前往项目里引入大量常用的依赖包，这样的做法也是违背 YAGNI 的。</p><p><code>KISS 原则强调“如何做”的问题（尽量保持简单），而 YAGNI 原则说的是“要不要做”的问题</code>。</p><p>指导原则：</p><ul><li>不要使用同事很多不懂的技术，至少现阶段不要使用。例如虽然 Optional 语法出来很久了，但有些同事不太熟悉，导致对一些代码不太理解。</li><li>不要重复造轮子，要善于利用已有的工具类库。</li><li>不要过度优化。</li></ul><h2 id="DRY"><a href="#DRY" class="headerlink" title="DRY"></a>DRY</h2><p>Don’t Repeat Yourself. 翻译一下就是不要重复你自己，即不要编写重复的代码。</p><ul><li>功能语义重复</li></ul><p>这里最常遇到的其实不是完全相同的代码，一般这种 IDEA 也会给出提示。最常遇到的其实是多个同事一起开发，各自写了同样功能但是不同命名且不同实现的函数，例如同事A使用正则进行校验，同事B定制编码校验。这样会有一些问题：一方面如何后期逻辑变化，两个函数的实现都需要修改；另一方面，对于新入手项目的同事也不友好，不清楚为什么有的地方校验调用 A 函数，而有些地方调用 B 函数。</p><ul><li>代码执行重复</li></ul><p>有些情况下某些校验可能被执行了两次，例如 controller 接口有一个根据 email 查找用户的功能</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserControleler</span> &#123;<br>    <span class="hljs-keyword">private</span> UserServiceImpl service;<span class="hljs-comment">// 通过依赖注入</span><br>    <span class="hljs-keyword">public</span> User <span class="hljs-title function_">getUserByEmail</span><span class="hljs-params">(String email)</span> &#123;<br>        <span class="hljs-keyword">if</span> (StringUtils.isBlank(email)) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;邮箱不能为空&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> service.getUserByEmail(email);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceImpl</span> &#123;<br>    <span class="hljs-keyword">private</span> UserDao userDao;<span class="hljs-comment">// 通过依赖注入</span><br>    <span class="hljs-keyword">public</span> User <span class="hljs-title function_">getUserByEmail</span><span class="hljs-params">(String email)</span> &#123;<br>        <span class="hljs-keyword">if</span> (StringUtils.isBlank(email)) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;邮箱不能为空&quot;</span>);<br>        &#125;<br>        <span class="hljs-keyword">return</span> userDao.findByEmail(email);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>判断 email 输入是否为空的校验做了两次，实际上是完全没有必要的。这个例子没有涉及到数据库IO，所以对性能影响并不会很大，但是如果设计数据库查询执行了多次，就可能影响性能了。</p><p>DRY 原则从定义上与代码的复用有点类似，但它只是一个指导原则，复用是一种行为，而可复用性是对代码的一种评价，不重复和可复用完全是两个概念。</p><h2 id="迪米特法则"><a href="#迪米特法则" class="headerlink" title="迪米特法则"></a>迪米特法则</h2><p>迪米特法则的英文翻译是：Law of Demeter，缩写是 LOD，最原汁原味的翻译其实是：每个模块（unit）只应该了解那些与它关系密切的模块的有限知识）。或者说，每个模块只和自己的朋友“说话”（talk），不和陌生人“说话”（talk）。常用来指导设计出“高内聚，低耦合”的系统。</p><p><img src="/img/image-20250918093704821.png" alt="image-20250918093704821"></p><p>如上图所示，左侧的系统就是高内聚低耦合的，每个模块都只与相邻的模块进行交互，而右侧就不满足这样的情况，如果AB中的代码修改，会同时影响 GI 和H 模块。</p><p>例如有一个序列化和反序列化接口，如果有的类只想依赖序列化接口，另一部分类只想依赖反序列化接口</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Serializer</span> &#123; <span class="hljs-comment">// 参看 JSON 的接口定义</span><br>  <span class="hljs-keyword">public</span> String <span class="hljs-title function_">serialize</span><span class="hljs-params">(Object object)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  <span class="hljs-keyword">public</span> String <span class="hljs-title function_">serializeMap</span><span class="hljs-params">(Map map)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  <span class="hljs-keyword">public</span> String <span class="hljs-title function_">serializeList</span><span class="hljs-params">(List list)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  <br>  <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">deserialize</span><span class="hljs-params">(String objectString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  <span class="hljs-keyword">public</span> Map <span class="hljs-title function_">deserializeMap</span><span class="hljs-params">(String mapString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  <span class="hljs-keyword">public</span> List <span class="hljs-title function_">deserializeList</span><span class="hljs-params">(String listString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>&#125;<br></code></pre></td></tr></table></figure><p>那么就可以定义两个接口 serialize 和 deserialize</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Serializer</span> &#123;<br>  String <span class="hljs-title function_">serialize</span><span class="hljs-params">(Object object)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  String <span class="hljs-title function_">serializeMap</span><span class="hljs-params">(Map map)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  String <span class="hljs-title function_">serializeList</span><span class="hljs-params">(List list)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">deserialize</span> &#123;<br>  Object <span class="hljs-title function_">deserialize</span><span class="hljs-params">(String objectString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  Map <span class="hljs-title function_">deserializeMap</span><span class="hljs-params">(String mapString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>  List <span class="hljs-title function_">deserializeList</span><span class="hljs-params">(String listString)</span> &#123; <span class="hljs-comment">//... &#125;</span><br>&#125;<br>      <br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DemoClass_1</span> &#123;<br>  <span class="hljs-keyword">private</span> Serializable serializer;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-title function_">Demo</span><span class="hljs-params">(Serializable serializer)</span> &#123;<br>    <span class="hljs-built_in">this</span>.serializer = serializer;<br>  &#125;<br>  <span class="hljs-comment">//...</span><br>&#125;<br>      <br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DemoClass_2</span> &#123;<br>  <span class="hljs-keyword">private</span> Deserializable deserializer;<br>  <br>  <span class="hljs-keyword">public</span> <span class="hljs-title function_">Demo</span><span class="hljs-params">(Deserializable deserializer)</span> &#123;<br>    <span class="hljs-built_in">this</span>.deserializer = deserializer;<br>  &#125;<br>  <span class="hljs-comment">//...</span><br>&#125;      <br></code></pre></td></tr></table></figure><p>这样就可以满足 DemoClass_1  对反序列化接口无感知，而 DemoClass_2 对序列化接口无感知的要求，既符合迪米特法则所说的 “依赖有限接口”，又体现了 “基于接口编程而非实现编程” 的设计原则。 </p>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
    </categories>
    
    
    <tags>
      
      <tag>设计模式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>面向对象设计指南</title>
    <link href="/2026/05/16/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E8%AE%BE%E8%AE%A1%E7%9A%84%E9%81%BF%E5%9D%91%E6%8C%87%E5%8D%97/"/>
    <url>/2026/05/16/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E8%AE%BE%E8%AE%A1%E7%9A%84%E9%81%BF%E5%9D%91%E6%8C%87%E5%8D%97/</url>
    
    <content type="html"><![CDATA[<h1 id="面向对象设计指南"><a href="#面向对象设计指南" class="headerlink" title="面向对象设计指南"></a>面向对象设计指南</h1><p>一般说设计模式都是指面向对象的设计模式，因为面向对象语言可以借助封装、继承、多态等特性更好的达到复用性、可拓展性、可维护性。</p><p>面向对象一般指以类、对象为组织代码的基本单元，并将封装、继承、多态、抽象四个特性（抽象有的定义里并不认为是四大特性）作为代码设计与实现的基石。</p><ul><li><p>封装：通过访问权限控制，只对外暴露必要的操作，保护数据；</p></li><li><p>继承：<strong>代码复用</strong>，结构美感。不过 Java 语言不支持多重继承，原因是如果 BC 都继承了 A 并重写了某个方法，D 同时继承 BC 会产生歧义；</p></li><li><p>多态：提高代码的复用性，主要通过两种方式实现：</p><ul><li>继承：父类引用指向子类对象；</li><li>实现：接口引用指向具体实现类；</li></ul></li><li><p>抽象：有时并不计入四大特性，用来保护实现，例如接口就是对实现的一种抽象，无需关注实现。</p></li></ul><blockquote><p>用了面向对象语言就是面向对象开发吗？</p></blockquote><ul><li>滥用 getter、setter 方法。lombok 的注解确实很方便，但这样其实违背了面向对象的封装特性，例如 createTime 等字段其实是不需要 setter 方法的，需要在创建对象的时候就确定。</li><li>滥用全局变量、全局方法（Constants、Utils）。这样会导致修改后所有引用的地方都重新编译，而且有的时候只需要其中的某几个变量（或方法）却导入了整个类。<ul><li>功能拆分，不要定义一个大而全的类。例如 Constants 拆分为 DateConstants、RedisConstants、MysqlConstants</li></ul></li><li>**<code>定义数据与方法分离的类</code>**。传统的 MVC 开发中，数据在相应的 BO、VO、PO 中，而操作却封装在对应的 Controller、Service 中，这就是典型的面向过程，也就是<code>“贫血模型”</code>的开发方法。不过这样的开发方式依然很流行，因为大部分的需求并不复杂，只是从数据库中找到一些字段，组织对应的 VO，先写 service 反推 controller。</li></ul><blockquote><p>如何理解接口与抽象类？</p></blockquote><p>随着 jdk 版本的更新，接口也可以有默认实现，也可以定义变量作为常量使用。抽象类依然不允许被实例化，继承抽象类必须重写抽象类的所有方法。</p><p>先说结论：<strong>抽象类的作用更多是为了代码复用，而接口的作用则更偏向与“协议”，具备什么样的功能</strong>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">BaseEntity</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Serializable</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">serialVersionUID</span> <span class="hljs-operator">=</span> <span class="hljs-number">8417380540303280008L</span>;<br>    <br>    <span class="hljs-meta">@ApiModelProperty(value = &quot;所属用户标识&quot;)</span><br>    <span class="hljs-meta">@Column(name = &quot;USER_ID&quot;)</span><br>    <span class="hljs-keyword">private</span> String userId;<br><br>    <span class="hljs-meta">@ApiModelProperty(value = &quot;记录是否有效，默认为1表示有效&quot;)</span><br>    <span class="hljs-meta">@Column(name = &quot;ACTIVE&quot;)</span><br>    <span class="hljs-keyword">private</span> String active;<br><br>    <span class="hljs-meta">@ApiModelProperty(value = &quot;创建时间，默认为当前时间&quot;)</span><br>    <span class="hljs-meta">@Column(name = &quot;CREATED_AT&quot;, updatable = false)</span><br>    <span class="hljs-meta">@DateTimeFormat(pattern = Constants.PATTERN_DATE_HOUR_MINUTE_SECOND)</span><br>    <span class="hljs-meta">@JsonFormat(pattern = Constants.PATTERN_DATE_HOUR_MINUTE_SECOND, timezone = Constants.TIMEZONE)</span><br>    <span class="hljs-keyword">private</span> LocalDateTime createdAt;<br><br>    <span class="hljs-meta">@ApiModelProperty(value = &quot;更新时间&quot;)</span><br>    <span class="hljs-meta">@Column(name = &quot;UPDATED_AT&quot;)</span><br>    <span class="hljs-meta">@DateTimeFormat(pattern = Constants.PATTERN_DATE_HOUR_MINUTE_SECOND)</span><br>    <span class="hljs-meta">@JsonFormat(pattern = Constants.PATTERN_DATE_HOUR_MINUTE_SECOND, timezone = Constants.TIMEZONE)</span><br>    <span class="hljs-keyword">private</span> LocalDateTime updatedAt;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>例如我们有一个上面的类，对于一个正常的删除来讲，一方面我们要查询这个数据是否存在（例如有些系统删除不存在的空数据会返回错误），另一方面判断当前登录用户是否具有删除权限（即资源的 USER_ID 是否为当前登录人或是否是当前登录人的下属），最后还需要记录日志。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">delete</span><span class="hljs-params">(String uuid)</span> &#123;<br>    <span class="hljs-type">Entity</span> <span class="hljs-variable">entity</span> <span class="hljs-operator">=</span> getEntityFromDB(uuid);<br>    <span class="hljs-keyword">if</span> (entity == <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-keyword">throw</span> ......<br>    &#125;<br>    <span class="hljs-keyword">if</span> (entity <span class="hljs-keyword">instanceof</span> BaseEntity) &#123;<br><span class="hljs-type">BaseEntity</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> (BaseEntity)entity;<br>        <span class="hljs-keyword">if</span> (!e.getActive.equals(<span class="hljs-string">&quot;1&quot;</span>)) &#123;<br>            <br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            e.setActive(<span class="hljs-string">&quot;0&quot;</span>);<br>            saveEntityToDB(entity);<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> T <span class="hljs-title function_">saveToDataBase</span><span class="hljs-params">(T entity)</span>;<br><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> Entity <span class="hljs-title function_">getEntityFromDB</span><span class="hljs-params">(String uuid)</span>;<br></code></pre></td></tr></table></figure><p>借助抽象类与多态，可以提高代码的复用性，减少重复代码。</p><blockquote><p>如何理解基于接口而非实现编程？</p></blockquote><p>假如目前有一个上传图片到公有云的需求</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">uploadPictureAliyunImpl</span> &#123;<br>    <br>    <span class="hljs-comment">// 获取合法 token</span><br>    <span class="hljs-keyword">public</span> String <span class="hljs-title function_">getToken</span><span class="hljs-params">()</span> &#123;<br>        <br>    &#125;;<br>    <br>    <span class="hljs-comment">// 如果目录不存在就创建目录</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">createDictoryIfNotExists</span><span class="hljs-params">()</span> &#123;<br>        <br>    &#125;;<br>    <br>    <span class="hljs-comment">// 上传图片</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">uploadPictureToAliyun</span><span class="hljs-params">()</span> &#123;<br>        <br>    &#125;;<br>    <br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Main</span>() &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">Main</span> <span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">uploadPictureAliyunImpl</span> <span class="hljs-variable">impl</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">uploadPictureAliyunImpl</span>();<br>        <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> impl.getToken();<br>        ........<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果这样实现，后期替换为其他云厂商，例如自有的私有云，就需要替换很多代码，实际上这种情况只需要定义一个上传图片的接口，由不同的存储来实现就行。</p><p>基于接口编程，即进行更好的抽象设计，不暴露过多实现。</p><blockquote><p>为什么说多用组合少用继承？</p></blockquote><p>以鸟（bird）为例，可以分为是否会飞、是否会下蛋……</p><p><img src="/img/image-20250916170508725.png" alt="image-20250916170508725"></p><p>当继承层次越来越深，关系会越来越复杂，会严重影响代码的稳定性与可维护性。但是当继承层次很浅且业务稳定时，依然可以利用继承和多态特性来实现特定功能。</p><p>继承实际上可以替换为组合来实现，例如定义两个接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Flyable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">fly</span><span class="hljs-params">()</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Eggable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">egg</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>每一种鸟类根据自己的情况来实现对应接口即可，但是这会引入新的问题，例如有 n 个鸟类实现的 fly 接口都是一样的，那代码重复会十分严重，解决方式就是“委托”：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultFlyableImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Flyable</span> &#123;<br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">fly</span><span class="hljs-params">()</span> &#123;<br>        ......<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AAABird</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Flyable</span> &#123;<br>    <span class="hljs-comment">// 一般是使用注解注入</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">DefaultFlyableImpl</span> <span class="hljs-variable">defaultFlyImpl</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultFlyableImpl</span>();<br>    <br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">fly</span><span class="hljs-params">()</span> &#123;<br>        defaultFlyImpl.fly();<br>    &#125;<br>    <br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>设计模式</category>
      
    </categories>
    
    
    <tags>
      
      <tag>面向对象</tag>
      
      <tag>设计模式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之ziplist篇</title>
    <link href="/2026/05/15/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bziplist%E7%AF%87/"/>
    <url>/2026/05/15/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bziplist%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之ziplist篇"><a href="#Redis源码分析之ziplist篇" class="headerlink" title="Redis源码分析之ziplist篇"></a>Redis源码分析之ziplist篇</h1><p>如果提到双向链表，我们应该很熟悉，每一个对象都有三个字段</p><ul><li>指向前驱节点的指针；</li><li>指向后向节点的指针；</li><li>该节点的数据。</li></ul><p>双向链表可以在两端执行 O(1) 级别的操作，缺点是可能产生内存碎片，对空间利用率低（每个节点除了存储数据还要存指向相邻节点的指针）</p><p>而 ziplist 就是一个特殊编码的双向链表，它特殊在哪里呢？</p><ul><li>ziplist 每个节点没有使用前后指针；</li><li>ziplist 整体分配一大块内存；</li><li>ziplist 中的每个元素会根据实际情况分配不同大小的空间，充分体现了 Redis 对于存储效率的追求。</li></ul><h2 id="ziplist"><a href="#ziplist" class="headerlink" title="ziplist"></a>ziplist</h2><p>以下是官方的注释信息：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs c">* ZIPLIST OVERALL LAYOUT<br>* ======================<br>*<br>* The general layout of the ziplist is as follows:<br>*<br>* &lt;zlbytes&gt; &lt;zltail&gt; &lt;zllen&gt; &lt;entry&gt; &lt;entry&gt; ... &lt;entry&gt; &lt;zlend&gt;<br>*<br>* NOTE: all fields are stored in little endian, <span class="hljs-keyword">if</span> not specified otherwise.<br>*<br>* &lt;<span class="hljs-type">uint32_t</span> zlbytes&gt; is an <span class="hljs-type">unsigned</span> integer to hold the number of bytes that<br>* the ziplist occupies, including the four bytes of the zlbytes field itself.<br>* This value needs to be stored to be able to resize the entire structure<br>* without the need to traverse it first.<br>*<br>* &lt;<span class="hljs-type">uint32_t</span> zltail&gt; is the offset to the last entry in the <span class="hljs-built_in">list</span>. This allows<br>* a pop operation on the far side of the <span class="hljs-built_in">list</span> without the need <span class="hljs-keyword">for</span> full<br>* traversal.<br>*<br>* &lt;<span class="hljs-type">uint16_t</span> zllen&gt; is the number of entries. When there are more than<br>* <span class="hljs-number">2</span>^<span class="hljs-number">16</span><span class="hljs-number">-2</span> entries, this value is <span class="hljs-built_in">set</span> to <span class="hljs-number">2</span>^<span class="hljs-number">16</span><span class="hljs-number">-1</span> and we need to traverse the<br>* entire <span class="hljs-built_in">list</span> to know how many items it holds.<br>*<br>* &lt;<span class="hljs-type">uint8_t</span> zlend&gt; is a special entry representing the end of the ziplist.<br>* Is encoded as a single byte equal to <span class="hljs-number">255.</span> No other normal entry starts<br>* with a byte <span class="hljs-built_in">set</span> to the value of <span class="hljs-number">255.</span><br></code></pre></td></tr></table></figure><p>大概意思是说，ziplist 通常长下面这个样子：</p><p><img src="/img/073af5eeabbac436e8878ef9f598072e.png" alt="image-20250313110018679"></p><ul><li>zlbytes：4 字节，ziplistBytes，标识该 ziplist 的长度；</li><li>zltail：4 字节，ziplistTail，标识尾节点偏移量；</li><li>zllen：2字节，ziplistLen，zipList 中的 entry 个数；</li><li>zlend：1字节，ziplistEnd，结束标识，<code>固定值 255</code>。</li></ul><p>根据这个定义，我们可以很容易的找到这个 ziplist 的首项和尾项，起始指针偏移 10 字节就是头节点，偏移 zltail 值对应的字节就是尾节点，不过你可能有其他的问题：zllen 字段只有 2 字节，那 ziplist 是不是只能存 2 ^16^ -1 个元素呢，这个问题的答案我们放在最后说，另外，请注意 zlend 的固定值 255。</p><h2 id="ziplist-entry"><a href="#ziplist-entry" class="headerlink" title="ziplist entry"></a>ziplist entry</h2><p>我们思考一个这样的问题，链表能够进行双向遍历是存储了指向相邻节点的指针，那 ziplist 没有这样的指针，该怎么进行遍历呢？</p><p><strong>ziplist 中每个元素使用一个字段标识了前一个元素的长度，一个字段标识了本元素的长度</strong>，这样只要找到其中的任意一个元素，都可以进行双向遍历了。</p><p>ziplist 中的元素我们叫 entry，其结构如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs c">*<br>* &lt;prevlen from <span class="hljs-number">0</span> to <span class="hljs-number">253</span>&gt; &lt;encoding&gt; &lt;entry&gt;<br>*<br>* |<span class="hljs-number">00</span>pppppp| - <span class="hljs-number">1</span> byte<br>*      String value with length less than or equal to <span class="hljs-number">63</span> bytes (<span class="hljs-number">6</span> bits).<br>*      <span class="hljs-string">&quot;pppppp&quot;</span> represents the <span class="hljs-type">unsigned</span> <span class="hljs-number">6</span> bit length.<br>* |<span class="hljs-number">01</span>pppppp|qqqqqqqq| - <span class="hljs-number">2</span> bytes<br>*      String value with length less than or equal to <span class="hljs-number">16383</span> bytes (<span class="hljs-number">14</span> bits).<br>*      IMPORTANT: The <span class="hljs-number">14</span> bit number is stored in big endian.<br>* |<span class="hljs-number">10000000</span>|qqqqqqqq|rrrrrrrr|ssssssss|tttttttt| - <span class="hljs-number">5</span> bytes<br>*      String value with length greater than or equal to <span class="hljs-number">16384</span> bytes.<br>*      Only the <span class="hljs-number">4</span> bytes following the first byte represents the length<br>*      up to <span class="hljs-number">32</span>^<span class="hljs-number">2</span><span class="hljs-number">-1.</span> The <span class="hljs-number">6</span> lower bits of the first byte are not used and<br>*      are <span class="hljs-built_in">set</span> to zero.<br>*      IMPORTANT: The <span class="hljs-number">32</span> bit number is stored in big endian.<br>* |<span class="hljs-number">11000000</span>| - <span class="hljs-number">3</span> bytes<br>*      Integer encoded as <span class="hljs-title function_">int16_t</span> <span class="hljs-params">(<span class="hljs-number">2</span> bytes)</span>.<br>* |11010000| - 5 bytes<br>*      Integer encoded as <span class="hljs-title function_">int32_t</span> <span class="hljs-params">(<span class="hljs-number">4</span> bytes)</span>.<br>* |11100000| - 9 bytes<br>*      Integer encoded as <span class="hljs-title function_">int64_t</span> <span class="hljs-params">(<span class="hljs-number">8</span> bytes)</span>.<br>* |11110000| - 4 bytes<br>*      Integer encoded as 24 bit <span class="hljs-title function_">signed</span> <span class="hljs-params">(<span class="hljs-number">3</span> bytes)</span>.<br>* |11111110| - 2 bytes<br>*      Integer encoded as 8 bit <span class="hljs-title function_">signed</span> <span class="hljs-params">(<span class="hljs-number">1</span> byte)</span>.<br>* |1111xxxx| - <span class="hljs-params">(with xxxx between <span class="hljs-number">0000</span> and <span class="hljs-number">1101</span>)</span> immediate 4 bit integer.<br>*      Unsigned integer from 0 to 12. The encoded value is actually from<br>*      1 to 13 because 0000 and 1111 can not be used, so 1 should be<br>*      subtracted from the encoded 4 bit value to obtain the right value.<br>* |11111111| - End of ziplist special entry.<br></code></pre></td></tr></table></figure><p><img src="/img/a87e3937feefd00f3e638677f66e200a.png" alt="image-20250313131315802"></p><ul><li><code>prevlen</code> 前一项的长度，是一个 0 ～ 253 的值；</li><li><code>encoding</code> 当前项的编码；</li><li><code>data</code> 存放真正的数据。</li></ul><p>在最开始提到：ziplist 中的每个元素会根据实际情况分配不同大小的空间，充分体现了 Redis 对于存储效率的追求。这是什么意思呢？以下就是 ziplist 最繁琐的部分了：</p><p>针对 prevlen 字段，如果说前一个 entry 非常小，不足 254 字节，那么 1 个字节就可以存下，prevlen 字段就用一个字节来存储；否则，prevlen 占用 5 字节，第 1 字节定义为特殊值 254，标识该字段占用 5 个字节，真正的长度在后面 4 字节。为什么不用 255 呢，原因是 255 就是我们上面提到的 zlend 标识，同时 254 也被赋予了特殊意义。</p><p>针对 encoding 字段，更复杂，共有以下 9 种情况：</p><ol><li>|00pppppp| - 1 byte。第1个字节最高两个 bit 是00，那么 <code>&lt;encoding&gt;</code> 字段只有1个字节，剩余的 6 个 bit 用来表示长度值，最高可以表示 63 (2^6^-1)。</li><li>|01pppppp|qqqqqqqq| - 2 bytes。第1个字节最高两个 bit 是01，那么 <code>&lt;encoding&gt;</code> 字段占2个字节，总共有14个 bit 用来表示长度值，最高可以表示 16383 (2^14^-1)。</li><li>|10**__**|qqqqqqqq|rrrrrrrr|ssssssss|tttttttt| - 5 bytes。第1个字节最高两个 bit 是 10，那么 <code>encoding</code> 字段占 5 个字节，总共使用 32 个 bit 来表示长度值（6个 bit 舍弃不用），最高可以表示 2^32^-1。需要注意的是：在前三种情况下，<code>&lt;data&gt;</code>都是按字符串来存储的；从下面第4种情况开始，<code>&lt;data&gt;</code>开始变为按整数来存储了。</li><li>|11000000| - 1 byte。<code>&lt;encoding&gt;</code>字段占用1个字节，值为0xC0，后面的数据<code>&lt;data&gt;</code>存储为 2 个字节的int16_t类型。</li><li>|11010000| - 1 byte。<code>&lt;encoding&gt;</code>字段占用1个字节，值为0xD0，后面的数据<code>&lt;data&gt;</code>存储为 4 个字节的int32_t类型。</li><li>|11100000| - 1 byte。<code>&lt;encoding&gt;</code>字段占用1个字节，值为0xE0，后面的数据<code>&lt;data&gt;</code>存储为 8 个字节的int64_t类型。</li><li>|11110000| - 1 byte。<code>&lt;encoding&gt;</code>字段占用1个字节，值为0xF0，后面的数据<code>&lt;data&gt;</code>存储为 3 个字节长的整数。</li><li>|11111110| - 1 byte。<code>&lt;encoding&gt;</code>字段占用1个字节，值为0xFE，后面的数据<code>&lt;data&gt;</code>存储为 1 个字节的整数。</li><li>|1111xxxx| - - (xxxx的值在0001和1101之间)。这是一种特殊情况，xxxx从 1 到 13 一共 13 个值，这时就用这 13 个值来表示真正的数据。注意，这里是表示真正的数据，而不是数据长度了。也就是说，在这种情况下，后面不再需要一个单独的<code>&lt;data&gt;</code>字段来表示真正的数据了，而是<code>&lt;encoding&gt;</code>和<code>&lt;data&gt;</code>合二为一了。另外，由于xxxx只能取 0001 和 1101 这 13 个值了<code>（其它可能的值和其它情况冲突了，比如0000和1110分别同前面第7种第8种情况冲突，1111跟结束标记冲突）</code>，而小数值应该从0开始，因此这13个值分别表示0到12，即xxxx的值减去 1 才是它所要表示的那个整数数据的值</li></ol><h2 id="关键代码分析"><a href="#关键代码分析" class="headerlink" title="关键代码分析"></a>关键代码分析</h2><ol><li>创建函数</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *<span class="hljs-title function_">ziplistNew</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span> &#123;<br>    <span class="hljs-comment">// 分配头空间和尾空间 11 字节</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> bytes = ZIPLIST_HEADER_SIZE + ZIPLIST_END_SIZE;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *zl = zmalloc(bytes);<br><br>    ZIPLIST_BYTES(zl) = intrev32ifbe(bytes);<br>    ZIPLIST_TAIL_OFFSET(zl) = intrev32ifbe(ZIPLIST_HEADER_SIZE);<br>    ZIPLIST_LENGTH(zl) = <span class="hljs-number">0</span>;<br><br>    <span class="hljs-comment">// 将列表尾设置为特定标识 255</span><br>    zl[bytes - <span class="hljs-number">1</span>] = ZIP_END;<br>    <span class="hljs-keyword">return</span> zl;<br>&#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>插入函数</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *<span class="hljs-title function_">ziplistPush</span><span class="hljs-params">(<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *zl, <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *s, <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> slen, <span class="hljs-type">int</span> where)</span> &#123;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *p;<br>    <span class="hljs-comment">// 注意,这里 p 对应的不是头节点就是尾节点</span><br>    p = (where == ZIPLIST_HEAD) ? ZIPLIST_ENTRY_HEAD(zl) : ZIPLIST_ENTRY_END(zl);<br>    <span class="hljs-keyword">return</span> __ziplistInsert(zl, p, s, slen);<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * 对 zipilst 执行插入操作，在指定的位置 p 插入一段新的数据</span><br><span class="hljs-comment"> * @param zl</span><br><span class="hljs-comment"> * @param p     ziplist 中某一个数据项的起始位置，或者在向尾端插入的时候，它指向 ziplist 的结束标记 &lt;zlend&gt;</span><br><span class="hljs-comment"> * @param s     待插入数据的地址指针</span><br><span class="hljs-comment"> * @param slen  待插入数据长度</span><br><span class="hljs-comment"> */</span><br><span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *__ziplistInsert(<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *zl, <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *p, <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *s, <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> slen) &#123;<br>    <span class="hljs-type">size_t</span> curlen = intrev32ifbe(ZIPLIST_BYTES(zl)), reqlen;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">int</span> prevlensize, prevlen = <span class="hljs-number">0</span>;<br>    <span class="hljs-type">size_t</span> offset;<br>    <span class="hljs-type">int</span> nextdiff = <span class="hljs-number">0</span>;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> encoding = <span class="hljs-number">0</span>;<br>    <span class="hljs-type">long</span> <span class="hljs-type">long</span> value = <span class="hljs-number">123456789</span>; <span class="hljs-comment">/* initialized to avoid warning. Using a value</span><br><span class="hljs-comment">                                    that is easy to see if for some reason</span><br><span class="hljs-comment">                                    we use it uninitialized. */</span><br>    zlentry tail;<br><br>    <span class="hljs-comment">// 查找前一个条目的长度</span><br>    <span class="hljs-keyword">if</span> (p[<span class="hljs-number">0</span>] != ZIP_END) &#123;<br>        <span class="hljs-comment">// 如果 p 不是 ziplist 的结束标志，从 p 中解码出前一个条目的长度</span><br>        ZIP_DECODE_PREVLEN(p, prevlensize, prevlen);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 如果 p 是结束标志，获取 ziplist 的尾部条目并提取该尾部条目的长度</span><br>        <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *ptail = ZIPLIST_ENTRY_TAIL(zl);<br>        <span class="hljs-keyword">if</span> (ptail[<span class="hljs-number">0</span>] != ZIP_END) &#123;<br>            prevlen = zipRawEntryLength(ptail);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 尝试将待插入的条目 s 编码为整数。如果成功，则返回所需的编码大小；否则，使用字符串长度</span><br>    <span class="hljs-keyword">if</span> (zipTryEncoding(s, slen, &amp;value, &amp;encoding)) &#123;<br>        <span class="hljs-comment">// 根据 encoding 编码类型,返回该编码类型对应的数据部分需要的字节数</span><br>        reqlen = zipIntSize(encoding);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        reqlen = slen;<br>    &#125;<br><br>    <span class="hljs-comment">// 还需要记录 （记录上一个条目的长度需要的字节数 1 or 5）</span><br>    reqlen += zipStorePrevEntryLength(<span class="hljs-literal">NULL</span>, prevlen);<br>    <span class="hljs-comment">// 当前条目的实际长度</span><br>    reqlen += zipStoreEntryEncoding(<span class="hljs-literal">NULL</span>, encoding, slen);<br><br>    <span class="hljs-comment">// 判断下一个条目是否能够容纳当前条目的长度。如果不能，需要调整下一个条目的长度字节数所需的空间。</span><br>    <span class="hljs-type">int</span> forcelarge = <span class="hljs-number">0</span>;<br>    nextdiff = (p[<span class="hljs-number">0</span>] != ZIP_END) ? zipPrevLenByteDiff(p, reqlen) : <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">if</span> (nextdiff == <span class="hljs-number">-4</span> &amp;&amp; reqlen &lt; <span class="hljs-number">4</span>) &#123;<br>        nextdiff = <span class="hljs-number">0</span>;<br>        forcelarge = <span class="hljs-number">1</span>;<br>    &#125;<br><br>    <span class="hljs-comment">// 计算插入位置的偏移量，并调整 ziplist 的大小，以容纳新的条目</span><br>    offset = p - zl;<br>    zl = ziplistResize(zl, curlen + reqlen + nextdiff);<br>    p = zl + offset;<br><br>    <span class="hljs-comment">// 如果 p 不是 ziplist 结束，则移动内存以为新的条目腾出空间</span><br>    <span class="hljs-keyword">if</span> (p[<span class="hljs-number">0</span>] != ZIP_END) &#123;<br>        <span class="hljs-comment">/* Subtract one because of the ZIP_END bytes */</span><br>        memmove(p + reqlen, p - nextdiff, curlen - offset - <span class="hljs-number">1</span> + nextdiff);<br><br>        <span class="hljs-comment">/* Encode this entry&#x27;s raw length in the next entry. */</span><br>        <span class="hljs-keyword">if</span> (forcelarge)<br>            zipStorePrevEntryLengthLarge(p + reqlen, reqlen);<br>        <span class="hljs-keyword">else</span><br>            zipStorePrevEntryLength(p + reqlen, reqlen);<br><br>        <span class="hljs-comment">/* Update offset for tail */</span><br>        ZIPLIST_TAIL_OFFSET(zl) =<br>                intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl)) + reqlen);<br><br>        <span class="hljs-comment">/* When the tail contains more than one entry, we need to take</span><br><span class="hljs-comment">         * &quot;nextdiff&quot; in account as well. Otherwise, a change in the</span><br><span class="hljs-comment">         * size of prevlen doesn&#x27;t have an effect on the *tail* offset. */</span><br>        zipEntry(p + reqlen, &amp;tail);<br>        <span class="hljs-keyword">if</span> (p[reqlen + tail.headersize + tail.len] != ZIP_END) &#123;<br>            ZIPLIST_TAIL_OFFSET(zl) =<br>                    intrev32ifbe(intrev32ifbe(ZIPLIST_TAIL_OFFSET(zl)) + nextdiff);<br>        &#125;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 更新尾指针以反映新条目的插入</span><br>        ZIPLIST_TAIL_OFFSET(zl) = intrev32ifbe(p - zl);<br>    &#125;<br><br>    <span class="hljs-comment">// 如果 nextdiff 不为零，表示后续条目的长度发生了变化，因此需要级联更新</span><br>    <span class="hljs-keyword">if</span> (nextdiff != <span class="hljs-number">0</span>) &#123;<br>        offset = p - zl;<br>        zl = __ziplistCascadeUpdate(zl, p + reqlen);<br>        p = zl + offset;<br>    &#125;<br><br>    <span class="hljs-comment">// 写入新条目</span><br>    p += zipStorePrevEntryLength(p, prevlen);<br>    p += zipStoreEntryEncoding(p, encoding, slen);<br>    <span class="hljs-keyword">if</span> (ZIP_IS_STR(encoding)) &#123;<br>        <span class="hljs-built_in">memcpy</span>(p, s, slen);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        zipSaveInteger(p, value, encoding);<br>    &#125;<br><br>    <span class="hljs-comment">// 增加 ziplist 的长度计数并返回更新后的 ziplist</span><br>    ZIPLIST_INCR_LENGTH(zl, <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">return</span> zl;<br>&#125;<br></code></pre></td></tr></table></figure><p>主要逻辑分析如下：</p><ul><li>查找前一个元素的长度，计算 <code>prevlen</code>；</li><li>计算当前数据项占用的总字节数<code>reqlen</code>，它包含三部分：<code>&lt;prevlen&gt;</code>, <code>&lt;encoding&gt;</code>和真正的数据。其中的数据部分会通过调用<code>zipTryEncoding</code>先来尝试转成整数，否则直接返回字符串长度；</li><li>判断下一个条目的 <code>prevlen</code> 是否能够容纳当前条目的长度。如果不能，需要调整下一个条目的长度字节数所需的空间，同时由于下一个条目的 <code>prevlen</code> 长度变化，可能导致后续元素的 <code>prevlen</code> 也需要进行级联更新；</li><li>计算出了最终需要的空间，调用 <code>ziplistResize</code> 去扩容，并把 p 后的元素继续移动，插入新元素，增加 ziplist 中的元素数量并返回更新后的 ziplist。</li></ul><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>hash 随着数据的增大，其底层数据结构的实现是会发生变化的，当然存储效率也就不同。在 field 比较少，各个 value 值也比较小的时候，hash 采用 ziplist 来实现；而随着 field 增多和 value 值增大，hash 可能会变成 dict 来实现，原因如下：</p><ul><li>ziplist 查询元素时只能进行遍历，效率较低，在整体数量不多时不明显，一旦数量过大，就会影响效率；</li><li>当元素越来越多或越来越大时，插入元素可能会引起较大的内存拷贝，如由于 prevlen 而引起的级联更新。</li></ul><p>而这个大或者多的定义是什么呢？</p><p>最大<code>键值对</code>数量(注意是键值对，如果是 ziplist 中的 zllen，其实是 1024)</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">hash_max_ziplist_entries <span class="hljs-comment">// 512</span><br></code></pre></td></tr></table></figure><p>最大元素长度，插入的任意一个 value 的长度超过 64</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">hash_max_ziplist_value<span class="hljs-comment">// 64</span><br></code></pre></td></tr></table></figure><p>回答最开始的问题：zllen 只有 2 字节，所以最多存 2^16 - 1 个元素吗，答案是否定的，如果 zllen &#x3D; 2^16 - 1，此时该字段就无效了，如果想知道具体数量，就需要遍历来查了。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之robj篇</title>
    <link href="/2026/05/14/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Brobj%E7%AF%87/"/>
    <url>/2026/05/14/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Brobj%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之robj篇"><a href="#Redis源码分析之robj篇" class="headerlink" title="Redis源码分析之robj篇"></a>Redis源码分析之robj篇</h1><p>在 Redis 中，一个 database 内的这个映射关系是用一个 dict 来维护的（ht[0]）。dict 的 key 固定用一种数据结构来表达就够了，即动态字符串 sds。而 value 则比较复杂，为了在同一个 dict 内能够存储不同类型的 value，需要一个通用的数据结构，这个通用的数据结构就是 robj（全称 RedisObject ）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">define</span> LRU_BITS 24</span><br><br><span class="hljs-comment">// redis 键值对中的 value 结构体，头占 16 字节，ptr 指针指向真正的数据</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisObject</span> &#123;</span><br>    <span class="hljs-type">unsigned</span> type: <span class="hljs-number">4</span>;           <span class="hljs-comment">// 4 bit    数据类型 string、list、set</span><br>    <span class="hljs-type">unsigned</span> encoding: <span class="hljs-number">4</span>;       <span class="hljs-comment">// 4 bit    编码方式 embStr、raw、ziplist</span><br>    <span class="hljs-type">unsigned</span> lru: LRU_BITS;     <span class="hljs-comment">// 24 bit   LRU 时间</span><br>                            <span class="hljs-comment">/* LRU time (relative to global lru_clock) or</span><br><span class="hljs-comment">                            * LFU data (least significant 8 bits frequency</span><br><span class="hljs-comment">                            * and most significant 16 bits access time). */</span><br>    <span class="hljs-type">int</span> refcount;               <span class="hljs-comment">// 4 字节     引用次数，主要针对共享对象</span><br>    <span class="hljs-type">void</span> *ptr;                  <span class="hljs-comment">// 8 字节     指向具体实现的指针</span><br>&#125; robj;<br></code></pre></td></tr></table></figure><p>首先解释下这5个字段的含义：</p><ul><li><code>type</code> 数据类型：string、list、hash…</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* The actual Redis Object */</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_STRING 0    <span class="hljs-comment">/* String object. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_LIST 1      <span class="hljs-comment">/* List object. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_SET 2       <span class="hljs-comment">/* Set object. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ZSET 3      <span class="hljs-comment">/* Sorted set object. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_HASH 4      <span class="hljs-comment">/* Hash object. */</span></span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_MODULE 5    <span class="hljs-comment">/* Module object. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_STREAM 6    <span class="hljs-comment">/* Stream object. */</span></span><br></code></pre></td></tr></table></figure><ul><li><code>encoding</code> 编码方式：比如 string 就有 embstr 和 raw 编码方式；</li></ul><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_RAW 0     <span class="hljs-comment">/* Raw representation */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_INT 1     <span class="hljs-comment">/* Encoded as integer */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_HT 2      <span class="hljs-comment">/* Encoded as hash table */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_ZIPMAP 3  <span class="hljs-comment">/* Encoded as zipmap */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_LINKEDLIST 4 <span class="hljs-comment">/* No longer used: old list encoding. */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_ZIPLIST 5 <span class="hljs-comment">/* Encoded as ziplist */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_INTSET 6  <span class="hljs-comment">/* Encoded as intset */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_SKIPLIST 7  <span class="hljs-comment">/* Encoded as skiplist */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_EMBSTR 8  <span class="hljs-comment">/* Embedded sds string encoding */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_QUICKLIST 9 <span class="hljs-comment">/* Encoded as linked list of ziplists */</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_STREAM 10 <span class="hljs-comment">/* Encoded as a radix tree of listpacks */</span></span><br></code></pre></td></tr></table></figure><ul><li><code>lru</code>：  ‌LRU（Least Recently Used）时间，在 redis 淘汰数据时会用；</li><li><code>refcount</code>： 引用计数。它允许 robj 对象在某些情况下被共享，例如 redis 在启动时对于常见的响应指令（ok、error），错误信息，0-9999 的数字对象都进行了创建并复用；</li><li><code>ptr</code>： 指针，指向真正的值。</li></ul><p>接着分析下这个结构体的定义方式</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">unsigned</span> type: <span class="hljs-number">4</span><br></code></pre></td></tr></table></figure><p>这是 C 语言里的位域定义方法，表示该字段所占的比特数（bit）</p><p>最后我们分析可以知道，一个这样的 redisObject 需要占用 4bit + 4bit + 24bit + 4B +8B 即 16 字节空间，这对我们使用 redis 有没有什么启发呢？</p><ul><li>如果我要存一个很短的字符串， 字符串可能不到16字节，但为了描述这个字符串的头结构就占了16字节，内存利用率是不是低了？</li><li>如果我要存一个数字，8字节就能搞定，还有必要让 ptr 指针去指向一个真实存储这个数字的空间吗？</li></ul><p>另外，目前的机器基本都是 64 位架构，通常处理器的缓存行大小是 64 字节（64B），这意味着每次从内存加载数据到缓存时，最小的单位是 64 字节。即使你只需要其中的一部分数据，剩余的部分也会被一并加载到缓存中。</p><p>Redis 中的创建的很多字符串，都会采用 SDS8 的结构（SDS5 无法记录有效容量，对后续拓展不友好），如果 SDS8 的字符串 + redisObject 的头大小，恰好能满足 64B，意味着就不用再去内存中取数据了。</p><p>这就是 Redis String 类型 embStr 编码方式：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// &lt;= 44 字节就使用 embStr 的 encoding 类型,否则使用 raw 编码类型</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> OBJ_ENCODING_EMBSTR_SIZE_LIMIT 44</span><br><br><span class="hljs-comment">// 创建字符串对象的函数</span><br>robj *<span class="hljs-title function_">createStringObject</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *ptr, <span class="hljs-type">size_t</span> len)</span> &#123;<br>    <span class="hljs-comment">// 字符串少于44字节,就 embStr 类型编码,否则 raw 类型编码</span><br>    <span class="hljs-keyword">if</span> (len &lt;= OBJ_ENCODING_EMBSTR_SIZE_LIMIT)<br>        <span class="hljs-keyword">return</span> createEmbeddedStringObject(ptr, len);<br>    <span class="hljs-keyword">else</span><br>        <span class="hljs-keyword">return</span> createRawStringObject(ptr, len);<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr8</span> &#123;</span><br>    <span class="hljs-type">uint8_t</span> len;            <span class="hljs-comment">// 现有长度 1 字节</span><br>    <span class="hljs-type">uint8_t</span> alloc;          <span class="hljs-comment">// 字符数组的空间长度(不包括结构体头和字符数组最后&#x27;\0&#x27;的一字节) 1 字节</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags;    <span class="hljs-comment">// 标识位 3 bit 用来标识类型,5 bit 暂时无用 1 字节</span><br>    <span class="hljs-type">char</span> buf[];             <span class="hljs-comment">// 真正存放字符串的数组,以 &#x27;\0&#x27; 结尾,注意其没有指明长度,这是一种特殊写法,柔性数组,初始化分配时不占用内存空间</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>指的一提的是，即使刚开始我们创建的是一个 embStr 编码的字符串，只要对其进行 append 操作，编码方式就会变为 raw，原因是变动会执行下面这个函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">appendCommand</span><span class="hljs-params">(client *c)</span> &#123;<br>    <span class="hljs-type">size_t</span> totlen;<br>    robj *o, *append;<br><br>    o = lookupKeyWrite(c-&gt;db, c-&gt;argv[<span class="hljs-number">1</span>]);<br>    <span class="hljs-keyword">if</span> (o == <span class="hljs-literal">NULL</span>) &#123;<br>        <span class="hljs-comment">/* Create the key */</span><br>        c-&gt;argv[<span class="hljs-number">2</span>] = tryObjectEncoding(c-&gt;argv[<span class="hljs-number">2</span>]);<br>        dbAdd(c-&gt;db, c-&gt;argv[<span class="hljs-number">1</span>], c-&gt;argv[<span class="hljs-number">2</span>]);<br>        incrRefCount(c-&gt;argv[<span class="hljs-number">2</span>]);<br>        totlen = stringObjectLen(c-&gt;argv[<span class="hljs-number">2</span>]);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">/* Key exists, check type */</span><br>        <span class="hljs-keyword">if</span> (checkType(c, o, OBJ_STRING))<br>            <span class="hljs-keyword">return</span>;<br><br>        <span class="hljs-comment">/* &quot;append&quot; is an argument, so always an sds */</span><br>        append = c-&gt;argv[<span class="hljs-number">2</span>];<br>        totlen = stringObjectLen(o) + sdslen(append-&gt;ptr);<br>        <span class="hljs-keyword">if</span> (checkStringLength(c, totlen) != C_OK)<br>            <span class="hljs-keyword">return</span>;<br><br>        <span class="hljs-comment">/* Append the value */</span><br>        <span class="hljs-comment">// 这里在追加值的时候 会把新生成的 str 转为 raw 编码</span><br>        o = dbUnshareStringValue(c-&gt;db, c-&gt;argv[<span class="hljs-number">1</span>], o);<br>        o-&gt;ptr = sdscatlen(o-&gt;ptr, append-&gt;ptr, sdslen(append-&gt;ptr));<br>        totlen = sdslen(o-&gt;ptr);<br>    &#125;<br>    signalModifiedKey(c-&gt;db, c-&gt;argv[<span class="hljs-number">1</span>]);<br>    notifyKeyspaceEvent(NOTIFY_STRING, <span class="hljs-string">&quot;append&quot;</span>, c-&gt;argv[<span class="hljs-number">1</span>], c-&gt;db-&gt;id);<br>    server.dirty++;<br>    addReplyLongLong(c, totlen);<br>&#125;<br><br>robj *<span class="hljs-title function_">dbUnshareStringValue</span><span class="hljs-params">(redisDb *db, robj *key, robj *o)</span> &#123;<br>    serverAssert(o-&gt;type == OBJ_STRING);<br>    <span class="hljs-comment">// 引用计数不为1 或 之前不是 raw 编码</span><br>    <span class="hljs-keyword">if</span> (o-&gt;refcount != <span class="hljs-number">1</span> || o-&gt;encoding != OBJ_ENCODING_RAW) &#123;<br>        robj *decoded = getDecodedObject(o);<br>        <span class="hljs-comment">// 创建 raw 编码的对象</span><br>        o = createRawStringObject(decoded-&gt;ptr, sdslen(decoded-&gt;ptr));<br>        decrRefCount(decoded);<br>        dbOverwrite(db, key, o);<br>    &#125;<br>    <span class="hljs-keyword">return</span> o;<br>&#125;<br></code></pre></td></tr></table></figure><p>关于 SDS 的 44 字节和 append 操作后会变为 raw 编码其实在上一篇 <a href="https://zhuwenjie0716.github.io/2026/05/11/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bsds%E7%AF%87/">Redis源码分析之sds篇</a> 中已经提过了~</p><p>Robj 在创建字符串对象时，还会尝试把字符串转为 64 位可表示的 long，如果能转成功：</p><ul><li>该值处于 0-9999 且不运行 LRU（如果运行 LRU，会影响对象的共享），会返回一个共享对象（前面提到 Redis 会共享 0-9999 的 Robj 对象）；</li><li>其他情况下 ptr 指针字段直接存成这个 long 型的值。</li></ul><p>只要该值可以被转为 64 位可表示的 long，最终的表示形式就是 <code>type=OBJ_STRING, ecoding=OBJ_ENCODING_INT</code> 的一个 string robj 对象。</p><p>Robj 不只是<strong>为值提供一个统一的表示方式</strong>，还<strong>允许同一类型的数据采用不同的内部表示</strong>，从而在某些情况下尽量节省内存，同时<strong>支持对象共享和引用计数</strong>。当对象被共享的时候，只占用一份内存拷贝，进一步节省内存。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>如何实现高吞吐的定时任务处理</title>
    <link href="/2026/05/13/%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E9%AB%98%E5%90%9E%E5%90%90%E7%9A%84%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1%E5%A4%84%E7%90%86/"/>
    <url>/2026/05/13/%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E9%AB%98%E5%90%9E%E5%90%90%E7%9A%84%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1%E5%A4%84%E7%90%86/</url>
    
    <content type="html"><![CDATA[<h1 id="如何实现高吞吐的定时任务处理"><a href="#如何实现高吞吐的定时任务处理" class="headerlink" title="如何实现高吞吐的定时任务处理"></a>如何实现高吞吐的定时任务处理</h1><p>实现一个定时任务的调度不算太难，一个简单的思路如下：一个存放定时任务的队列，一个按照固定时间间隔扫描的线程，这个线程每隔一段时间扫描队列里的所有任务，如果需要执行就进行调度。</p><p>首先，这个任务一定要暴露一些属性，使得线程可以判断某个时刻是否需要执行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Runnable runnable;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> period;<br><br>    <span class="hljs-meta">@Getter</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> deadline;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusTask</span><span class="hljs-params">(<span class="hljs-type">long</span> delayMs, <span class="hljs-type">int</span> period, Runnable runnable)</span> &#123;<br>        <span class="hljs-built_in">this</span>.deadline = System.currentTimeMillis() + delayMs;<br>        <span class="hljs-built_in">this</span>.period = period;<br>        <span class="hljs-built_in">this</span>.runnable = runnable;<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">updateDeadline</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">if</span> (period &gt; <span class="hljs-number">0</span>) &#123;<br>            deadline += period;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            deadline = Long.MAX_VALUE;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        runnable.run();<br>        updateDeadline();<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>其次，这个线程里需要存储所有添加进来的任务，并每隔一段时间扫描任务是否需要执行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusThread</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Thread</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> List&lt;CusTask&gt; taskList;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> durationMs;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusThread</span><span class="hljs-params">(<span class="hljs-type">long</span> durationMs)</span> &#123;<br>        <span class="hljs-built_in">this</span>.taskList = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-built_in">this</span>.durationMs = durationMs;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>            <span class="hljs-type">long</span> <span class="hljs-variable">nextExecuteTime</span> <span class="hljs-operator">=</span> System.currentTimeMillis() + durationMs;<br>            <span class="hljs-keyword">for</span> (CusTask task : taskList) &#123;<br>                <span class="hljs-keyword">if</span> (task.getDeadline() &lt;= System.currentTimeMillis()) &#123;<br>                    task.run();<br>                &#125;<br>            &#125;<br>            <span class="hljs-keyword">if</span> (System.currentTimeMillis() &lt; nextExecuteTime) &#123;<br>                LockSupport.parkUntil(nextExecuteTime);<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>分析一下上述代码都有什么问题：</p><ol><li>任务队列 taskList 是一个简单的 ArrayList，多线程并发添加任务时可能出现线程安全问题；</li><li>在取出 task 后，直接在当前的线程里执行了 task 的 run 方法，如果耗时很长，会延迟后续任务的执行甚至影响到下次循环；</li><li>每个 durationMs 间隔周期，都需要遍历 taskList 中的所有任务，当任务很多时会耗时很长。</li></ol><p>为了解决这些问题可以将 ArrayList 修改为线程安全的队列，这个队列最好还能按照某个顺序排列（例如按照预计执行时间，这样取出的第一个元素如果都不能满足执行条件，就无需向后遍历了）。</p><p>那么这个队列有什么选项呢？</p><h2 id="ScheduledThreadPoolExecutor"><a href="#ScheduledThreadPoolExecutor" class="headerlink" title="ScheduledThreadPoolExecutor"></a>ScheduledThreadPoolExecutor</h2><p>JDK 的定时任务执行器 ScheduledThreadPoolExecutor 供了周期执行任务和延迟执行任务的特性：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ScheduledExecutorServiceTest</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        <span class="hljs-type">ScheduledExecutorService</span> <span class="hljs-variable">executor</span> <span class="hljs-operator">=</span> Executors.newScheduledThreadPool(<span class="hljs-number">5</span>);<br>        executor.scheduleAtFixedRate(() -&gt; System.out.println(<span class="hljs-string">&quot;Hello World&quot;</span>), <span class="hljs-number">1000</span>, <span class="hljs-number">2000</span>, TimeUnit.MILLISECONDS); <span class="hljs-comment">// 1s 延迟后开始执行任务，每 2s 重复执行一次</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>查看 ScheduledThreadPoolExecutor 的构造方法，其使用了 DelayedWorkQueue 作为任务队列：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-title function_">ScheduledThreadPoolExecutor</span><span class="hljs-params">(<span class="hljs-type">int</span> corePoolSize)</span> &#123;<br>    <span class="hljs-built_in">super</span>(corePoolSize, Integer.MAX_VALUE,<br>          DEFAULT_KEEPALIVE_MILLIS, MILLISECONDS,<br>          <span class="hljs-keyword">new</span> <span class="hljs-title class_">DelayedWorkQueue</span>());<br>&#125;<br></code></pre></td></tr></table></figure><p>DelayedQueue 是 JDK 中一种可以延迟获取对象的阻塞队列，其内部是采用优先级队列 PriorityQueue 存储对象。DelayQueue 中的每个对象都必须实现 Delayed 接口，并重写 compareTo 和 getDelay 方法，在 <a href="https://zhuwenjie0716.github.io/2026/05/12/Netty%E7%9A%84EventLoopGroup%E6%98%AF%E4%BB%80%E4%B9%88/#%E6%89%8B%E5%86%99-EventLoopGroup">自定义EventLoopGroup</a> 中有过使用 。DelayQueue 提供了 put() 和 take() 的阻塞方法，可以向队列中添加对象和取出对象。对象被添加到 DelayQueue 后，会根据 compareTo() 方法进行优先级排序。getDelay() 方法用于计算消息延迟的剩余时间，只有 getDelay &lt;&#x3D;0 时，该对象才能从 DelayQueue 中取出。</p><p>DelayedQueue 依赖 PriorityQueue（优先级队列实现），是线程安全的，PriorityQueue 是一个小顶堆的结构，任务会根据 compareTo() 方法进行排序，所以队列的第一个元素一定是最先需要执行的（但是 PriorityQueue 只保证第一个元素是最先执行的，不保证整体有序）。</p><p>由于 DelayedQueue 每次插入任务时都需要重新排序，平均的时间复杂度 O(logN)，在面对很多任务时会有明显的性能瓶颈。</p><p>那么一个支持大量延迟任务的数据结构该如何实现？</p><h2 id="时间轮"><a href="#时间轮" class="headerlink" title="时间轮"></a>时间轮</h2><p><strong>如果想要在大量数据里迅速找到某个元素，哈希表是唯一的选择。</strong></p><p>想象一下时钟的秒针，把 1 分钟分为 60 秒，时间轮也是类似的结构，举个例子：一个对象里有一个长度为 60 的数组，每个数组代表 1s，当我们拿到了一个定时任务，知道了它下次执行的时间 deadline（毫秒），那么 (deadline &#x2F; 1000) % 60 就是这个任务需要放置的数组位置，数组使用链表法解决哈希冲突。</p><p>注意：上述只是为了举例，在计算机系统里，1s 可以做很多事情了，所以一个元素代表的时间一般要很小，而且数组长度通常设置为 2 的幂，方便位运算取模。</p><p><a href="https://dl.acm.org/doi/epdf/10.1145/37499.37504">Hashed and Hierarchical Timing Wheels</a> 这篇文章比较了实现定时器的各种数据结构，并提出了层次化的 timing wheel 和 hash timing wheel 等结构。</p><p>时间轮可以理解为一种环形结构，像钟表一样被分为多个 slot 槽位。每个 slot 代表一个时间段，每个 slot 中可以存放多个任务，使用的是链表结构保存该时间段到期的所有任务。时间轮通过一个时针随着时间一个个 slot 转动，并执行 slot 中的所有到期任务。</p><p><img src="/img/image-20260513131808772.png" alt="image-20260513131808772"></p><p>以上图为例，这个时间轮中有一个长度为 8 的数组，假设每个代表 1s，现在指针指向 2 的位置，如果需要添加一个 12s 后执行的任务，那么就需要放置到 ((2 + 12) &#x2F; 1 % 8) 也就是 slot6 的位置。</p><p>当指针走到 slot6 时，怎么判断每个任务是现在就执行还是在后续的轮次中执行呢？第一种方式是遍历所有的任务，取任务的 deadline &lt;&#x3D; 对应时间（时间轮启动时间 + 转动次数 * 每次转动代表的周期）的任务，第二种方式在添加任务到 slot 时就计算会在第几轮执行，像现在添加一个 12s 后执行的任务对应的轮次就是 (2 + 12) &#x2F; 8，也就是下一轮次执行，这样每一个 slot 只取轮次为 0 的任务执行即可，其他任务的轮次减去1。</p><p>时间轮有点类似 HashMap，如果多个任务如果对应同一个 slot，处理冲突的方法采用的是拉链法。在任务数量比较多的场景下，适当增加时间轮的 slot 数量，可以减少时针转动时遍历的任务个数。</p><p>时间轮通过<strong>牺牲任务精度的方式换来大量定时任务插入时 O(1) 的时间复杂度以及大量任务的尽可能及时执行</strong>。这句话该怎么理解呢？</p><ul><li>插入时 O(1) 的时间复杂度：指的是通过取余的方式可以直接计算出新任务需要放置的位置；</li><li>大量任务的尽可能及时执行：指的是在由于任务被分配在不同的 slot，所以每次只需要遍历该 slot 的任务，对比需要遍历所有任务判断是否需要执行的数据结构来说，减少了由于遍历任务容器耗时导致在容器靠后位置的任务进一步推迟执行的可能（遍历的任务数少了，为之前的 1 &#x2F; slot）；</li><li>牺牲任务精度：这就是时间轮缺点了<ul><li>假如两个任务分别在 1.2 和 1.3 秒后执行，二者落到了同一个 slot，甚至由于插入顺序的不同，1.3 秒的任务早于 1.2 秒的任务先被执行；</li><li>假如插入任务是头插法，此时时间轮线程已经遍历到了链表中间的位置，所以新插入的任务会延迟到下一轮执行，如果是图示中的结构，会延迟到 8 秒后执行，可以通过控制一个元素代表的时间来缩小这个周期。</li></ul></li></ul><h2 id="Netty-的时间轮实现"><a href="#Netty-的时间轮实现" class="headerlink" title="Netty 的时间轮实现"></a>Netty 的时间轮实现</h2><p>HashedWheelTimer 是 Netty 中时间轮算法的实现类，有几个核心成员：</p><ul><li><strong>HashedWheelTimeout</strong>，任务的封装类，包含任务的到期时间 deadline、需要经历的圈数 remainingRounds 等属性。</li><li><strong>HashedWheelBucket</strong>，相当于时间轮的每个 slot，内部采用双向链表保存了当前需要执行的 HashedWheelTimeout 列表。</li><li><strong>Worker</strong>，HashedWheelTimer 的核心工作引擎，负责处理定时任务。</li></ul><p>来看一个 demo：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <br>    <span class="hljs-type">Timer</span> <span class="hljs-variable">timer</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashedWheelTimer</span>();<br>    <span class="hljs-type">Timeout</span> <span class="hljs-variable">timeout1</span> <span class="hljs-operator">=</span> timer.newTimeout(<span class="hljs-keyword">new</span> <span class="hljs-title class_">TimerTask</span>() &#123;<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">(Timeout timeout)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;timeout1: &quot;</span> + <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br>        &#125;<br>    &#125;, <span class="hljs-number">10</span>, TimeUnit.SECONDS);<br>    <span class="hljs-keyword">if</span> (!timeout1.isExpired()) &#123;<br>        timeout1.cancel();<br>    &#125;<br>    <br>    timer.newTimeout(<span class="hljs-keyword">new</span> <span class="hljs-title class_">TimerTask</span>() &#123;<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">(Timeout timeout)</span> <span class="hljs-keyword">throws</span> InterruptedException &#123;<br>            System.out.println(<span class="hljs-string">&quot;timeout2: &quot;</span> + <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br>            Thread.sleep(<span class="hljs-number">5000</span>);<br>        &#125;<br>    &#125;, <span class="hljs-number">1</span>, TimeUnit.SECONDS);<br>    <br>    timer.newTimeout(<span class="hljs-keyword">new</span> <span class="hljs-title class_">TimerTask</span>() &#123;<br>        <span class="hljs-meta">@Override</span><br>        <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">(Timeout timeout)</span> &#123;<br>            System.out.println(<span class="hljs-string">&quot;timeout3: &quot;</span> + <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>());<br>        &#125;<br>    &#125;, <span class="hljs-number">3</span>, TimeUnit.SECONDS);<br>&#125;<br></code></pre></td></tr></table></figure><p>输出如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">timeout2: Wed May 13 07:44:30 CST 2026<br>timeout3: Wed May 13 07:44:35 CST 2026<br></code></pre></td></tr></table></figure><p>timeout2 和 timeout3 相差 5s，这其实是因为 timeout2 中 <code>Thread.sleep(5000)</code> 导致的，但是这反映出一个问题：HashedWheelTimer 默认是使用一个线程来执行时间轮的任务，当一个任务执行的时间过长，会影响后续任务的调度和执行，很可能产生任务堆积的情况。</p><p>Netty 自然也考虑到了这样的情况，提供了相应的构造方法来使用线程池处理 slot 中的任务，这样时间轮线程只负责指针的转动，即使业务逻辑执行缓慢，也不会拖慢时间轮的整体进度。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment">    * Creates a new timer.</span><br><span class="hljs-comment">    *</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> threadFactory        a &#123;<span class="hljs-doctag">@link</span> ThreadFactory&#125; that creates a</span><br><span class="hljs-comment">    *                             background &#123;<span class="hljs-doctag">@link</span> Thread&#125; which is dedicated to</span><br><span class="hljs-comment">    *                             &#123;<span class="hljs-doctag">@link</span> TimerTask&#125; execution.</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> tickDuration         the duration between tick</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> unit                 the time unit of the &#123;<span class="hljs-doctag">@code</span> tickDuration&#125;</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> ticksPerWheel        the size of the wheel</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> leakDetection        &#123;<span class="hljs-doctag">@code</span> true&#125; if leak detection should be enabled always,</span><br><span class="hljs-comment">    *                             if false it will only be enabled if the worker thread is not</span><br><span class="hljs-comment">    *                             a daemon thread.</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> maxPendingTimeouts   The maximum number of pending timeouts after which call to</span><br><span class="hljs-comment">    *                             &#123;<span class="hljs-doctag">@code</span> newTimeout&#125; will result in</span><br><span class="hljs-comment">    *                             &#123;<span class="hljs-doctag">@link</span> java.util.concurrent.RejectedExecutionException&#125;</span><br><span class="hljs-comment">    *                             being thrown. No maximum pending timeouts limit is assumed if</span><br><span class="hljs-comment">    *                             this value is 0 or negative.</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@param</span> taskExecutor         The &#123;<span class="hljs-doctag">@link</span> Executor&#125; that is used to execute the submitted &#123;<span class="hljs-doctag">@link</span> TimerTask&#125;s.</span><br><span class="hljs-comment">    *                             The caller is responsible to shutdown the &#123;<span class="hljs-doctag">@link</span> Executor&#125; once it is not needed</span><br><span class="hljs-comment">    *                             anymore.</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@throws</span> NullPointerException     if either of &#123;<span class="hljs-doctag">@code</span> threadFactory&#125; and &#123;<span class="hljs-doctag">@code</span> unit&#125; is &#123;<span class="hljs-doctag">@code</span> null&#125;</span><br><span class="hljs-comment">    * <span class="hljs-doctag">@throws</span> IllegalArgumentException if either of &#123;<span class="hljs-doctag">@code</span> tickDuration&#125; and &#123;<span class="hljs-doctag">@code</span> ticksPerWheel&#125; is &amp;lt;= 0</span><br><span class="hljs-comment">    */</span><br>   <span class="hljs-keyword">public</span> <span class="hljs-title function_">HashedWheelTimer</span><span class="hljs-params">(</span><br><span class="hljs-params">           ThreadFactory threadFactory,</span><br><span class="hljs-params">           <span class="hljs-type">long</span> tickDuration, TimeUnit unit, <span class="hljs-type">int</span> ticksPerWheel, <span class="hljs-type">boolean</span> leakDetection,</span><br><span class="hljs-params">           <span class="hljs-type">long</span> maxPendingTimeouts, Executor taskExecutor)</span> &#123;<br><br>       &#125;<br>   &#125;<br></code></pre></td></tr></table></figure><p>对于时间轮来说，可能有多个线程同时向其中追加任务，同时时间轮线程也在扫描任务并移除放入到线程池中执行，这是一个典型的多生产者单消费者场景。Netty 并不会把任务直接添加到时间轮中，而是先放到一个 <a href="https://zhuwenjie0716.github.io/2026/05/06/Netty%E7%9A%84Mpsc%E9%98%9F%E5%88%97%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0/">Mpsc Queue</a> 队列里，借助 Mpsc Queue 保证多线程向时间轮添加任务的线程安全性。</p><p>另外 Netty 中的时间轮是通过固定的时间间隔 tickDuration 进行推动的，如果长时间没有到期任务，那么会存在时间轮空推进的现象，从而造成一定的性能损耗。</p><h2 id="Kafka-的时间轮实现"><a href="#Kafka-的时间轮实现" class="headerlink" title="Kafka 的时间轮实现"></a>Kafka 的时间轮实现</h2><p>Kafka 也有时间轮的应用，它的实现思路与 Netty 存在区别的，但数据结构部分类似，Kafka 的时间轮也是采用环形数组存储定时任务，数组中的每个 slot 代表一个 Bucket，每个 Bucket 保存了定时任务列表 TimerTaskList，TimerTaskList 同样采用双向链表的结构实现，链表的每个节点代表真正的定时任务 TimerTaskEntry。</p><p><img src="/img/image-20260513140812939.png" alt="image-20260513140812939"></p><p>为了解决空推进的问题，Kafka 借助 JDK 的 DelayQueue 来负责推进时间轮。DelayQueue 保存了时间轮中的每个 Bucket，并且根据 Bucket 的到期时间进行排序，最近的到期时间被放在 DelayQueue 的队头。Kafka 中会有一个线程来读取 DelayQueue 中的任务列表，如果时间没有到，那么 DelayQueue 会一直处于阻塞状态，从而解决空推进的问题。DelayQueue 只存放了 Bucket，Bucket 的数量并不多，相比空推进带来的影响是利大于弊的。</p><p>Kafka 还使用了多级时间轮来处理时间跨度过大的问题：</p><p><img src="/img/image-20260513141029686.png" alt="image-20260513141029686"></p><p>从图中可以看出，第一层时间轮每个时间格为 1ms，整个时间轮的跨度为 20ms；第二层时间轮每个时间格为 20ms，整个时间轮跨度为 400ms；第三层时间轮每个时间格为 400ms，整个时间轮跨度为 8000ms。每一层时间轮都有自己的指针，每层时间轮走完一圈后，上层时间轮也会相应推进一格。</p><p>假设现在有一个任务到期时间是 450ms 之后，应该放在第三层时间轮的第一格。随着时间的流逝，当指针指向该时间格时，发现任务到期时间还有 50ms，这里就涉及时间轮降级的操作，它会将任务重新提交到时间轮中。此时发现第一层时间轮整体跨度不够，需要放在第二层时间轮中第三格。当时间再经历 40ms 之后，该任务又会触发一次降级操作，放入到第一层时间轮，最后等到 10ms 后执行任务。</p><p>层级时间轮对时间粒度进行了更好的控制，可以应对更加复杂的定时任务处理场景，适用的范围更广。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本文从基础定时任务实现的缺陷切入，逐步剖析了高吞吐定时任务的演进方案，对比了 JDK、Netty、Kafka 三种经典实现，梳理了定时任务调度的优化思路与核心原理：</p><ol><li><strong>基础定时方案的瓶颈</strong>：简单队列 + 轮询扫描的实现，存在<strong>线程不安全、任务执行阻塞轮询、全量遍历耗时</strong>三大问题，无法支撑大量定时任务场景；JDK 的<code>ScheduledThreadPoolExecutor</code>基于优先级队列解决了排序和线程安全问题，但插入任务复杂度为O(logN)，高并发下性能瓶颈明显。</li><li><strong>时间轮算法核心优势</strong>：借鉴时钟环形结构，通过<strong>哈希取模</strong>的思想定位任务槽位，任务插入复杂度为O(1)，仅需遍历当前槽位任务，大幅提升吞吐；以牺牲任务执行精度为代价，实现了大量定时任务的高效调度，是高吞吐场景的最优解。</li><li><strong>主流框架时间轮实现差异</strong><ul><li><strong>Netty HashedWheelTimer</strong>：单时间轮设计，采用 <strong>MPSC 队列</strong> 保证多线程安全，默认单线程执行任务易阻塞，支持自定义线程池解耦调度与执行，存在空推进损耗；</li><li><strong>Kafka 时间轮</strong>：基于 <strong>DelayQueue</strong> 解决空推进问题，仅在任务到期时唤醒线程，同时采用<strong>多级时间轮</strong>适配超大时间跨度的定时任务，通过层级降级实现精准调度，适用性更广。</li></ul></li></ol><p>整体而言，时间轮是解决<strong>海量、高吞吐定时任务</strong>的核心方案，Netty 与 Kafka 的实现分别适配了轻量调度、大规模分布式调度的不同场景。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Netty</tag>
      
      <tag>TimeWheel</tag>
      
      <tag>Kafka</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty的EventLoopGroup是什么</title>
    <link href="/2026/05/12/Netty%E7%9A%84EventLoopGroup%E6%98%AF%E4%BB%80%E4%B9%88/"/>
    <url>/2026/05/12/Netty%E7%9A%84EventLoopGroup%E6%98%AF%E4%BB%80%E4%B9%88/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty的EventLoopGroup是什么"><a href="#Netty的EventLoopGroup是什么" class="headerlink" title="Netty的EventLoopGroup是什么"></a>Netty的EventLoopGroup是什么</h1><p>近期看到了一个 <a href="https://www.bilibili.com/video/BV1yH3EzgEZ6/">手写EventLoopGroup理解原理</a> 的视频，觉得挺有意思的。</p><h2 id="EventLoopGroup-与-EventLoop"><a href="#EventLoopGroup-与-EventLoop" class="headerlink" title="EventLoopGroup 与 EventLoop"></a>EventLoopGroup 与 EventLoop</h2><p>如果查看一下 Netty EventLoopGroup 的继承关系，我们会发现什么：</p><p><img src="/img/image-20260511153523304.png" alt="image-20260511153523304"></p><p>EventLoopGroup 继承了 ScheduledExecutorService，进而也继承了 ExecutorService，所以 <strong>EventLoopGroup 其实就是一个线程池，并且是能执行定时任务的线程池</strong>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">EventLoop</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">OrderedEventExecutor</span>, EventLoopGroup &#123;<br>    <span class="hljs-meta">@Override</span><br>    EventLoopGroup <span class="hljs-title function_">parent</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>而 EventLoop 又继承了 EventLoopGroup，所以 <strong>EventLoop 其实也是一个线程池，但它是使用一个线程执行所有任务的线程池</strong>。</p><p>那 EventLoopGroup 相比 JDK 提供的线程池有什么不同呢？</p><h2 id="EventLoopGroup-与-JDK-线程池"><a href="#EventLoopGroup-与-JDK-线程池" class="headerlink" title="EventLoopGroup 与 JDK 线程池"></a>EventLoopGroup 与 JDK 线程池</h2><p>来看一段 demo：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>    <span class="hljs-type">EventLoopGroup</span> <span class="hljs-variable">eventLoopGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DefaultEventLoopGroup</span>(<span class="hljs-number">6</span>);<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>        eventLoopGroup.execute(() -&gt; System.out.println(Thread.currentThread().getName()));<br>    &#125;<br><br>    <span class="hljs-type">ExecutorService</span> <span class="hljs-variable">executorService</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>(<br>            <span class="hljs-number">6</span>, <span class="hljs-number">6</span>, <span class="hljs-number">120L</span>, TimeUnit.SECONDS,<br>            <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="hljs-number">1000000</span>),<br>            <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadPoolExecutor</span>.AbortPolicy()<br>    );<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>        executorService.execute(() -&gt; System.out.println(Thread.currentThread().getName()));<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>无论执行多少次，eventLoopGroup 总是 1-4 号线程打印了两次，而 executorService 则不固定。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs bash">defaultEventLoopGroup-2-1<br>defaultEventLoopGroup-2-2<br>defaultEventLoopGroup-2-3<br>defaultEventLoopGroup-2-4<br>defaultEventLoopGroup-2-5<br>defaultEventLoopGroup-2-6<br>defaultEventLoopGroup-2-1<br>defaultEventLoopGroup-2-2<br>defaultEventLoopGroup-2-4<br>defaultEventLoopGroup-2-3<br>pool-1-thread-2<br>pool-1-thread-1<br>pool-1-thread-4<br>pool-1-thread-1<br>pool-1-thread-3<br>pool-1-thread-6<br>pool-1-thread-2<br>pool-1-thread-1<br>pool-1-thread-5<br>pool-1-thread-4<br></code></pre></td></tr></table></figure><p>据此我们合理推测：<code>EventLoopGroup 里的线程其实是有顺序的,换句话说,EventLoopGroup 可以保证由某个线程来处理提交的任务</code>。</p><p>Netty 的 “无锁串行化” 设计，要求一个 channel 的所有事件只能被同一个线程执行，这也是 Netty 设计 EventLoopGroup 的原因，它会把传进来的任务进行分组，交给对应的线程执行。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AbstractEventExecutorGroup</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">EventExecutorGroup</span> &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable command)</span> &#123;<br>        next().execute(command);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>而 <code>next()</code> 方法的返回值就是一个 EventLoop：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">EventLoopGroup</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">EventExecutorGroup</span> &#123;<br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * Return the next &#123;<span class="hljs-doctag">@link</span> EventLoop&#125; to use</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-meta">@Override</span><br>    EventLoop <span class="hljs-title function_">next</span><span class="hljs-params">()</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>由于 next() 调度的原因，在 demo 中前 4 个 线程会再执行一次打印线程名的任务，同时由于 EventLoopGroup 继承了 ScheduledExecutorService，它还拥有执行定时任务的能力。</p><p>EventLoopGroup 与 JDK 线程池还有显著不同的一点，在它的构造方法里：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-title function_">MultithreadEventExecutorGroup</span><span class="hljs-params">(<span class="hljs-type">int</span> nThreads, Executor executor,</span><br><span class="hljs-params">                                        EventExecutorChooserFactory chooserFactory, Object... args)</span> &#123;<br><br>    children = <span class="hljs-keyword">new</span> <span class="hljs-title class_">EventExecutor</span>[nThreads];<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; nThreads; i ++) &#123;<br>        <span class="hljs-type">boolean</span> <span class="hljs-variable">success</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>        <span class="hljs-keyword">try</span> &#123;<br>            children[i] = newChild(executor, args);<br>            success = <span class="hljs-literal">true</span>;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-comment">// <span class="hljs-doctag">TODO:</span> Think about if this is a good exception type</span><br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;failed to create a child event loop&quot;</span>, e);<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br><br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>即：<code>EventLoopGroup 会在创建时就启动所有的核心线程</code>，这与常规的 JDK 线程池的工作方式截然不同。</p><p>由于 EventLoopGroup 的每一个线程都是线程池，线程池中会有任务队列存放来不及执行的任务，所以每一个 EventLoop 都有一个任务队列，这点也导致了一个显著不同：EventLoopGroup 每个线程绑定一个任务队列，而 JDK 线程池多线程共享线程队列，所以 JDK 线程池中的任务可能被任意空闲线程执行，存在线程切换和锁竞争。</p><p>整理一下 EventLoopGroup 与 JDK 线程池的区别：</p><ul><li>EventLoopGroup 线程有顺序，可以保证指定线程处理提交的某个任务；</li><li>EventLoopGroup 在创建时就启动所有的核心线程，JDK 线程池则是来任务先创建到核心线程数，再加到任务队列，最后增加到最大线程数的逻辑；</li><li>EventLoopGroup 每个线程负责队列任务的全生命周期，而 JDK 线程池可能存在执行任务的线程切换。</li></ul><h2 id="手写-EventLoopGroup"><a href="#手写-EventLoopGroup" class="headerlink" title="手写 EventLoopGroup"></a>手写 EventLoopGroup</h2><p>由上述分析知，EventLoopGroup 其实是实现了执行普通任务和定时任务的线程池：</p><p>自定义 CusEventLoopGroup：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">CusEventLoopGroup</span> &#123;<br><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable task)</span>;<br><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">schedule</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, TimeUnit unit)</span>;<br><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">scheduleAtFixedRate</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, <span class="hljs-type">long</span> period, TimeUnit unit)</span>;<br><br>    CusEventLoop <span class="hljs-title function_">next</span><span class="hljs-params">()</span>;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>自定义 CusEventLoop：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">CusEventLoop</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">CusEventLoopGroup</span> &#123;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>自定义 CusEventLoopGroup 实现类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusEventLoopGroupImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">CusEventLoopGroup</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> CusEventLoop[] children;<br><br>    <span class="hljs-type">AtomicInteger</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicInteger</span>(<span class="hljs-number">0</span>);<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusEventLoopGroupImpl</span><span class="hljs-params">(<span class="hljs-type">int</span> threadNum)</span> &#123;<br>        children = <span class="hljs-keyword">new</span> <span class="hljs-title class_">CusEventLoopImpl</span>[threadNum];<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; threadNum; i++) &#123;<br>            children[i] = <span class="hljs-keyword">new</span> <span class="hljs-title class_">CusEventLoopImpl</span>();<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable task)</span> &#123;<br>        next().execute(task);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">schedule</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, TimeUnit unit)</span> &#123;<br>        next().schedule(command, initialDelay, unit);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">scheduleAtFixedRate</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, <span class="hljs-type">long</span> period, TimeUnit unit)</span> &#123;<br>        next().scheduleAtFixedRate(command, initialDelay, period, unit);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> CusEventLoop <span class="hljs-title function_">next</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> children[index.getAndIncrement() % children.length];<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>CusEventLoopGroupImpl 的两个特点：</p><ul><li>创建时就初始化所有线程；</li><li>真正的实现调用 next() 获得的 CusEventLoop 来实现。</li></ul><p>自定义 CusEventLoop 实现类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusEventLoopImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">CusEventLoop</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> BlockingQueue&lt;Runnable&gt; taskQueue = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="hljs-number">1024</span>);<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusEventLoopImpl</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                <span class="hljs-keyword">try</span> &#123;<br>                    <span class="hljs-type">Runnable</span> <span class="hljs-variable">take</span> <span class="hljs-operator">=</span> taskQueue.take();<br>                    take.run();<br>                &#125; <span class="hljs-keyword">catch</span> (InterruptedException e) &#123;<br>                    <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(e);<br>                &#125;<br>            &#125;<br>        &#125;);<br>        thread.start();<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable task)</span> &#123;<br>        <span class="hljs-keyword">if</span> (!taskQueue.offer(task)) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;队列已满,无法再分配任务&quot;</span>);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">schedule</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, TimeUnit unit)</span> &#123;<br><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">scheduleAtFixedRate</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, <span class="hljs-type">long</span> period, TimeUnit unit)</span> &#123;<br><br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> CusEventLoop <span class="hljs-title function_">next</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>由于 EventLoop 还需要执行定时任务，所以还需要一个队列存放定时任务，常见的实现有 DelayedWorkQueue、PriorityQueue，我们可以自定义任务类型实现 Delayed 接口，这个接口的方法可以知道距离下次执行的时间，以及如何与其他 task 进行比较：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * 定时任务封装类，用于支持延迟执行和固定频率执行</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ScheduledTask</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Delayed</span>, Runnable &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Runnable command;<br>    <span class="hljs-meta">@Getter</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">long</span> executeTime;  <span class="hljs-comment">// 执行时间戳（毫秒）</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> period;          <span class="hljs-comment">// 执行周期（毫秒），0表示一次性任务</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">AtomicBoolean</span> <span class="hljs-variable">cancelled</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicBoolean</span>(<span class="hljs-literal">false</span>);<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> CusEventLoop eventLoop;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">ScheduledTask</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> delay, TimeUnit unit, CusEventLoop eventLoop)</span> &#123;<br>        <span class="hljs-built_in">this</span>(command, delay, <span class="hljs-number">0</span>, unit, eventLoop);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">ScheduledTask</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, <span class="hljs-type">long</span> period, TimeUnit unit, CusEventLoop eventLoop)</span> &#123;<br>        <span class="hljs-built_in">this</span>.command = command;<br>        <span class="hljs-built_in">this</span>.executeTime = System.currentTimeMillis() + unit.toMillis(initialDelay);<br>        <span class="hljs-built_in">this</span>.period = unit.toMillis(period);<br>        <span class="hljs-built_in">this</span>.eventLoop = eventLoop;<br>    &#125;<br><br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 更新下一次执行时间（用于固定频率任务）</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">updateExecuteTime</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-built_in">this</span>.executeTime = System.currentTimeMillis() + period;<br>    &#125;<br><br>    <span class="hljs-comment">/**</span><br><span class="hljs-comment">     * 取消任务</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">cancel</span><span class="hljs-params">()</span> &#123;<br>        cancelled.set(<span class="hljs-literal">true</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">long</span> <span class="hljs-title function_">getDelay</span><span class="hljs-params">(TimeUnit unit)</span> &#123;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">delay</span> <span class="hljs-operator">=</span> executeTime - System.currentTimeMillis();<br>        <span class="hljs-keyword">return</span> unit.convert(delay, TimeUnit.MILLISECONDS);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">compareTo</span><span class="hljs-params">(Delayed other)</span> &#123;<br>        <span class="hljs-keyword">if</span> (<span class="hljs-built_in">this</span> == other) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>        &#125;<br>        <span class="hljs-type">ScheduledTask</span> <span class="hljs-variable">otherTask</span> <span class="hljs-operator">=</span> (ScheduledTask) other;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">diff</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.executeTime - otherTask.executeTime;<br>        <span class="hljs-keyword">return</span> Long.compare(diff, <span class="hljs-number">0</span>);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">if</span> (!cancelled.get()) &#123;<br>            <span class="hljs-keyword">try</span> &#123;<br>                command.run();<br>                <span class="hljs-keyword">if</span> (period &gt; <span class="hljs-number">0</span>) &#123;<br>                    updateExecuteTime();<br>                    eventLoop.getScheduledTaskQueue().offer(<span class="hljs-built_in">this</span>);<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>                <span class="hljs-comment">// 防止任务异常影响事件循环</span><br>                e.printStackTrace();<br>            &#125;<br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>再添加完定时任务队列后，线程获取一个任务就比较复杂了，因为完全有可能在等待定时任务的过程中来了一个普通任务，重新实现的 CusEventLoopImpl 如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">CusEventLoopImpl</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">CusEventLoop</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> BlockingQueue&lt;Runnable&gt; taskQueue = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="hljs-number">1024</span>);<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> DelayQueue&lt;ScheduledTask&gt; scheduledTaskQueue = <span class="hljs-keyword">new</span> <span class="hljs-title class_">DelayQueue</span>&lt;&gt;();<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Runnable</span> <span class="hljs-variable">WAKE_UP</span> <span class="hljs-operator">=</span> () -&gt; &#123;<br><br>    &#125;;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">CusEventLoopImpl</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">thread</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Thread</span>(() -&gt; &#123;<br>            <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>                <span class="hljs-type">Runnable</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> getTask();<br>                <span class="hljs-keyword">if</span> (task != <span class="hljs-literal">null</span>) &#123;<br>                    task.run();<br>                &#125;<br>            &#125;<br>        &#125;);<br>        thread.start();<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> Runnable <span class="hljs-title function_">getTask</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-type">ScheduledTask</span> <span class="hljs-variable">scheduledTask</span> <span class="hljs-operator">=</span> scheduledTaskQueue.peek();<br>        <span class="hljs-keyword">if</span> (scheduledTask == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-type">Runnable</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>            <span class="hljs-keyword">try</span> &#123;<br>                <span class="hljs-comment">// 有可能在这里阻塞的时候来了一个可以执行的定时任务</span><br>                task = taskQueue.take();<br>                <span class="hljs-keyword">if</span> (task == WAKE_UP) &#123;<br>                    task = <span class="hljs-literal">null</span>;<br>                &#125;<br>            &#125; <span class="hljs-keyword">catch</span> (InterruptedException ignore) &#123;<br>            &#125;<br>            <span class="hljs-keyword">return</span> task;<br>        &#125;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">executeTime</span> <span class="hljs-operator">=</span> scheduledTask.getExecuteTime();<br>        <span class="hljs-keyword">if</span> (System.currentTimeMillis() &gt;= executeTime) &#123;<br>            <span class="hljs-comment">// 这里注意, scheduledTaskQueue.poll() 未必是上面的 scheduledTask,有可能在这个判断的时间内有新的 scheduledTask 来了</span><br>            <span class="hljs-comment">// 但是既然能 poll 出来,说明它的执行时间肯定也到了</span><br>            <span class="hljs-keyword">return</span> scheduledTaskQueue.poll();<br>        &#125;<br><br>        <span class="hljs-type">Runnable</span> <span class="hljs-variable">task</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">// 有可能在这里阻塞的时候来了一个可以执行的定时任务</span><br>            task = taskQueue.poll(System.currentTimeMillis() - executeTime, TimeUnit.MILLISECONDS);<br>            <span class="hljs-keyword">if</span> (task == WAKE_UP) &#123;<br>                task = <span class="hljs-literal">null</span>;<br>            &#125;<br>        &#125; <span class="hljs-keyword">catch</span> (InterruptedException ignore) &#123;<br>        &#125;<br>        <span class="hljs-keyword">return</span> task;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">execute</span><span class="hljs-params">(Runnable task)</span> &#123;<br>        <span class="hljs-keyword">if</span> (!taskQueue.offer(task)) &#123;<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;队列已满,无法再分配任务&quot;</span>);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">schedule</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, TimeUnit unit)</span> &#123;<br>        <span class="hljs-type">ScheduledTask</span> <span class="hljs-variable">scheduledTask</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ScheduledTask</span>(command, initialDelay, unit, <span class="hljs-built_in">this</span>);<br>        scheduledTaskQueue.offer(scheduledTask);<br>        execute(WAKE_UP);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">scheduleAtFixedRate</span><span class="hljs-params">(Runnable command, <span class="hljs-type">long</span> initialDelay, <span class="hljs-type">long</span> period, TimeUnit unit)</span> &#123;<br>        <span class="hljs-type">ScheduledTask</span> <span class="hljs-variable">scheduledTask</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ScheduledTask</span>(command, initialDelay, period, unit, <span class="hljs-built_in">this</span>);<br>        scheduledTaskQueue.offer(scheduledTask);<br>        execute(WAKE_UP);<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> CusEventLoop <span class="hljs-title function_">next</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Queue&lt;ScheduledTask&gt; <span class="hljs-title function_">getScheduledTaskQueue</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> scheduledTaskQueue;<br>    &#125;<br>&#125;<br><br></code></pre></td></tr></table></figure><p>由于固定频率执行的任务在执行完后还要加到定时任务队列中，所以需要在创建定时任务时传递一个 EventLoop（暴露出获取定时任务队列的方法）。同时如果需要实现指定线程执行：可以通过自定义 Runnable 添加一个指定线程在 EventLoopGroup 的索引，或重写 next() 方法来实现。</p><p>现在思考一个这样的场景：EventLoop 执行完任务，可能需要重新把任务添加到队列中，同时 acceptor 可能接收了新连接，需要把这个新连接的后续任务交给 EventLoop 处理，双方可能同时要添加任务到队列，Netty 使用了 <a href="https://zhuwenjie0716.github.io/2026/05/06/Netty%E7%9A%84Mpsc%E9%98%9F%E5%88%97%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0/">MpscQueue</a> 使多生产者可以无锁添加任务。 </p><p>虽然这样的 demo 并不能处理网络请求，但描述了 netty 处理任务的大致流程。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p><code>EventLoopGroup</code> 底层是<strong>支持普通任务 + 定时任务的特殊线程池</strong>，继承了 JDK 定时线程池接口；而 <code>EventLoop</code> 既是组也是单个事件循环线程，<strong>一个 EventLoop 绑定唯一线程</strong>，独享任务队列。和 JDK 线程池对比有以下区别：</p><ul><li>任务调度：EventLoopGroup 按固定规则分配线程，<strong>同一个 Channel 所有事件始终绑定同一个 EventLoop 线程</strong>，实现<strong>无锁串行化</strong>；JDK 线程池任务随机被空闲线程执行，存在线程切换与锁竞争。</li><li>线程创建：EventLoopGroup <strong>初始化时就一次性创建并启动所有工作线程</strong>；JDK 线程池是按需懒创建线程。</li><li>队列模型：EventLoop 是<strong>每个线程独享一个任务队列</strong>；JDK 线程池是<strong>所有线程共享一个全局任务队列</strong>。</li></ul><p>Netty 的这套 EventLoop 模型，通过<strong>线程绑定、串行执行、队列隔离</strong>，规避了多线程锁竞争和上下文切换开销，是 Netty 高性能、高并发、低延迟的核心基石。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之sds篇</title>
    <link href="/2026/05/11/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bsds%E7%AF%87/"/>
    <url>/2026/05/11/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bsds%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之sds篇"><a href="#Redis源码分析之sds篇" class="headerlink" title="Redis源码分析之sds篇"></a>Redis源码分析之sds篇</h1><p>Redis 是 C 语言编写的，C 语言里的字符串其实就是 char *，那 Redis 也是这样实现的吗？</p><p>如果不是的话，Redis 为什么要单独设计数据结构来存储字符串呢？</p><h2 id="传统字符串"><a href="#传统字符串" class="headerlink" title="传统字符串"></a>传统字符串</h2><p>在 C 语言的传统字符串里，判断字符串结束是通过末尾的 ‘\0’ 标记，这也意味着字符串里不能有该字符，否则会导致字符串提前结束。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;string.h&gt;</span></span><br><br><span class="hljs-type">int</span> <span class="hljs-title function_">main</span> <span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">char</span> *stra = <span class="hljs-string">&quot;red\0is&quot;</span>;<br>    <span class="hljs-type">char</span> *strb = <span class="hljs-string">&quot;redis\0&quot;</span>;<br>    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%lu\n&quot;</span>, <span class="hljs-built_in">strlen</span>(stra));<span class="hljs-comment">// 3</span><br>    <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%lu\n&quot;</span>, <span class="hljs-built_in">strlen</span>(strb));<span class="hljs-comment">// 5</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>另外我们知道，Redis 如此盛行的一个原因就是，每秒10万级别的读写速率和丰富的数据结构类型，我们在存取字符串时，经常要获取长度（len）和进行拼接操作（append），在 C 语言里，这个操作需要遍历一边操作的字符串，即需要 O(N) 的复杂度，如果空间不足还需要分配新的空间，而 Redis 作为高性能的数据库中间件，肯定要对这种慢操作进行优化。</p><p>所以传统字符串有以下缺点：</p><ul><li>二进制不安全；</li><li>操作时间复杂度高。</li></ul><h2 id="SDS"><a href="#SDS" class="headerlink" title="SDS"></a>SDS</h2><p>为了解决这样的问题，Redis 中的字符串采用了 SDS （simple dynamic string）简单动态字符串结构，其定义如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-type">char</span> *sds;<br><br><span class="hljs-comment">// SDS 5 比较特殊</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr5</span> &#123;</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, and 5 msb of string length */</span><br>    <span class="hljs-type">char</span> buf[];<br>&#125;;<br><br><span class="hljs-comment">// 除 SDS8、SDS16外，还有SDS32、SDS64</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr8</span> &#123;</span><br>    <span class="hljs-type">uint8_t</span> len; <span class="hljs-comment">// 已经使用的空间</span><br>    <span class="hljs-type">uint8_t</span> alloc; <span class="hljs-comment">// 字符串数组可分配的最大空间，不包括最后的&#x27;\0&#x27;字符</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags; <span class="hljs-comment">// 1字节,低3bit用来区分是哪一种SDS,高5bit暂未使用</span><br>    <span class="hljs-type">char</span> buf[];<span class="hljs-comment">// 真正的字符串</span><br>&#125;;<br><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr16</span> &#123;</span><br>    <span class="hljs-type">uint16_t</span> len; <span class="hljs-comment">/* used */</span><br>    <span class="hljs-type">uint16_t</span> alloc; <span class="hljs-comment">/* excluding the header and null terminator */</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags; <span class="hljs-comment">/* 3 lsb of type, 5 unused bits */</span><br>    <span class="hljs-type">char</span> buf[];<br>&#125;;<br></code></pre></td></tr></table></figure><ul><li><code>len</code> 已经使用的空间；</li><li><code>alloc</code> 字符串数组可分配的最大空间，不包括最后的’\0’字符；</li><li><code>flags</code> 低 3bit 用来区分是哪一种SDS,高 5bit 暂未使用；</li><li><code>buf[]</code> 真正的字符串。</li></ul><p>为什么会有这样的设计？</p><p>首先，字符串肯定也是有大小的，有的可能很短，有的可能很长，如果设置为统一的长度，对于短字符串来说太浪费了，所以 Redis 针对不同长度的字符串设置了不同的数据结构。</p><blockquote><p>怎么解决传统字符串中的问题呢</p></blockquote><ol><li><p>使用 len 字段直接标识当前字符串用了多少空间，那么用的时候直接读取这么多字节的数据就行了，自然不用关心中间出现的字符；</p></li><li><p>使用 len 字段和 alloc 字段，取消了获取字符串长度时的遍历操作，在追加时也可以根据 alloc 是否可以容纳 len + 新字符串的长度 来判断。</p></li></ol><p>有一些细节需要注意：</p><ul><li>在 struct 结构体后，有 <code>__attribute__ ((__packed__))</code>的代码，它的意思是告诉编译器把结构体里的成员紧凑排列，原因是，sds *s 的这个指针，指向的其实是 char[] 的内容，如果紧凑排列，s-1 就是 flags 字段的内容，再取低 3bit 就知道当前 SDS 的类型了。</li><li>在各个header的定义中最后有一个char buf[]。注意这是一个没有指明长度的字符数组，这是 C 语言中定义字符数组的一种特殊写法，称为柔性数组，只能定义在一个结构体的最后一个字段上。它在这里只是起到一个标记的作用，表示在 flags 字段后面就是一个字符数组，或者说，它指明了紧跟在 flags 字段后面的这个字符数组在结构体中的偏移位置。而程序在为 header 分配的内存的时候，它并不占用内存空间。如果计算 sizeof(struct sdshdr16) 的值，那么结果是 5个字节，其中没有 buf 字段。</li></ul><p>看一下获取 SDS 长度的函数 </p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_MASK 7</span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_5  0</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_8  1</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_16 2</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_32 3</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_64 4</span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_BITS 3<span class="hljs-comment">// 给SDS5使用</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SDS_TYPE_5_LEN(f) ((f)&gt;&gt;SDS_TYPE_BITS)<span class="hljs-comment">// SDS5的flags字段右移3位</span></span><br><br><span class="hljs-type">static</span> <span class="hljs-keyword">inline</span> <span class="hljs-type">size_t</span> <span class="hljs-title function_">sdslen</span><span class="hljs-params">(<span class="hljs-type">const</span> sds s)</span> &#123;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags = s[<span class="hljs-number">-1</span>];<br>    <span class="hljs-keyword">switch</span>(flags&amp;SDS_TYPE_MASK) &#123;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_5:<br>            <span class="hljs-keyword">return</span> SDS_TYPE_5_LEN(flags);<br>        <span class="hljs-keyword">case</span> SDS_TYPE_8:<br>            <span class="hljs-keyword">return</span> SDS_HDR(<span class="hljs-number">8</span>,s)-&gt;len;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_16:<br>            <span class="hljs-keyword">return</span> SDS_HDR(<span class="hljs-number">16</span>,s)-&gt;len;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_32:<br>            <span class="hljs-keyword">return</span> SDS_HDR(<span class="hljs-number">32</span>,s)-&gt;len;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_64:<br>            <span class="hljs-keyword">return</span> SDS_HDR(<span class="hljs-number">64</span>,s)-&gt;len;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>flags &amp; 7</code>其实就是取 flags 低 3bit 的值，进而知道 SDS 的类型，接着传入该类型对应的标识，获取结构体里的 len 字段，SDS5则比较特殊，flags 字段既标识类型也标识长度。</p><p>那 SDS 的创建函数就很清晰了</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/*</span><br><span class="hljs-comment"> * 创建新的字符串</span><br><span class="hljs-comment"> * mystring = sdsnewlen(&quot;abc&quot;,3);</span><br><span class="hljs-comment"> */</span><br>sds <span class="hljs-title function_">sdsnewlen</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">void</span> *init, <span class="hljs-type">size_t</span> initlen)</span> &#123;<br>    <span class="hljs-type">void</span> *sh;<br>    sds s;<br>    <span class="hljs-type">char</span> type = sdsReqType(initlen);<br><br>    <span class="hljs-comment">// 当前传的是空字符串,不要用 SDS5 类型,原因是 SDS5 没法标识出剩余容量等,而空字符串接下来大概率要进行拼接操作,直接使用 SDS8</span><br>    <span class="hljs-keyword">if</span> (type == SDS_TYPE_5 &amp;&amp; initlen == <span class="hljs-number">0</span>) type = SDS_TYPE_8;<br><br>    <span class="hljs-type">int</span> hdrlen = sdsHdrSize(type);<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *fp; <span class="hljs-comment">/* flags pointer. */</span><br><br>    <span class="hljs-comment">// 新建 SDS 结构,并分配内存空间</span><br>    sh = s_malloc(hdrlen + initlen + <span class="hljs-number">1</span>);<br>    <span class="hljs-keyword">if</span> (sh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>    <span class="hljs-keyword">if</span> (!init)<br>        <span class="hljs-built_in">memset</span>(sh, <span class="hljs-number">0</span>, hdrlen + initlen + <span class="hljs-number">1</span>);<br><br>    s = (<span class="hljs-type">char</span> *) sh + hdrlen;   <span class="hljs-comment">// s 指向内部的 buf 字符串数组</span><br><br>    fp = ((<span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> *) s) - <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">switch</span> (type) &#123;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_5: &#123;<br>            *fp = type | (initlen &lt;&lt; SDS_TYPE_BITS);<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_8: &#123;<br>            SDS_HDR_VAR(<span class="hljs-number">8</span>, s);<br>            sh-&gt;len = initlen;<br>            sh-&gt;alloc = initlen;<br>            *fp = type;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_16: &#123;<br>            SDS_HDR_VAR(<span class="hljs-number">16</span>, s);<br>            sh-&gt;len = initlen;<br>            sh-&gt;alloc = initlen;<br>            *fp = type;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_32: &#123;<br>            SDS_HDR_VAR(<span class="hljs-number">32</span>, s);<br>            sh-&gt;len = initlen;<br>            sh-&gt;alloc = initlen;<br>            *fp = type;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        <span class="hljs-keyword">case</span> SDS_TYPE_64: &#123;<br>            SDS_HDR_VAR(<span class="hljs-number">64</span>, s);<br>            sh-&gt;len = initlen;<br>            sh-&gt;alloc = initlen;<br>            *fp = type;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (initlen &amp;&amp; init)<br>        <span class="hljs-built_in">memcpy</span>(s, init, initlen);<br><br>    <span class="hljs-comment">// 给字符串最后一位设置为 &#x27;\0&#x27;,兼容 C 语言原生字符串</span><br>    s[initlen] = <span class="hljs-string">&#x27;\0&#x27;</span>;<br>    <span class="hljs-keyword">return</span> s;<br>&#125;<br><br><span class="hljs-comment">/* Create an empty (zero length) sds string. Even in this case the string</span><br><span class="hljs-comment"> * always has an implicit null term. */</span><br>sds <span class="hljs-title function_">sdsempty</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span> &#123;<br>    <span class="hljs-keyword">return</span> sdsnewlen(<span class="hljs-string">&quot;&quot;</span>, <span class="hljs-number">0</span>);<br>&#125;<br><br><span class="hljs-comment">/* Create a new sds string starting from a null terminated C string. */</span><br>sds <span class="hljs-title function_">sdsnew</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *init)</span> &#123;<br>    <span class="hljs-type">size_t</span> initlen = (init == <span class="hljs-literal">NULL</span>) ? <span class="hljs-number">0</span> : <span class="hljs-built_in">strlen</span>(init);<br>    <span class="hljs-keyword">return</span> sdsnewlen(init, initlen);<br>&#125;<br><br><span class="hljs-comment">/* Duplicate an sds string. */</span><br>sds <span class="hljs-title function_">sdsdup</span><span class="hljs-params">(<span class="hljs-type">const</span> sds s)</span> &#123;<br>    <span class="hljs-keyword">return</span> sdsnewlen(s, sdslen(s));<br>&#125;<br><br><span class="hljs-comment">/* Free an sds string. No operation is performed if &#x27;s&#x27; is NULL. */</span><br><span class="hljs-type">void</span> <span class="hljs-title function_">sdsfree</span><span class="hljs-params">(sds s)</span> &#123;<br>    <span class="hljs-keyword">if</span> (s == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span>;<br>    s_free((<span class="hljs-type">char</span> *) s - sdsHdrSize(s[<span class="hljs-number">-1</span>]));<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 根据字符串长度大小,返回合适的字符串类型</span><br><span class="hljs-type">static</span> <span class="hljs-keyword">inline</span> <span class="hljs-type">char</span> <span class="hljs-title function_">sdsReqType</span><span class="hljs-params">(<span class="hljs-type">size_t</span> string_size)</span> &#123;<br>    <span class="hljs-keyword">if</span> (string_size &lt; <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">5</span>)<span class="hljs-comment">// string_size &lt; (1 &lt;&lt; 5) 即长度 &lt; 2^5</span><br>        <span class="hljs-keyword">return</span> SDS_TYPE_5;<br>    <span class="hljs-keyword">if</span> (string_size &lt; <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">8</span>)<br>        <span class="hljs-keyword">return</span> SDS_TYPE_8;<br>    <span class="hljs-keyword">if</span> (string_size &lt; <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">16</span>)<br>        <span class="hljs-keyword">return</span> SDS_TYPE_16;<br><span class="hljs-meta">#<span class="hljs-keyword">if</span> (LONG_MAX == LLONG_MAX)</span><br>    <span class="hljs-keyword">if</span> (string_size &lt; <span class="hljs-number">1ll</span> &lt;&lt; <span class="hljs-number">32</span>)<br>        <span class="hljs-keyword">return</span> SDS_TYPE_32;<br>    <span class="hljs-keyword">return</span> SDS_TYPE_64;<br><span class="hljs-meta">#<span class="hljs-keyword">else</span></span><br>    <span class="hljs-keyword">return</span> SDS_TYPE_32;<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;<br></code></pre></td></tr></table></figure><p>以上代码需要注意的点：</p><ul><li>新建 SDS 时候，如果字符串长度为0，则选择 SDS8，而不是 SDS5，原因是空字符串接下来很可能进行追加操作，而 SDS5 类型不适合追加数据（会引发内存的重新分配）；</li><li>新建 SDS 时候，存放数据的字符数组最后一位设置为了’\0’，这样可以兼容原生的 C 语言字符串；</li><li>alloc 初始化成了 initlen，alloc 不是最开始至今被分配为当前 SDS 类型的最大值（2^8^，2^16^），而是慢慢增加的。</li></ul><p>alloc 的变动具体可以参考字符串拼接，<code>sdscatlen</code>函数</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 字符串拼接函数,拼接中可能由于原类型长度不够导致重新分配空间,因此所有引用必须用调用返回的新指针来替换</span><br><span class="hljs-comment">// 参数为 要拼接的字符串 t,原字符串 s,t 字符串的长度 len</span><br>sds <span class="hljs-title function_">sdscatlen</span><span class="hljs-params">(sds s, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *t, <span class="hljs-type">size_t</span> len)</span> &#123;<br>    <span class="hljs-comment">// 获取字符串现在的长度</span><br>    <span class="hljs-type">size_t</span> curlen = sdslen(s);<br><br>    <span class="hljs-comment">// 根据要追加的长度 len,和现有字符串 s 判断是否需要扩大</span><br>    s = sdsMakeRoomFor(s, len);<br>    <span class="hljs-keyword">if</span> (s == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br><br>    <span class="hljs-comment">// 将要追加的字符串 t 追加到 s 后面</span><br>    <span class="hljs-built_in">memcpy</span>(s + curlen, t, len);<br><br>    <span class="hljs-comment">// 给 sds 设置最新的长度</span><br>    sdssetlen(s, curlen + len);<br><br>    <span class="hljs-comment">// 末尾设置为 &#x27;\0&#x27;</span><br>    s[curlen + len] = <span class="hljs-string">&#x27;\0&#x27;</span>;<br>    <span class="hljs-keyword">return</span> s;<br>&#125;<br></code></pre></td></tr></table></figure><p>关键步骤就是在 <code>sdsMakeRoomFor</code> 函数</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><code class="hljs c">sds <span class="hljs-title function_">sdsMakeRoomFor</span><span class="hljs-params">(sds s, <span class="hljs-type">size_t</span> addlen)</span> &#123;<br>    <span class="hljs-type">void</span> *sh, *newsh;<br><br>    <span class="hljs-comment">// 获取该 SDS 的剩余容量  alloc - len</span><br>    <span class="hljs-type">size_t</span> avail = sdsavail(s);<br>    <span class="hljs-type">size_t</span> len, newlen;<br>    <span class="hljs-type">char</span> type, oldtype = s[<span class="hljs-number">-1</span>] &amp; SDS_TYPE_MASK;<br>    <span class="hljs-type">int</span> hdrlen;<br><br>    <span class="hljs-comment">/* Return ASAP if there is enough space left. */</span><br>    <span class="hljs-comment">// 剩余容量够,直接返回</span><br>    <span class="hljs-keyword">if</span> (avail &gt;= addlen) <span class="hljs-keyword">return</span> s;<br><br>    len = sdslen(s);<br>    sh = (<span class="hljs-type">char</span>*)s-sdsHdrSize(oldtype);<br><br>    <span class="hljs-comment">// 记录追加完后的长度</span><br>    newlen = (len+addlen);<br><br>    <span class="hljs-comment">// 新的长度少于 1024 * 1024,扩大为该值的2倍，否则新增 1M</span><br>    <span class="hljs-keyword">if</span> (newlen &lt; SDS_MAX_PREALLOC)<br>        newlen *= <span class="hljs-number">2</span>;<br>    <span class="hljs-keyword">else</span><br>        newlen += SDS_MAX_PREALLOC;<br><br>    <span class="hljs-comment">// 获取新长度对应的 SDS 类型</span><br>    type = sdsReqType(newlen);<br><br>    <span class="hljs-comment">/* Don&#x27;t use type 5: the user is appending to the string and type 5 is</span><br><span class="hljs-comment">     * not able to remember empty space, so sdsMakeRoomFor() must be called</span><br><span class="hljs-comment">     * at every appending operation. */</span><br>    <span class="hljs-comment">// 不要使用 SDS5，追加会重新内存分配，不友好</span><br>    <span class="hljs-keyword">if</span> (type == SDS_TYPE_5) type = SDS_TYPE_8;<br><br>    <span class="hljs-comment">// 新类型对应的头长度</span><br>    hdrlen = sdsHdrSize(type);<br><br>    <span class="hljs-comment">// 不需要改变类型</span><br>    <span class="hljs-keyword">if</span> (oldtype==type) &#123;<br>        newsh = s_realloc(sh, hdrlen+newlen+<span class="hljs-number">1</span>);<br>        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>        s = (<span class="hljs-type">char</span>*)newsh+hdrlen;<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">/* Since the header size changes, need to move the string forward,</span><br><span class="hljs-comment">         * and can&#x27;t use realloc */</span><br>        newsh = s_malloc(hdrlen+newlen+<span class="hljs-number">1</span>);<br>        <span class="hljs-keyword">if</span> (newsh == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>        <span class="hljs-built_in">memcpy</span>((<span class="hljs-type">char</span>*)newsh+hdrlen, s, len+<span class="hljs-number">1</span>);<br>        s_free(sh);<br>        s = (<span class="hljs-type">char</span>*)newsh+hdrlen;<br>        s[<span class="hljs-number">-1</span>] = type;<br>        sdssetlen(s, len);<br>    &#125;<br><br>    <span class="hljs-comment">// 给新的 SDS 设置新的 alloc</span><br>    sdssetalloc(s, newlen);<br>    <span class="hljs-keyword">return</span> s;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="44字节"><a href="#44字节" class="headerlink" title="44字节"></a>44字节</h2><p>Redis 在存储字符串时，如果长度不超过 44 字节，会采用 embstr 形式，否则采用 row 形式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash">127.0.0.1:6379&gt; <span class="hljs-built_in">set</span> name zhangsan<br>OK<br>127.0.0.1:6379&gt; object encoding name<br><span class="hljs-string">&quot;embstr&quot;</span><br>127.0.0.1:6379&gt; <span class="hljs-built_in">set</span> address zheshiyigechaoguosishisiweidezifuchuandaibiaodeshizhangsandedizhi<br>OK<br>127.0.0.1:6379&gt; object encoding address<br><span class="hljs-string">&quot;raw&quot;</span><br></code></pre></td></tr></table></figure><p>这个 44 字节是哪来的呢？</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> __<span class="hljs-title">attribute__</span> ((__<span class="hljs-title">packed__</span>)) <span class="hljs-title">sdshdr8</span> &#123;</span><br>    <span class="hljs-type">uint8_t</span> len; <br>    <span class="hljs-type">uint8_t</span> alloc; <span class="hljs-comment">// 字符串数组可分配的最大空间，不包括最后的&#x27;\0&#x27;字符</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> flags; <br>    <span class="hljs-type">char</span> buf[];<br>&#125;;<br></code></pre></td></tr></table></figure><p>在 sds8 中，结构体长度占 3 字节，字符串由于末尾要有 ‘\0’，所以整体是 4 字节。由于 Redis 的 Value 是用一个叫 RedisObject 的类型包装的，RedisObject 占 16 字节。</p><p>cpu cache line 的大小通常是 64B，所以如果字符串长度不大于 44 字节时，redis 会将字符串与 RedisObject 紧密存储，这样 cpu 在读取数据时无需再次访问内存，充分利用 cpu cache。</p><p>但是注意，如果对某个字符串进行了 append 等操作，redis 会将其修改为 raw 类型，即使依然不超 44 字节。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs bash">127.0.0.1:6379&gt; append name feng<br>(<span class="hljs-built_in">integer</span>) 12<br>127.0.0.1:6379&gt; get name<br><span class="hljs-string">&quot;zhangsanfeng&quot;</span><br>127.0.0.1:6379&gt; object encoding name<br><span class="hljs-string">&quot;raw&quot;</span><br>127.0.0.1:6379&gt; <span class="hljs-built_in">set</span> name2 zhangsanfeng<br>OK<br>127.0.0.1:6379&gt; object encoding name2<br><span class="hljs-string">&quot;embstr&quot;</span><br></code></pre></td></tr></table></figure><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Redis 摒弃 C 语言原生字符串，使用 SDS 简单动态字符串，核心是用带元信息的结构体替代纯 char*，完美解决传统字符串二进制不安全、获取长度 &#x2F; 拼接操作效率低的痛点。</p><p>SDS 根据字符串长度划分 5 种类型（SDS5&#x2F;8&#x2F;16&#x2F;32&#x2F;64），通过<code>len</code>记录已用长度、<code>alloc</code>记录总分配容量，剩余空间 &#x3D; alloc-len；创建字符串时不预分配冗余空间，<code>alloc</code>初始等于<code>len</code>，拼接扩容时按需倍增分配，兼顾内存节省与操作性能。</p><p>其指针指向字符串缓冲区，通过紧凑排列的结构体和<code>flags</code>字段快速识别类型，兼容 C 语言<code>\0</code>结尾规则；结合 RedisObject 实现，≤44 字节的字符串采用 embstr 编码，利用 CPU 缓存提升读取效率，修改后转为 raw 编码。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis源码分析之dict篇</title>
    <link href="/2026/05/09/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bdict%E7%AF%87/"/>
    <url>/2026/05/09/Redis%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bdict%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis源码分析之dict篇"><a href="#Redis源码分析之dict篇" class="headerlink" title="Redis源码分析之dict篇"></a>Redis源码分析之dict篇</h1><p>在上一篇 <a href="https://zhuwenjie0716.github.io/2026/05/08/Redis%E9%95%BF%E4%BB%80%E4%B9%88%E6%A0%B7/">Redis长什么样</a> 提到了 Redis 其实用了全局的 hash 表来存储所有的键值对，即下方图示的 dict，dict 中有两个数组，其中 ht[1] 只在 rehash 时候才真正用到，平时都是指向 null，hash 的底层又是使用数组来实现的，那就引出了接下来的几个问题：</p><ul><li>redis 初始化这个 dict 时数组容量是多少</li><li>如果发生了 hash 冲突怎么办</li><li>扩容机制（扩容的时机，扩容的实现)</li></ul><p><img src="/img/0398e9ef1ff47347cb54dd36a32da19c.png" alt="image-20250106162507568"> </p><h2 id="dict-的创建"><a href="#dict-的创建" class="headerlink" title="dict 的创建"></a>dict 的创建</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Create a new hash table */</span><br>dict *<span class="hljs-title function_">dictCreate</span><span class="hljs-params">(dictType *type, <span class="hljs-type">void</span> *privDataPtr)</span><br>&#123;<br>    dict *d = zmalloc(<span class="hljs-keyword">sizeof</span>(*d));<br>    _dictInit(d,type,privDataPtr);<br>    <span class="hljs-keyword">return</span> d;<br>&#125;<br><br><span class="hljs-comment">/* Initialize the hash table */</span><br><span class="hljs-type">int</span> _dictInit(dict *d, dictType *type, <span class="hljs-type">void</span> *privDataPtr)<br>&#123;<br>    _dictReset(&amp;d-&gt;ht[<span class="hljs-number">0</span>]);<br>    _dictReset(&amp;d-&gt;ht[<span class="hljs-number">1</span>]);<br>    d-&gt;type = type;<br>    d-&gt;privdata = privDataPtr;<br>    d-&gt;rehashidx = <span class="hljs-number">-1</span>;<br>    d-&gt;iterators = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">return</span> DICT_OK;<br>&#125;<br><br><span class="hljs-comment">/* Reset a hash table already initialized with ht_init().</span><br><span class="hljs-comment"> * <span class="hljs-doctag">NOTE:</span> This function should only be called by ht_destroy(). */</span><br><span class="hljs-type">static</span> <span class="hljs-type">void</span> _dictReset(dictht *ht)<br>&#123;<br>    ht-&gt;table = <span class="hljs-literal">NULL</span>;<br>    ht-&gt;size = <span class="hljs-number">0</span>;<span class="hljs-comment">// table 的长度, 一定是2的幂数值, 2^2,2^3...</span><br>    ht-&gt;sizemask = <span class="hljs-number">0</span>;<span class="hljs-comment">// size - 1,具体原因参见上篇</span><br>    ht-&gt;used = <span class="hljs-number">0</span>;<span class="hljs-comment">// 已经存的元素总数</span><br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>2行，创建变量时的 <code>dictType *type</code>，包含很多函数指针，无论是添加或查找 key 时 hash 函数还是删除 dictEntry 时的析构函数；</li><li>25行，<code>ht-&gt;table = NULL</code>，也就是说 dict 在被创建时 table 并没有分配内存空间，意味着第一个数据插入时才会真正分配空间。</li></ul><h2 id="dict-的扩容时机"><a href="#dict-的扩容时机" class="headerlink" title="dict 的扩容时机"></a>dict 的扩容时机</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Expand the hash table if needed */</span><br><span class="hljs-type">static</span> <span class="hljs-type">int</span> _dictExpandIfNeeded(dict *d)<br>&#123;<br>    <span class="hljs-comment">/* Incremental rehashing already in progress. Return. */</span><br>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) <span class="hljs-keyword">return</span> DICT_OK;<br><br>    <span class="hljs-comment">/* If the hash table is empty expand it to the initial size. */</span><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].size == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> dictExpand(d, DICT_HT_INITIAL_SIZE);<br><br>    <span class="hljs-comment">/* If we reached the 1:1 ratio, and we are allowed to resize the hash</span><br><span class="hljs-comment">     * table (global setting) or we should avoid it but the ratio between</span><br><span class="hljs-comment">     * elements/buckets is over the &quot;safe&quot; threshold, we resize doubling</span><br><span class="hljs-comment">     * the number of buckets. */</span><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used &gt;= d-&gt;ht[<span class="hljs-number">0</span>].size &amp;&amp;<br>        (dict_can_resize ||<br>         d-&gt;ht[<span class="hljs-number">0</span>].used/d-&gt;ht[<span class="hljs-number">0</span>].size &gt; dict_force_resize_ratio))<br>    &#123;<br>        <span class="hljs-keyword">return</span> dictExpand(d, d-&gt;ht[<span class="hljs-number">0</span>].used*<span class="hljs-number">2</span>);<br>    &#125;<br>    <span class="hljs-keyword">return</span> DICT_OK;<br>&#125;<br><br><span class="hljs-comment">/* Expand or create the hash table */</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">dictExpand</span><span class="hljs-params">(dict *d, <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> size)</span><br>&#123;<br>    <span class="hljs-comment">/* the size is invalid if it is smaller than the number of</span><br><span class="hljs-comment">     * elements already inside the hash table */</span><br>    <span class="hljs-keyword">if</span> (dictIsRehashing(d) || d-&gt;ht[<span class="hljs-number">0</span>].used &gt; size)<br>        <span class="hljs-keyword">return</span> DICT_ERR;<br><br>    dictht n; <span class="hljs-comment">/* the new hash table */</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> realsize = _dictNextPower(size);<span class="hljs-comment">// _dictNextPower 根据传入的 size 获得最终分配的 size</span><br><br>    <span class="hljs-comment">/* Rehashing to the same table size is not useful. */</span><br>    <span class="hljs-keyword">if</span> (realsize == d-&gt;ht[<span class="hljs-number">0</span>].size) <span class="hljs-keyword">return</span> DICT_ERR;<br><br>    <span class="hljs-comment">/* Allocate the new hash table and initialize all pointers to NULL */</span><br>    n.size = realsize;<br>    n.sizemask = realsize<span class="hljs-number">-1</span>;<br>    n.table = zcalloc(realsize*<span class="hljs-keyword">sizeof</span>(dictEntry*));<br>    n.used = <span class="hljs-number">0</span>;<br><br>    <span class="hljs-comment">/* Is this the first initialization? If so it&#x27;s not really a rehashing</span><br><span class="hljs-comment">     * we just set the first hash table so that it can accept keys. */</span><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].table == <span class="hljs-literal">NULL</span>) &#123;<br>        d-&gt;ht[<span class="hljs-number">0</span>] = n;<br>        <span class="hljs-keyword">return</span> DICT_OK;<br>    &#125;<br><br>    <span class="hljs-comment">/* Prepare a second hash table for incremental rehashing */</span><br>    d-&gt;ht[<span class="hljs-number">1</span>] = n;<br>    d-&gt;rehashidx = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">return</span> DICT_OK;<br>&#125;<br><br><span class="hljs-comment">/* Our hash table capability is a power of two */</span><br><span class="hljs-type">static</span> <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> _dictNextPower(<span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> size)<br>&#123;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> i = DICT_HT_INITIAL_SIZE;<br><br>    <span class="hljs-keyword">if</span> (size &gt;= LONG_MAX) <span class="hljs-keyword">return</span> LONG_MAX + <span class="hljs-number">1LU</span>;<br>    <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>) &#123;<br>        <span class="hljs-keyword">if</span> (i &gt;= size)<br>            <span class="hljs-keyword">return</span> i;<br>        i *= <span class="hljs-number">2</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>8行，如果是第一次插入（ht[0].size &#x3D;&#x3D; 0），用初始化大小（DICT_HT_INITIAL_SIZE）分配，这个值默认是 4；</li><li>14行，扩容的判断条件，负载因子（d-&gt;ht[0].used &#x2F; d-&gt;ht[0].size），即 ht 中元素总数与 bucket 个数的比值；<ul><li>负载因子 &gt;&#x3D; 1，且 dict_can_resize，dict_can_resize 的值受此时 redis 状态的影响，例如 redis 在进行 aof 重写或 rdb生成时，不想造成太多的内存拷贝，就会关闭；</li><li>负载因子 &gt;&#x3D; 5，即 dict_force_resize_ratio 的默认值，此时 hash 冲突已经很严重了，不管此时处于什么状态，都会进行扩容。</li></ul></li></ul><h2 id="dict-的销毁"><a href="#dict-的销毁" class="headerlink" title="dict 的销毁"></a>dict 的销毁</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Clear &amp; Release the hash table */</span><br><span class="hljs-type">void</span> <span class="hljs-title function_">dictRelease</span><span class="hljs-params">(dict *d)</span><br>&#123;<br>    _dictClear(d,&amp;d-&gt;ht[<span class="hljs-number">0</span>],<span class="hljs-literal">NULL</span>);<br>    _dictClear(d,&amp;d-&gt;ht[<span class="hljs-number">1</span>],<span class="hljs-literal">NULL</span>);<br>    zfree(d);<br>&#125;<br><br><span class="hljs-comment">/* Destroy an entire dictionary */</span><br><span class="hljs-type">int</span> _dictClear(dict *d, dictht *ht, <span class="hljs-type">void</span>(callback)(<span class="hljs-type">void</span> *)) &#123;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> i;<br><br>    <span class="hljs-comment">/* Free all the elements */</span><br>    <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; ht-&gt;size &amp;&amp; ht-&gt;used &gt; <span class="hljs-number">0</span>; i++) &#123;<br>        dictEntry *he, *nextHe;<br><br>        <span class="hljs-keyword">if</span> (callback &amp;&amp; (i &amp; <span class="hljs-number">65535</span>) == <span class="hljs-number">0</span>) callback(d-&gt;privdata);<br><br>        <span class="hljs-keyword">if</span> ((he = ht-&gt;table[i]) == <span class="hljs-literal">NULL</span>) <span class="hljs-keyword">continue</span>;<br>        <span class="hljs-keyword">while</span>(he) &#123;<br>            nextHe = he-&gt;next;<br>            dictFreeKey(d, he);<br>            dictFreeVal(d, he);<br>            zfree(he);<br>            ht-&gt;used--;<br>            he = nextHe;<br>        &#125;<br>    &#125;<br>    <span class="hljs-comment">/* Free the table and the allocated cache structure */</span><br>    zfree(ht-&gt;table);<br>    <span class="hljs-comment">/* Re-initialize the table */</span><br>    _dictReset(ht);<br>    <span class="hljs-keyword">return</span> DICT_OK; <span class="hljs-comment">/* never fails */</span><br>&#125;<br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> dictFreeKey(d, entry) \</span><br><span class="hljs-meta">    <span class="hljs-keyword">if</span> ((d)-&gt;type-&gt;keyDestructor) \</span><br><span class="hljs-meta">        (d)-&gt;type-&gt;keyDestructor((d)-&gt;privdata, (entry)-&gt;key)</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> dictFreeVal(d, entry) \</span><br><span class="hljs-meta">    <span class="hljs-keyword">if</span> ((d)-&gt;type-&gt;valDestructor) \</span><br><span class="hljs-meta">        (d)-&gt;type-&gt;valDestructor((d)-&gt;privdata, (entry)-&gt;v.val)</span><br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>20-27行：删除每个 dictEntry 时会调用 key 和 value 的析构函数（keyDestructor 和 valDestructor），用于释放键和值所占的内存。</li></ul><h2 id="dict-添加元素"><a href="#dict-添加元素" class="headerlink" title="dict 添加元素"></a>dict 添加元素</h2><p>主要涉及两个函数，dictAdd 和 dictReplace。</p><h3 id="dictAdd"><a href="#dictAdd" class="headerlink" title="dictAdd"></a>dictAdd</h3><p>该函数向 hash table 中添加数据，如果 key 已经存在会返回失败。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Add an element to the target hash table */</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">dictAdd</span><span class="hljs-params">(dict *d, <span class="hljs-type">void</span> *key, <span class="hljs-type">void</span> *val)</span><br>&#123;<br>    dictEntry *entry = dictAddRaw(d,key,<span class="hljs-literal">NULL</span>);<br>    <span class="hljs-keyword">if</span> (!entry) <span class="hljs-keyword">return</span> DICT_ERR;<br>    dictSetVal(d, entry, val);<br>    <span class="hljs-keyword">return</span> DICT_OK;<br>&#125;<br><br><span class="hljs-comment">/* Low level add or find:</span><br><span class="hljs-comment"> * This function adds the entry but instead of setting a value returns the</span><br><span class="hljs-comment"> * dictEntry structure to the user, that will make sure to fill the value</span><br><span class="hljs-comment"> * field as he wishes.</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * This function is also directly exposed to the user API to be called</span><br><span class="hljs-comment"> * mainly in order to store non-pointers inside the hash value, example:</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * entry = dictAddRaw(dict,mykey,NULL);</span><br><span class="hljs-comment"> * if (entry != NULL) dictSetSignedIntegerVal(entry,1000);</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * Return values:</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * If key already exists NULL is returned, and &quot;*existing&quot; is populated</span><br><span class="hljs-comment"> * with the existing entry if existing is not NULL.</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * If key was added, the hash entry is returned to be manipulated by the caller.</span><br><span class="hljs-comment"> */</span><br>dictEntry *<span class="hljs-title function_">dictAddRaw</span><span class="hljs-params">(dict *d, <span class="hljs-type">void</span> *key, dictEntry **existing)</span><br>&#123;<br>    <span class="hljs-type">long</span> index;<br>    dictEntry *entry;<br>    dictht *ht;<br><br>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);<br><br>    <span class="hljs-comment">/* Get the index of the new element, or -1 if</span><br><span class="hljs-comment">     * the element already exists. */</span><br>    <span class="hljs-keyword">if</span> ((index = _dictKeyIndex(d, key, dictHashKey(d,key), existing)) == <span class="hljs-number">-1</span>)<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br><br>    <span class="hljs-comment">/* Allocate the memory and store the new entry.</span><br><span class="hljs-comment">     * Insert the element in top, with the assumption that in a database</span><br><span class="hljs-comment">     * system it is more likely that recently added entries are accessed</span><br><span class="hljs-comment">     * more frequently. */</span><br>    ht = dictIsRehashing(d) ? &amp;d-&gt;ht[<span class="hljs-number">1</span>] : &amp;d-&gt;ht[<span class="hljs-number">0</span>];<br>    entry = zmalloc(<span class="hljs-keyword">sizeof</span>(*entry));<br>    entry-&gt;next = ht-&gt;table[index];<br>    ht-&gt;table[index] = entry;<br>    ht-&gt;used++;<br><br>    <span class="hljs-comment">/* Set the hash entry fields. */</span><br>    dictSetKey(d, entry, key);<br>    <span class="hljs-keyword">return</span> entry;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>34行判断此时是否正在 rehash，如果在，则将 rehash 向前推进一步；</li><li>38行，<code>_dictKeyIndex</code>函数根据传入的 key 寻找插入的位置，如果该 key 已经存在，会返回-1；</li><li>45行，如果此时正在 rehash，就把数据插入到 ht[1]，否则插入到 ht[0]；</li><li>47-49行，使用<code>头插法插入数据</code>。</li></ul><p>上面有两个关键函数，一个是<code>_dictKeyIndex</code>，另一个是<code>_dictRehashStep</code></p><h4 id="dictKeyIndex"><a href="#dictKeyIndex" class="headerlink" title="_dictKeyIndex"></a>_dictKeyIndex</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Returns the index of a free slot that can be populated with</span><br><span class="hljs-comment"> * a hash entry for the given &#x27;key&#x27;.</span><br><span class="hljs-comment"> * If the key already exists, -1 is returned</span><br><span class="hljs-comment"> * and the optional output parameter may be filled.</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * Note that if we are in the process of rehashing the hash table, the</span><br><span class="hljs-comment"> * index is always returned in the context of the second (new) hash table. */</span><br><span class="hljs-type">static</span> <span class="hljs-type">long</span> _dictKeyIndex(dict *d, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *key, <span class="hljs-type">uint64_t</span> hash, dictEntry **existing)<br>&#123;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> idx, table;<br>    dictEntry *he;<br>    <span class="hljs-keyword">if</span> (existing) *existing = <span class="hljs-literal">NULL</span>;<br><br>    <span class="hljs-comment">/* Expand the hash table if needed */</span><br>    <span class="hljs-keyword">if</span> (_dictExpandIfNeeded(d) == DICT_ERR)<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">for</span> (table = <span class="hljs-number">0</span>; table &lt;= <span class="hljs-number">1</span>; table++) &#123;<br>        idx = hash &amp; d-&gt;ht[table].sizemask;<br>        <span class="hljs-comment">/* Search if this slot does not already contain the given key */</span><br>        he = d-&gt;ht[table].table[idx];<br>        <span class="hljs-keyword">while</span>(he) &#123;<br>            <span class="hljs-keyword">if</span> (key==he-&gt;key || dictCompareKeys(d, key, he-&gt;key)) &#123;<br>                <span class="hljs-keyword">if</span> (existing) *existing = he;<br>                <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>            &#125;<br>            he = he-&gt;next;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">break</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> idx;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>15行，会判断此时是否需要扩容</li><li>28行，查找时会先从 ht[0] 查找，如果 ht[0] 中没有数据且此时不在进行 rehash，就直接返回</li></ul><p>_dictKeyIndex 如果 key 已经存在会返回 -1，否则返回该 key 需要插入的桶的索引。</p><h4 id="dictRehashStep"><a href="#dictRehashStep" class="headerlink" title="_dictRehashStep"></a>_dictRehashStep</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* This function performs just a step of rehashing, and only if there are</span><br><span class="hljs-comment"> * no safe iterators bound to our hash table. When we have iterators in the</span><br><span class="hljs-comment"> * middle of a rehashing we can&#x27;t mess with the two hash tables otherwise</span><br><span class="hljs-comment"> * some element can be missed or duplicated.</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * This function is called by common lookup or update operations in the</span><br><span class="hljs-comment"> * dictionary so that the hash table automatically migrates from H1 to H2</span><br><span class="hljs-comment"> * while it is actively used. */</span><br><span class="hljs-type">static</span> <span class="hljs-type">void</span> _dictRehashStep(dict *d) &#123;<br>    <span class="hljs-keyword">if</span> (d-&gt;iterators == <span class="hljs-number">0</span>) dictRehash(d,<span class="hljs-number">1</span>);<br>&#125;<br><br><span class="hljs-comment">/* Performs N steps of incremental rehashing. Returns 1 if there are still</span><br><span class="hljs-comment"> * keys to move from the old to the new hash table, otherwise 0 is returned.</span><br><span class="hljs-comment"> *</span><br><span class="hljs-comment"> * Note that a rehashing step consists in moving a bucket (that may have more</span><br><span class="hljs-comment"> * than one key as we use chaining) from the old to the new hash table, however</span><br><span class="hljs-comment"> * since part of the hash table may be composed of empty spaces, it is not</span><br><span class="hljs-comment"> * guaranteed that this function will rehash even a single bucket, since it</span><br><span class="hljs-comment"> * will visit at max N*10 empty buckets in total, otherwise the amount of</span><br><span class="hljs-comment"> * work it does would be unbound and the function may block for a long time. */</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">dictRehash</span><span class="hljs-params">(dict *d, <span class="hljs-type">int</span> n)</span> &#123;<br>    <span class="hljs-type">int</span> empty_visits = n*<span class="hljs-number">10</span>; <span class="hljs-comment">/* Max number of empty buckets to visit. */</span><br>    <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br><br>    <span class="hljs-keyword">while</span>(n-- &amp;&amp; d-&gt;ht[<span class="hljs-number">0</span>].used != <span class="hljs-number">0</span>) &#123;<br>        dictEntry *de, *nextde;<br><br>        <span class="hljs-comment">/* Note that rehashidx can&#x27;t overflow as we are sure there are more</span><br><span class="hljs-comment">         * elements because ht[0].used != 0 */</span><br>        assert(d-&gt;ht[<span class="hljs-number">0</span>].size &gt; (<span class="hljs-type">unsigned</span> <span class="hljs-type">long</span>)d-&gt;rehashidx);<br>        <span class="hljs-keyword">while</span>(d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx] == <span class="hljs-literal">NULL</span>) &#123;<br>            d-&gt;rehashidx++;<br>            <span class="hljs-keyword">if</span> (--empty_visits == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>        &#125;<br>        de = d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx];<br>        <span class="hljs-comment">/* Move all the keys in this bucket from the old to the new hash HT */</span><br>        <span class="hljs-keyword">while</span>(de) &#123;<br>            <span class="hljs-type">uint64_t</span> h;<br><br>            nextde = de-&gt;next;<br>            <span class="hljs-comment">/* Get the index in the new hash table */</span><br>            h = dictHashKey(d, de-&gt;key) &amp; d-&gt;ht[<span class="hljs-number">1</span>].sizemask;<br>            de-&gt;next = d-&gt;ht[<span class="hljs-number">1</span>].table[h];<br>            d-&gt;ht[<span class="hljs-number">1</span>].table[h] = de;<br>            d-&gt;ht[<span class="hljs-number">0</span>].used--;<br>            d-&gt;ht[<span class="hljs-number">1</span>].used++;<br>            de = nextde;<br>        &#125;<br>        d-&gt;ht[<span class="hljs-number">0</span>].table[d-&gt;rehashidx] = <span class="hljs-literal">NULL</span>;<br>        d-&gt;rehashidx++;<br>    &#125;<br><br>    <span class="hljs-comment">/* Check if we already rehashed the whole table... */</span><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used == <span class="hljs-number">0</span>) &#123;<br>        zfree(d-&gt;ht[<span class="hljs-number">0</span>].table);<br>        d-&gt;ht[<span class="hljs-number">0</span>] = d-&gt;ht[<span class="hljs-number">1</span>];<br>        _dictReset(&amp;d-&gt;ht[<span class="hljs-number">1</span>]);<br>        d-&gt;rehashidx = <span class="hljs-number">-1</span>;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>    &#125;<br><br>    <span class="hljs-comment">/* More to rehash... */</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>32-35行，如果 rehashidx 指向的 bucket 里一个 dictEntry 也没有，那么它就没有可迁移的数据。这时它尝试在 ht[0].table 数组中不断向后遍历，直到找到下一个存有数据的 bucket 位置。如果一直找不到，则最多走 n*10 步（empty_visits）。避免在该操作上一直向后遍历，影响响应时间；</li><li>43行，原 bucket 里的元素重新 hash 计算后对新的 sizemask 进行余操作，确定在新 table 上的位置；</li><li>55-61行，如果 ht[0] 上的数据都迁移到 ht[1] 上了（即d-&gt;ht[0].used &#x3D;&#x3D; 0），那么整个重哈希结束，ht[0] 变成 ht[1] 的内容，而 ht[1] 重置为空。</li></ul><h4 id="Rehash-图示"><a href="#Rehash-图示" class="headerlink" title="Rehash 图示"></a>Rehash 图示</h4><p>rehash 的逻辑可以参考下面这张图：</p><ol><li>此时负载因子&gt;1，系统不忙可以扩容，新建一个长度为16的数组：</li></ol><p><img src="/img/image-20260509094313971.png" alt="image-20260509094313971"></p><ol start="2"><li>迁移第一个桶（bucket）的数据，如果元素本来 %4&#x3D;0，那么 %16 可选是 [0,4,8,12]（出于图片长度的考虑，图示按照新数组长度为8模拟）。迁移完成后 rehashidx&#x3D;1，标识该迁移数组下标为1的桶里的数据了：</li></ol><p><img src="/img/image-20260509092051735.png" alt="image-20260509092051735"></p><ol start="3"><li>迁移数组下标为1的桶（bucket）的数据：</li></ol><p><img src="/img/image-20260509092431819.png" alt="image-20260509092431819"></p><p>Rehash （也叫渐进式重哈希）将数据迁移分散到多次操作中完成，有效地避免了一次性迁移大量数据导致系统阻塞的情况。</p><h3 id="dictReplace"><a href="#dictReplace" class="headerlink" title="dictReplace"></a>dictReplace</h3><p>该函数向 hash table 中添加数据，如果 key 已经存在会替换</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Add or Overwrite:</span><br><span class="hljs-comment"> * Add an element, discarding the old value if the key already exists.</span><br><span class="hljs-comment"> * Return 1 if the key was added from scratch, 0 if there was already an</span><br><span class="hljs-comment"> * element with such key and dictReplace() just performed a value update</span><br><span class="hljs-comment"> * operation. */</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">dictReplace</span><span class="hljs-params">(dict *d, <span class="hljs-type">void</span> *key, <span class="hljs-type">void</span> *val)</span><br>&#123;<br>    dictEntry *entry, *existing, auxentry;<br><br>    <span class="hljs-comment">/* Try to add the element. If the key</span><br><span class="hljs-comment">     * does not exists dictAdd will succeed. */</span><br>    entry = dictAddRaw(d,key,&amp;existing);<br>    <span class="hljs-keyword">if</span> (entry) &#123;<br>        dictSetVal(d, entry, val);<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;<br>    &#125;<br><br>    <span class="hljs-comment">/* Set the new value and free the old one. Note that it is important</span><br><span class="hljs-comment">     * to do that in this order, as the value may just be exactly the same</span><br><span class="hljs-comment">     * as the previous one. In this context, think to reference counting,</span><br><span class="hljs-comment">     * you want to increment (set), and then decrement (free), and not the</span><br><span class="hljs-comment">     * reverse. */</span><br>    auxentry = *existing;<br>    dictSetVal(d, existing, val);<br>    dictFreeVal(d, &amp;auxentry);<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>12行，如果插入成功（key不存在）entry 就是插入的对象，如果 key 已经存在，则 existing 变量就是对应的 dictEntry；</li><li>23-26行，注意顺序是先创建并设置新值，再释放旧值。</li></ul><h2 id="dict-删除元素"><a href="#dict-删除元素" class="headerlink" title="dict 删除元素"></a>dict 删除元素</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/* Remove an element, returning DICT_OK on success or DICT_ERR if the</span><br><span class="hljs-comment"> * element was not found. */</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">dictDelete</span><span class="hljs-params">(dict *ht, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *key)</span> &#123;<br>    <span class="hljs-keyword">return</span> dictGenericDelete(ht,key,<span class="hljs-number">0</span>) ? DICT_OK : DICT_ERR;<br>&#125;<br><br><span class="hljs-comment">/* Search and remove an element. This is an helper function for</span><br><span class="hljs-comment"> * dictDelete() and dictUnlink(), please check the top comment</span><br><span class="hljs-comment"> * of those functions. */</span><br><span class="hljs-type">static</span> dictEntry *<span class="hljs-title function_">dictGenericDelete</span><span class="hljs-params">(dict *d, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *key, <span class="hljs-type">int</span> nofree)</span> &#123;<br>    <span class="hljs-type">uint64_t</span> h, idx;<br>    dictEntry *he, *prevHe;<br>    <span class="hljs-type">int</span> table;<br><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used == <span class="hljs-number">0</span> &amp;&amp; d-&gt;ht[<span class="hljs-number">1</span>].used == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br><br>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);<br>    h = dictHashKey(d, key);<br><br>    <span class="hljs-keyword">for</span> (table = <span class="hljs-number">0</span>; table &lt;= <span class="hljs-number">1</span>; table++) &#123;<br>        idx = h &amp; d-&gt;ht[table].sizemask;<br>        he = d-&gt;ht[table].table[idx];<br>        prevHe = <span class="hljs-literal">NULL</span>;<br>        <span class="hljs-keyword">while</span>(he) &#123;<br>            <span class="hljs-keyword">if</span> (key==he-&gt;key || dictCompareKeys(d, key, he-&gt;key)) &#123;<br>                <span class="hljs-comment">/* Unlink the element from the list */</span><br>                <span class="hljs-keyword">if</span> (prevHe)<br>                    prevHe-&gt;next = he-&gt;next;<br>                <span class="hljs-keyword">else</span><br>                    d-&gt;ht[table].table[idx] = he-&gt;next;<br>                <span class="hljs-keyword">if</span> (!nofree) &#123;<br>                    dictFreeKey(d, he);<br>                    dictFreeVal(d, he);<br>                    zfree(he);<br>                &#125;<br>                d-&gt;ht[table].used--;<br>                <span class="hljs-keyword">return</span> he;<br>            &#125;<br>            prevHe = he;<br>            he = he-&gt;next;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">break</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>; <span class="hljs-comment">/* not found */</span><br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>17行，如果在 rehash，也推进一步；</li><li>25-38行，其实就是首先根据 hash 找到对应的 bucket，在链表中删除该元素，找不到则返回 null，根据是否在 rehash 决定只查找 ht[0] 还是 ht[1]；</li><li>32-33行，删除后会调用 key 和 value 的析构函数（keyDestructor 和 valDestructor）。</li></ul><h2 id="dict-查找元素"><a href="#dict-查找元素" class="headerlink" title="dict 查找元素"></a>dict 查找元素</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs c">dictEntry *<span class="hljs-title function_">dictFind</span><span class="hljs-params">(dict *d, <span class="hljs-type">const</span> <span class="hljs-type">void</span> *key)</span><br>&#123;<br>    dictEntry *he;<br>    <span class="hljs-type">uint64_t</span> h, idx, table;<br><br>    <span class="hljs-keyword">if</span> (d-&gt;ht[<span class="hljs-number">0</span>].used + d-&gt;ht[<span class="hljs-number">1</span>].used == <span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>; <span class="hljs-comment">/* dict is empty */</span><br>    <span class="hljs-keyword">if</span> (dictIsRehashing(d)) _dictRehashStep(d);<br>    h = dictHashKey(d, key);<br>    <span class="hljs-keyword">for</span> (table = <span class="hljs-number">0</span>; table &lt;= <span class="hljs-number">1</span>; table++) &#123;<br>        idx = h &amp; d-&gt;ht[table].sizemask;<br>        he = d-&gt;ht[table].table[idx];<br>        <span class="hljs-keyword">while</span>(he) &#123;<br>            <span class="hljs-keyword">if</span> (key==he-&gt;key || dictCompareKeys(d, key, he-&gt;key))<br>                <span class="hljs-keyword">return</span> he;<br>            he = he-&gt;next;<br>        &#125;<br>        <span class="hljs-keyword">if</span> (!dictIsRehashing(d)) <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">NULL</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码分析：</p><ul><li>7行，如果此时在 rehash，它也会推进一步；</li><li>10行，hash 值与 sizemask 取余 &#x3D; hash % size，这样写的原因是 &amp; 一步到位，而 % 是一直除下去；</li><li>17行，如果 ht[0] 中没有，且当前不在 rehash，直接返回 null，否则去 ht[1] 中查找。</li></ul><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>dict 不仅用于存储全局键值对，在实现其它数据结构的时候也有用到。本文梳理了 dict 的创建、扩容、渐进式 rehash、增删改查等关键流程：</p><ul><li>dict 在创建时只做初始化，<code>table</code> 指针为 <code>NULL</code>，直到第一次插入数据时才真正分配内存，默认数组大小为 <strong>4</strong>（<code>DICT_HT_INITIAL_SIZE</code>）。</li><li>扩容由负载因子（<code>used/size</code>）触发：负载因子 ≥ 1 且 <code>dict_can_resize</code> 允许，或者负载因子 ≥ 5（<code>dict_force_resize_ratio</code>）时强制扩容，新大小为 <code>used*2</code> 的上一个 2 的幂。</li><li>采用<strong>渐进式 rehash</strong>：将旧表 <code>ht[0]</code> 的数据分多次、每次一小步迁移到新表 <code>ht[1]</code>，避免一次性大量拷贝导致阻塞。每次查找、插入、删除操作都会调用 <code>_dictRehashStep</code> 推动一步（迁移一个非空桶），若 bucket 为空则会遍历最多 <code>n*10</code> 个空桶以防空耗。</li><li>冲突解决采用<strong>链地址法</strong>，且新元素使用<strong>头插法</strong>插入链表头部，提高最近访问的效率。</li><li>删除和替换操作会通过 <code>keyDestructor</code> &#x2F; <code>valDestructor</code> 释放内存，且替换时先设置新值再释放旧值，保证引用计数的正确性。</li></ul><p>通过渐进式 rehash 与灵活的扩容策略，Redis 在保证高并发读写的同时，平滑地完成哈希表的动态扩展，避免了传统“一次 rehash”带来的长时间停顿。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Redis长什么样</title>
    <link href="/2026/05/08/Redis%E9%95%BF%E4%BB%80%E4%B9%88%E6%A0%B7/"/>
    <url>/2026/05/08/Redis%E9%95%BF%E4%BB%80%E4%B9%88%E6%A0%B7/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis长什么样"><a href="#Redis长什么样" class="headerlink" title="Redis长什么样"></a>Redis长什么样</h1><p>我们都知道 Redis 是一个 &lt;key,value&gt; 的键值数据库，也就是一个 Map。我们平时是怎么实现这样一个 Map 的呢？</p><p>用数组，当一个 key 来的时候，首先进行 hash 运算，接着对数据的 length 取余，把这个键值对放在对应位置，这样能以 O(1) 的时间复杂度进行某个 key 的定位。</p><p>Redis 对外暴露了五种常见的数据结构，string、list、hash、set、sorted set；从内部实现的角度，则是 dict、sds、ziplist、quicklist、skiplist，Redis 通过这几种内部实现，组成了对外暴露的数据结构，充分的考虑了内存占用和操作速度。</p><p>本篇暂且介绍 Redis 的整体样貌，与数据结构相关的后续再补。</p><h2 id="设计与分析"><a href="#设计与分析" class="headerlink" title="设计与分析"></a>设计与分析</h2><p>我们来看一下 Redis 的设计：</p><p><code>redisDb</code>就是我们说的数据库，redis 有16个这样的数据库，其中的 <code>dict *dict</code> 是我们数据存放的字段</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">redisDb</span> &#123;</span><br>    dict *dict;                 <span class="hljs-comment">/* The keyspace for this DB */</span><br>    dict *expires;              <span class="hljs-comment">/* Timeout of keys with a timeout set */</span><br>    dict *blocking_keys;        <span class="hljs-comment">/* Keys with clients waiting for data (BLPOP)*/</span><br>    dict *ready_keys;           <span class="hljs-comment">/* Blocked keys that received a PUSH */</span><br>    dict *watched_keys;         <span class="hljs-comment">/* WATCHED keys for MULTI/EXEC CAS */</span><br>    <span class="hljs-type">int</span> id;                     <span class="hljs-comment">/* Database ID */</span><br>    <span class="hljs-type">long</span> <span class="hljs-type">long</span> avg_ttl;          <span class="hljs-comment">/* Average TTL, just for stats */</span><br>    <span class="hljs-built_in">list</span> *defrag_later;         <span class="hljs-comment">/* List of key names to attempt to defrag one by one, gradually. */</span><br>&#125; redisDb;<br></code></pre></td></tr></table></figure><ul><li>前几个字段的类型都是 dict，分别是正常的键值对，设置了过期时间的键值对，客户端正在阻塞等待的键值对，已经有数据、可以唤醒阻塞客户端的键值对和被 redis 事务监控的键值对；</li><li>id 字段取值为 0~15，也就是 redis 的数据库编号；</li><li>记录这个数据库中所有带过期时间键值对的平均剩余存活时间；</li><li>等待主动碎片整理的键列表，当开启主动碎片整理时，把需要整理的 key 放进这个链，逐个整理避免卡顿。</li></ul><p>再看一下 <code>dict</code> 结构体的设计：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dict</span> &#123;</span><br>    dictType *type;<br>    <span class="hljs-type">void</span> *privdata;<br>    dictht ht[<span class="hljs-number">2</span>];<br>    <span class="hljs-type">long</span> rehashidx; <span class="hljs-comment">/* rehashing not in progress if rehashidx == -1 */</span><br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> iterators; <span class="hljs-comment">/* number of iterators currently running */</span><br>&#125; dict;<br></code></pre></td></tr></table></figure><ul><li><code>dictType *type</code>里有一堆函数指针，当有 key 来时，需要通过函数指针里的 hash 函数求 hash 值，对数组长度取余后还要调用比较函数判断对应位置的 key 与当前 key 是否相等（原因是会有 hash 冲突，redis 首先使用链表法解决冲突，头插法）；</li><li><code>void *privdata</code>是私有数据指针，由调用者在创建dict的时候传进来；</li><li><code> dictht ht[2]</code>，一般情况下 ht[1] 都是指向 NULL，只有在 rehash 的时候才有值。Redis 使用两个数组，当需要扩容时，读请求首先从 ht[0] 读，没有的话再去 ht[1]，写请求直接写 ht[1]，更新请求会略微复杂些；</li><li><code>rehashidx</code>，rehash 的索引，如果是-1，代表此时没有进行 rehash，否则代表需要进行迁移的数组下标。</li></ul><p>再看一下<code>dictht</code>结构体的设计，ht，即 hashtable，学 java 的应该很熟悉</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictht</span> &#123;</span><br>    dictEntry **table;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> size;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> sizemask;<br>    <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> used;<br>&#125; dictht;<br></code></pre></td></tr></table></figure><ul><li><code>dictEntry **table</code>：一个 dictEntry 指针数组。key的哈希值最终映射到这个数组的某个位置上（对应一个bucket）。如果多个 key 映射到同一个位置，就发生了冲突，那么就拉出一个 dictEntry 链表；</li><li><code>size</code>：标识 dictEntry 指针数组的长度。它总是 2 的幂；</li><li><code>sizemask</code>：key 的 hash 值对 size 取模的结果其实等于 hash 值 &amp; (size - 1)，而且%运算会一直除，而&amp;运算一次到位；</li><li><code>used</code>：记录dict中现有的数据个数。它与size的比值就是装载因子（load factor）。这个比值越大，哈希值冲突概率越高。</li></ul><p>最终的数据是在 dictEntry 中，一起来看下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> &#123;</span><br>    <span class="hljs-type">void</span> *key;<br>    <span class="hljs-class"><span class="hljs-keyword">union</span> &#123;</span><br>        <span class="hljs-type">void</span> *val;<br>        <span class="hljs-type">uint64_t</span> u64;<br>        <span class="hljs-type">int64_t</span> s64;<br>        <span class="hljs-type">double</span> d;<br>    &#125; v;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dictEntry</span> *<span class="hljs-title">next</span>;</span><br>&#125; dictEntry;<br></code></pre></td></tr></table></figure><p>一个 key 的指针，一个联合类型，由于 hash 函数可能会冲突，所以还有 next 指针用来解决冲突（链表头插法）：</p><ul><li>key 总是指向一个 sds 结构，也就是键值对的键；</li><li>value 是一个联合类型，当值可以直接存储时（int、long、double）就直接存值，否则使用指针指向一个 robj 结构。</li></ul><h2 id="图示"><a href="#图示" class="headerlink" title="图示"></a>图示</h2><p>Redis 整体结构的分析还是比较简单的，经过上述分析，我们可以得到这样的一个 Redis 结构图：</p><p><img src="/img/image-20250106162507568.png" alt="image-20250106162507568">  </p><p>关于 Redis 的 dict、sds、ziplist、quicklist、skiplist 核心结构，以及涉及到的渐进式哈希在后续介绍~</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之TCP篇</title>
    <link href="/2026/05/07/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BTCP%E7%AF%87/"/>
    <url>/2026/05/07/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BTCP%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之TCP篇"><a href="#系统性能优化之TCP篇" class="headerlink" title="系统性能优化之TCP篇"></a>系统性能优化之TCP篇</h1><p>TCP 是一个可以双向传输的全双工协议，常见的 HTTP 等协议都是基于 TCP 实现的。</p><p>TCP 数据交互的主要过程有三次握手建立连接，拥塞控制，四次挥手断开连接，接下来分别介绍几个主要过程涉及到的可能优化方向。</p><p>文中的配置参数均可通过 <code>sysctl 参数</code> 查看，<code>syn -w 参数=newValue</code> 修改。</p><h2 id="三次握手与TFO"><a href="#三次握手与TFO" class="headerlink" title="三次握手与TFO"></a>三次握手与TFO</h2><p><strong>三次握手建立连接的首要目的是同步序列号</strong>。只有同步了序列号才有可靠的传输，TCP 协议的许多特性都是依赖序列号实现的，比如流量控制、消息丢失后的重发等等，这也是三次握手中的报文被称为 SYN 的原因，因为 SYN 的全称就叫做 Synchronize Sequence Numbers。</p><p>三次握手的流程由下图所示：</p><p><img src="/img/image-20260507101236510.png" alt="image-20260507101236510"></p><ol><li>客户端发送 SYN 开启了三次握手，此时在客户端上用 netstat 命令可以看到连接的状态是 SYN_SENT（但持续时间一般非常短，几毫秒）。</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@node-17 ~]# netstat | grep SYN_SENT<br>tcp        0      1 node-17:45506           192.168.**.**:*****     SYN_SENT<br></code></pre></td></tr></table></figure><ol start="2"><li>当服务器收到 SYN 报文后，会立刻回复 SYN+ACK 报文，既确认了客户端的序列号，也把自己的序列号发给了对方。此时，服务器端出现了新连接，状态是 SYN_RCV。</li><li>当客户端接收到服务器发来的 SYN+ACK 报文后，就会回复 ACK 去通知服务器，同时己方连接状态从 SYN_SENT 转换为 ESTABLISHED，表示连接建立成功。</li><li>服务器端连接成功建立的时间还要再往后，到它收到 ACK 后状态才变为 ESTABLISHED。</li></ol><p>那么握手流程中有什么可以优化的地方呢？</p><h3 id="客户端优化"><a href="#客户端优化" class="headerlink" title="客户端优化"></a>客户端优化</h3><p>客户端在发送 SYN 后会等待服务端的回复，如果不回复会进行重试，重试的次数由 net.ipv4.tcp_syn_retries 参数控制，默认是 6 次。其中第一次重试发生在 1s 后，接着等待双倍时间后重试，所有重试结束需要 2 分钟。内网中通讯时，就可以适当调低重试次数，尽快把错误暴露给应用程序。</p><h3 id="服务端优化"><a href="#服务端优化" class="headerlink" title="服务端优化"></a>服务端优化</h3><p>服务器收到 SYN 报文后，必须建立一个 SYN 半连接队列来维护未完成的握手信息（SYN_RCV），当这个队列溢出后，服务器将无法再建立新连接。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@iZm5e8izu4t87flkkvb649Z ~]# netstat -s | grep <span class="hljs-string">&quot;SYNs to LISTEN&quot;</span><br>    1173823 SYNs to LISTEN sockets dropped<br></code></pre></td></tr></table></figure><p>上述指令可以查看新连接由于队列已满而引发的失败次数，这是一个累计值，如果数值在持续增加，则应该调大 SYN 半连接队列。对应的参数为：net.ipv4.tcp_max_syn_backlog。</p><p>有一种攻击方式叫：SYN 泛洪攻击（攻击者恶意构造大量的 SYN 报文发送给服务器，造成 SYN 半连接队列溢出，导致正常客户端的连接无法建立）。Linux 开启 syncookies 功能可以在不使用 SYN 队列的情况下成功建立连接，对应的参数是：net.ipv4.tcp_syncookies（0不开启，1只在半连接队列满时开启，2无条件开启）。</p><p><code>syncookie</code> 是这么做的：服务器根据当前状态计算出一个值，放在己方发出的 SYN+ACK 报文中发出，当客户端返回 ACK 报文时，取出该值验证，如果合法，就认为连接建立成功。但是注意，这种方式<code>建立的 TCP 连接，很多特性会无法使用</code>。</p><p><img src="/img/image-20260507103753944.png" alt="image-20260507103753944"></p><p>服务器在发送完 SYN+ACK 后如果得不到客户端的 ACK 回复，会进行重试，重试的次数由 net.ipv4.tcp_synack_retries 参数控制，默认是 5 次。</p><p>服务器收到 ACK 后连接建立成功，此时，内核会把连接从 SYN 半连接队列中移出，再移入 accept 队列，等待进程调用 accept 函数时把连接取出来。如果进程不能及时地调用 accept 函数，就会造成 accept 队列溢出，最终导致建立好的 TCP 连接被丢弃（丢弃是 Linux 的默认行为，如果设置 net.ipv4.tcp_abort_on_overflow 为1则会告诉客户端连接建立失败。推荐设置为0，原因是如果服务器上的进程只是短暂的繁忙造成 accept 队列满，那么当 accept 队列有空位时，再次接收到的请求报文由于含有 ACK，仍然会触发服务器端成功建立连接）。</p><p>在 Java 网络编程中，指定的 backlog 参数就是 accept 队列的长度：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">serverSocket.bind(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InetSocketAddress</span>(port), backlog);<br></code></pre></td></tr></table></figure><p>不过这个参数还受到 linux 系统级的队列长度上限控制，对应参数：net.core.somaxconn。</p><p>Linux 可以通过 <code>ss -ltn</code> 查看每个监听端口对应的 accept 队列长度：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@iZm5e8izu4t87flkkvb649Z ~]# ss -ltn<br>State       Recv-Q Send-Q                                                  Local Address:Port                                                                 Peer Address:Port<br>LISTEN      0      128                                                                 *:80                                                                              *:*<br>LISTEN      0      128                                                                 *:3344                                                                            *:*<br>LISTEN      0      128                                                                 *:22                                                                              *:*<br>LISTEN      0      128                                                                 *:9998                                                                            *:*<br>LISTEN      0      128                                                                 *:9999                                                                            *:*<br></code></pre></td></tr></table></figure><p>至于是否需要调整可以通过 <code>netstat -s | grep &quot;listen queue&quot;</code> 命令给出的统计结果，看究竟有多少个连接因为队列溢出而被丢弃。</p><p>如果持续不断地有连接因为 accept 队列溢出被丢弃，就应该调大这两个参数。</p><h3 id="TFO"><a href="#TFO" class="headerlink" title="TFO"></a>TFO</h3><p>三次握手建立连接造成的后果就是，HTTP 请求必须在一次 RTT（Round Trip Time，从客户端到服务器一个往返的时间）后才能发送，对于小请求来说，握手的时间占整个请求的比例是很可观的。</p><p>因此，Google 提出了 Tcp fast open 的概念，简称 TFO，客户端可以在<strong>后续请求</strong>的首个 SYN 报文中就携带请求，这节省了 1 个 RTT 的时间。</p><p>TFO 节省的其实是后续请求的时间，它的流程如下：</p><p><img src="/img/image-20260507110127575.png" alt="image-20260507110127575"></p><p>首次建立连接，这时走正常的三次握手，但在客户端的 SYN 报文会明确地告诉服务器它想使用 TFO 功能，这样服务器会把客户端 IP 地址用只有自己知道的密钥加密（比如 AES 加密算法），作为 Cookie 携带在返回的 SYN+ACK 报文中，客户端收到后会将 Cookie 缓存在本地。之后，如果客户端再次向服务器建立连接，就可以在第一个 SYN 报文中携带请求数据，同时还要附带缓存的 Cookie。</p><p>TFO 功能通过 net.ipv4.tcp_fastopen 参数查看是否支持，由于只有客户端和服务器同时支持时，TFO 功能才能使用，<strong>所以该参数是按比特位控制的。其中，第 1 个比特位为 1 时，表示作为客户端时支持 TFO；第 2 个比特位为 1 时，表示作为服务器时支持 TFO</strong>，所以当 tcp_fastopen 的值为 3 时（比特为 0x11）就表示完全支持 TFO 功能。</p><h2 id="拥塞控制与缓冲区"><a href="#拥塞控制与缓冲区" class="headerlink" title="拥塞控制与缓冲区"></a>拥塞控制与缓冲区</h2><p>TCP 报文结构如下图：</p><p><img src="/img/image-20250627153818521.png" alt="image-20250627153818521"></p><p>可以看到只有 2 字节即 16 位的接收窗口大小，最多标识 65KB，后来 RFC 文档定义了窗口扩大协议，linux 通过 net.ipv4.tcp_window_scaling 参数控制开启。</p><h3 id="缓冲区"><a href="#缓冲区" class="headerlink" title="缓冲区"></a>缓冲区</h3><p>每个 TCP 连接都有发送缓冲区和接收缓冲区，发送缓冲区存已发送未确认数据和待发送数据，接收缓冲区存接收但是没有被上层服务读取的数据。由于 TCP 存在 ACK 机制，因此发送出去的数据不能立刻删除，需要先缓存在内核缓冲区。</p><p>在 linux 系统上执行 <code>free</code> 指令，其中的 buff&#x2F;cache 会随着进程新建 TCP 连接和传输数据而变化。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@localhost ~]# free<br>              total        used        free      shared  buff/cache   available<br>Mem:      131829100    19243504     1255584    20380876   111330012    91410724<br>Swap:       4194300       14848     4179452<br></code></pre></td></tr></table></figure><p>这是因为 TCP 连接是由内核维护的，内核为每个连接建立的内存缓冲区，既要为网络传输服务，也要充当进程与网络间的缓冲桥梁。如果连接的内存配置过小，就无法充分使用网络带宽，TCP 传输速度就会很慢；如果连接的内存配置过大，那么服务器内存会很快用尽，新连接就无法建立成功。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 单个TCP连接的写缓冲区 分别代表 最小值,默认值,最大值,单位 Byte</span><br><span class="hljs-comment"># sysctl net.ipv4.tcp_wmem</span><br>net.ipv4.tcp_wmem = 4096        16384   4194304<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 单个TCP连接的读缓冲区 分别代表 最小值,默认值,最大值,单位 Byte</span><br><span class="hljs-comment"># sysctl net.ipv4.tcp_rmem</span><br>net.ipv4.tcp_rmem = 4096        87380   6291456<br></code></pre></td></tr></table></figure><p>发送缓冲区的调节功能是自动开启的，而接收缓冲区则需要配置 tcp_moderate_rcvbuf 为 1 来开启调节功能</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 接收缓冲区自动调解功能</span><br><span class="hljs-comment"># sysctl net.ipv4.tcp_moderate_rcvbuf</span><br>net.ipv4.tcp_moderate_rcvbuf = 1<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 其中 mem 代表当前 TCP 连接占用的页面数</span><br>[root@iZm5e8izu4t87flkkvb649Z ~]# <span class="hljs-built_in">cat</span> /proc/net/sockstat<br>sockets: used 203<br>TCP: inuse 7 orphan 0 tw 5 alloc 9 mem 1<br>UDP: inuse 2 mem 1<br>UDPLITE: inuse 0<br>RAW: inuse 0<br>FRAG: inuse 0 memory 0<br><br><span class="hljs-comment"># Linux 对 TCP接收缓冲区的调解控制 分别代表 最小值,默认值,最大值 单位是页(page)</span><br><span class="hljs-comment"># sysctl net.ipv4.tcp_mem</span><br>net.ipv4.tcp_mem = 3087300      4116401  6174600<br><br><span class="hljs-comment"># 获取页面大小 单位 Byte</span><br><span class="hljs-comment"># getconf PAGESIZE</span><br>4096<br></code></pre></td></tr></table></figure><p>tcp_mem 是 Linux 判断系统内存是否紧张的依据，当 TCP 内存小于第 1 个值时，不需要进行自动调节；在第 1 和第 2 个值之间时，内核开始调节接收缓冲区的大小；大于第 3 个值时，内核不再为 TCP 分配新内存，此时新连接是无法建立的。</p><p>在高并发服务器中，为了兼顾网速与大量的并发连接，<strong>我们应当保证缓冲区的动态调整上限达到带宽时延积，而下限保持默认的 4K 不变即可。而对于内存紧张的服务而言，调低默认值是提高并发的有效手段。</strong></p><p>同时，如果这是网络 IO 型服务器，那么，<strong>调大 tcp_mem 的上限可以让 TCP 连接使用更多的系统内存，这有利于提升并发能力。</strong>需要注意的是，tcp_wmem 和 tcp_rmem 的单位是字节，而 tcp_mem 的单位是页面大小。而且，<strong>网络编程千万不要在 socket 上直接设置 SO_SNDBUF （写缓冲区上限）或者 SO_RCVBUF（读缓冲区上限），这样会关闭缓冲区的动态调整功能。</strong></p><h3 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h3><p>在拥塞控制中，首先会进行慢开始（每收到1个 ACK 窗口就+1，相当于1个 RTT 翻一倍），慢开始指初始窗口比较小，而不是增长的慢，增长是指数级的，其实很快。这就涉及到初始拥塞窗口的大小：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看当前连接的初始拥塞窗口</span><br>ss -nli | fgrep cwnd<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 修改初始拥塞窗口 10 MSS,如果网络特别好可以继续加大,有些高速 CDN 站点，甚至把初始拥塞窗口提升到 70 个 MSS</span><br>ip route | <span class="hljs-keyword">while</span> <span class="hljs-built_in">read</span> r; <span class="hljs-keyword">do</span><br>           ip route change <span class="hljs-variable">$r</span> initcwnd 10;<br>       <span class="hljs-keyword">done</span><br></code></pre></td></tr></table></figure><p>初始拥塞窗口越大，小请求就可以更快发送完毕，也会更快的结束慢开始，结束慢开始的情况一般有以下三类：</p><ul><li>定时器超时，触发重传</li><li>拥塞窗口的增长到达了慢启动阈值 ssthresh（全称为 slow start threshold）</li><li>接收到重复的 ACK 报文，可能存在丢包</li></ul><p>在第一种情况下，说明网络拥塞已经比较严重了，不同的算法会有不同的调整，目前主流的调整算法 CUBIC 算法会把拥塞窗口降为原先的 0.8 倍。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 查看当前系统支持的拥塞调整算法</span><br><span class="hljs-comment"># sysctl net.ipv4.tcp_available_congestion_control</span><br>net.ipv4.tcp_available_congestion_control = reno cubic bbr<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 配置具体的拥塞调整算法</span><br>net.ipv4.tcp_congestion_control = cubic<br></code></pre></td></tr></table></figure><p>这种算法在内网中效率高于 BBR，因为内网本来就时延低，但在高 RT 场景下表现不如 BBR，<strong>BBR 基于测量实现拥塞窗口的动态调整，在丢包率较高的网络中应用效果尤其好</strong>，Linux 4.9 版本之后都支持 BBR 算法，同样使用 tcp_congestion_control 进行配置。</p><p>在第二种情况下，已经到了慢启动阈值，可能接下来就会出现拥塞，因此拥塞窗口不再指数级增长，而是线性增长，也叫做拥塞避免阶段。</p><p>在第三种情况下，对方连续发送重复 ACK（例如3个） 说明网络情况尚可，可能由于中间设备等原因造成某包丢失或失序，不应认为网络出现严重拥塞，因此触发快速重传，略微降低发送速度，这里有个问题其实是，比如报文 6 7 8 一起发送，对方发了 3 个连续的 6 的 ACK，触发报文 6 的重传时，是否要发 7 和 8，如果发可能对方已经收到了只是 6 一直没到，就浪费带宽了，如果不发也可能网络不好丢失了，待会还要重发，<a href="https://zhuanlan.zhihu.com/p/681163827">SACK （Selective Acknowledgment）</a>选择性确认机制解决了这个问题，接收方通过 TCP 头部选项字段精准反馈已接收的非连续数据段信息，使发送方仅重传真正丢失的报文段。</p><h2 id="四次挥手"><a href="#四次挥手" class="headerlink" title="四次挥手"></a>四次挥手</h2><p>关闭连接的主动方一般是服务器，而服务器要服务成千上万的客户端，因此其行为必须慎重。</p><h3 id="孤儿连接"><a href="#孤儿连接" class="headerlink" title="孤儿连接"></a>孤儿连接</h3><p>关闭连接一般有 close 和 shutdown 两种方法，简单来说：close 会销毁 socket，释放文件描述符；shutdown 可以选择性关闭读、写，保留部分内容。</p><p>主动调用 close 一方的进程，调用之后，相关句柄已经释放，此连接已经和进程无关，由内核和另一方进行四次挥手交互，此时这个连接叫做孤儿(orphan)连接，或者叫做孤儿 socket。</p><p>可以使用如下指令查看孤儿进程的数量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@iZm5e8izu4t87flkkvb649Z ~]# <span class="hljs-built_in">cat</span> /proc/net/sockstat<br>sockets: used 205<br>TCP: inuse 8 orphan 0 tw 3 alloc 10 mem 1<br>UDP: inuse 2 mem 1<br>UDPLITE: inuse 0<br>RAW: inuse 0<br>FRAG: inuse 0 memory 0<br></code></pre></td></tr></table></figure><p>如果使用 shutdown 函数关闭连接，即使主动方进入到 FIN_WAIT1 或 FIN_WAIT2 状态，这个时候进程仍然可以收到被动方发送过来的数据，这个时候不能称为孤儿进程。</p><p>先来看四次挥手的流程：</p><p><img src="/img/image-20260507111541997.png" alt="image-20260507111541997"></p><ol><li>主动方关闭连接时，会发送 FIN 报文，此时主动方的连接状态由 ESTABLISHED 变为 FIN_WAIT1。</li><li>被动方收到 FIN 报文后，内核自动回复 ACK 报文，连接状态由 ESTABLISHED 变为 CLOSE_WAIT，意为等待上层的进程调用 close 关闭连接。</li><li>主动方收到关于发送的 FIN 报文的 ACK，进入 FIN_WAIT2，发送通道就关闭了。</li><li>被动方进入 CLOSE_WAIT 状态时，进程的 read 函数会返回 0。开发人员在程序中就可以针对性地调用 close 函数，进而触发内核发送 FIN 报文，此时被动方连接的状态变为 LAST_ACK。</li><li>当主动方收到被动方 FIN 报文时，内核会自动回复 ACK，同时连接的状态由 FIN_WAIT2 变为 TIME_WAIT，Linux 系统下大约 1 分钟后 TIME_WAIT 状态的连接才会彻底关闭。</li><li>而被动方收到 ACK 报文后，连接就会关闭。</li></ol><h3 id="主动方优化"><a href="#主动方优化" class="headerlink" title="主动方优化"></a>主动方优化</h3><p>主动方发送 FIN 报文后，连接就处于 FIN_WAIT1 状态下，该状态通常应在数十毫秒内转为 FIN_WAIT2。只有迟迟收不到对方返回的 ACK 时，才能用 netstat 命令观察到 FIN_WAIT1 状态。此时，内核会定时重发 FIN 报文，其中重发次数由 <code>net.ipv4.tcp_orphan_retries</code> 参数控制（注意，orphan 虽然是孤儿的意思，该参数却不只对孤儿连接有效，事实上，它对所有 FIN_WAIT1 状态下的连接都有效），<code>默认值是 0，特指 8 次</code>。如果 FIN_WAIT1 状态连接有很多，可以考虑降低 tcp_orphan_retries 的值。当重试次数达到 tcp_orphan_retries 时，连接就会直接关闭掉。</p><p>TCP 有流控功能，当接收方将接收窗口设为 0 时，发送方就不能再发送数据。所以，当攻击者下载大文件时，就可以通过将接收窗口设为 0，导致 FIN 报文无法发送，进而导致连接一直处于 FIN_WAIT1 状态。解决这种问题的方案是调整 net.ipv4.tcp_max_orphans 参数，该参数定义了孤儿连接的最大数量，如果孤儿连接数量大于它，新增的孤儿连接将不再走四次挥手，而是直接发送 RST 复位报文强制关闭。</p><p>当主动方收到 ACK 进入 FIN_WAIT2 状态后，就表示主动方的发送通道已经关闭，接下来将等待对方发送 FIN 报文，关闭对方的发送通道。这时，如果连接是用 shutdown 函数关闭的，连接可以一直处于 FIN_WAIT2 状态。但对于 close 函数关闭的孤儿连接，这个状态不可以持续太久，而 net.ipv4.tcp_fin_timeout 控制了这个状态下连接的持续时长，默认值是 60 秒，如果 60 秒还未收到对方的 FIN 报文，连接会直接关闭。</p><p>TIME_WAIT 状态的持续时间也是 60 秒，这个数字是怎么定义的呢？在 Linux 系统中，MSL （Maximum Segment Lifetime，一个报文在网络中的最长生存时间）的值固定为 30 秒，所以 60 秒就是 2MSL。2MSL 的时长相当于允许报文有一次丢失，试想如果没有 TIME_WAIT 状态，主动方直接关闭连接并回复 ACK，但这个 ACK 在网络中丢失，被动方重传 FIN，此时主动方的一个新连接占用了这个端口，FIN 报文会导致这个新连接出现异常状态。</p><p>Linux 提供了 net.ipv4.tcp_max_tw_buckets 限制 TIME_WAIT 连接的最大个数，当 TIME_WAIT 连接数达到该值时，会不再经历 TIME_WAIT 状态直接关闭，不过这个值也不是越大越好，毕竟内存和端口号都是有限的。如果主动方也会充当客户端（例如 Nginx 服务器会作为客户端连接上游服务），那么可以设置 net.ipv4.tcp_tw_reuse 参数<strong>允许作为客户端的新连接，在安全条件下使用 TIME_WAIT 状态下的端口</strong>，这个值需要配合 net.ipv4.tcp_timestamps 使用（tcp_timestamps 双方都要开启）。</p><p>如果连接双方同时关闭连接，都认为自己是主动方，所以都进入了 FIN_WAIT1 状态，FIN 报文的重发次数仍由 tcp_orphan_retries 参数控制。</p><p><img src="/img/image-20260507133507512.png" alt="image-20260507133507512"></p><p>双方在等待 ACK 报文的过程中，都等来了 FIN 报文。这是一种新情况，所以连接会进入一种叫做 CLOSING 的新状态，它替代了 FIN_WAIT2 状态。这是一种特殊情况，不用在意。</p><h3 id="被动方优化"><a href="#被动方优化" class="headerlink" title="被动方优化"></a>被动方优化</h3><p>当被动方收到 FIN 报文时，就开启了被动方的四次挥手流程。内核自动回复 ACK 报文后，连接就进入 CLOSE_WAIT 状态，顾名思义，它表示等待进程调用 close 函数关闭连接。</p><p>进程调用 close 函数后，内核就会发出 FIN 报文关闭发送通道，同时连接进入 LAST_ACK 状态，等待主动方返回 ACK 来确认连接关闭。如果迟迟等不到 ACK，内核就会重发 FIN 报文，重发次数仍然由 net.ipv4.tcp_orphan_retries 参数控制。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>文中提到了诸多的 TCP 参数，整理如下：</p><table><thead><tr><th align="left">参数</th><th align="left">描述</th><th align="left">默认值 &#x2F; 推荐值</th></tr></thead><tbody><tr><td align="left"><code>net.ipv4.tcp_syn_retries</code></td><td align="left">客户端 SYN 报文重试次数</td><td align="left">6（约 2 分钟）</td></tr><tr><td align="left"><code>net.ipv4.tcp_max_syn_backlog</code></td><td align="left">SYN 半连接队列最大长度</td><td align="left">根据系统内存调整</td></tr><tr><td align="left"><code>net.ipv4.tcp_syncookies</code></td><td align="left">SYN 泛洪防护（0: 关闭, 1: 半连接队列满时开启, 2: 无条件开启）</td><td align="left">1（推荐）</td></tr><tr><td align="left"><code>net.ipv4.tcp_synack_retries</code></td><td align="left">服务端 SYN+ACK 报文重试次数</td><td align="left">5</td></tr><tr><td align="left"><code>net.ipv4.tcp_abort_on_overflow</code></td><td align="left">accept 队列溢出时是否发送 RST（0: 丢弃连接, 1: 通知客户端失败）</td><td align="left">0（推荐）</td></tr><tr><td align="left"><code>net.core.somaxconn</code></td><td align="left">系统级 accept 队列长度上限</td><td align="left">128（可调大）</td></tr><tr><td align="left"><code>net.ipv4.tcp_fastopen</code></td><td align="left">TFO 支持（比特位：1&#x3D;客户端，2&#x3D;服务端）</td><td align="left">根据系统情况</td></tr><tr><td align="left"><code>net.ipv4.tcp_window_scaling</code></td><td align="left">窗口扩大协议（0: 关闭, 1: 开启）</td><td align="left">1（开启）</td></tr><tr><td align="left"><code>net.ipv4.tcp_wmem</code></td><td align="left">写缓冲区（最小值, 默认值, 最大值，单位 Byte）</td><td align="left">根据系统情况</td></tr><tr><td align="left"><code>net.ipv4.tcp_rmem</code></td><td align="left">读缓冲区（最小值, 默认值, 最大值，单位 Byte）</td><td align="left">根据系统情况</td></tr><tr><td align="left"><code>net.ipv4.tcp_moderate_rcvbuf</code></td><td align="left">接收缓冲区自动调节（0: 关闭, 1: 开启）</td><td align="left">1（开启）</td></tr><tr><td align="left"><code>net.ipv4.tcp_mem</code></td><td align="left">TCP 内存页面数（最小值, 压力值, 最大值，单位 page）</td><td align="left">根据系统内存动态设置</td></tr><tr><td align="left"><code>net.ipv4.tcp_available_congestion_control</code></td><td align="left">可用的拥塞控制算法</td><td align="left">cubic</td></tr><tr><td align="left"><code>net.ipv4.tcp_congestion_control</code></td><td align="left">当前使用的拥塞控制算法</td><td align="left"><code>cubic</code></td></tr><tr><td align="left"><code>net.ipv4.tcp_orphan_retries</code></td><td align="left">FIN &#x2F; LAST_ACK 重试次数（0 特指 8 次）</td><td align="left">0（8 次）</td></tr><tr><td align="left"><code>net.ipv4.tcp_max_orphans</code></td><td align="left">孤儿连接最大数量</td><td align="left">根据系统内存设置</td></tr><tr><td align="left"><code>net.ipv4.tcp_fin_timeout</code></td><td align="left">FIN_WAIT2 状态持续时间（秒）</td><td align="left">60</td></tr><tr><td align="left"><code>net.ipv4.tcp_max_tw_buckets</code></td><td align="left">TIME_WAIT 连接最大数量</td><td align="left">可调，过大占用内存&#x2F;端口</td></tr><tr><td align="left"><code>net.ipv4.tcp_tw_reuse</code></td><td align="left">作为客户端时允许重用 TIME_WAIT 端口</td><td align="left">需配合 <code>tcp_timestamps</code> 开启</td></tr><tr><td align="left"><code>net.ipv4.tcp_timestamps</code></td><td align="left">TCP 时间戳选项（0: 关闭, 1: 开启）</td><td align="left">1（开启，双方均需开启）</td></tr></tbody></table><p>除此之外，TCP 还有很多的参数，诸如与 keepalive 相关的内容等，TCP 协议的优化方案还是比较复杂的，这也是享受 TCP 优势时我们必须要付出的代价。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ol><li><a href="https://time.geekbang.org/column/intro/100051201">系统性能调优必知必会</a></li><li><a href="https://zhuanlan.zhihu.com/p/436979513">TCP参数参数调优全面解读</a></li><li><a href="https://www.cnblogs.com/lshs/p/6038735.html">慢启动与拥塞避免概述</a></li><li><a href="https://raybyte.cn/post/2025/7/23/4bf7c3b1">BBR、CUBIC 与 TCP Reno 的性能对比与配置</a></li><li><a href="https://www.baeldung-cn.com/cs/sockets-close-vs-shutdown">Sockets：Close 与 Shutdown 的区别</a></li><li><a href="https://zhuanlan.zhihu.com/p/681163827">Wireshark 网络包分析实战二：SACK</a></li></ol>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>性能优化</tag>
      
      <tag>TCP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty的Mpsc队列如何实现</title>
    <link href="/2026/05/06/Netty%E7%9A%84Mpsc%E9%98%9F%E5%88%97%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0/"/>
    <url>/2026/05/06/Netty%E7%9A%84Mpsc%E9%98%9F%E5%88%97%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty的Mpsc队列如何实现"><a href="#Netty的Mpsc队列如何实现" class="headerlink" title="Netty的Mpsc队列如何实现"></a>Netty的Mpsc队列如何实现</h1><p>队列是一种 FIFO（先进先出）的数据结构，JDK 中定义了 java.util.Queue 的队列接口，与 List、Set 接口类似，java.util.Queue 也继承于 Collection 集合接口。此外，JDK 还提供了一种双端队列接口 java.util.Deque，我们最常用的 LinkedList 就是实现了 Deque 接口。JDK 提供了一些并发队列如：ArrayBlockingQueue、ConcurrentLinkedQueue，但是在大规模流量的高并发系统中，如果对性能要求严苛，JDK 的并发队列性能并不够出色。</p><p>高并发情况下使用的数据结构通常希望是非阻塞的，Netty 采用了开源项目 <a href="https://github.com/JCTools/JCTools">JCTools</a> 的并发数据结构，其中非阻塞队列可以分为四种类型，可以根据不同的场景选择使用。</p><ul><li>Spsc 单生产者单消费者；</li><li>Mpsc 多生产者单消费者；</li><li>Spmc 单生产者多消费者；</li><li>Mpmc 多生产者多消费者。</li></ul><h2 id="伪共享"><a href="#伪共享" class="headerlink" title="伪共享"></a>伪共享</h2><p>在 <a href="https://zhuwenjie0716.github.io/2026/04/23/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BCPU%E7%AF%87/">性能优化之CPU篇</a> 中，有介绍多核 CPU 的结构。程序在执行时，会先将内存中的数据载入三级缓存，再进入每颗核心独有的二级缓存，最后进入最快的一级缓存，之后才会被 CPU 使用。</p><p>伪共享的场景就与CPU的多核架构有关：</p><p><img src="/img/image-20260506113245673.png" alt="image-20260506113245673"></p><p>假设变量 A、B、C、D 被加载到同一个 Cache Line，它们会被高频地修改。当线程 1 在 CPU Core1 中中对变量 A 进行修改，修改完成后 CPU Core1 会通知其他 CPU Core 该缓存行已经失效。然后线程 2 在 CPU Core2 中对变量 C 进行修改时，发现 Cache line 已经失效，此时 CPU Core1 会将数据重新写回内存，CPU Core2 再从内存中读取数据加载到当前 Cache line 中。</p><p>所以同一个 Cache line 被越多的线程修改，那么造成的写竞争就会越激烈，数据会频繁写入内存，导致性能浪费。</p><p><strong>解决伪共享问题的方式就是让变量处于不同的 cache line 上</strong>。假如这是个 long 变量占 8B，cache line 大小通常是 64B，在这个变量的前后各填充 7 个 long 类型的变量，就可以让不同线程共享的对象加载到不同的缓存行。</p><p>Mpsc Queue 为了解决伪共享问题填充了大量的 long 类型变量，这也导致源码不易阅读。伪共享问题一般是非常隐蔽的，在实际开发的过程中，<strong>并不是项目中所有地方都需要花费大量的精力去优化伪共享问题，因为 CPU Cache 的填充本身也是比较珍贵的</strong>。</p><h2 id="入队与出队"><a href="#入队与出队" class="headerlink" title="入队与出队"></a>入队与出队</h2><p>JCTools 提供了 MpscArrayQuene、MpscUnboundedArrayQueue、MpscChunkedArrayQueue 多个具有特色功能的队列，这里分析比较简单的 MpscArrayQuene。</p><p>源代码中 MpscArrayQuene 连续继承了很多类，所以这些核心属性也分布在很多类中，整理如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// ConcurrentCircularArrayQueue</span><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> mask; <span class="hljs-comment">// 计算数组下标的掩码</span><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> E[] buffer; <span class="hljs-comment">// 存放队列数据的数组</span><br><br><span class="hljs-comment">// MpmcArrayQueueProducerIndexField</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">long</span> producerIndex; <span class="hljs-comment">// 生产者的索引</span><br><br><span class="hljs-comment">// MpscArrayQueueProducerLimitField</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">long</span> producerLimit; <span class="hljs-comment">// 生产者索引的最大值</span><br><br><span class="hljs-comment">// MpscArrayQueueConsumerIndexField</span><br><span class="hljs-keyword">protected</span> <span class="hljs-type">long</span> consumerIndex; <span class="hljs-comment">// 消费者索引</span><br></code></pre></td></tr></table></figure><p>读过  redis 或者一些哈希表源码的人一眼就觉得 mask 就是数组的长度 - 1，原因是在哈希表获取元素所在位置时，取模运算需要重复计算，而 &amp; 位运算只需要一次， (% 2^n) 和 (&amp; 2^n -1) 结果是一样的。</p><p>producerIndex、producerLimit 都是用 volatile 进行修饰的，因为一个生产者线程的修改需要对其他生产者线程可见。</p><p>队列的核心操作就是入队和出队。</p><h3 id="offer"><a href="#offer" class="headerlink" title="offer"></a>offer</h3><p>offer(E e) 的作用是将元素加入队列，满了则返回 false，成功则返回 true。</p><p>这里有几个重要变量：</p><ul><li>mask 数组长度-1</li><li>producerLimit 缓存的生产者索引上限（如果不缓存，每次都要读取 consumerIndex 来判断队列是否已满，而 consumerIndex 需要 volatile 去读，频繁跨缓存行读取会降低性能）</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">offer</span><span class="hljs-params">(<span class="hljs-keyword">final</span> E e)</span> &#123;<br>    <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == e) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NullPointerException</span>();<br>    &#125;<br><br>    <span class="hljs-comment">// use a cached view on consumer index (potentially updated in loop)</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">mask</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.mask;<br>    <span class="hljs-comment">// 获取生产者索引最大限制</span><br>    <span class="hljs-type">long</span> <span class="hljs-variable">producerLimit</span> <span class="hljs-operator">=</span> lvProducerLimit(); <span class="hljs-comment">// LoadLoad</span><br>    <span class="hljs-type">long</span> pIndex;<br>    <span class="hljs-keyword">do</span> &#123;<br>        <span class="hljs-comment">// 获取生产者索引</span><br>        pIndex = lvProducerIndex(); <span class="hljs-comment">// LoadLoad</span><br>        <span class="hljs-keyword">if</span> (pIndex &gt;= producerLimit) &#123;<br>            <span class="hljs-comment">// 获取消费者索引</span><br>            <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">cIndex</span> <span class="hljs-operator">=</span> lvConsumerIndex(); <span class="hljs-comment">// LoadLoad</span><br>            producerLimit = cIndex + mask + <span class="hljs-number">1</span>;<br><br>            <span class="hljs-keyword">if</span> (pIndex &gt;= producerLimit) &#123;<br>                <span class="hljs-comment">// 队列真的满了</span><br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>; <span class="hljs-comment">// FULL :(</span><br>            &#125;<br>            <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-comment">// update producer limit to the next index that we must recheck the consumer index</span><br>                <span class="hljs-comment">// this is racy, but the race is benign</span><br>                <span class="hljs-comment">// 更新 producerLimit</span><br>                soProducerLimit(producerLimit);<br>            &#125;<br>        &#125;<br>    &#125; <span class="hljs-keyword">while</span> (!casProducerIndex(pIndex, pIndex + <span class="hljs-number">1</span>));<span class="hljs-comment">// cas 抢夺 pIndex+1 的位置</span><br><br>    <span class="hljs-comment">// Won CAS, move on to storing</span><br>    <span class="hljs-comment">// 计算生产者索引在数组中下标</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">offset</span> <span class="hljs-operator">=</span> calcElementOffset(pIndex, mask);<br>    <span class="hljs-comment">// 向数组中放入数据</span><br>    soElement(buffer, offset, e); <span class="hljs-comment">// StoreStore</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>; <span class="hljs-comment">// AWESOME :)</span><br>&#125;<br></code></pre></td></tr></table></figure><p>在 do-while 循环内的逻辑，需要两次 if(pIndex &gt;&#x3D; producerLimit) 判断，因为当生产者索引大于 producerLimit 阈值时，可能存在两种情况：producerLimit 缓存值过期了或者队列已经满了</p><ul><li>读取最新的消费者索引 consumerIndex，之前读取过的数据位置都可以被重复使用，重新做一次 producerLimit 计算；</li><li>然后再做一次 if(pIndex &gt;&#x3D; producerLimit) 判断，如果生产者索引还是大于 producerLimit 阈值，说明队列的真的满了。</li></ul><p>在最后向数组指定位置放数据时的 soElement 的实现如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;E&gt; <span class="hljs-keyword">void</span> <span class="hljs-title function_">soElement</span><span class="hljs-params">(E[] buffer, <span class="hljs-type">long</span> offset, E e)</span> &#123;<br>    UNSAFE.putOrderedObject(buffer, offset, e);<br>&#125;<br></code></pre></td></tr></table></figure><p>putOrderedObject() 和 putObject() 都可以用于更新对象的值，但是 putOrderedObject() 并不会立刻将数据更新到内存中，并把其他 Cache Line 置为失效。putOrderedObject() 使用的是 LazySet 延迟更新机制，所以性能方面 putOrderedObject() 要比 putObject() 高很多。</p><p>其实这是一个环形数据，长度是2的幂次方，<code>calcElementOffset(pIndex, mask)</code>就是计算本次该写在数组的哪个位置。</p><h3 id="poll"><a href="#poll" class="headerlink" title="poll"></a>poll</h3><p>poll() 方法的作用是移除队列的首个元素并返回，如果队列为空则返回 NULL。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> E <span class="hljs-title function_">poll</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">cIndex</span> <span class="hljs-operator">=</span> lpConsumerIndex();<span class="hljs-comment">// 直接返回消费者索引 consumerIndex</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">long</span> <span class="hljs-variable">offset</span> <span class="hljs-operator">=</span> calcElementOffset(cIndex);<span class="hljs-comment">// 计算消费者索引在环形数组中的位置</span><br>    <span class="hljs-comment">// Copy field to avoid re-reading after volatile load</span><br>    <span class="hljs-keyword">final</span> E[] buffer = <span class="hljs-built_in">this</span>.buffer;<br><br>    <span class="hljs-comment">// If we can&#x27;t see the next available element we can&#x27;t poll</span><br>    <span class="hljs-comment">// 获得要消费的元素</span><br>    <span class="hljs-type">E</span> <span class="hljs-variable">e</span> <span class="hljs-operator">=</span> lvElement(buffer, offset); <span class="hljs-comment">// LoadLoad</span><br>    <span class="hljs-keyword">if</span> (<span class="hljs-literal">null</span> == e) &#123;<br>        <span class="hljs-keyword">if</span> (cIndex != lvProducerIndex()) &#123;<span class="hljs-comment">// 队列已满</span><br>            <span class="hljs-keyword">do</span> &#123;<br>                e = lvElement(buffer, offset);<br>            &#125; <span class="hljs-keyword">while</span> (e == <span class="hljs-literal">null</span>);<span class="hljs-comment">// 等待生产者填充元素</span><br>        &#125;<br>        <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 向数组中放入数据,其实是设置这个位置的元素为 null</span><br>    spElement(buffer, offset, <span class="hljs-literal">null</span>);<br>    soConsumerIndex(cIndex + <span class="hljs-number">1</span>); <span class="hljs-comment">// StoreStore</span><br>    <span class="hljs-keyword">return</span> e;<br>&#125;<br></code></pre></td></tr></table></figure><p>因为只有一个消费者线程，所以 poll() 的过程没有 CAS 操作。poll() 方法核心思路是获取消费者索引 consumerIndex，然后根据 consumerIndex 计算得出数组对应的偏移量，然后将数组对应位置的元素取出并返回，最后将 consumerIndex 移动到环形数组下一个位置。</p><p>lvElement 取出数组中 offset 对应的元素使用了 UNSAFE 系列方法，getObjectVolatile() 方法使用的是 LoadLoad Barrier（对于 Load1，LoadLoad，Load2 操作序列，在 Load2 以及后续读取操作之前，会保证 Load1 的读取操作执行完毕），所以 getObjectVolatile() 方法可以保证每次读取数据都可以从内存中拿到最新值。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;E&gt; E <span class="hljs-title function_">lvElement</span><span class="hljs-params">(E[] buffer, <span class="hljs-type">long</span> offset)</span> &#123;<br>    <span class="hljs-keyword">return</span> (E) UNSAFE.getObjectVolatile(buffer, offset);<br>&#125;<br></code></pre></td></tr></table></figure><p>与 offer() 不同，poll() 比较关注队列为空的情况。当调用 lvElement() 方法获取到的元素为 NULL 时，有两种可能的情况：队列为空或者生产者填充的元素还没有对消费者可见。如果消费者索引 consumerIndex 等于生产者 producerIndex，说明队列为空。只要两者不相等，消费者需要等待生产者填充数据完毕。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>MpscArrayQueue 中蕴藏着丰富的技术细节：</p><ul><li>通过大量填充 long 类型变量解决伪共享问题；</li><li>环形数组的容量设置为 2 的次幂，可以通过位运算快速定位到数组对应下标；</li><li>入队 offer() 操作中 producerLimit 的巧妙设计，大幅度减少了主动获取消费者索引 consumerIndex 的次数，性能提升显著；</li><li>入队和出队操作中都大量使用了 UNSAFE 系列方法，针对生产者和消费者的场景不同，使用的 UNSAFE 方法也是不一样的。</li></ul><p>在 Netty 的 EventLoop 模型中，多个 Channel 可能会同时触发可读事件，然后 EventLoop 将事件派发给业务线程池去执行业务操作，业务线程处理完业务逻辑后，可能并发调用EventLoop提交任务的方法，提交写数据的IO任务，这些任务会添加到 EventLoop 的任务队列中。而 EventLoop 本质上是一个单线程的执行器，它会从任务队列中取出任务并执行。</p><p>这个场景完美契合 Mpsc（Multi Producer Single Consumer）模型。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Netty</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty如何实现轻量级对象池</title>
    <link href="/2026/05/06/Netty%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E8%BD%BB%E9%87%8F%E7%BA%A7%E5%AF%B9%E8%B1%A1%E6%B1%A0/"/>
    <url>/2026/05/06/Netty%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E8%BD%BB%E9%87%8F%E7%BA%A7%E5%AF%B9%E8%B1%A1%E6%B1%A0/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty如何实现轻量级对象池"><a href="#Netty如何实现轻量级对象池" class="headerlink" title="Netty如何实现轻量级对象池"></a>Netty如何实现轻量级对象池</h1><p>对于 Netty 这种高吞吐、低延迟的网络框架中，对象（尤其是 ByteBuf）的创建和释放极为频繁。如果每次都通过 new 来创建对象，容易产生以下问题：</p><ul><li><strong>GC 频繁</strong>：大量短生命周期对象会频繁触发 Young GC，影响系统稳定。</li><li><strong>延迟抖动</strong>：GC 的 Stop-The-World 事件会导致请求延迟不稳定。</li><li><strong>内存碎片</strong>：频繁的分配和释放会导致堆内存碎片化。</li></ul><p>Netty 实现了轻量级的对象池 Recycler，专门用来重用那些创建开销大、使用频繁的对象，从而显著降低 GC 压力，提升系统性能。它被广泛应用于 Netty 的核心组件中，例如 PooledDirectByteBuf。</p><h2 id="Recycler-结构"><a href="#Recycler-结构" class="headerlink" title="Recycler 结构"></a>Recycler 结构</h2><p>Recycler 还是比较复杂的，核心结构如图：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java">【线程 A (比如 Netty 的 EventLoop)】<br>    │<br>    └─── Stack (线程A的私有栈，LIFO)<br>            │<br>            ├─── [<span class="hljs-number">0</span>] → Handle (对象X，空闲)  <span class="hljs-comment">// A自己用完放回的</span><br>            ├─── [<span class="hljs-number">1</span>] → Handle (对象Y，空闲)<br>            ├─── [<span class="hljs-number">2</span>] → Handle (对象Z，使用中) <span class="hljs-comment">// A正在用的</span><br>            ├─── ...<br>            │<br>            └─── head (指针) ----------------------┐<br>                                                     │<br>        【WeakOrderQueue 链表 (接收其他线程的归还)】   │<br>                    │                               │<br>                    ▼                               │<br>             WeakOrderQueue (<span class="hljs-keyword">for</span> 线程B) ◄────────────┘<br>                    │<br>                    └─── Link1 → [Handle(对象M), Handle(对象N), ...]<br>                    └─── Link2 → [Handle(对象P), ...]<br>                    │<br>                    ▼<br>             WeakOrderQueue (<span class="hljs-keyword">for</span> 线程C)<br>                    │<br>                    └─── Link1 → [Handle(对象Q), ...]<br>                    │<br>                    ▼<br>             WeakOrderQueue (<span class="hljs-keyword">for</span> 线程D) ...<br></code></pre></td></tr></table></figure><p>上图的核心组件包括：Stack 和 WeakOrderQueue。</p><p>当程序在调用 Recycler.get() 方法时，代码会首先尝试获取当前线程的 Stack，如果不存在就会创建，Stack 中存的是当前线程创建的已经空闲的对象。</p><p>Stack 中会有 head 指针指向一个 WeakOrderQueue 的链表，这里存其它线程归还给该线程的对象。例如线程A创建了一个对象，使用完后传递给了线程B处理，线程B使用完后进行归还，最终就会放到线程A 的某个 WeakOrderQueue 中（其中都是线程B归还的属于线程A创建的对象）。</p><p>注意对象会被封装为 DefaultHandle。</p><h2 id="Recycler-获取对象"><a href="#Recycler-获取对象" class="headerlink" title="Recycler 获取对象"></a>Recycler 获取对象</h2><p>Stack 的核心定义如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Stack</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">final</span> Recycler&lt;T&gt; parent; <span class="hljs-comment">// 所属的 Recycler</span><br>    <span class="hljs-keyword">final</span> WeakReference&lt;Thread&gt; threadRef; <span class="hljs-comment">// 所属线程的弱引用</span><br>    <span class="hljs-keyword">final</span> AtomicInteger availableSharedCapacity; <span class="hljs-comment">// 异线程回收对象时，其他线程能保存的被回收对象的最大个数</span><br>    <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> maxDelayedQueues; <span class="hljs-comment">// WeakOrderQueue最大个数</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> maxCapacity; <span class="hljs-comment">// 对象池的最大大小，默认最大为 4k</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> ratioMask; <span class="hljs-comment">// 控制对象的回收比率，默认只回收 1/8 的对象</span><br>    <span class="hljs-keyword">private</span> DefaultHandle&lt;?&gt;[] elements; <span class="hljs-comment">// 存储缓存数据的数组</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> size; <span class="hljs-comment">// 缓存的 DefaultHandle 对象个数</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">handleRecycleCount</span> <span class="hljs-operator">=</span> -<span class="hljs-number">1</span>; <br>    <span class="hljs-comment">// WeakOrderQueue 链表的三个重要节点</span><br>    <span class="hljs-keyword">private</span> WeakOrderQueue cursor, prev;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> WeakOrderQueue head;<br>    <span class="hljs-comment">// 省略其他代码</span><br>&#125;<br></code></pre></td></tr></table></figure><p>获取对象的大致逻辑是：优先寻找线程私有的 Stack，再找其他线程归还给该线程的 WeakOrderQuene，如果都没有这样的对象，就新建一个。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> T <span class="hljs-title function_">get</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">if</span> (maxCapacityPerThread == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">return</span> newObject((Handle&lt;T&gt;) NOOP_HANDLE);<br>    &#125;<br>    Stack&lt;T&gt; stack = threadLocal.get(); <span class="hljs-comment">// 获取当前线程缓存的 Stack</span><br>    DefaultHandle&lt;T&gt; handle = stack.pop(); <span class="hljs-comment">// 从 Stack 中弹出一个 DefaultHandle 对象</span><br>    <span class="hljs-keyword">if</span> (handle == <span class="hljs-literal">null</span>) &#123;<br>        handle = stack.newHandle();<br>        handle.value = newObject(handle); <span class="hljs-comment">// 创建的对象并保存到 DefaultHandle</span><br>    &#125;<br>    <span class="hljs-keyword">return</span> (T) handle.value;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到对象是通过 <code>stack.pop()</code> 找到的，但该对象并不一定是本来就在 stack 中的，看下 pop 的源码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs java">DefaultHandle&lt;T&gt; <span class="hljs-title function_">pop</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">size</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.size;<br>    <span class="hljs-keyword">if</span> (size == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-comment">// 就尝试从其他线程回收的对象中转移一些到 elements 数组当中</span><br>        <span class="hljs-keyword">if</span> (!scavenge()) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>        &#125;<br>        size = <span class="hljs-built_in">this</span>.size;<br>    &#125;<br>    size --;<br>    <span class="hljs-type">DefaultHandle</span> <span class="hljs-variable">ret</span> <span class="hljs-operator">=</span> elements[size]; <span class="hljs-comment">// 将实例从栈顶弹出</span><br>    elements[size] = <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">if</span> (ret.lastRecycledId != ret.recycleId) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;recycled multiple times&quot;</span>);<br>    &#125;<br>    ret.recycleId = <span class="hljs-number">0</span>;<br>    ret.lastRecycledId = <span class="hljs-number">0</span>;<br>    <span class="hljs-built_in">this</span>.size = size;<br>    <span class="hljs-keyword">return</span> ret;<br>&#125;<br></code></pre></td></tr></table></figure><p>如果 Stack 的 elements 数组中有可用的对象实例，直接将对象实例弹出；如果 elements 数组中没有可用的对象实例，会调用 scavenge 方法。scavenge 翻译为拾荒，作用是从其他线程回收的对象实例中转移一些到 elements 数组中。</p><p>scavenge 方法会从 stack 对象 cursor 指针指向的 WeakOrderQueue 开始遍历，cursor 会保留上次遍历的位置，这样的好处是不会每次都优先遍历前面位置的 WeakOrderQueue，分摊跨线程回收对象的压力。</p><p><img src="/img/image-20260506081504533.png" alt="image-20260506081504533"></p><p>此外，每次移动 cursor 时，都会检查 WeakOrderQueue 对应的线程是否已经退出了，如果线程已经退出，那么线程中的对象实例都会被回收，然后将 WeakOrderQueue 节点从链表中移除。</p><p>每个 WeakOrderQueue 中都包含一个 Link 链表，Netty 每次会回收其中的一个 Link 节点所存储的对象。从图中可以看出，Link 内部会包含一个读指针 readIndex，每个 Link 节点默认存储 16 个对象，读指针到链表尾部就是可以用于回收的对象实例，每次回收对象时，readIndex 都会从上一次记录的位置开始回收。</p><h2 id="Recycler-对象回收"><a href="#Recycler-对象回收" class="headerlink" title="Recycler 对象回收"></a>Recycler 对象回收</h2><p>Recycler 对象回收比获取对象要简单一些。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// DefaultHandle#recycle</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">recycle</span><span class="hljs-params">(Object object)</span> &#123;<br>    <span class="hljs-keyword">if</span> (object != value) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<span class="hljs-string">&quot;object does not belong to handle&quot;</span>);<br>    &#125;<br>    Stack&lt;?&gt; stack = <span class="hljs-built_in">this</span>.stack;<br>    <span class="hljs-keyword">if</span> (lastRecycledId != recycleId || stack == <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;recycled already&quot;</span>);<br>    &#125;<br>    stack.push(<span class="hljs-built_in">this</span>);<br>&#125;<br><br><span class="hljs-comment">// Stack#push</span><br><span class="hljs-keyword">void</span> <span class="hljs-title function_">push</span><span class="hljs-params">(DefaultHandle&lt;?&gt; item)</span> &#123;<br>    <span class="hljs-type">Thread</span> <span class="hljs-variable">currentThread</span> <span class="hljs-operator">=</span> Thread.currentThread();<br>    <span class="hljs-keyword">if</span> (threadRef.get() == currentThread) &#123;<br>        pushNow(item);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        pushLater(item, currentThread);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到会根据当前线程是否是该创建该对象的线程，走 pushNow 和 pushLater 的逻辑。</p><h3 id="同线程回收"><a href="#同线程回收" class="headerlink" title="同线程回收"></a>同线程回收</h3><p>同线程回收对象的逻辑非常简单，就是直接向 Stack 的 elements 数组中添加数据，对象会被存放在栈顶指针指向的位置。如果超过了 Stack 的最大容量，那么对象会被直接丢弃，同样这里使用了 dropHandle 方法控制对象的回收速率，每 8 个对象会有一个被回收到 Stack 中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">pushNow</span><span class="hljs-params">(DefaultHandle&lt;?&gt; item)</span> &#123;<br>    <span class="hljs-keyword">if</span> ((item.recycleId | item.lastRecycledId) != <span class="hljs-number">0</span>) &#123; <span class="hljs-comment">// 防止被多次回收</span><br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;recycled already&quot;</span>);<br>    &#125;<br>    item.recycleId = item.lastRecycledId = OWN_THREAD_ID;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">size</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.size;<br>    <span class="hljs-comment">// 1. 超出最大容量 2. 控制回收速率</span><br>    <span class="hljs-keyword">if</span> (size &gt;= maxCapacity || dropHandle(item)) &#123;<br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (size == elements.length) &#123;<br>        elements = Arrays.copyOf(elements, min(size &lt;&lt; <span class="hljs-number">1</span>, maxCapacity));<br>    &#125;<br>    elements[size] = item;<br>    <span class="hljs-built_in">this</span>.size = size + <span class="hljs-number">1</span>;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="异线程回收"><a href="#异线程回收" class="headerlink" title="异线程回收"></a>异线程回收</h3><p>异线程回收对象时，异线程会在自己的本地看是否有归还给原线程的 WeakOrderQueue，如果没有则新建，然后把要归还的对象添加到这个 WeakOrderQueue 中。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">pushLater</span><span class="hljs-params">(DefaultHandle&lt;?&gt; item, Thread thread)</span> &#123;<br>    Map&lt;Stack&lt;?&gt;, WeakOrderQueue&gt; delayedRecycled = DELAYED_RECYCLED.get(); <span class="hljs-comment">// 当前线程帮助其他线程回收对象的缓存</span><br>    <span class="hljs-type">WeakOrderQueue</span> <span class="hljs-variable">queue</span> <span class="hljs-operator">=</span> delayedRecycled.get(<span class="hljs-built_in">this</span>); <span class="hljs-comment">// 取出对象绑定的 Stack 对应的 WeakOrderQueue</span><br>    <span class="hljs-keyword">if</span> (queue == <span class="hljs-literal">null</span>) &#123;<br>        <span class="hljs-comment">// 最多帮助 2*CPU 核数的线程回收线程</span><br>        <span class="hljs-keyword">if</span> (delayedRecycled.size() &gt;= maxDelayedQueues) &#123;<br>            delayedRecycled.put(<span class="hljs-built_in">this</span>, WeakOrderQueue.DUMMY); <span class="hljs-comment">// WeakOrderQueue.DUMMY 表示当前线程无法再帮助该 Stack 回收对象</span><br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br>        <span class="hljs-comment">// 新建 WeakOrderQueue</span><br>        <span class="hljs-keyword">if</span> ((queue = WeakOrderQueue.allocate(<span class="hljs-built_in">this</span>, thread)) == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-comment">// drop object</span><br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br>        delayedRecycled.put(<span class="hljs-built_in">this</span>, queue);<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (queue == WeakOrderQueue.DUMMY) &#123;<br>        <span class="hljs-comment">// drop object</span><br>        <span class="hljs-keyword">return</span>;<br>    &#125;<br>    queue.add(item); <span class="hljs-comment">// 添加对象到 WeakOrderQueue 的 Link 链表中</span><br>&#125;<br></code></pre></td></tr></table></figure><p>添加到 quene 的源码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">add</span><span class="hljs-params">(DefaultHandle&lt;?&gt; handle)</span> &#123;<br>    handle.lastRecycledId = id;<br>    <span class="hljs-type">Link</span> <span class="hljs-variable">tail</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.tail;<br>    <span class="hljs-type">int</span> writeIndex;<br>    <span class="hljs-comment">// 如果链表尾部的 Link 已经写满，那么再新建一个 Link 追加到链表尾部</span><br>    <span class="hljs-keyword">if</span> ((writeIndex = tail.get()) == LINK_CAPACITY) &#123;<br>        <span class="hljs-comment">// 检查是否超过对应 Stack 可以存放的其他线程帮助回收的最大对象数</span><br>        <span class="hljs-keyword">if</span> (!head.reserveSpace(LINK_CAPACITY)) &#123;<br>            <span class="hljs-comment">// Drop it.</span><br>            <span class="hljs-keyword">return</span>;<br>        &#125;<br>        <span class="hljs-built_in">this</span>.tail = tail = tail.next = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Link</span>();<br>        writeIndex = tail.get();<br>    &#125;<br>    tail.elements[writeIndex] = handle; <span class="hljs-comment">// 添加对象到 Link 尾部</span><br>    handle.stack = <span class="hljs-literal">null</span>; <span class="hljs-comment">// handle 的 stack 属性赋值为 null</span><br>    tail.lazySet(writeIndex + <span class="hljs-number">1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>在向 WeakOrderQueue 写入对象之前，会先判断 Link 链表的 tail 节点是否还有空间存放对象。如果还有空间，直接向 tail Link 尾部写入数据，否则直接丢弃对象。如果 tail Link 已经没有空间，会新建一个 Link 之后再存放对象，新建 Link 之前会检查异线程帮助回收的对象总数超过了 Stack 设置的阈值，如果超过了阈值，那么对象也会被丢弃掉。</p><p>所以异线程B在归还对象给线程A时，经过以下的步骤：</p><ol><li>获取自己的归还对象的容器 Map&lt;Stack&lt;?&gt;, WeakOrderQueue&gt;；</li><li>通过要归还对象关联的 Stack&lt;?&gt; （对象是线程A创建的那么就是线程A的 Stack）获取对应的 WeakOrderQueue，如果不存在则新建（注意新建时会关联到线程A的 WeakOrderQueue 链表上）；</li><li>将要归还的对象添加到 WeakOrderQueue 中。</li></ol><p>注意：是从线程B获取要归还给线程A的 WeakOrderQueue，而不是从线程A获取线程B要归还的 WeakOrderQueue。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Netty 的轻量级对象池 Recycler 在 Netty 里面使用是非常频繁的，比较常用的像 PooledHeapByteBuf 和 PooledDirectByteBuf，分别对应的堆内存和堆外内存的池化实现。</p><p>我们在使用 PooledDirectByteBuf 的时候，并不是每次都去创建新的对象实例，而是从对象池中获取预先分配好的对象实例，不再使用 PooledDirectByteBuf 时，被回收归还到对象池中。</p><p>对象池和连接池有些相似之处，例如都复用对象减少创建销毁对象时的开销。不过连接池一般都是 Thread 的容器，向其中提交任务，选择一个 Thread 执行任务；而对象池是获取对象，由取对象的这个线程进行后续操作。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Netty</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty的ByteBuf好用在哪</title>
    <link href="/2026/05/05/Netty%E7%9A%84ByteBuf%E5%A5%BD%E7%94%A8%E5%9C%A8%E5%93%AA/"/>
    <url>/2026/05/05/Netty%E7%9A%84ByteBuf%E5%A5%BD%E7%94%A8%E5%9C%A8%E5%93%AA/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty的ByteBuf好用在哪"><a href="#Netty的ByteBuf好用在哪" class="headerlink" title="Netty的ByteBuf好用在哪"></a>Netty的ByteBuf好用在哪</h1><p>ByteBuf 是 Netty 的数据容器，所有网络通信中字节流的传输都是通过 ByteBuf 完成的。很多开发者已经使用 ByteBuf 代替 ByteBuffer，即便并不是在写一个网络应用，也会单独使用 ByteBuf。</p><p>相比 JDK 的 ByteBuffer，Netty 的 ByteBuf 好在哪？</p><h2 id="ByteBuffer"><a href="#ByteBuffer" class="headerlink" title="ByteBuffer"></a>ByteBuffer</h2><p>JDK 的 ByteBuffer 结构如下：</p><p><img src="/img/image-20260505101713649.png" alt="image-20260505101713649"></p><p>从图中可知， ByteBuffer 包含以下四个基本属性：</p><ul><li>mark：为某个读取过的关键位置做标记，方便回退到该位置；</li><li>position：当前读取的位置；</li><li>limit： buffer 中有效的数据长度大小；</li><li>capacity：初始化时的空间容量。</li></ul><p>从属性中也可以看出 ByteBuffer 的一些缺点：</p><ul><li>容量在创建时指定，无法进行动态扩容，很难控制需要分配多大的容量；</li><li>读写操作都用 position 指针，需要频繁调用 flip、 rewind 方法切换读写状态。</li></ul><p>接下来以 ByteBuffer 处理粘包写一段示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ByteBufferDemo</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><br>        <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">buffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(<span class="hljs-number">1024</span>);<br><br>        <span class="hljs-comment">// 第一次只发长度和部分数据</span><br>        <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">part1</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(<span class="hljs-number">1024</span>);<br>        part1.putInt(<span class="hljs-number">10</span>);      <span class="hljs-comment">// 长度=10</span><br>        part1.put(<span class="hljs-string">&quot;Hello&quot;</span>.getBytes()); <span class="hljs-comment">// 只发了5字节</span><br>        part1.flip();<br>        buffer.put(part1);<br><br>        List&lt;<span class="hljs-type">byte</span>[]&gt; bytes = parsePackets(buffer);<br>        System.out.println(<span class="hljs-string">&quot;第一次解析: &quot;</span> + bytes.size() + <span class="hljs-string">&quot;个包&quot;</span>);<br><br>        <span class="hljs-comment">// 第二次收到：剩余5字节</span><br>        <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">part2</span> <span class="hljs-operator">=</span> ByteBuffer.wrap(<span class="hljs-string">&quot;World&quot;</span>.getBytes()); <span class="hljs-comment">// 补齐10字节</span><br>        buffer.put(part2);<br><br>        bytes = parsePackets(buffer);<br>        System.out.println(<span class="hljs-string">&quot;第二次解析: &quot;</span> + bytes.size() + <span class="hljs-string">&quot;个包&quot;</span>);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">byte</span>[] p : bytes) &#123;<br>            System.out.println(<span class="hljs-string">&quot;完整包: &quot;</span> + <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(p));<br>        &#125;<br><br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> List&lt;<span class="hljs-type">byte</span>[]&gt; parsePackets(ByteBuffer buffer) &#123;<br>        List&lt;<span class="hljs-type">byte</span>[]&gt; packets = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        buffer.flip(); <span class="hljs-comment">// 切换到读模式</span><br><br>        <span class="hljs-keyword">while</span> (buffer.remaining() &gt;= <span class="hljs-number">4</span>) &#123;<br>            buffer.mark(); <span class="hljs-comment">// 标记位置，用于回滚</span><br>            <span class="hljs-type">int</span> <span class="hljs-variable">dataLen</span> <span class="hljs-operator">=</span> buffer.getInt();<br>            <span class="hljs-keyword">if</span> (dataLen &lt; <span class="hljs-number">0</span> || dataLen &gt; <span class="hljs-number">1024</span> * <span class="hljs-number">1024</span>) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;Invalid length&quot;</span>);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (buffer.remaining() &gt;= dataLen) &#123;<br>                <span class="hljs-type">byte</span>[] data = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[dataLen];<br>                buffer.get(data);<br>                packets.add(data);<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                buffer.reset(); <span class="hljs-comment">// 数据不够，回滚到长度字段前</span><br>                <span class="hljs-keyword">break</span>;<br>            &#125;<br>        &#125;<br>        buffer.compact(); <span class="hljs-comment">// 将未读数据移到头部，为下次写入准备</span><br>        <span class="hljs-keyword">return</span> packets;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码中读之前需要先 ByteBuffer.flip() 切换读模式，ByteBuffer.mark() 标记，如果解析失败，需要 ByteBuffer.reset() 回滚位置，ByteBuffer.compact() 移动未读数据到头部，position 位置到未读数据之后，</p><h2 id="ByteBuf"><a href="#ByteBuf" class="headerlink" title="ByteBuf"></a>ByteBuf</h2><p>Netty 实现了一个性能更高、易用性更强的 ByteBuf，其结构如下：</p><p><img src="/img/image-20260505104303542.png" alt="image-20260505104303542"></p><p>从图中可以看出， ByteBuf 包含三个指针： <strong>读指针 readerIndex</strong> 、 <strong>写指针 writeIndex</strong> 、 <strong>最大容量 maxCapacity</strong>，根据指针的位置又可以将 ByteBuf 内部结构可以分为四个部分：</p><p>第一部分是 <strong>废弃字节</strong>，表示已经丢弃的无效字节数据。</p><p>第二部分是 <strong>可读字节</strong>，表示 ByteBuf 中可以被读取的字节内容，可以通过 writeIndex - readerIndex 计算得出。从 ByteBuf 读取 N 个字节， readerIndex 就会自增 N， readerIndex 不会大于 writeIndex，当 readerIndex &#x3D;&#x3D; writeIndex 时，表示 ByteBuf 已经不可读。</p><p>第三部分是 <strong>可写字节</strong>，向 ByteBuf 中写入数据都会存储到可写字节区域。向 ByteBuf 写入 N 字节数据， writeIndex 就会自增 N，当 writeIndex 超过 capacity，表示 ByteBuf 容量不足，需要扩容。</p><p>第四部分是 <strong>可扩容字节</strong>，表示 ByteBuf 最多还可以扩容多少字节，当 writeIndex 超过 capacity 时，会触发 ByteBuf 扩容，最多扩容到 maxCapacity 为止，超过 maxCapacity 再写入就会出错。</p><p>Netty 重新设计的 ByteBuf 有效地区分了可读、可写以及可扩容数据，解决了 ByteBuffer 无法扩容以及读写模式切换烦琐的缺陷。</p><p>接下来以 ByteBuf 处理粘包写一段示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ByteBufMain</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br><br>        <span class="hljs-type">ByteBuf</span> <span class="hljs-variable">buffer</span> <span class="hljs-operator">=</span> Unpooled.buffer(<span class="hljs-number">1024</span>);<br><br>        <span class="hljs-comment">// 第一次只发长度和部分数据</span><br>        buffer.writeInt(<span class="hljs-number">10</span>);<br>        buffer.writeBytes(<span class="hljs-string">&quot;Hello&quot;</span>.getBytes());<br><br>        List&lt;<span class="hljs-type">byte</span>[]&gt; bytes = parsePackets(buffer);<br>        System.out.println(<span class="hljs-string">&quot;第一次解析: &quot;</span> + bytes.size() + <span class="hljs-string">&quot;个包&quot;</span>);<br><br>        <span class="hljs-comment">// 第二次收到：剩余5字节</span><br>        buffer.writeBytes(<span class="hljs-string">&quot;World&quot;</span>.getBytes()); <span class="hljs-comment">// 补齐10字节</span><br><br>        bytes = parsePackets(buffer);<br>        System.out.println(<span class="hljs-string">&quot;第二次解析: &quot;</span> + bytes.size() + <span class="hljs-string">&quot;个包&quot;</span>);<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">byte</span>[] p : bytes) &#123;<br>            System.out.println(<span class="hljs-string">&quot;完整包: &quot;</span> + <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(p));<br>        &#125;<br><br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> List&lt;<span class="hljs-type">byte</span>[]&gt; parsePackets(ByteBuf buffer) &#123;<br>        List&lt;<span class="hljs-type">byte</span>[]&gt; packets = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-keyword">while</span> (buffer.readableBytes() &gt;= <span class="hljs-number">4</span>) &#123;<br>            <span class="hljs-type">int</span> <span class="hljs-variable">readerIndex</span> <span class="hljs-operator">=</span> buffer.readerIndex();<br>            <span class="hljs-type">int</span> <span class="hljs-variable">dataLen</span> <span class="hljs-operator">=</span> buffer.getInt(readerIndex);   <span class="hljs-comment">// 这里的 getInt 不会移动读指针, readInt 会移动</span><br>            <span class="hljs-keyword">if</span> (dataLen &lt; <span class="hljs-number">0</span> || dataLen &gt; <span class="hljs-number">1024</span> * <span class="hljs-number">1024</span>) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>(<span class="hljs-string">&quot;Invalid length&quot;</span>);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (buffer.readableBytes() &gt;= dataLen + <span class="hljs-number">4</span>) &#123;<br>                <span class="hljs-comment">// 跳过4字节的长度字段</span><br>                buffer.skipBytes(<span class="hljs-number">4</span>);<br>                <span class="hljs-type">byte</span>[] data = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[dataLen];<br>                buffer.readBytes(data);<br>                packets.add(data);<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-keyword">break</span>;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">return</span> packets;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>相比 ByteBuffer，ByteBuf 不再需要繁琐的 flip、compact 操作，而且提供了 read&#x2F;get, write&#x2F;set 的数据读写 API，区别就是 get&#x2F;set 不会移动 readIndex 和 writeIndex。</p><h3 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h3><p>ByteBuf 有多种实现类，每种都有不同的特性，下图是 ByteBuf 的家族图谱，可以划分为三个不同的维度： <strong>Heap&#x2F;Direct</strong> 、 <strong>Pooled&#x2F;Unpooled</strong> 和 <strong>Unsafe&#x2F;非 Unsafe</strong>，我逐一介绍这三个维度的不同特性。</p><p><img src="/img/image-20260505105641761.png" alt="image-20260505105641761"></p><ul><li><strong>Heap&#x2F;Direct 就是堆内和堆外内存</strong>。 Heap 指的是在 JVM 堆内分配，底层依赖的是字节数据； Direct 则是堆外内存，不受 JVM 限制，分配方式依赖 JDK 底层的 ByteBuffer。</li><li><strong>Pooled&#x2F;Unpooled 表示池化还是非池化内存</strong>。 Pooled 是从预先分配好的内存中取出，使用完可以放回 ByteBuf 内存池，等待下一次分配。而 Unpooled 是直接调用系统 API 去申请内存，确保能够被 JVM GC 管理回收。</li><li><strong>Unsafe&#x2F;非 Unsafe 的区别在于操作方式是否安全。</strong> Unsafe 表示每次调用 JDK 的 Unsafe 对象操作物理内存，依赖 offset + index 的方式操作数据。非 Unsafe 则不需要依赖 JDK 的 Unsafe 对象，直接通过数组下标的方式操作数据。</li></ul><p>这些涉及到 Netty 如何高效的使用内存，未来有时间会单独介绍。</p><h3 id="引用计数"><a href="#引用计数" class="headerlink" title="引用计数"></a>引用计数</h3><p>ByteBuf 中还有一个重要的概念：引用计数，用来管理 ByteBuf 的生命周期，特别是池化内存的自动回收，防止内存泄漏。</p><p>当新创建一个 ByteBuf 对象时，它的初始引用计数为 1 ，当 ByteBuf 调用 release() 后，引用计数减 1。</p><p>引用计数对于 Netty 设计缓存池化有非常大的帮助，当引用计数为 0 ，该 ByteBuf 可以被放入到对象池中，避免每次使用 ByteBuf 都重复创建，对于实现高性能的内存管理有着很大的意义。</p><p>所以在写 InboundHandler 时，推荐直接继承 <code>SimpleChannelInboundHandler</code>，它在 channelRead0 方法执行后会自动帮我们处理释放</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">channelRead</span><span class="hljs-params">(ChannelHandlerContext ctx, Object msg)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">release</span> <span class="hljs-operator">=</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-keyword">if</span> (acceptInboundMessage(msg)) &#123;<br>            <span class="hljs-meta">@SuppressWarnings(&quot;unchecked&quot;)</span><br>            <span class="hljs-type">I</span> <span class="hljs-variable">imsg</span> <span class="hljs-operator">=</span> (I) msg;<br>            <span class="hljs-comment">// 执行我们重写的 channelRead0 方法</span><br>            channelRead0(ctx, imsg);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            release = <span class="hljs-literal">false</span>;<br>            ctx.fireChannelRead(msg);<br>        &#125;<br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        <span class="hljs-keyword">if</span> (autoRelease &amp;&amp; release) &#123;<br>            ReferenceCountUtil.release(msg);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Netty 提供的数据容器 ByteBuf，不仅解决了 JDK NIO 中 ByteBuffer 的缺陷，而且提供了易用性更强的接口，相比于 ByteBuffer 它提供了很多非常酷的特性：</p><ul><li>容量可以按需动态扩展，类似于 StringBuffer；</li><li>读写采用了不同的指针，读写模式可以随意切换，不需要调用 flip 方法；</li><li>通过内置的复合缓冲类型可以实现零拷贝（CompositeByteBuf）；</li><li>支持引用计数；</li><li>支持缓存池。</li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty的FastThreadLocal快在哪</title>
    <link href="/2026/05/04/Netty%E7%9A%84FastThreadLocal%E5%BF%AB%E5%9C%A8%E5%93%AA/"/>
    <url>/2026/05/04/Netty%E7%9A%84FastThreadLocal%E5%BF%AB%E5%9C%A8%E5%93%AA/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty的FastThreadLocal快在哪"><a href="#Netty的FastThreadLocal快在哪" class="headerlink" title="Netty的FastThreadLocal快在哪"></a>Netty的FastThreadLocal快在哪</h1><p>Netty 不仅是高性能的网络编程框架，也有很多优秀的设计，譬如本篇要介绍的 FastThreadLocal。</p><p>FastThreadLocal 是相比 ThreadLocal 来讲的，那它究竟 fast 在哪？</p><h2 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h2><h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>ThreadLocal 可以用来实现线程内单例。</p><p>对于线程内单例，简单的实现可以维护一个 ConcurrentHashMap，其中 Key 是 Thread 对象，Value 是 ThreadLocal 的类型。ThreadLocal 不是这么实现的，因为在高并发的场景并发修改 Map 如果需要加锁，势必会降低性能。</p><p>JDK 为了避免加锁，采用了相反的设计思路。JDK 在每个 Thread 内维护一个 Map，Map 的元素称为 Entry，Map 的 Key 是 ThreadLocal，Value 是这个 ThreadLocal 的类型的值。</p><p><img src="/img/image-20260504224251962.png" alt="image-20260504224251962"></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">ThreadLocal&lt;Integer&gt; threadLocal = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>&lt;&gt;();<br>threadLocal.set(<span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>即该线程内的 ThreadLocalMap，会有一个 Entry 元素 &lt;threadLocal, 1&gt;。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">Thread对象<br>  └─ ThreadLocalMap (哈希表)<br>       ├─ Entry[<span class="hljs-number">16</span>] (哈希槽位，可能冲突)<br>       ├─ Entry[<span class="hljs-number">17</span>] (哈希槽位)<br>       └─ Entry[<span class="hljs-number">18</span>] (哈希槽位)<br></code></pre></td></tr></table></figure><p>ThreadLocalMap 是一种使用线性探测法实现的哈希表，底层采用数组存储数据。当发生哈希冲突时，要向后寻找空闲的位置，当冲突频率很高时，<code>解决哈希冲突的成本很大</code>，而且在查找数据时也需要向后查找（哈希值对应的位置可能被其它节点线性探测占用了）。当容量达到阈值时，需要进行扩容，扩容后需要再哈希确定元素位置。</p><h3 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h3><p>提起 ThreadLocal 也不得不提内存泄漏，系统中为了避免频繁的创建销毁线程，一般都会使用线程池复用线程。想一下下面的场景：</p><ol><li>线程在执行过程中向某个方法内创建的 ThreadLocal 对象中设置了值；</li><li>方法执行结束，线程回收到线程池；</li><li>由于线程内的 ThreadLocalMap 中引用了第一步的 ThreadLocal 对象，所以即使方法执行结束，ThreadLocal 对象也无法被回收。</li></ol><p>JDK 为了避免这样的情况，做了以下措施：</p><ol><li>ThreadLocalMap 的 Key 采用弱引用类型，即当没有强引用时只要发生 gc 就会回收；但只有 Key 时弱引用，Value 依然是强引用；</li><li>在执行 ThreadLocal.set()&#x2F;get() 方法时，ThreadLocal 会清除 ThreadLocalMap 中 key 为 NULL 的 Entry 对象，让它能够被 GC 回收。</li></ol><p>但是为了避免内存泄漏，还是要求某个 ThreadLocal 对象不再使用时，立即调用 remove() 方法删除 Entry 对象。</p><h2 id="FastThreadLocal"><a href="#FastThreadLocal" class="headerlink" title="FastThreadLocal"></a>FastThreadLocal</h2><p>通过对 ThreadLocal 的介绍，它有以下缺点：</p><ul><li>解决哈希冲突的成本大，可能会存在性能瓶颈；</li><li>如果不及时 remove，可能产生内存泄漏；</li></ul><h3 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a>实现</h3><p>FastThreadLocalThread 是对 Thread 类的一层包装，每个线程对应一个 InternalThreadLocalMap 实例。只有 FastThreadLocal 和 FastThreadLocalThread 组合使用时，才能发挥 FastThreadLocal 的性能优势。</p><p>InternalThreadLocalMap 与 ThreadLocalMap 的实现差别很大，虽然也是采用数组的方式存储元素，但它可以做到 O(1) 的访问速度，而且只存储 Value，原因就藏在源码里：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> index;<br><br><span class="hljs-keyword">public</span> <span class="hljs-title function_">FastThreadLocal</span><span class="hljs-params">()</span> &#123;<br>    index = InternalThreadLocalMap.nextVariableIndex();<br>&#125;<br></code></pre></td></tr></table></figure><p>在创建 FastThreadLocal 时，会为其中的 index 属性赋一个值，赋值的逻辑很简单：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">AtomicInteger</span> <span class="hljs-variable">nextIndex</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicInteger</span>();    <br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">nextVariableIndex</span><span class="hljs-params">()</span> &#123;<br>       <span class="hljs-type">int</span> <span class="hljs-variable">index</span> <span class="hljs-operator">=</span> nextIndex.getAndIncrement();<br>       <span class="hljs-keyword">if</span> (index &lt; <span class="hljs-number">0</span>) &#123;<br>           nextIndex.decrementAndGet();<br>           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;too many thread-local indexed variables&quot;</span>);<br>       &#125;<br>       <span class="hljs-keyword">return</span> index;<br>   &#125;<br></code></pre></td></tr></table></figure><p>也就是每一个 FastThreadLocal 都有唯一的一个 index，那在 Thread 中存储 FastThreadLocal值 的时候，直接存到 index 对应的位置不就直接解决哈希冲突了吗？</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java">FastThreadLocalThread对象<br>  └─ InternalThreadLocalMap<br>       └─ Object[] indexedVariables (直接寻址数组)<br>            ├─ [<span class="hljs-number">0</span>] → 特殊索引（存储待清理列表）<br>            ├─ [<span class="hljs-number">1</span>] → FastThreadLocal_1的值<br>            ├─ [<span class="hljs-number">2</span>] → FastThreadLocal_2的值<br>            ├─ [<span class="hljs-number">3</span>] → UNSET（未使用）<br>            └─ [<span class="hljs-number">4</span>] → FastThreadLocal_4的值<br></code></pre></td></tr></table></figure><p>当数组容量满时，需要根据 index 的值进行扩容（注意这里不能是简单的 2 倍关系，例如当前长度是 32，但是插入了一个 index 是 72 的 FastThreadLocal，那么扩容2倍是不够的），但这并不会浪费很多空间，那些没使用的位置只是存放了同一个缺省对象的引用。</p><h3 id="内存泄漏-1"><a href="#内存泄漏-1" class="headerlink" title="内存泄漏"></a>内存泄漏</h3><p>FastThreadLocalThread 对于内存泄漏又有什么改进呢？</p><p>Netty 通过 DefaultThreadFactory 创建的所有线程都是 FastThreadLocalThread（Netty 的 EventLoop 就是 FastThreadLocalThread），并且所有提交的 Runnable 任务会被包装成 FastThreadLocalRunnable。</p><p>当一个线程（特别是线程池中的线程）执行完任务后，其实就可以清理当前线程上绑定的所有 FastThreadLocal 变量了。在 FastThreadLocalThread 的设计中，如果一个 index 被设置了值，那么这个 FastThreadLocal 就会放到一个 Set 中，这个 Set 就存在 indexedVariables[0] 的位置。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">FastThreadLocalThread对象<br>  └─ InternalThreadLocalMap<br>       └─ Object[] indexedVariables (直接寻址数组)<br>            ├─ [<span class="hljs-number">0</span>] → 特殊索引（存储待清理列表）<br></code></pre></td></tr></table></figure><p>Netty 还封装了 FastThreadLocalRunnable，FastThreadLocalRunnable 最后会执行 FastThreadLocal.removeAll() 将 Set 集合中所有 FastThreadLocal 对象都清理掉。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultRunnableDecorator</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Runnable</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Runnable r;<br><br>    DefaultRunnableDecorator(Runnable r) &#123;<br>        <span class="hljs-built_in">this</span>.r = r;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">run</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">try</span> &#123;<br>            r.run();<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            FastThreadLocal.removeAll();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>相比 JDK 原生的 ThreadLocal，Netty 的 FastThreadLocal 一方面提高了并发情况下的性能，也优化了内存泄漏。</p><p>但 FastThreadLocal 不一定比 ThreadLocal 快，只有是 FastThreadLocalThread 类型的线程才会更快，如果是普通线程反而会更慢。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Netty</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty逻辑架构与核心组件</title>
    <link href="/2026/05/03/Netty%E9%80%BB%E8%BE%91%E6%9E%B6%E6%9E%84%E4%B8%8E%E6%A0%B8%E5%BF%83%E7%BB%84%E4%BB%B6/"/>
    <url>/2026/05/03/Netty%E9%80%BB%E8%BE%91%E6%9E%B6%E6%9E%84%E4%B8%8E%E6%A0%B8%E5%BF%83%E7%BB%84%E4%BB%B6/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty逻辑架构与核心组件"><a href="#Netty逻辑架构与核心组件" class="headerlink" title="Netty逻辑架构与核心组件"></a>Netty逻辑架构与核心组件</h1><p><a href="https://netty.io/">Netty官网</a> 里有一张 Netty 的整体功能模块结构图，如下：</p><p><img src="/img/image-20260503090643582.png" alt="image-20260503090643582"></p><p>Netty 被分为三大模块，分别是 Core、Protocol Support 和 Transport Services。</p><ul><li>Core 即核心层，提供了底层网络通信的通用抽象和实现，包括可扩展的事件模型、通用的通信 API、支持零拷贝的 ByteBuffer 等。</li><li>Protocol Support 即协议支持层，基本上覆盖了主流协议的编解码实现，如 HTTP、 SSL、 Protobuf、压缩、大文件传输、 WebSocket、文本、二进制等主流协议，此外 Netty 还支持自定义应用层协议。</li><li>Transport Services 即传输服务层，提供了网络传输能力的定义和实现方法。支持 Socket、 HTTP 隧道、虚拟机管道等传输方式。 Netty 对 TCP、 UDP 等数据传输做了抽象和封装，用户可以更聚焦在业务逻辑实现上，而不必关系底层数据传输的细节。</li></ul><p>这是官网对 Netty 主要模块的总结，接下来介绍 Netty 的逻辑架构。</p><h2 id="逻辑架构"><a href="#逻辑架构" class="headerlink" title="逻辑架构"></a>逻辑架构</h2><p>以 <a href="https://zhuwenjie0716.github.io/2026/05/02/Netty%E5%A6%82%E4%BD%95%E5%A4%84%E7%90%86%E5%8D%8A%E5%8C%85&%E7%B2%98%E5%8C%85/">自定义通信协议实践</a> 的服务端启动代码为例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Server</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException, ExecutionException &#123;<br><br>        <span class="hljs-type">NioEventLoopGroup</span> <span class="hljs-variable">bossGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>(<span class="hljs-number">1</span>);<br>        <span class="hljs-type">NioEventLoopGroup</span> <span class="hljs-variable">workGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>();<br>        <span class="hljs-type">ServerBootstrap</span> <span class="hljs-variable">serverBootstrap</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerBootstrap</span>();<br>        serverBootstrap.channel(NioServerSocketChannel.class);<br>        serverBootstrap.handler(<span class="hljs-keyword">new</span> <span class="hljs-title class_">LoggingHandler</span>(LogLevel.INFO));<br>        serverBootstrap.group(bossGroup, workGroup);<br><br>        serverBootstrap.childHandler(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ChannelInitializer</span>&lt;NioSocketChannel&gt;() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">initChannel</span><span class="hljs-params">(NioSocketChannel ch)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>                <span class="hljs-type">ChannelPipeline</span> <span class="hljs-variable">pipeline</span> <span class="hljs-operator">=</span> ch.pipeline();<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameDecoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameEncoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolEncoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolDecoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">LoggingHandler</span>(LogLevel.INFO));<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerProcessHandler</span>());<br>            &#125;<br>        &#125;);<br><br>        <span class="hljs-type">ChannelFuture</span> <span class="hljs-variable">channelFuture</span> <span class="hljs-operator">=</span> serverBootstrap.bind(<span class="hljs-number">8090</span>).sync();<br>        channelFuture.channel().closeFuture().get();<br><br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>其中涉及到的组件几乎都在下面这张图里有体现：</p><p><img src="/img/image-20260503091549781.png" alt="image-20260503091549781"></p><p>Netty 逻辑架构可以分为网络通信层、事件调度层和服务编排层。</p><h3 id="网络通信层"><a href="#网络通信层" class="headerlink" title="网络通信层"></a>网络通信层</h3><p>网络通信层的职责是执行网络 I&#x2F;O 的操作。它支持多种网络协议和 I&#x2F;O 模型的连接操作。当网络数据读取到内核缓冲区后，会触发各种网络事件，这些网络事件会分发给事件调度层进行处理。</p><p>网络通信层的 <strong>核心组件</strong> 包含 <strong>BootStrap、ServerBootStrap、Channel</strong> 三个组件。</p><p>BootStrap 译为引导器，作为整个 Netty 客户端和服务端的程序入口（客户端使用 BootStrap，服务端使用 ServerBootStrap），可以把 Netty 的核心组件像搭积木一样组装在一起。</p><p>ServerBootstrap 需要 <a href="https://zhuwenjie0716.github.io/2026/04/30/Netty%E5%A6%82%E4%BD%95%E6%94%AF%E6%8C%813%E7%A7%8DReactor/">配置Reactor模型</a> 使用，也就是图中的 EventLoopGroup，其中 MainReactor 处理连接事件，通常叫 BossGroup，SubReactor 处理其它 IO 事件，通常叫 WorkGroup。</p><p>Channel 的字面意思是“通道”，它是网络通信的载体，提供了与底层 Socket 交互的能力。Channel 会有多种状态，如 <strong>连接建立、连接注册、数据读写、连接销毁</strong> 等，每一种状态都会绑定相应的事件回调。</p><p><img src="/img/image-20260503092703476.png" alt="image-20260503092703476"></p><h3 id="事件调度层"><a href="#事件调度层" class="headerlink" title="事件调度层"></a>事件调度层</h3><p>事件调度层的职责是通过 Reactor 线程模型对各类事件进行聚合处理，通过 Selector 主循环线程集成多种事件（ I&#x2F;O 事件、信号事件、定时事件等），实际的业务处理逻辑是交由服务编排层中相关的 Handler 完成。</p><p>事件调度层的 <strong>核心组件</strong> 包括 <strong>EventLoopGroup、EventLoop</strong>。</p><p>EventLoopGroup 本质是一个 EventLoop 的线程池，主要负责接收 I&#x2F;O 请求，并分配线程执行处理请求。</p><p><img src="/img/image-20260503094239342.png" alt="image-20260503094239342"></p><p>在 MainReactor（bossGroup） 与客户端建立连接成功后，会存在一个与客户端进行通信的 Channel，接着这个 Channel 会被注册给 SubReactor（workGroup）中的一个（EventLoop），这个 EventLoop 就负责该客户端的后续事件处理，也就是说一个 EventLoop 会负责多个 Channel 的处理。</p><p>从 Netty4 开始 Netty 采用了 <strong>无锁串行化</strong> 设计，其实就是每个 EventLoop 被设计成单线程的，维护一个 Selector 选择器和任务队列 taskQueue，负责处理所管理的 Socket 的 I&#x2F;O 事件、普通任务和定时任务。这样的设计避免了线程切换，但是它的缺陷就是不能执行时间过长的 I&#x2F;O 操作，一旦某个 I&#x2F;O 事件发生阻塞，那么后续的所有 I&#x2F;O 事件都无法执行，甚至造成事件积压，因此在 Handler 中如果有耗时的业务操作，需要单独分配业务线程池使用。</p><p>EventLoop 不仅负责处理 I&#x2F;O 事件，还要兼顾执行任务队列中的任务。任务队列遵循 FIFO 规则，可以保证任务执行的公平性。NioEventLoop 处理的任务类型基本可以分为三类。</p><ul><li><strong>普通任务</strong>：通过 NioEventLoop 的 execute() 方法向任务队列 taskQueue 中添加任务。例如 Netty 在写数据时会封装 WriteAndFlushTask 提交给 taskQueue。taskQueue 的实现类是多生产者单消费者队列 MpscChunkedArrayQueue，在多线程并发添加任务时，可以保证线程安全。</li><li><strong>定时任务</strong>：通过调用 NioEventLoop 的 schedule() 方法向定时任务队列 scheduledTaskQueue 添加一个定时任务，用于周期性执行该任务。例如，心跳消息发送等。定时任务队列 scheduledTaskQueue 采用优先队列 PriorityQueue 实现。</li><li><strong>尾部队列</strong>：tailTasks 相比于普通任务队列优先级较低，在每次执行完 taskQueue 中任务后会去获取尾部队列中任务执行。尾部任务并不常用，主要用于做一些收尾工作，例如统计事件循环的执行时间、监控信息上报等。</li></ul><h3 id="服务编排层"><a href="#服务编排层" class="headerlink" title="服务编排层"></a>服务编排层</h3><p>服务编排层的职责是负责组装各类服务，它是 Netty 的核心处理链，用以实现网络事件的动态编排和有序传播。</p><p>服务编排层的 <strong>核心组件</strong> 包括 <strong>ChannelPipeline</strong> 、 <strong>ChannelHandler、ChannelHandlerContext</strong>。</p><p>ChannelPipeline 是 Netty 的核心编排组件，<strong>负责组装各种 ChannelHandler</strong>，实际数据的编解码以及加工处理操作都是由 ChannelHandler 完成的。 ChannelPipeline 可以理解为 <strong>ChannelHandler 的实例列表</strong> ——内部通过双向链表将不同的 ChannelHandler 链接在一起。当 I&#x2F;O 读写事件触发时， ChannelPipeline 会依次调用 ChannelHandler 列表对 Channel 的数据进行拦截和处理。</p><p>ChannelPipeline 是线程安全的，因为每一个新的 Channel 都会对应绑定一个新的 ChannelPipeline。一个 ChannelPipeline 关联一个 EventLoop，一个 EventLoop 仅会绑定一个线程（单线程处理单 Channel，且该 Channel 有独立的 ChannelPipeline，自然没有线程安全问题）。</p><p>ChannelPipeline 中包含入站 ChannelInboundHandler 和出站 ChannelOutboundHandler 两种处理器，我们结合客户端和服务端的数据收发流程来理解 Netty 的这两个概念。</p><p><img src="/img/image-20260503100458313.png" alt="image-20260503100458313"></p><p>客户端和服务端都有各自的 ChannelPipeline。以客户端为例，数据从客户端发向服务端，该过程称为 <strong>出站</strong>，反之则称为 <strong>入站</strong>。数据入站会由一系列 InBoundHandler 处理，然后再以相反方向的 OutBoundHandler 处理后完成出站。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">initChannel</span><span class="hljs-params">(NioSocketChannel ch)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>    <span class="hljs-type">ChannelPipeline</span> <span class="hljs-variable">pipeline</span> <span class="hljs-operator">=</span> ch.pipeline();<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameDecoder</span>());<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameEncoder</span>());<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolEncoder</span>());<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolDecoder</span>());<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">LoggingHandler</span>(LogLevel.INFO));<br>    pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerProcessHandler</span>());<br>&#125;<br></code></pre></td></tr></table></figure><p>我们经常使用的编码 Encoder 是出站操作，解码 Decoder 是入站操作。服务端接收到客户端数据后，需要先经过 Decoder 入站处理后，再通过 Encoder 出站通知客户端。所以客户端和服务端一次完整的请求应答过程可以分为三个步骤：客户端出站（请求数据）、服务端入站（解析数据并执行业务逻辑）、服务端出站（响应结果）。</p><p>不过实际上，ChannelPipeline 中并不是多个的 ChannelHandler，而是多个 ChannelHandlerContext，所有 ChannelHandlerContext 之间组成了双向链表。ChannelHandlerContext 用于保存 ChannelHandler 上下文并包含了 ChannelHandler 生命周期的所有事件，复用前置后置的通用逻辑。</p><p>ChannelPipeline 的双向链表分别维护了 HeadContext 和 TailContext 的头尾节点，这两个节点在 Netty 中已经默认实现了，自定义的 ChannelHandler 会插入到 Head 和 Tail 之间。</p><p><img src="/img/image-20260503101515403.png" alt="image-20260503101515403"></p><p>HeadContext 既是 Inbound 处理器，也是 Outbound 处理器。它分别实现了 ChannelInboundHandler 和 ChannelOutboundHandler。网络数据写入操作的入口就是由 HeadContext 节点完成的。 HeadContext 作为 Pipeline 的头结点负责读取数据并开始传递 InBound 事件，当数据处理完成后，数据会反方向经过 Outbound 处理器，最终传递到 HeadContext，所以 HeadContext 又是处理 Outbound 事件的最后一站。此外 HeadContext 在传递事件之前，还会执行一些前置操作。</p><p>TailContext 只实现了 ChannelInboundHandler 接口。它会在 ChannelInboundHandler 调用链路的最后一步执行，主要用于终止 Inbound 事件传播，例如释放 Message 数据资源等。 TailContext 节点作为 OutBound 事件传播的第一站，仅仅是将 OutBound 事件传递给上一个节点。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>本篇介绍了 Netty 的逻辑架构和核心组件，并简要讲解了 Netty 处理请求的流程（多 Reactor 模型）。</p><ol><li>bossGroup 中有一个 EventLoop，绑定某个特定端口进行监听。一旦有新的连接进来触发 accept 类型事件，就会在当前 EventLoop 的 I&#x2F;O 事件处理阶段，将这个连接注册到 workGroup 中的某一个 EventLoop，进行后续事件的监听。</li><li>workGroup （如果不指定线程数，默认cpu核数*2）中的每一个 EventLoop，会通过 selcetor 对绑定到自身的 channel 进行轮询，获取 I&#x2F;O 事件。</li><li>workGroup 中的每一个 EventLoop 获取到 I&#x2F;O 事件后，会在事件处理阶段通过对应的 ChannelPipeline 进行处理。</li><li>workGroup 中的每一个 EventLoop 每次 select() 循环处理完 I&#x2F;O 事件后，还会处理其任务队列（taskQueue）的任务（不一定执行完，有超时时间，避免影响下一轮的 I&#x2F;O 事件的处理）。</li></ol>]]></content>
    
    
    
    <tags>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty如何处理半包和粘包（附自定义通信协议实践）</title>
    <link href="/2026/05/02/Netty%E5%A6%82%E4%BD%95%E5%A4%84%E7%90%86%E5%8D%8A%E5%8C%85&amp;%E7%B2%98%E5%8C%85/"/>
    <url>/2026/05/02/Netty%E5%A6%82%E4%BD%95%E5%A4%84%E7%90%86%E5%8D%8A%E5%8C%85&amp;%E7%B2%98%E5%8C%85/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty如何处理半包-粘包"><a href="#Netty如何处理半包-粘包" class="headerlink" title="Netty如何处理半包&amp;粘包"></a>Netty如何处理半包&amp;粘包</h1><p>TCP 传输协议是面向流的，没有数据包界限。当程序要发送一个较大的包时，由于滑动窗口以及 MTU 的影响，有可能会拆分为多个包传输；当程序要发送较小的包时，为了提高通信效率，Linux 的 Nagle 算法可能会将多个报文合并成一个大的报文进行发送。</p><p><strong>MTU（Maxitum Transmission Unit）</strong> 是链路层一次最大传输数据的大小。 MTU 一般来说大小为 1500 Byte。<strong>MSS（Maximum Segement Size）</strong> 是指 TCP 最大报文段长度，一般是 1460 Byte。滑动窗口则是用于流量控制，用于接收方反馈接受窗口大小，进而控制发送方发送速度。</p><p><img src="/img/image-20260502224426365.png" alt="image-20260502224426365"></p><p>受这些因素，应用程序收到的字节流可能是不完整的（半包）也可能是粘在一起的（粘包），需要进行一定的操作才能得到想要的数据。也就是说，这个问题依赖<code>自定义应用层通信协议</code>解决。</p><h2 id="一次解码"><a href="#一次解码" class="headerlink" title="一次解码"></a>一次解码</h2><p>通常把找出消息的边界的过程称为“一次解码”。</p><h3 id="固定长度法"><a href="#固定长度法" class="headerlink" title="固定长度法"></a>固定长度法</h3><p>双方约定，每次数据长度为 N 个 Byte，如果收到的数据长度不足则继续等待读取符，发送方的数据如果不足该长度则使用空位进行补齐，会造成一定的空间浪费。</p><p>该方法比较简单，但设定固定长度的值是个难题，如果长度太大会造成字节浪费，长度太小又会影响消息传输，所以在一般情况下不会被采用。</p><p><code>Netty: FixedLengthFrameDecoder</code>。</p><h3 id="特定分割符"><a href="#特定分割符" class="headerlink" title="特定分割符"></a>特定分割符</h3><p>发送方发送数据时在末尾追加特定分割符，接收方根据分隔符进行消息拆分。</p><p>该方法也比较简单，常见的做法是将消息进行编码（如:base64），再选择以外的字符作为分割符，否则需要扫描消息内容，对内容中与分割符相同的符号进行转义处理。</p><p><code>Netty: DelimiterBasedFrameDecoder</code>。</p><h3 id="固定字段存长度"><a href="#固定字段存长度" class="headerlink" title="固定字段存长度"></a>固定字段存长度</h3><p>在消息的最前方固定字段（如:4B）存这个消息的长度值，接着向后读取该值个长度的字节数据。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">消息头     消息体<br>+--------+----------+<br>| Length |  Content |<br>+--------+----------+<br></code></pre></td></tr></table></figure><p>这是项目开发中最常用的一种协议，既能精准定位数据，内容也不需要转义。</p><p><code>Netty: LengthFieldBasedFrameDecoder</code>。</p><h2 id="二次解码"><a href="#二次解码" class="headerlink" title="二次解码"></a>二次解码</h2><p>“一次解码”获得了想要的消息（字节），还需要进行“二次解码”转为程序中需要的对象，其实也是我们平时说的反序列化。</p><p>反序列化与序列化是相对的，一般需要在性能、可读性中做权衡，这也是实现 RPC 需要考虑的，常见的方式有：Java 序列化、xml、json、protobuf 等，可以参考：<a href="https://zhuwenjie0716.github.io/2026/04/29/%E9%80%89%E6%8B%A9%E5%90%88%E9%80%82%E7%9A%84RPC/">选择合适的RPC</a>。</p><h2 id="自定义通信协议实践"><a href="#自定义通信协议实践" class="headerlink" title="自定义通信协议实践"></a>自定义通信协议实践</h2><p>接下来以客户端向服务端发送认证操作为例进行实践，按照这个方案，即使未来增加新的操作类型，也只需要新建对应的操作类和操作结果类，并在枚举类中新增对应操作即可。</p><h3 id="交互消息体设计"><a href="#交互消息体设计" class="headerlink" title="交互消息体设计"></a>交互消息体设计</h3><p>我们采用“固定长度存字段”法进行一次解码。在消息最前方加2字节标识消息长度，并定义消息由 header 和 body 组成。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 消息类,每个消息就代表一个请求或响应</span><br><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Message</span>&lt;T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MessageBody</span>&gt; &#123;<br><br>    <span class="hljs-comment">// 消息头</span><br>    <span class="hljs-keyword">private</span> MessageHeader messageHeader;<br>    <span class="hljs-comment">// 消息体</span><br>    <span class="hljs-keyword">private</span> T messageBody;<br><br>    <span class="hljs-comment">// 消息的二次编码逻辑</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">encode</span><span class="hljs-params">(ByteBuf byteBuf)</span> &#123;<br>        byteBuf.writeInt(messageHeader.getVersion());<br>        byteBuf.writeLong(messageHeader.getStreamId());<br>        byteBuf.writeInt(messageHeader.getOpCode());<br>        byteBuf.writeBytes(JsonUtil.toJson(messageBody).getBytes());<br>    &#125;<br><br>    <span class="hljs-comment">// 定义抽象方法，由消息头中的操作码寻找对应的消息类</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> Class&lt;T&gt; <span class="hljs-title function_">getMessageBodyDecodeClass</span><span class="hljs-params">(<span class="hljs-type">int</span> opcode)</span>;<br><br>    <span class="hljs-comment">// 消息的二次解码逻辑</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">decode</span><span class="hljs-params">(ByteBuf msg)</span> &#123;<br>        <br>        <span class="hljs-comment">// 解码出 header 部分</span><br>        <span class="hljs-type">int</span> <span class="hljs-variable">version</span> <span class="hljs-operator">=</span> msg.readInt();<br>        <span class="hljs-type">long</span> <span class="hljs-variable">streamId</span> <span class="hljs-operator">=</span> msg.readLong();<br>        <span class="hljs-type">int</span> <span class="hljs-variable">opCode</span> <span class="hljs-operator">=</span> msg.readInt();<br>        <span class="hljs-type">MessageHeader</span> <span class="hljs-variable">messageHeader</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageHeader</span>();<br>        messageHeader.setVersion(version);<br>        messageHeader.setOpCode(opCode);<br>        messageHeader.setStreamId(streamId);<br><br>        <span class="hljs-comment">// 解码出 body 部分</span><br>        Class&lt;T&gt; bodyClazz = getMessageBodyDecodeClass(opCode);<br>        <span class="hljs-comment">// JsonUtil 其实只是封装了一个 google 的 Gson 对象</span><br>        <span class="hljs-type">T</span> <span class="hljs-variable">body</span> <span class="hljs-operator">=</span> JsonUtil.fromJson(msg.toString(StandardCharsets.UTF_8), bodyClazz);<br>        <br>        <span class="hljs-built_in">this</span>.messageHeader = messageHeader;<br>        <span class="hljs-built_in">this</span>.messageBody = body;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>MessageHeader 中的内容由业务定义，可以自行拓展，但记得修改上述 Message 类的 encode 和 decode 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageHeader</span> &#123;<br>    <span class="hljs-comment">// 版本</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-variable">version</span> <span class="hljs-operator">=</span> <span class="hljs-number">1</span>;<br>    <span class="hljs-comment">// 操作码（标识本次消息的类型）</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> opCode;<br>    <span class="hljs-comment">// 请求ID（UUID或自增ID均可，推荐自增）</span><br>    <span class="hljs-keyword">private</span> <span class="hljs-type">long</span> streamId;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MessageBody</span> &#123;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><p>MessageBody 即消息体可以分为两大类，一类是操作（Operation），一类是操作结果（OperationResult）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Operation</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MessageBody</span> &#123;<br><br>    <span class="hljs-comment">// 获取该操作对应的操作结果</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> OperationResult <span class="hljs-title function_">execute</span><span class="hljs-params">()</span>;<br><br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">OperationResult</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MessageBody</span> &#123;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>操作类消息即为请求（RequestMessage），操作结果类消息即为响应（ResponseMessage）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">RequestMessage</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Message</span>&lt;Operation&gt; &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class <span class="hljs-title function_">getMessageBodyDecodeClass</span><span class="hljs-params">(<span class="hljs-type">int</span> opcode)</span> &#123;<br>        <span class="hljs-comment">// 对于请求而言，根据操作码找到的是对应的操作类</span><br>        <span class="hljs-keyword">return</span> OperationType.fromOpCode(opcode).getOperationClazz();<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">RequestMessage</span><span class="hljs-params">(Long streamId, Operation operation)</span> &#123;<br>        <span class="hljs-type">MessageHeader</span> <span class="hljs-variable">messageHeader</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageHeader</span>();<br>        messageHeader.setStreamId(streamId);<br>        messageHeader.setOpCode(OperationType.fromOperation(operation).getOpCode());<br>        <span class="hljs-built_in">this</span>.setMessageHeader(messageHeader);<br>        <span class="hljs-built_in">this</span>.setMessageBody(operation);<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ResponseMessage</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Message</span>&lt;OperationResult&gt; &#123;<br>    <br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class <span class="hljs-title function_">getMessageBodyDecodeClass</span><span class="hljs-params">(<span class="hljs-type">int</span> opcode)</span> &#123;<br>        <span class="hljs-comment">// 对于响应而言，根据操作码找的是对应的操作结果类</span><br>        <span class="hljs-keyword">return</span> OperationType.fromOpCode(opcode).getOperationResultClazz();<br>    &#125;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><p>其中的 OperationType 为枚举类，定义了三元组（操作码、操作类、操作结果类）。</p><h3 id="操作消息体设计"><a href="#操作消息体设计" class="headerlink" title="操作消息体设计"></a>操作消息体设计</h3><p>接下来以认证请求作为一种操作类型作为示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AuthOperation</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Operation</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String userName;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> String password;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> AuthOperationResult <span class="hljs-title function_">execute</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-comment">// 简单判断，如果用户名是 admin 就认为成功</span><br>        <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;admin&quot;</span>.equalsIgnoreCase(<span class="hljs-built_in">this</span>.userName)) &#123;<br>            <span class="hljs-type">AuthOperationResult</span> <span class="hljs-variable">response</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AuthOperationResult</span>(<span class="hljs-literal">true</span>);<br>            <span class="hljs-keyword">return</span> response;<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AuthOperationResult</span>(<span class="hljs-literal">false</span>);<br>    &#125;<br>    <br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AuthOperationResult</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">OperationResult</span> &#123;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> passAuth;<br><br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Getter</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">enum</span> <span class="hljs-title class_">OperationType</span> &#123;<br><br>    AUTH(<span class="hljs-number">1</span>, AuthOperation.class, AuthOperationResult.class);<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> opCode;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Operation</span>&gt; operationClazz;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">OperationResult</span>&gt; operationResultClazz;<br><br>    OperationType(<span class="hljs-type">int</span> opCode, Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Operation</span>&gt; operationClazz, Class&lt;? <span class="hljs-keyword">extends</span> <span class="hljs-title class_">OperationResult</span>&gt; responseClass) &#123;<br>        <span class="hljs-built_in">this</span>.opCode = opCode;<br>        <span class="hljs-built_in">this</span>.operationClazz = operationClazz;<br>        <span class="hljs-built_in">this</span>.operationResultClazz = responseClass;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> OperationType <span class="hljs-title function_">fromOpCode</span><span class="hljs-params">(<span class="hljs-type">int</span> type)</span> &#123;<br>        <span class="hljs-keyword">return</span> getOperationType(requestType -&gt; requestType.opCode == type);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> OperationType <span class="hljs-title function_">fromOperation</span><span class="hljs-params">(Operation operation)</span> &#123;<br>        <span class="hljs-keyword">return</span> getOperationType(requestType -&gt; requestType.operationClazz == operation.getClass());<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> OperationType <span class="hljs-title function_">getOperationType</span><span class="hljs-params">(Predicate&lt;OperationType&gt; predicate)</span> &#123;<br>        OperationType[] values = values();<br>        <span class="hljs-keyword">for</span> (OperationType operationType : values) &#123;<br>            <span class="hljs-keyword">if</span> (predicate.test(operationType)) &#123;<br>                <span class="hljs-keyword">return</span> operationType;<br>            &#125;<br>        &#125;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AssertionError</span>(<span class="hljs-string">&quot;no found type&quot;</span>);<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><h3 id="编解码设计"><a href="#编解码设计" class="headerlink" title="编解码设计"></a>编解码设计</h3><p>业务定义写好了，接下来是服务端的编解码器。</p><h4 id="一次解码-1"><a href="#一次解码-1" class="headerlink" title="一次解码"></a>一次解码</h4><p>定义最前方2字节长度为消息体长度，一次解码找到消息的边界，并把消息读取为 Bytebuf，构造器中第二三个参数分别是长度字段偏移量，长度字段长度：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FrameDecoder</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">LengthFieldBasedFrameDecoder</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">FrameDecoder</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-built_in">super</span>(Integer.MAX_VALUE, <span class="hljs-number">0</span>, <span class="hljs-number">2</span>, <span class="hljs-number">0</span>, <span class="hljs-number">2</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="二次解码-1"><a href="#二次解码-1" class="headerlink" title="二次解码"></a>二次解码</h4><p>经过“一次解码”，我们可以拿到一个消息对应的字节流（Bytebuf），由于这是服务端，所以收到的消息一定是 RequestMessage，“二次编码”将消息转为 RequestMessage 对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ProtocolDecoder</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MessageToMessageDecoder</span>&lt;ByteBuf&gt; &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">decode</span><span class="hljs-params">(ChannelHandlerContext ctx, ByteBuf byteBuf, List&lt;Object&gt; out)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">RequestMessage</span> <span class="hljs-variable">requestMessage</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RequestMessage</span>();<br>        requestMessage.decode(byteBuf);<br>        out.add(requestMessage);<br>    &#125; <br>&#125;<br></code></pre></td></tr></table></figure><h4 id="业务处理"><a href="#业务处理" class="headerlink" title="业务处理"></a>业务处理</h4><p>经过解码，此时已经拿到了对应的 RequestMessage，获取响应结果并写回客户端即可：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ServerProcessHandler</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">SimpleChannelInboundHandler</span>&lt;RequestMessage&gt; &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">channelRead0</span><span class="hljs-params">(ChannelHandlerContext ctx, RequestMessage requestMessage)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">Operation</span> <span class="hljs-variable">operation</span> <span class="hljs-operator">=</span> requestMessage.getMessageBody();<br>        <span class="hljs-type">OperationResult</span> <span class="hljs-variable">operationResult</span> <span class="hljs-operator">=</span> operation.execute();<br>        <span class="hljs-type">ResponseMessage</span> <span class="hljs-variable">responseMessage</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ResponseMessage</span>();<br>        responseMessage.setMessageHeader(requestMessage.getMessageHeader());<br>        responseMessage.setMessageBody(operationResult);<br>        ctx.writeAndFlush(responseMessage);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="二次编码"><a href="#二次编码" class="headerlink" title="二次编码"></a>二次编码</h4><p>业务处理流程中写出的是个 ResponseMessage 对象，需要进行序列化才能发送到网络中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ProtocolEncoder</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MessageToMessageEncoder</span>&lt;ResponseMessage&gt; &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">encode</span><span class="hljs-params">(ChannelHandlerContext ctx, ResponseMessage responseMessage, List&lt;Object&gt; out)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">ByteBuf</span> <span class="hljs-variable">buffer</span> <span class="hljs-operator">=</span> ctx.alloc().buffer();<br>        responseMessage.encode(buffer);<br>        out.add(buffer);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="一次编码"><a href="#一次编码" class="headerlink" title="一次编码"></a>一次编码</h4><p>“二次编码”得到了消息的字节流数据（Bytebuf），需要在最前方新增2字节数据标识长度：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">FrameEncoder</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">LengthFieldPrepender</span> &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">FrameEncoder</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-built_in">super</span>(<span class="hljs-number">2</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><p>服务端代码中需要按照顺序组装起对应的编解码器及业务处理器。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Server</span> &#123;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> InterruptedException, ExecutionException &#123;<br><br>        <span class="hljs-type">NioEventLoopGroup</span> <span class="hljs-variable">bossGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>(<span class="hljs-number">1</span>);<br>        <span class="hljs-type">NioEventLoopGroup</span> <span class="hljs-variable">workGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>();<br>        serverBootstrap.channel(NioServerSocketChannel.class);<br>        serverBootstrap.handler(<span class="hljs-keyword">new</span> <span class="hljs-title class_">LoggingHandler</span>(LogLevel.INFO));<br>        serverBootstrap.group(bossGroup, workGroup);<br><br>        serverBootstrap.childHandler(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ChannelInitializer</span>&lt;NioSocketChannel&gt;() &#123;<br>            <span class="hljs-meta">@Override</span><br>            <span class="hljs-keyword">protected</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">initChannel</span><span class="hljs-params">(NioSocketChannel ch)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>                <span class="hljs-type">ChannelPipeline</span> <span class="hljs-variable">pipeline</span> <span class="hljs-operator">=</span> ch.pipeline();<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameDecoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">FrameEncoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolEncoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ProtocolDecoder</span>());<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">LoggingHandler</span>(LogLevel.INFO));<br>                pipeline.addLast(<span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerProcessHandler</span>());<br>            &#125;<br>        &#125;);<br><br>        <span class="hljs-type">ChannelFuture</span> <span class="hljs-variable">channelFuture</span> <span class="hljs-operator">=</span> serverBootstrap.bind(<span class="hljs-number">8090</span>).sync();<br>        channelFuture.channel().closeFuture().get();<br><br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>上述过程中，客户端发送来的请求会先经过一次解码器变为 Bytebuf，再经过二次解码器变为 RequestMessage，在业务处理中得到对应的响应并写回客户端，经二次编码器转为 Bytebuf，最后由一次编码器在最前方2字节增加消息体长度字段。</p><p>类图定义如下：</p><p><img src="/img/image-20260503000529419.png" alt="image-20260503000529419"></p><p>注：上述实践中仅包含了服务端代码，未包含客户端代码。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>为了处理 TCP 编程中粘包半包以及反序列化的问题，Netty 内置了一些常见的解码器，解码就是找到数据流边界并转为系统中对象的流程。</p><p>Netty 中提供了 ByteToMessageDecoder 和 MessageToMessageDecoder 类来进行解码操作。</p><p><img src="/img/image-20260502231913235.png" alt="image-20260502231913235"></p><p>相应的，选择了对应的解码器后也要编写对应的编码器代码。</p><p>同时，本文展示了一种关于自定义通信协议的实践。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Netty</tag>
      
      <tag>计算机网络</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty如何支持3种Reactor</title>
    <link href="/2026/04/30/Netty%E5%A6%82%E4%BD%95%E6%94%AF%E6%8C%813%E7%A7%8DReactor/"/>
    <url>/2026/04/30/Netty%E5%A6%82%E4%BD%95%E6%94%AF%E6%8C%813%E7%A7%8DReactor/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty如何支持3种Reactor"><a href="#Netty如何支持3种Reactor" class="headerlink" title="Netty如何支持3种Reactor"></a>Netty如何支持3种Reactor</h1><p>Java 并发包作者 Doug Lea，在 <a href="https://gee.cs.oswego.edu/dl/cpjslides/nio.pdf">Scalable I&#x2F;O in Java</a> 一文中阐述了服务端开发中 I&#x2F;O 模型的演进过程，也被 Netty、Mina 等大多数高性能 IO 服务框架所采用。</p><p>Reactor 是一种开发模式，核心流程如下：</p><ol><li>注册感兴趣的事件；</li><li>扫描是否有感兴趣的事件发生；</li><li>对感兴趣事件做出相应的处理。</li></ol><p>在网络IO中：Reactor &#x3D; I&#x2F;O 多路复用（底层监听） + 事件分发 + 处理器回调（高层调度）。</p><h2 id="Reactor-线程模型"><a href="#Reactor-线程模型" class="headerlink" title="Reactor 线程模型"></a>Reactor 线程模型</h2><p>先不提 Reactor 的三种线程模型，拿生活中饭店的发展举例子：</p><ol><li>前期刚刚成立，老板一个人负责迎宾，做菜，上菜，送客；</li><li>招了几个伙计，大家一起做这些事情；</li><li>伙计中找数人单独做迎宾，其他人做剩下的事情。</li></ol><p>Reactor 线程模型的发展也是如此~</p><h3 id="单线程模型"><a href="#单线程模型" class="headerlink" title="单线程模型"></a>单线程模型</h3><p>在单线程模型中，这个线程不但要负责迎宾接收连接（accept），还需要负责后续的编解码及业务处理，流程如图：</p><p><img src="/img/image-20260430160839817.png" alt="image-20260430160839817"></p><p>单线程模型逻辑简单，缺陷也十分明显：</p><ul><li>一个线程支持处理的连接数非常有限，无法充分利用多核 CPU，性能方面有明显瓶颈；</li><li>当多个事件被同时触发时，只要有一个事件没有处理完，其他后面的事件就无法执行，这就会造成消息积压及请求超时；</li><li>线程在处理 I&#x2F;O 事件时，Select 无法同时处理连接建立、事件分发等操作；</li><li>如果 I&#x2F;O 线程一直处于满负荷状态，很可能造成服务端节点不可用。</li></ul><h3 id="单-Reactor-多线程模型"><a href="#单-Reactor-多线程模型" class="headerlink" title="单 Reactor 多线程模型"></a>单 Reactor 多线程模型</h3><p>为了提高单线程模型的瓶颈，多线程模型中多了业务线程池处理业务逻辑，流程如图：</p><p><img src="/img/image-20260430161050885.png" alt="image-20260430161050885"></p><p>多线程模型中，一个 Reactor 线程负责连接建立和数据读写，数据读取完毕后交给业务线程池处理，优点是可以充分利用多核 CPU 的处理能力，缺点是单线程承担所有事件的监听和响应，高并发场景下容易成为性能瓶颈。</p><h3 id="主从-Reactor-模型"><a href="#主从-Reactor-模型" class="headerlink" title="主从 Reactor 模型"></a>主从 Reactor 模型</h3><p>单 Reactor 多线程模型的缺陷在于连接建立和数据读写的事件都由一个 Reactor 线程负责，主从 Reactor 模型中，MainReactor 线程负责连接建立（accept），连接建立成功后将新创建的连接对象注册至 SubReactor。再由 SubReactor 分配线程池中的 I&#x2F;O 线程与其连接绑定，它将负责连接生命周期内所有的 I&#x2F;O 事件。流程如图：</p><p><img src="/img/image-20260430161949864.png" alt="image-20260430161949864"></p><p>Netty 推荐使用主从多线程模型，这样就可以轻松达到成千上万规模的客户端连接。在海量客户端并发请求的场景下，主从多线程模式甚至可以适当增加 SubReactor 线程的数量，从而利用多核能力提升系统的吞吐量。</p><h2 id="Netty-与-Reactor"><a href="#Netty-与-Reactor" class="headerlink" title="Netty 与 Reactor"></a>Netty 与 Reactor</h2><p>Netty 在 Java NIO 基础上进行了更高层次的封装，屏蔽了 NIO 的复杂性；封装了更加人性化的 API，提供了良好的可拓展性，大大降低了开发者的上手难度。</p><p>Netty 的可扩展性其中之一就是可定制化的线程模型。</p><h3 id="单线程模型-1"><a href="#单线程模型-1" class="headerlink" title="单线程模型"></a>单线程模型</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">EventLoopGroup</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>(<span class="hljs-number">1</span>);<br><span class="hljs-type">ServerBootstrap</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerBootstrap</span>();<br>b.group(group);<br></code></pre></td></tr></table></figure><h3 id="单-Reactor-多线程模型-1"><a href="#单-Reactor-多线程模型-1" class="headerlink" title="单 Reactor 多线程模型"></a>单 Reactor 多线程模型</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">EventLoopGroup</span> <span class="hljs-variable">group</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>();<br><span class="hljs-type">ServerBootstrap</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerBootstrap</span>();<br>b.group(group)<br></code></pre></td></tr></table></figure><h3 id="主从-Reactor-模型-1"><a href="#主从-Reactor-模型-1" class="headerlink" title="主从 Reactor 模型"></a>主从 Reactor 模型</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">EventLoopGroup</span> <span class="hljs-variable">bossGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>(<span class="hljs-number">1</span>);<br><span class="hljs-type">EventLoopGroup</span> <span class="hljs-variable">workerGroup</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NioEventLoopGroup</span>();<br><span class="hljs-type">ServerBootstrap</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ServerBootstrap</span>();<br>b.group(bossGroup, workerGroup)<br></code></pre></td></tr></table></figure><p>Netty 只是修改了几行简单的启动代码，就切换了不同的线程模型。</p><p>初学者可能会有疑问，这些代码里完全没有看到 workThreads（即业务处理线程），这里涉及到 Netty 中 Handler 的逻辑，<strong>如果在添加 Handler 时不指定额外的线程池，那么 SubReactor 线程既充当 I&#x2F;O 事件分发器，又充当业务逻辑的执行线程</strong>。</p><p>代码中的 ServerBootstrap 姑且可以认为是服务端引导器，通过它来启动服务端（本来 Reactor 模型也是用于服务端处理高并发客户端连接的）。</p><p>NioEventLoopGroup 是 Netty 中的核心概念之一，NIO 是一种 IO 模型，EventLoop 事件循环，字面意思其实就是在做 NIO 模型的事件循环，NioEventLoop 中维护着一个 Selector 选择器和任务队列 taskQueue， 每次循环的处理流程都包含事件轮询 select、事件处理 processSelectedKeys、任务处理 runAllTasks 几个步骤，是典型的 Reactor 线程模型的运行机制。NioEventLoopGroup 就是这样的组件的组成的 group。</p><p>SubReactor 中的每一个线程其实就是一个 NioEventLoop，由于默认情况下每个 SubReactor 要处理分配的所有 Socket 的操作，因此<strong>不要在 <code>EventLoop</code> 线程中执行耗时操作</strong>。</p>]]></content>
    
    
    
    <tags>
      
      <tag>IO模型</tag>
      
      <tag>Netty</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>工作两年后,我有什么感受</title>
    <link href="/2026/04/30/%E5%B7%A5%E4%BD%9C%E4%B8%A4%E5%B9%B4%E5%90%8E-%E6%88%91%E6%9C%89%E4%BB%80%E4%B9%88%E6%84%9F%E5%8F%97/"/>
    <url>/2026/04/30/%E5%B7%A5%E4%BD%9C%E4%B8%A4%E5%B9%B4%E5%90%8E-%E6%88%91%E6%9C%89%E4%BB%80%E4%B9%88%E6%84%9F%E5%8F%97/</url>
    
    <content type="html"><![CDATA[<h1 id="工作两年后-我有什么感受"><a href="#工作两年后-我有什么感受" class="headerlink" title="工作两年后,我有什么感受"></a>工作两年后,我有什么感受</h1><p>从24年7月正式工作到现在（26年4月底），其实距工作两年还差一些，不过我毕业前还有十个月的实习生涯，姑且算是工作两年了吧。那么正式工作两年后，我有什么感受？</p><h2 id="过去"><a href="#过去" class="headerlink" title="过去"></a>过去</h2><p>大学期间，我主要是按照<a href="https://space.bilibili.com/95256449?spm_id_from=333.337.search-card.all.click">遇见狂神说</a>的 Java 路线学习，一路跟了数十个视频，以为 Java 开发就是注入 Mapper，写写 sql，说出来不怕人笑话，我当时真的连 Bean 是什么，以及默认扫描 Bean 只扫描启动类下的文件都不知道。</p><p>好在看了那么多视频，“照葫芦画瓢”的能力倒是学了一些，还是很轻松的完成了数据库、Java Web、 Django 这样的课程设计，也导致一段时间被大家认为技术好，代码强。有点印象的是最开始写网络编程，对着视频写完还是一脸蒙圈，怎么这样写个 Socket 就通信上了，什么叫阻塞，为什么这样的一行代码就能卡住进程呢？对于中间件的了解也很空洞，知道在项目中怎么用，对背后的实现一无所知，面试也是临时找了点八股文。</p><p>机缘巧合之下，大四进入超聚变（至少我了解到的是算力领域全国第二）实习，遇到了一批优秀的人（导师制，我导师是上海大学的本硕，PL 是西交的硕），加上超聚变本身是华为子公司拆分出来，有比较完善的规章制度和开发流程。优点提完了，缺点我就不提了（我PL原话：从这里出去，90%的公司你都能很快适应），总之最后虽然给我发了 offer 我还是交违约金离开了，这里提高了我对云原生知识的掌握，代码质量的要求，但项目进度比较紧张，没有学很多新的知识。</p><p>顺便一提，因为实习和饮食、作息习惯的原因，实习期间还动了个小手术，也是我决定要离开的原因之一。</p><h2 id="成长"><a href="#成长" class="headerlink" title="成长"></a>成长</h2><p>校招还是拿了几个 Offer 的，最后选择来了这里。刚来的时候由于实习一直在用 go 语言，Java 有点不会写了，给我分第一个小需求的时候我还写成 Feign 调用的了，领导就给我时间学习，抽空重新学了 Spring 系列（<a href="https://cloud.tencent.com/tvp/member/251">丁雪丰</a>老师的 Spring 全家桶），看丁雪丰老师课的时候，我觉得他好厉害，对 Spring 的各项知识点侃侃而谈，还补充了很多小的知识诸如 EventListener，Docker 轻松运行各种服务（Mysql、Redis、Kafka、Consul 等）。当时只觉得老师技术很厉害，后续去搜才知道还是美团研究员，<a href="https://cloud.tencent.com/tvp">腾讯云TVP</a>。</p><p>慢慢的我已经可以做一些简单的业务了，公司发公告说软考可以开始报名了。我问了下同事，了解了公司的职称补贴，觉得报名费也不贵就报名了，详情可以看：<a href="https://zhuwenjie0716.github.io/2026/04/17/%E8%B0%88%E8%B0%88%E6%88%91%E7%9A%84%E8%BD%AF%E8%80%83%E7%BB%8F%E5%8E%86/">谈谈我的软考经历</a>，总之就是连着考过了中、高级。当时高级本来觉得过不了，在办公室查成绩前我给办公室其他人说：这次要是能过，在座的各位一人一份 KFC，最后虽然花了钱还是很开心的。</p><p>软考的过程中免不了遇到不会的知识，于是我就各种补充知识点，同时通过考试的良好反馈也让我更有兴趣去学习了。</p><p>因为好奇一个 Web 请求从浏览器发出到 SpringBoot 中是如何处理的，于是我去学习了 Tomcat。后来证明这不是一个很好的选择：一上来直接给我整懵了。第一个遇到的问题就是 IO 模型，几种模型给我绕进去了，又扯上 Reactor，费了一段时间才懂。后来看代码结构只觉得写的很好，经常是一个抽象类，然后多种实现，充分的利用多态走不同的逻辑，后来才知道原来这是模板设计模式，就开始补<a href="https://space.bilibili.com/7968519/lists/4686906?type=season">设计模式</a>。</p><p>补了一些设计模式后，我意识到能写功能是一回事，能优雅的写好功能是另一回事。所谓的优雅一方面是指代码风格（诸如单行尽量不超过 120 那个IDE分界线，函数功能不要超过 100 行，参数不要超过 5 个），另一方面则是业务规范（诸如异常如何处理（无论如何不能生吞异常，有遗留代码 catch 完直接丢出自定义异常了，日志都没打印），线程池该如何选择，null 和空的处理，数字的运算使用 BigDecimal）。这些主要来自对 Java 业务开发常见错误的学习。</p><p>当然这段时间也学习了一些其他的知识模块：Stream、Optional 的使用，Oauth2 的四种授权模式，guava 高性能工具包等。</p><p>看到项目中也在经常使用 Redis，我就想着去深入学习一下，找到了<a href="https://zhangtielei.com/">张铁蕾</a>老师的博客，张老师当时主要有 Redis 源码解读系列和分布式系列，学了 String、Hash、Set 等结构的实现，才觉得 Redis 这么流行不是没有原因的，高效的数据结构一点也不夸张，对于 cache line 的考虑，渐进式哈希等都很精妙。除此之外，也通过一些资源学习了 cluster 集群，缓存区的异步复制。学完这些，其实软考里关于 Redis 的部分几乎没有问题了。</p><p>看了 Redis 这样的设计，我对性能优化感兴趣了起来（想必做开发的都知道被评价代码写的性能又好又易读的成就感吧）。首先去学习了通用的一些性能优化，包括但不限于 计算机硬件（cpu、内存、磁盘）、网络通信（RPC、序列化、Tcp）、数据库（索引、锁、事务）和分布式（一致性哈希、NWR、CAP）。在学习性能优化的期间，也免不了要去补四大件（数据结构、计算机网络、操作系统和计算机组成原理）。</p><p>但是在看了性能优化后，摆在我面前的问题是，你都找不到性能问题在哪怎么优化？我又去看了些网络相关的知识和监控工具。网络相关的主要是指 tcpdump 语法，通信协议演进，监控工具诸如 arthas，火焰图。</p><p>业务开发中有次遇到性能瓶颈是通过 Nginx 解决的，我就又去学习了 Nginx。大概知道了处理请求的 11 个阶段，常见的参数，编译和升级流程，负载均衡配置等。</p><p>做 Java 开发的自然免不了要去啃 JVM 这个山，也拜读了周志明老师的深入剖析Java虚拟机，这本书确实很经典，但类的字节码格式那部分还有编译优化的部分我没有很理解。字节码格式我不认为现在的我需要掌握，编译优化则是诸如标量替换，方法内联，锁优化锁消除更像是告知读者 JVM 做了什么优化，让读者在开发时可以主动关注，而非让读者如何优化 JVM。</p><p>近期正在学习高性能网络编程框架 Netty，一方面业务开始涉及一些 IOT 领域，可能需要报文解析，另一方面对 Netty 的大名也早有耳闻，它在内存使用，锁，网络编程部分代码也非常出色，借这个机会深入学习一下。</p><p>除上述内容外：我对大名鼎鼎的 “削峰填谷” 一词自然也有所耳闻，也去学习了下 Kafka，但业务中用的主要是 RabbitMQ，所以没有太多的实践机会，只是自己测试了下基本功能，了解了核心概念。ES 也是，项目中确实在用，只是了解了下倒排索引，写过几个接口，未进入深入研究。</p><p>学习哪有不走弯路的，有硬去啃发现确实啃不动的（例如操作系统代码），有啃完不实战依旧云里雾里的（例如Kafka），但总体来说收获还是蛮多的。</p><h2 id="不足"><a href="#不足" class="headerlink" title="不足"></a>不足</h2><p>我这个人是有点懒的，又对很多东西都感兴趣，导致列了很多计划又完不成（electron、神经网络等）。</p><p>有些东西只求看完，至于有没有真正理解透彻，那就是另一回事了。</p><p>还有的时候属实觉得自己天赋一般，别人很轻松讲出来的我要仔细思考后才能领会，有的还没办法领会。</p><p>疑问为什么网上刷到那么多人，校招的时候就能对各个中间件对答如流，在我看了 Redis 和 Netty 的一些源码后，真的觉得能熟练掌握一个中间件是很费时间的事。</p><h2 id="致谢"><a href="#致谢" class="headerlink" title="致谢"></a>致谢</h2><p>首先感谢下我的领导，看我在需求少的时候吭哧吭哧看笔记，刷网站也不责怪我，还允许我在项目中实践自己的一些方案。</p><p>其次感谢下互联网上的诸位老师吧，学习过程中的很多内容都来自于老师的奉献，印象比较深刻的是：丁雪丰老师、陶辉老师、张铁蕾老师和朱晔老师，他们严谨的态度和专业的知识给了我很多帮助。当然也还有很多老师未曾提及，感谢。</p><p>最后感谢下我自己。</p>]]></content>
    
    
    
    <tags>
      
      <tag>思考</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>选择合适的RPC</title>
    <link href="/2026/04/29/%E9%80%89%E6%8B%A9%E5%90%88%E9%80%82%E7%9A%84RPC/"/>
    <url>/2026/04/29/%E9%80%89%E6%8B%A9%E5%90%88%E9%80%82%E7%9A%84RPC/</url>
    
    <content type="html"><![CDATA[<h1 id="选择合适的RPC"><a href="#选择合适的RPC" class="headerlink" title="选择合适的RPC"></a>选择合适的RPC</h1><p>RPC（Remote Procedure Call）即远程过程调用，随着微服务的兴起，每个服务都拥有自己的数据库，负责各自的模块，例如 keystone（认证服务）负责用户信息、权限认证的内容，workOrder（工单服务）负责工单的流程流转。在 workOrder 中，可能也需要查询一些用户的信息，但用户信息表并不在该服务的数据库中，因此就需要调用 keystone 服务来获取用户信息，调用其他服务获取信息就是一次远程过程调用。</p><p>说个题外话，也不是完全都要这么实现。我见的对实时性要求不高的服务，可能会在服务中建一个叫 UserCache 的表，定时从真正的用户表同步数据，虽然这也需要服务调用，但针对本服务获取用户信息来讲，就可以查询 Cache 表而不用进行服务间调用。另外，也可以直连对方的数据库，使用 jdbcTemplate 等进行数据库直接查询等，不过这里只讨论 RPC 就是了。</p><h2 id="RPC-中的三个问题"><a href="#RPC-中的三个问题" class="headerlink" title="RPC 中的三个问题"></a>RPC 中的三个问题</h2><p>我们知道 Socket 网络通信，实际上就可以在不同的服务间进行请求，但是如果调用一下对方的服务，都需要手动来创建 socket，也太复杂难懂了，而且还会有以下的问题。</p><p>RPC 既然叫远程过程调用，那肯定跟本地调用是不一样的，不一样在什么地方呢？思考一个减法的场景：</p><p>如果是本地调用，你直接调用这个方法就行了:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> Long <span class="hljs-title function_">sub</span><span class="hljs-params">(Long a, Long b)</span> &#123;<br>    <span class="hljs-keyword">return</span> a - b;<br>&#125;<br></code></pre></td></tr></table></figure><p>但是在 RPC 中是不可以的，首先，你需要让对方知道，你想要调用对方的 “减法” 方法，接着还需要传入两个数字，还需要标识哪个是被减数，哪个是减数，就像下面这样，甚至更极端一点，每个数字是大端存储还是小端存储 (<a href="https://blog.csdn.net/qq_3517289697/article/details/141259989">大端存储与小端存储</a>) 都要进行规定</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">sub 10 3<br>10 3 sub<br>10 sub 3<br></code></pre></td></tr></table></figure><p>这就是第一个问题，<code>协议约定问题：如何确定传输的语法，如何传递需要的参数</code></p><p>封装好了查询参数，怎么知道你想要调用的服务的地址呢（ip:port），假设服务端实现了多个远程调用，每个可能实现在不同的进程中，监听的端口也不一样，而且由于服务端都是自己实现的，不可能使用一个大家都公认的端口，而且有可能多个进程部署在一台机器上，大家需要抢占端口，为了防止冲突，往往使用随机端口，那客户端如何找到这些监听的端口呢？</p><p>这是第二个问题，<code>服务发现问题：如何找到要进行调用的服务端信息</code></p><p>有了服务端地址，也封装好了请求参数，终于可以发送请求了，那么如果此时网络比较拥堵，请求包丢失了怎么办？应用需不需要进行重试，对性能的影响大不大？</p><p>这是第三个问题，<code>传输问题：如何处理丢包、超时等问题</code></p><p>综上，RPC 框架需要解决如下三个问题：</p><ul><li>协议约定</li><li>服务发现</li><li>传输</li></ul><h2 id="RPC-规范"><a href="#RPC-规范" class="headerlink" title="RPC 规范"></a>RPC 规范</h2><p>RPC 有一篇很经典的论文：<a href="http://www.cs.cmu.edu/~dga/15-712/F07/papers/birrell842.pdf">Implementing Remote Procedure Calls</a>，定义了 RPC 的调用标准。后面所有 RPC 框架，都是按照这个标准模式来的。</p><h3 id="协议约定"><a href="#协议约定" class="headerlink" title="协议约定"></a>协议约定</h3><p><img src="/img/image-20250408134939549.png" alt="image-20250408134939549"></p><p>当客户端的应用想发起一个远程调用时，它实际是通过本地调用本地调用方的 Stub。它负责将调用的接口、方法和参数，通过约定的协议规范进行编码，并通过本地的 RPCRuntime 进行传输，将调用网络包发送到服务器。</p><p>服务器端的 RPCRuntime 收到请求后，交给提供方 Stub 进行解码，然后调用服务端的方法，服务端执行方法，返回结果，提供方 Stub 将返回结果编码后，发送给客户端，客户端的 RPCRuntime 收到结果，发给调用方 Stub 解码得到结果，返回给客户端。</p><p>也就是双方都有 stub 的存在，用来进行编码解码，类似于序列化和反序列化</p><h3 id="服务发现"><a href="#服务发现" class="headerlink" title="服务发现"></a>服务发现</h3><p>常用的方案为提供服务注册中心：</p><p>如 SpringCloud 集成 Eureka</p><p>如 Dubbo 集成 Zookeeper</p><h3 id="传输"><a href="#传输" class="headerlink" title="传输"></a>传输</h3><p>早期有的实现是由公司网络人员封装出一套类库，供大家开发使用，现在一般集成市面上比较优秀的网络框架实现，如 netty。</p><h2 id="Sun-RPC（ONC-RPC）"><a href="#Sun-RPC（ONC-RPC）" class="headerlink" title="Sun RPC（ONC RPC）"></a>Sun RPC（ONC RPC）</h2><p>最早的 RPC 的一种实现方式称为 Sun RPC 或 ONC RPC。Sun 公司是第一个提供商业化 RPC 库和 RPC 编译器的公司。这个 RPC 框架是在 NFS 协议中使用的。</p><h3 id="协议约定-1"><a href="#协议约定-1" class="headerlink" title="协议约定"></a>协议约定</h3><p><img src="/img/image-20250408140948466.png" alt="image-20250408140948466"></p><p>NFS（Network File System）就是网络文件系统。要使 NFS 成功运行，要启动两个服务端，一个是 mountd，用来挂载文件路径；一个是 nfsd，用来读写文件。NFS 可以在本地 mount 一个远程的目录到本地的一个目录，从而本地的用户在这个目录里面写入、读出任何文件的时候，其实操作的是远程另一台机器上的文件。</p><p>倒数第二部分的 XDR（External Data Representation，外部数据表示法）是一个标准的数据压缩格式，可以表示基本的数据类型，也可以表示结构体，主要用于编码和解码参数，客户端和服务端共享。</p><p>在客户端和服务端实现 RPC 的时候，首先要定义一个双方都认可的程序、版本、方法、参数等，双方约定为一个协议定义文件，接着通过 ONC 提供的工具生成双方的 Stub 程序。</p><h3 id="传输-1"><a href="#传输-1" class="headerlink" title="传输"></a>传输</h3><p>同时，大牛们写了 ONC RPC 的类库来解决传输中可能出现的错误、丢包等问题，通过状态机实现 Socket 的异步模型，流程非常复杂，参考下图：</p><p><img src="/img/image-20250408141637213.png" alt="image-20250408141637213"></p><h3 id="服务发现-1"><a href="#服务发现-1" class="headerlink" title="服务发现"></a>服务发现</h3><p>在 ONC RPC 中，服务发现是通过 portmapper 实现的。</p><p><img src="/img/image-20250408141829179.png" alt="image-20250408141829179"></p><p>portmapper 会启动在一个众所周知的端口上，RPC 程序由于是用户自己写的，会监听在一个随机端口上，但是 RPC 程序启动的时候，会向 portmapper 注册。客户端要访问 RPC 服务端这个程序的时候，首先查询 portmapper，获取 RPC 服务端程序的随机端口，然后向这个随机端口建立连接，开始 RPC 调用。从图中可以看出，mount 命令的 RPC 调用，就是这样实现的。</p><p>早期 ONC RPC 框架，以及 NFS 的实现，给出了解决 RPC 三大问题的示范性实现，也即<code>协议约定要公用协议描述文件，并通过这个文件生成 Stub 程序</code>；<code>RPC 的传输一般需要一个状态机</code>；需要另外<code>一个进程专门做服务发现</code>。</p><p>【1】协议修改不灵活，尤其是版本变更（部分用户希望新增字段）</p><p>【2】双方的压缩格式（例如字段顺序）需要完全一致</p><h2 id="SOAP"><a href="#SOAP" class="headerlink" title="SOAP"></a>SOAP</h2><p>如果使用二进制编码传输协议，其实是很晦涩的，而使用文本传输协议，就变得通俗易懂多了，xml 就是一种常见的文本传输协议</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.alibaba<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>druid-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.2.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure><p>相比于 ONC RPC 来讲，如果提供出去的服务中有使用者希望进行字段新增，直接修改就行：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.alibaba<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>druid-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.2.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br>          <span class="hljs-tag">&lt;<span class="hljs-name">test</span>&gt;</span>666<span class="hljs-tag">&lt;/<span class="hljs-name">test</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure><p>而对于那些不需要该字段的，忽略不解析即可，而且即使字段直接交换顺序也不影响最后的解析结果</p><h3 id="协议约定和传输"><a href="#协议约定和传输" class="headerlink" title="协议约定和传输"></a>协议约定和传输</h3><p>基于 XML 的最著名的通信协议就是<strong>SOAP</strong>了，全称<strong>简单对象访问协议</strong>（Simple Object Access Protocol）。它使用 XML 编写简单的请求和回复消息，<code>通常</code>用 HTTP 协议进行传输。</p><p>一般来讲，还需要对服务进行描述，因为调用的人不认识你，所以没办法找到你，问你的服务应该如何调用。</p><p>当然你可以写文档，然后放在官方网站上，但是你的文档不一定更新得那么及时，而且你也写的文档也不一定那么严谨，所以常常会有调试不成功的情况。因而，我们需要一种相对比较严谨的<strong>Web 服务描述语言</strong>，<strong>WSDL</strong>（Web Service Description Languages）。它也是一个 XML 文件。</p><p>具体可参考 <a href="https://blog.csdn.net/2302_76489021/article/details/139361979">XML Web 服务技术解析：WSDL 与 SOAP 原理、应用案例一览</a>，总之就是对服务的信息进行了详细的描述，包括请求-响应类、数据名、数据类型等。</p><h3 id="服务发现问题"><a href="#服务发现问题" class="headerlink" title="服务发现问题"></a>服务发现问题</h3><p>有一个<strong>UDDI</strong>（Universal Description, Discovery, and Integration），也即<strong>统一描述、发现和集成协议</strong>。它其实是一个注册中心，服务提供方可以将上面的 WSDL 描述文件，发布到这个注册中心，注册完毕后，服务使用方可以查找到服务的描述，封装为本地的客户端进行调用。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><ul><li>原来的二进制 RPC 有很多缺点，格式要求严格，修改过于复杂，不面向对象，于是产生了基于文本的调用方式——基于 XML 的 SOAP。</li><li>SOAP 有三大要素：协议约定用 WSDL、传输协议用 HTTP、服务发现用 UDDI。</li></ul><h2 id="Restful"><a href="#Restful" class="headerlink" title="Restful"></a>Restful</h2><p>虽然 SOAP 使用 HTTP 协议，但一般只会使用 POST 请求方式，原因是它传输的内容包含 XML 数据，容易超出 URL限制，而且 Get 请求一般用于请求资源，不满足 SOAP  <strong>请求&#x2F;响应模型</strong>的设计。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs xml">POST /create HTTP/1.1<br>Host: 127.0.0.1<br>Content-Type: application/xml; charset=utf-8<br>Content-Length: nnn<br> <br><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span>?&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.alibaba<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>druid-spring-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>1.2.5<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure><h3 id="协议约定和传输-1"><a href="#协议约定和传输-1" class="headerlink" title="协议约定和传输"></a>协议约定和传输</h3><p>于是出现了更为简单的文本化方式：Json + 请求方式</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs json">POST /create HTTP/<span class="hljs-number">1.1</span><br>Host<span class="hljs-punctuation">:</span> <span class="hljs-number">127.0</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span><br>Content-Type<span class="hljs-punctuation">:</span> application/json; charset=utf<span class="hljs-number">-8</span><br>Content-Length<span class="hljs-punctuation">:</span> nnn<br> <br><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;dependency&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;groupId&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;com.alibaba&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;artifactId&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;druid-spring-boot-starter&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;version&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;1.2.5&quot;</span><br>  <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure><p>协议约定间使用 json，传输使用 HTTP</p><h3 id="服务发现-2"><a href="#服务发现-2" class="headerlink" title="服务发现"></a>服务发现</h3><p>Spring 系列里非常经典的微服务方案 Spring Cloud。在 Spring Cloud 中有一个组件叫 Eureka，就是用来做注册中心的。</p><p>服务分服务提供方（server），它向 Eureka 做服务注册、续约和下线等操作，注册的主要数据包括服务名、机器 IP、端口号、域名等等。</p><p>另外一方是服务消费方（client），向 Eureka 获取服务提供方的注册信息。为了实现负载均衡和容错，服务提供方可以注册多个。</p><p>当消费方要调用提供方服务的时候，会从注册中心读出多个服务来，进行负载均衡后选择一个利用 RestTemplate 工具，将请求对象转换为 JSON，并发起 Rest 调用，RestTemplate 的调用也是分 POST、PUT、GET、 DELETE 的，当结果返回的时候，根据返回的 JSON 解析成对象。</p><p>通过这样的封装，调用起来已经很方便了。顺便一提：也可以使用 Feign 通过声明式的接口进行更加简单的调用。</p><p>不过，虽然这样的方式更加的通俗易懂，也有缺点，文本类的序列化相比二进制来讲，一般情况下都是要占用更多的空间，也即需要传输更大的数据量。</p><h2 id="二进制类-RPC-协议"><a href="#二进制类-RPC-协议" class="headerlink" title="二进制类 RPC 协议"></a>二进制类 RPC 协议</h2><p>出于对远程调用性能的考虑，很多公司选型的时候，还是希望采用更加省空间和带宽的二进制的方案。一个著名的例子就是 Dubbo 服务化框架二进制的 RPC 方式。</p><p><img src="/img/image-20250408152726493.png" alt="image-20250408152726493"></p><h3 id="协议约定-2"><a href="#协议约定-2" class="headerlink" title="协议约定"></a>协议约定</h3><p>Dubbo 中默认的 RPC 协议是 Hessian2。为了保证传输的效率，Hessian2 将远程调用序列化为二进制进行传输，并且可以进行一定的压缩。</p><p>相比于之前的二进制，Hessian2 有很大提升：</p><ul><li>原来要定义一个协议文件，然后通过这个文件生成客户端和服务端的 Stub，才能进行相互调用，这样使得修改就会不方便。Hessian2 不需要定义这个协议文件，而是自描述的。关于调用哪个函数，参数是什么，另一方不需要拿到协议文件，而是拿到二进制，靠它本身根据 Hessian2 的规则，就能解析出来。</li><li>序列化效率提高。具体参看 <a href="https://blog.csdn.net/HongZeng_CSDN/article/details/130218223">Hessian2 序列化, 与 Protobuf 相比如何?</a></li></ul><h3 id="传输-2"><a href="#传输-2" class="headerlink" title="传输"></a>传输</h3><p>Dubbo 会在客户端的本地启动一个 Proxy，其实就是客户端的 Stub，对于远程的调用都通过这个 Stub 进行封装。接下来，Dubbo 会从注册中心获取服务端的列表，根据路由规则和负载均衡规则，在多个服务端中选择一个最合适的服务端进行调用。</p><p>在 <code>Dubbo 里面，使用了 Netty 的网络传输框架</code>。</p><p>调用服务端的时候，首先要进行编码和序列化，形成 Dubbo 头和序列化的方法和参数。将编码好的数据，交给网络客户端进行发送，网络服务端收到消息后，进行解码。然后将任务分发给某个线程进行处理，在线程中会调用服务端的代码逻辑，然后返回结果。</p><h3 id="服务发现-3"><a href="#服务发现-3" class="headerlink" title="服务发现"></a>服务发现</h3><p>Dubbo 搭配服务注册中心实现服务发现，一般配合 Zookeeper 使用。</p><p>基于此，简单对比下 SpringCloud 和 Dubbo：</p><ul><li>SpringCloud 有一套完整的微服务处理方案，包括服务注册与发现、网关、负载均衡、熔断降级、等</li><li>Dubbo 只实现了 RPC 相关的一些服务治理内容</li></ul><h2 id="跨语言类-RPC-协议"><a href="#跨语言类-RPC-协议" class="headerlink" title="跨语言类 RPC 协议"></a>跨语言类 RPC 协议</h2><p>通过上面所提到的 RPC 协议，我们对理想的 RPC 框架的定义也愈加明确了：</p><ul><li>首先，传输性能很重要。因为服务之间的调用如此频繁了，还是二进制的越快越好。</li><li>其次，跨语言很重要。因为服务多了，什么语言写成的都有，而且不同的场景适宜用不同的语言，不能一个语言走到底。</li><li>最好既严谨又灵活，添加个字段不用重新编译和发布程序。</li><li>最好既有服务发现，也有服务治理，就像 Dubbo 和 Spring Cloud 一样。</li></ul><p>那有没有一个比较理想的 RPC 框架呢，GRPC！</p><h3 id="协议约定-3"><a href="#协议约定-3" class="headerlink" title="协议约定"></a>协议约定</h3><p>在性能方面，GRPC 使用 protobuf 来作为序列化方式，压缩效率极高，有很多设计精巧的序列化方法。可以类比 redis 的数据结构设计，非常精妙。</p><p>在灵活性方面，Protocol Buffers 考虑了兼容性。在协议文件中，每一个字段都有修饰符：</p><ul><li>required：这个值不能为空，一定要有这么一个字段出现；</li><li>optional：可选字段，可以设置，也可以不设置，如果不设置，则使用默认值；</li><li>repeated：可以重复 0 到多次。</li></ul><p>这样的设计方式给了客户端和服务端很大的灵活性。</p><h3 id="传输-3"><a href="#传输-3" class="headerlink" title="传输"></a>传输</h3><p>如果是 Java 技术栈，GRPC 的客户端和服务器之间通过 Netty Channel 作为数据通道，每个请求都被封装成 HTTP 2.0 的 Stream。</p><p>同时在 Go、Python、C++ 中，也都有性能优异的实现。</p><h3 id="服务发现-4"><a href="#服务发现-4" class="headerlink" title="服务发现"></a>服务发现</h3><p>GRPC 本身没有提供服务发现的机制，需要借助其他的组件，发现要访问的服务端，在多个服务端之间进行容错和负载均衡。</p><p>有一种对于 GRPC 支持比较好的负载均衡器 Envoy。其实 Envoy 不仅仅是负载均衡器，它还是一个高性能的 C++ 写的 Proxy 转发器，可以配置非常灵活的转发规则。</p><p>如果比较了解 k8s，应该会知道 istio，istio 的核心支持就是 Envoy。</p><h3 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h3><ul><li>GRPC 是一种二进制，性能好，跨语言，还灵活，同时可以进行服务治理的多快好省的 RPC 框架，唯一不足就是还是要写协议文件。</li><li>GRPC 序列化使用 Protocol Buffers，网络传输使用 HTTP 2.0，服务治理可以使用基于 Envoy 的 Service Mesh。</li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>计算机网络</tag>
      
      <tag>RPC</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>IO多路复用不同实现及对比</title>
    <link href="/2026/04/28/IO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8%E4%B8%8D%E5%90%8C%E5%AE%9E%E7%8E%B0%E5%8F%8A%E5%AF%B9%E6%AF%94/"/>
    <url>/2026/04/28/IO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8%E4%B8%8D%E5%90%8C%E5%AE%9E%E7%8E%B0%E5%8F%8A%E5%AF%B9%E6%AF%94/</url>
    
    <content type="html"><![CDATA[<h1 id="IO多路复用不同实现及对比"><a href="#IO多路复用不同实现及对比" class="headerlink" title="IO多路复用不同实现及对比"></a>IO多路复用不同实现及对比</h1><p>select、poll、epoll 都是用来实现 IO 多路复用的，而多路复用发展的背景就是 socket 网络通信下提升系统的并发处理能力，单纯依靠多进程或多线程来实现对硬件要求很高，而且上限比较低，采用 IO 多路复用，可以很轻易的处理上千的并发。</p><p>虽然多路复用的机制有多种，但每一种都有一些通用的我们需要关注的问题:</p><ul><li>该机制监听套接字上的什么事件</li><li>该机制能监听多少套接字</li><li>当有套接字就绪时，该机制是如何找到就绪套接字的</li></ul><p>Linux 针对每一个套接字都会有一个<code>文件描述符（fd）</code>，也就是一个非负整数，用来唯一标识该套接字。所以，在多路复用机制的函数中，Linux 通常会用文件描述符作为参数。有了文件描述符，函数也就能找到对应的套接字，进而进行监听、读写等操作。</p><h2 id="select"><a href="#select" class="headerlink" title="select"></a>select</h2><p>select 机制的核心函数就是 select 函数</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/*</span><br><span class="hljs-comment">@Param __nfds 监听的套接字个数</span><br><span class="hljs-comment">@Param __readfds 监听读事件的套接字数组</span><br><span class="hljs-comment">@Param __writefds 监听写事件的套接字数组</span><br><span class="hljs-comment">@Param __exceptfds 监听异常事件的套接字数组</span><br><span class="hljs-comment">@Param __timeout 本次调用的阻塞时间</span><br><span class="hljs-comment">*/</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">select</span> <span class="hljs-params">(<span class="hljs-type">int</span> __nfds, fd_set *__readfds, fd_set *__writefds, fd_set *__exceptfds, <span class="hljs-keyword">struct</span> timeval *__timeout)</span><br>    <br>调用后 select 函数会阻塞，直到有描述符就绪（有数据 可读、可写、或者有except），或者超时（timeout 指定等待时间），函数返回。当 select 函数返回后，可以通过遍历 fdset，来找到就绪的描述符。    <br></code></pre></td></tr></table></figure><p>fd_set 的结构体定义是这样的：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta"># <span class="hljs-keyword">define</span> __FD_SETSIZE 1024</span><br><span class="hljs-meta"># <span class="hljs-keyword">define</span> __NFDBITS 32</span><br><span class="hljs-meta"># <span class="hljs-keyword">define</span> (long int) __fd_mask</span><br><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> &#123;</span><br>   …<br>   __fd_mask  __fds_bits[__FD_SETSIZE / __NFDBITS];<br>   …<br>&#125; fd_set<br></code></pre></td></tr></table></figure><p>所以 __fds_bits 字段其实就是 long int 的一个数组，数组中每个元素 32 位，共有 (1024&#x2F;32 &#x3D; 32) 个元素，每一位用来标识一个 fd，因此<code>最多标识 1024 个 fd</code>。</p><p>使用时，系统需要首先创建好需要监听的读事件、写事件、异常事件的描述符集合，接着调用 select 函数，<code>阻塞等待</code>返回就绪的描述符数目。</p><p><img src="/img/image-20260428102712549.png" alt="image-20260428102712549"></p><p>示例代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> sock_fd,conn_fd; <span class="hljs-comment">//监听套接字和已连接套接字的变量</span><br>sock_fd = socket() <span class="hljs-comment">//创建套接字</span><br>bind(sock_fd)   <span class="hljs-comment">//绑定套接字</span><br>listen(sock_fd) <span class="hljs-comment">//在套接字上进行监听，将套接字转为监听套接字</span><br><br>fd_set rset;  <span class="hljs-comment">//被监听的描述符集合，关注描述符上的读事件</span><br> <br><span class="hljs-type">int</span> max_fd = sock_fd<br><br><span class="hljs-comment">//初始化rset数组，使用FD_ZERO宏设置每个元素为0 </span><br>FD_ZERO(&amp;rset);<br><span class="hljs-comment">//使用FD_SET宏设置rset数组中位置为sock_fd的文件描述符为1，表示需要监听该文件描述符</span><br>FD_SET(sock_fd,&amp;rset);<br><br><span class="hljs-comment">//设置超时时间 </span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">timeval</span> <span class="hljs-title">timeout</span>;</span><br>timeout.tv_sec = <span class="hljs-number">3</span>;<br>timeout.tv_usec = <span class="hljs-number">0</span>;<br> <br><span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>) &#123;<br>   <span class="hljs-comment">//调用select函数，检测rset数组保存的文件描述符是否已有读事件就绪，返回就绪的文件描述符个数</span><br>   n = select(max_fd+<span class="hljs-number">1</span>, &amp;rset, <span class="hljs-literal">NULL</span>, <span class="hljs-literal">NULL</span>, &amp;timeout);<br> <br>   <span class="hljs-comment">//调用FD_ISSET宏，在rset数组中检测sock_fd对应的文件描述符是否就绪</span><br>   <span class="hljs-keyword">if</span> (FD_ISSET(sock_fd, &amp;rset)) &#123;<br>       <span class="hljs-comment">//如果sock_fd已经就绪，表明已有客户端连接；调用accept函数建立连接</span><br>       conn_fd = accept();<br>       <span class="hljs-comment">//设置rset数组中位置为conn_fd的文件描述符为1，表示需要监听该文件描述符</span><br>       FD_SET(conn_fd, &amp;rset);<br>   &#125;<br><br>   <span class="hljs-comment">//依次检查已连接套接字的文件描述符</span><br>   <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; maxfd; i++) &#123;<br>        <span class="hljs-comment">//调用FD_ISSET宏，在rset数组中检测文件描述符是否就绪</span><br>       <span class="hljs-keyword">if</span> (FD_ISSET(i, &amp;rset)) &#123;<br>         <span class="hljs-comment">//有数据可读，进行读数据处理</span><br>       &#125;<br>   &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>虽然 select 函数可以一次性监听多个 socket，但是当有 socket 就绪时，需要遍历来查找就绪套接字。</p><h2 id="poll"><a href="#poll" class="headerlink" title="poll"></a>poll</h2><p>Poll 机制的核心函数是 poll</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/*</span><br><span class="hljs-comment">   @Param __fds  包括套接字fd的一个结构体数组</span><br><span class="hljs-comment">   @Param __nfds __fds 数组的元素个数</span><br><span class="hljs-comment">   @Param __timeout    阻塞等待的超时时间</span><br><span class="hljs-comment">*/</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">poll</span> <span class="hljs-params">(<span class="hljs-keyword">struct</span> pollfd *__fds, <span class="hljs-type">nfds_t</span> __nfds, <span class="hljs-type">int</span> __timeout)</span>;<br></code></pre></td></tr></table></figure><p>pollfd 的定义是这样的:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">pollfd</span> &#123;</span><br>    <span class="hljs-type">int</span> fd;         <span class="hljs-comment">//进行监听的文件描述符</span><br>    <span class="hljs-type">short</span> <span class="hljs-type">int</span> events;        <span class="hljs-comment">//要监听的事件类型</span><br>    <span class="hljs-type">short</span> <span class="hljs-type">int</span> revents;       <span class="hljs-comment">//实际发生的事件类型</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>Poll 机制的主要流程如下：</p><ul><li>创建 pollfd 数组和监听套接字，并进行绑定</li><li>将监听套接字加入 pollfd 数组，并设置其监听读事件，也就是客户端的连接请求</li><li>循环调用 poll 函数，检测 pollfd 数组中是否有就绪的文件描述符。<ul><li>如果是连接套接字就绪，这表明是有客户端连接，我们可以调用 accept 接受连接，并创建已连接套接字，并将其加入 pollfd 数组，并监听读事件；</li><li>如果是已连接套接字就绪，这表明客户端有读写请求，我们可以调用 recv&#x2F;send 函数处理读写请求。</li></ul></li></ul><p>流程图：</p><p><img src="/img/image-20260428102727020.png" alt="image-20260428102727020"></p><p>示例代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> sock_fd,conn_fd; <span class="hljs-comment">//监听套接字和已连接套接字的变量</span><br>sock_fd = socket() <span class="hljs-comment">//创建套接字</span><br>bind(sock_fd)   <span class="hljs-comment">//绑定套接字</span><br>listen(sock_fd) <span class="hljs-comment">//在套接字上进行监听，将套接字转为监听套接字</span><br><br><span class="hljs-comment">//poll函数可以监听的文件描述符数量，可以大于1024</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> MAX_OPEN = 2048</span><br><br><span class="hljs-comment">//pollfd结构体数组，对应文件描述符</span><br><span class="hljs-keyword">struct</span> pollfd client[MAX_OPEN];<br><br><span class="hljs-comment">//将创建的监听套接字加入pollfd数组，并监听其可读事件</span><br>client[<span class="hljs-number">0</span>].fd = sock_fd;<br>client[<span class="hljs-number">0</span>].events = POLLRDNORM; <br>maxfd = <span class="hljs-number">0</span>;<br><br><span class="hljs-comment">//初始化client数组其他元素为-1</span><br><span class="hljs-keyword">for</span> (i = <span class="hljs-number">1</span>; i &lt; MAX_OPEN; i++)<br>    client[i].fd = <span class="hljs-number">-1</span>; <br><br><span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>) &#123;<br>   <span class="hljs-comment">//调用poll函数，检测client数组里的文件描述符是否有就绪的，返回就绪的文件描述符个数</span><br>   n = poll(client, maxfd+<span class="hljs-number">1</span>, &amp;timeout);<br>   <span class="hljs-comment">//如果监听套件字的文件描述符有可读事件，则进行处理</span><br>   <span class="hljs-keyword">if</span> (client[<span class="hljs-number">0</span>].revents &amp; POLLRDNORM) &#123;<br>       <span class="hljs-comment">//有客户端连接；调用accept函数建立连接</span><br>       conn_fd = accept();<br><br>       <span class="hljs-comment">//保存已建立连接套接字</span><br>       <span class="hljs-keyword">for</span> (i = <span class="hljs-number">1</span>; i &lt; MAX_OPEN; i++)&#123;<br>         <span class="hljs-keyword">if</span> (client[i].fd &lt; <span class="hljs-number">0</span>) &#123;<br>           client[i].fd = conn_fd; <span class="hljs-comment">//将已建立连接的文件描述符保存到client数组</span><br>           client[i].events = POLLRDNORM; <span class="hljs-comment">//设置该文件描述符监听可读事件</span><br>           <span class="hljs-keyword">break</span>;<br>          &#125;<br>       &#125;<br>       maxfd = i; <br>   &#125;<br>   <br>   <span class="hljs-comment">//依次检查已连接套接字的文件描述符</span><br>   <span class="hljs-keyword">for</span> (i = <span class="hljs-number">1</span>; i &lt; MAX_OPEN; i++) &#123;<br>       <span class="hljs-keyword">if</span> (client[i].revents &amp; (POLLRDNORM | POLLERR)) &#123;<br>         <span class="hljs-comment">//有数据可读或发生错误，进行读数据处理或错误处理</span><br>       &#125;<br>   &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>和 select 函数相比，poll 函数的改进之处主要就在于，它允许一次监听超过 1024 个文件描述符。但是当调用了 poll 函数后，我们仍然需要遍历每个文件描述符，检测该描述符是否就绪，然后再进行处理。</p><h2 id="epoll"><a href="#epoll" class="headerlink" title="epoll"></a>epoll</h2><p>epoll 机制就稍微复杂些了</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">union</span> <span class="hljs-title">epoll_data</span></span><br><span class="hljs-class">&#123;</span><br>  ...<br>  <span class="hljs-type">int</span> fd;  <span class="hljs-comment">//记录文件描述符</span><br>  ...<br>&#125; <span class="hljs-type">epoll_data_t</span>;<br><br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">epoll_event</span></span><br><span class="hljs-class">&#123;</span><br>  <span class="hljs-type">uint32_t</span> events;  <span class="hljs-comment">// epoll 监听的事件类型</span><br>  <span class="hljs-type">epoll_data_t</span> data; <span class="hljs-comment">// 应用程序数据</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>对于 epoll 机制来说，我们则需要先调用 epoll_create 函数，创建一个 epoll 实例。这个 epoll 实例内部维护了两个结构，分别是记录<strong>要监听的文件描述符</strong>和<strong>已经就绪的文件描述符</strong>，而对于已经就绪的文件描述符来说，它们会被返回给用户程序进行处理，而<code>不需要用户遍历查找</code>。</p><p>epoll 机制中有以下几个重要函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 创建一个 epoll 的句柄(通过它操作 epoll 实例对象),size 参数已经没有实际意义了</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">epoll_create</span><span class="hljs-params">(<span class="hljs-type">int</span> size)</span>;<br><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">  对指定描述符 fd 执行 op 操作</span><br><span class="hljs-comment">  @Param epfd epoll_create(int size)函数返回的 epoll 实例句柄</span><br><span class="hljs-comment">  @Param op 操作标识(添加、删除、修改)</span><br><span class="hljs-comment">  @Param fd 需要监听的那个描述符</span><br><span class="hljs-comment">  @Param event 需要监听的描述符对应的事件</span><br><span class="hljs-comment">*/</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">epoll_ctl</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-type">int</span> op, <span class="hljs-type">int</span> fd, <span class="hljs-keyword">struct</span> epoll_event *event)</span>;<br><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">  等待 epfd 上的 io 事件，最多返回 maxevents 个事件。</span><br><span class="hljs-comment">  @Param epfd epoll_create(int size)函数返回的 epoll 实例句柄</span><br><span class="hljs-comment">  @Param events 存放返回的就绪事件</span><br><span class="hljs-comment">  @Param maxevents 最多返回的事件数,也是标识 events 的容量</span><br><span class="hljs-comment">  @Param timeout 超时时间（毫秒，正整数时间，0是非阻塞，-1 永久阻塞直到事件发生）</span><br><span class="hljs-comment">*/</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">epoll_wait</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-keyword">struct</span> epoll_event * events, <span class="hljs-type">int</span> maxevents, <span class="hljs-type">int</span> timeout)</span>;<br></code></pre></td></tr></table></figure><blockquote><p>epoll_create</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-number">1.</span> <span class="hljs-type">int</span> <span class="hljs-title function_">epoll_create</span><span class="hljs-params">(<span class="hljs-type">int</span> size)</span>;<br>创建一个 epoll 的句柄，size 用来告诉内核这个监听的数目一共有多大，在<span class="hljs-number">2.6</span>.<span class="hljs-number">8</span>之后这个参数就没有实际价值了，因为内核维护一个动态的队列了。<br><br>当创建好 epoll 句柄后，它就会占用一个 fd 值，在 linux下 如果查看/proc/进程id/fd/，是能够看到这个 fd 的，所以在使用完 epoll 后，必须调用 close() 关闭，否则可能导致 fd 被耗尽。当某一进程调用 epoll_create 方法时，Linux内核会创建一个 eventpoll 结构体，这个结构体中有两个成员与 epoll 的使用方式密切相关。<br>    <br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">eventpoll</span> &#123;</span><br>    <span class="hljs-comment">// 红黑树的根节点，这棵树中存储着所有添加到epoll中的需要监控的事件</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">rb_root</span> <span class="hljs-title">rbr</span>;</span><br>    <span class="hljs-comment">// 双链表中则存放着将要通过epoll_wait返回给用户的满足条件的事件</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">rdlist</span>;</span><br>    ...<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>在使用完 epoll 后，必须调用close()关闭，否则可能导致 fd 被耗尽。</li></ul><blockquote><p>epoll_ctl</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-number">2.</span> <span class="hljs-type">int</span> <span class="hljs-title function_">epoll_ctl</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-type">int</span> op, <span class="hljs-type">int</span> fd, <span class="hljs-keyword">struct</span> epoll_event *event)</span>;<br>用于向内核注册新的描述符或者是改变某个文件描述符的状态。已注册的描述符在内核中会被维护在一棵红黑树上<br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">eventpoll</span> &#123;</span><br>    <span class="hljs-comment">// 红黑树的根节点，这棵树中存储着所有添加到epoll中的需要监控的事件</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">rb_root</span> <span class="hljs-title">rbr</span>;</span><br>    <span class="hljs-comment">// 双链表中则存放着将要通过epoll_wait返回给用户的满足条件的事件</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">rdlist</span>;</span><br>    ...<br>&#125;<br><br>每一个 epoll 对象都有一个独立的 eventpoll 结构体，用于存放通过 epoll_ctl 方法向 epoll 对象中添加进来的事件。这些事件都会挂载在红黑树中，如此，重复添加的事件就可以通过红黑树而高效的识别出来(红黑树的插入时间效率是lgn，其中n为树的高度)。<br></code></pre></td></tr></table></figure><ul><li><code>所有添加到 epoll 中的事件都会与设备(网卡)驱动程序建立回调关系</code>，也就是说，当相应的事件发生时会调用这个回调方法。这个<code>回调方法在内核中叫 **ep_poll_callback**,它会将发生的事件添加到 rdlist 双链表中</code>。</li></ul><blockquote><p>epoll_wait</p></blockquote><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-number">3.</span> <span class="hljs-type">int</span> <span class="hljs-title function_">epoll_wait</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-keyword">struct</span> epoll_event * events, <span class="hljs-type">int</span> maxevents, <span class="hljs-type">int</span> timeout)</span>;<br><br>等待 epfd 上的 io 事件，最多返回 maxevents 个事件。<br>通过回调函数内核会将 I/O 准备好的描述符添加到 rdlist 双链表管理，进程调用 epoll_wait() 便可以得到事件完成的描述符。参数 events 用来从内核得到事件的集合，maxevents 告之内核这个 events 有多大，参数 timeout 是超时时间（毫秒，正整数时间，<span class="hljs-number">0</span> 是非阻塞，<span class="hljs-number">-1</span> 永久阻塞直到事件发生）。该函数返回需要处理的事件数目，如返回 <span class="hljs-number">0</span> 表示已超时。<br><br>当然epoll对文件描述符的操作有两种模式：LT(level trigger)（默认）和ET (edge trigger)。LT模式是默认模式。<br></code></pre></td></tr></table></figure><p>流程图：</p><p><img src="/img/image-20260428102848504.png" alt="image-20260428102848504"></p><p>示例代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> sock_fd,conn_fd; <span class="hljs-comment">//监听套接字和已连接套接字的变量</span><br>sock_fd = socket() <span class="hljs-comment">//创建套接字</span><br>bind(sock_fd)   <span class="hljs-comment">//绑定套接字</span><br>listen(sock_fd) <span class="hljs-comment">//在套接字上进行监听，将套接字转为监听套接字</span><br>    <br>epfd = epoll_create(EPOLL_SIZE); <span class="hljs-comment">//创建epoll实例，</span><br><span class="hljs-comment">//创建epoll_event结构体数组，保存套接字对应文件描述符和监听事件类型    </span><br>ep_events = (epoll_event*)<span class="hljs-built_in">malloc</span>(<span class="hljs-keyword">sizeof</span>(epoll_event) * EPOLL_SIZE);<br><br><span class="hljs-comment">//创建epoll_event变量</span><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">epoll_event</span> <span class="hljs-title">ee</span></span><br><span class="hljs-class">//监听读事件</span><br><span class="hljs-class"><span class="hljs-title">ee</span>.<span class="hljs-title">events</span> =</span> EPOLLIN;<br><span class="hljs-comment">//监听的文件描述符是刚创建的监听套接字</span><br>ee.data.fd = sock_fd;<br><br><span class="hljs-comment">//将监听套接字加入到监听列表中    </span><br>epoll_ctl(epfd, EPOLL_CTL_ADD, sock_fd, &amp;ee); <br>    <br><span class="hljs-keyword">while</span> (<span class="hljs-number">1</span>) &#123;<br>   <span class="hljs-comment">//等待返回已经就绪的描述符 </span><br>   n = epoll_wait(epfd, ep_events, EPOLL_SIZE, <span class="hljs-number">-1</span>); <br>   <span class="hljs-comment">//遍历所有就绪的描述符     </span><br>   <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++) &#123;<br>       <span class="hljs-comment">//如果是监听套接字描述符就绪，表明有一个新客户端连接到来 </span><br>       <span class="hljs-keyword">if</span> (ep_events[i].data.fd == sock_fd) &#123; <br>          conn_fd = accept(sock_fd); <span class="hljs-comment">//调用accept()建立连接</span><br>          ee.events = EPOLLIN;  <br>          ee.data.fd = conn_fd;<br>          <span class="hljs-comment">//添加对新创建的已连接套接字描述符的监听，监听后续在已连接套接字上的读事件      </span><br>          epoll_ctl(epfd, EPOLL_CTL_ADD, conn_fd, &amp;ee); <br>                <br>       &#125; <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">//如果是已连接套接字描述符就绪，则可以读数据</span><br>           ...<span class="hljs-comment">//读取数据并处理</span><br>       &#125;<br>   &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>JDK 的 epoll 实现可以看另一篇文章：<a href="https://zhuwenjie0716.github.io/2026/04/21/Linux-JDK-NIO%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/">Linux-JDK-NIO 源码分析</a>。</p><h2 id="对比-小结"><a href="#对比-小结" class="headerlink" title="对比 &amp; 小结"></a>对比 &amp; 小结</h2><p>select</p><ul><li><p>优点</p><ul><li>select 目前几乎在所有的平台(POSIX)上支持，具有良好的跨平台支持。</li></ul></li><li><p>缺点</p><ul><li>单个进程能够监视的文件描述符的数量存在最大限制，它由 FD_SETSIZE 设置，默认值是1024。虽然能够修改宏定义或重新编译内核等进行修改，但可能造成效率的降低。</li><li>fd 集合在内核被置位过，与传入的 fd 集合不同，不可重用。（内核在发现对应的 fd 有事件后，可能会直接修改 fd 的一些标志，导致 fd 与传入的时候不同，再次调用 select 前需要恢复）。</li><li>调用时 fd 集合需要从用户态拷贝到内核态，当 fd 很多时开销会很大。</li><li>需要遍历寻找发生事件的 fd 集合，当 fd 很多时开销会很大。</li></ul></li></ul><p>poll</p><ul><li>优点<ul><li>利用 pollfd 数组可以监视的文件描述符无上限，解决了 select 的缺点1。</li><li>每次在扫描完成后，只需要重置 pollfd 结构体中的 revents 即可复原，解决了 select 的缺点2。</li></ul></li><li>缺点<ul><li>调用时 pollfd 数组需要从用户态拷贝到内核态，当 fd 很多时开销会很大。</li><li>需要遍历 pollfd 数组寻找发生事件的 fd 集合，当 fd 很多时开销会很大。</li></ul></li></ul><p>epoll</p><ul><li>优点<ul><li>灵活，没有描述符限制。epoll 使用一个文件描述符管理多个描述符，将用户关系的文件描述符的事件存放到内核的一个事件表中，这样在用户空间和内核空间的 copy 只需一次。</li><li>无需遍历，当调用 epoll_wait 检查是否有事件发生时，只需要检查 eventpoll 对象中的 rdlist 双链表中是否有 epitem 元素即可。如果 rdlist 不为空，则把发生的事件复制到用户态，同时将事件数量返回给用户 。</li></ul></li><li>缺点<ul><li>每次只遍历活跃的 fd (如果是 LT，也会遍历先前活跃的 fd)，在活跃 fd 较少的情况下就会很有优势，如果大部分 fd 都是活跃的，epoll 的效率可能还不如 select&#x2F;poll（红黑树结构维护，就绪链表的复杂性）。</li></ul></li></ul><p><img src="/img/image-20260428102947947.png" alt="image-20260428102947947"></p><p>关于 epoll 触发模式 (LT(level trigger) 和 ET (edge trigger)。LT模式是默认模式），可以参考这篇文章：<a href="https://developer.aliyun.com/article/1541647">水平触发（LT）和边缘触发模式（ET）详解</a>。</p>]]></content>
    
    
    
    <tags>
      
      <tag>IO模型</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>详解5种IO模型</title>
    <link href="/2026/04/28/%E8%AF%A6%E8%A7%A35%E7%A7%8DIO%E6%A8%A1%E5%9E%8B/"/>
    <url>/2026/04/28/%E8%AF%A6%E8%A7%A35%E7%A7%8DIO%E6%A8%A1%E5%9E%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="详解五种IO模型"><a href="#详解五种IO模型" class="headerlink" title="详解五种IO模型"></a>详解五种IO模型</h1><p>IO 模型是网络编程的基础，几乎所有的高性能的中间件都会提到使用了高效的 IO 模型（Redis、Kafka、Tomcat、Nginx 等）。</p><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Unix 系统下的五种基本 I&#x2F;O 模型大家应该都有所耳闻，分别是：</p><ul><li>blocking I&#x2F;O（同步阻塞IO，BIO）</li><li>nonblocking I&#x2F;O(同步非阻塞IO，NIO)</li><li>I&#x2F;O multiplexing (I&#x2F;O多路复用)</li><li>signal driven I&#x2F;O（信号驱动I&#x2F;O）</li><li>asynchronous I&#x2F;O（异步I&#x2F;O，AIO）</li></ul><p>一次网络 IO 的流程如下：</p><p><img src="/img/a7c537ec3f863e5225042b7135277b1a.png" alt="image-20260427203121800"></p><p>真正的I&#x2F;O过程，主要分为下面两个阶段，所有 I&#x2F;O 模型的区别就在这两个阶段上。</p><ul><li><strong>用户线程等待内核将数据从网卡拷贝到内核空间。</strong></li><li><strong>内核将数据从内核空间拷贝到用户空间。</strong></li></ul><h2 id="同步阻塞-IO"><a href="#同步阻塞-IO" class="headerlink" title="同步阻塞 IO"></a>同步阻塞 IO</h2><p>用户线程发起 read 调用后就阻塞了，让出 CPU。内核等待网卡数据到来，把数据从网卡拷贝到内核空间，接着把数据拷贝到用户空间，再把用户线程叫醒。</p><p><img src="/img/3f8d8d99b0e6aa0c066544cffce9e41e.png" alt="image-20260427203732818"></p><p>多线程可以提高 BIO 的吞吐量，因为在阻塞等待数据返回用户态的过程中 CPU 可以执行其它线程的任务。</p><p>一般为了避免过多的创建线程占据系统资源，系统中会使用“线程池”或者“连接池”减少创建和销毁线程的频率，让空闲的线程重新承担新的执行任务，维持一个合理的线程数量，降低系统开销。</p><p>但由于 BIO 每个线程负责一份 IO 请求的特性，当 IO 请求数量过大时就无能为力了～</p><p>BIO 在 IO 的两阶段都是阻塞的。</p><h2 id="同步非阻塞-IO"><a href="#同步非阻塞-IO" class="headerlink" title="同步非阻塞 IO"></a>同步非阻塞 IO</h2><p>用户线程不断的发起 read 调用，数据没到内核空间时，每次都返回失败，直到数据到了内核空间，这一次 read 调用后，在等待数据从内核空间拷贝到用户空间这段时间里，线程还是阻塞的，等数据到了用户空间再把线程叫醒。</p><p><img src="/img/96b7610b65cd8b0811045eed0e1e726d.png" alt="image-20260427204809951"></p><p>所以在非阻塞式 IO 中，用户进程其实是需要不断地主动询问 kernel 数据准备好了没有，但是这样采用轮询方式，会导致系统上下文切换开销很大，大幅度提高 CPU 占用率。</p><p>因此，<strong>单独使用非阻塞 I&#x2F;O 模型的效率并不高</strong>。而且随着并发量的提升，非阻塞 I&#x2F;O 会存在严重的性能浪费。</p><p>该模型轮询的目的只是检测“单个 IO 数据是否已经就绪”，操作系统提供了更为高效的检测接口，一次检测多个 IO 数据是否已经就绪，这就是接下来的 IO 多路复用。</p><p>同步非阻塞 IO 是一阶段非阻塞，二阶段阻塞。</p><h2 id="多路复用-IO"><a href="#多路复用-IO" class="headerlink" title="多路复用 IO"></a>多路复用 IO</h2><p>多路复用实现了一个线程处理多个 I&#x2F;O 句柄的操作，这种 IO 方式也被称为事件驱动 IO(event driven IO)。</p><ul><li>多路 指的是多个数据通道</li><li>复用 指的是使用一个或多个固定线程来处理每一个 Socket。</li></ul><p>大名鼎鼎的 select、poll、epoll 就是该模式的不同实现。</p><p><img src="/img/5cd727e048b681ee5efae04f2f140ed0.png" alt="image-20260427205743353"></p><p>多个的进程的 IO 可以注册到一个复用器（selector）上，然后用一个进程调用 select，select 会监听所有注册进来的 IO。</p><p>如果 selector 所有监听的 IO 在内核缓冲区都没有可读数据，select 调用进程会被阻塞（也可以设置超时时间，超过后就返回）；同时，kernel 会“监视”本次 select 负责的 socket，如果任何一个 socket 中的数据准备好了，select 就会返回；之后就由业务进程对有 IO 事件发生的 socket 进行处理了。</p><p><strong>多路复用 IO 模型只有一个 select 调用进程被阻塞</strong>，它也是非阻塞 IO，因为用户线程阻塞在 select 方法上，不像其他 IO 阻塞在 read write 方法调用。</p><p>多路复用解决了同步阻塞 I&#x2F;O 和同步非阻塞 I&#x2F;O 最大的问题（即并发量上来后也无需创建多个进程），单个 process 就可以同时处理多个网络连接的 IO。不过如果处理的 IO 数不多的情况下，使用多路复用 IO 的 web server 不一定比使用 池化+BIO 的 web server 性能更好，可能延迟还更大。考虑极端情况下，只有一个IO，多路复用需要 2 次系统调用（select + recvfrom），而BIO只需要 1 次系统调用（recvfrom）。</p><p>所以，<strong>多路复用 IO 的优势并不是对于单个连接能处理得更快，而是在于能处理更多的连接。</strong></p><h2 id="信号驱动-IO"><a href="#信号驱动-IO" class="headerlink" title="信号驱动 IO"></a>信号驱动 IO</h2><p>在使用信号驱动 I&#x2F;O 时，当数据准备就绪后，内核通过发送一个 SIGIO 信号通知应用进程，应用进程就可以开始读取数据了。相比同步非阻塞 IO 来说，它<strong>不需要线程不断去轮询数据是否已经准备就绪</strong>。</p><p><img src="/img/22e7129ad7beccae34c1ba9fc32c360a.png" alt="image-20260427212636343"></p><p>注意在“数据由内核空间拷贝到用户空间”阶段，它仍然是“阻塞”的。</p><h2 id="异步-IO"><a href="#异步-IO" class="headerlink" title="异步 IO"></a>异步 IO</h2><p>用户线程发起 read 调用的同时注册一个回调函数，read 立即返回，等内核将数据准备好后，再调用指定的回调函数完成处理。在这个过程中，用户线程一直没有阻塞。</p><p><img src="/img/e6b08315adff2a4240ca486beba06910.png" alt="image-20260427210340277"></p><p>AIO最重要的一点是 <strong>从内核缓冲区拷贝数据到用户态缓冲区的过程也是由系统异步完成</strong>，应用进程只需要在指定的数组中引用数据即可。</p><p>AIO 与信号驱动 I&#x2F;O 的主要区别：信号驱动 I&#x2F;O 由内核通知何时可以开始一个 I&#x2F;O 操作，而异步 I&#x2F;O 由内核通知 I&#x2F;O 操作何时已经完成。</p><p><strong>AIO 是真正的异步模型，它不会对请求进程产生任何的阻塞。</strong></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><blockquote><p>阻塞与非阻塞</p></blockquote><p>日常使用过程中，我们往往把 同步I&#x2F;O 等同于 阻塞I&#x2F;O，异步I&#x2F;O 等同于 非阻塞I&#x2F;O，但严格意义来说，这两组概念还是有很大的区别的。</p><p>结合 I&#x2F;O模型 来说，阻塞I&#x2F;O 会一直 block 对应的进程直到操作完成，而 非阻塞IO 在”等待数据从网卡拷贝到内核”阶段会立刻返回。</p><p>所以我们一般认为，阻塞I&#x2F;O 只有 BIO，另外四个模型都是属于 非阻塞I&#x2F;O。</p><blockquote><p>同步与异步</p></blockquote><p>根据 POSIX 的定义：</p><ul><li>同步I&#x2F;O : A synchronous I&#x2F;O operation causes the requesting process to be blocked until that I&#x2F;O operation completes;</li><li>异步I&#x2F;O : An asynchronous I&#x2F;O operation does not cause the requesting process to be blocked;</li></ul><p>两者的区别就在于 同步I&#x2F;O 做 “IO operation” 的时候会将 process 阻塞。</p><p>那么按照这个定义，BIO，NIO，IO多路复用、信号驱动IO 四种模型都属于 同步IO。因为它们在 IO 的第二阶段，真正执行“数据拷贝”的阶段，都是“阻塞”的。</p>]]></content>
    
    
    
    <tags>
      
      <tag>IO模型</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之锁篇</title>
    <link href="/2026/04/27/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E9%94%81%E7%AF%87/"/>
    <url>/2026/04/27/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E9%94%81%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之锁篇"><a href="#系统性能优化之锁篇" class="headerlink" title="系统性能优化之锁篇"></a>系统性能优化之锁篇</h1><p>多线程下为了确保程序不会出错，必须加锁后才能访问共享资源。锁也是有层级之分的，在不同的场景下加不同的锁，对系统性能也有着很大影响。</p><h2 id="互斥锁与自旋锁"><a href="#互斥锁与自旋锁" class="headerlink" title="互斥锁与自旋锁"></a>互斥锁与自旋锁</h2><p>互斥锁与自旋锁是最底层的两个锁，其他的锁几乎都是基于此实现的。**<code>互斥锁加锁失败时会释放CPU进入阻塞状态，自旋锁则会忙等待</code>**。</p><p>**<code>当无法确定锁住的代码需要执行多久时，应当首选互斥锁。</code>**互斥锁是一种独占锁，当被线程取得后，除非线程释放，其他线程的取锁代码都会阻塞。阻塞通常是由操作系统内核实现的，取锁失败时将线程置于休眠状态，当锁释放后寻找合适的时机唤醒线程。</p><p><img src="/img/image-20250625105013432.png" alt="image-20250625105013432"></p><p>但是线程取锁失败时的阻塞与唤醒增加了上下文切换的成本，或许这段时间比锁住的代码段执行时间还长。而且，线程主动进入休眠是高并发服务无法容忍的行为，这让其他异步请求都无法执行。</p><p><strong><code>如果被锁住的代码执行时间很短，就应该用自旋锁取代互斥锁。</code></strong></p><p>自旋锁比互斥锁快得多，因为它通过 CPU 提供的 CAS 函数（全称 Compare And Swap），在用户态代码中完成加锁与解锁操作。它将比较与交换合成为一条硬件级原子指令，避免了多线程下的可能出现的问题。</p><p>假设锁为变量 lock，整数 0 表示锁是空闲状态，整数 pid 表示线程 ID，那么 CAS(lock, 0, pid) 就表示自旋锁的加锁操作，CAS(lock, pid, 0) 则表示解锁操作。<code>多线程竞争锁的时候，加锁失败的线程会进入“忙等待”，但这并不意味着一直执行 CAS 函数</code>，生产级的自旋锁在“忙等待”时，会与 CPU 紧密配合 ，它通过 CPU 提供的 PAUSE 指令，减少循环等待时的耗电量；<code>对于单核 CPU，忙等待并没有意义，此时它会主动把线程休眠</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>  <span class="hljs-comment">// 因为判断lock变量的值比CAS操作更快，所以先判断 lock 再调用 CAS 效率更高</span><br>  <span class="hljs-keyword">if</span> (lock == <span class="hljs-number">0</span> &amp;&amp;  CAS(lock, <span class="hljs-number">0</span>, pid) == <span class="hljs-number">1</span>) <span class="hljs-keyword">return</span>;<br>  <br>  <span class="hljs-keyword">if</span> (CPU_count &gt; <span class="hljs-number">1</span> ) &#123; <span class="hljs-comment">//如果是多核CPU，“忙等待”才有意义</span><br>      <span class="hljs-keyword">for</span> (n = <span class="hljs-number">1</span>; n &lt; <span class="hljs-number">2048</span>; n &lt;&lt;= <span class="hljs-number">1</span>) &#123;<span class="hljs-comment">// pause 的时间，应当越来越长</span><br>        <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; n; i++) pause();<span class="hljs-comment">// CPU 专为自旋锁设计了 pause 指令</span><br>        <span class="hljs-keyword">if</span> (lock == <span class="hljs-number">0</span> &amp;&amp; CAS(lock, <span class="hljs-number">0</span>, pid)) <span class="hljs-keyword">return</span>;<span class="hljs-comment">// pause后再尝试获取锁</span><br>      &#125;<br>  &#125;<br>    <br>  <span class="hljs-comment">// 单核 CPU，或者长时间不能获取到锁，应主动休眠，让出CPU </span><br>  sched_yield();<br>&#125;<br></code></pre></td></tr></table></figure><p>当取不到锁时，互斥锁用“线程切换”来面对，自旋锁则用“忙等待”来面对。<strong>这是两种最基本的处理方式，更高级别的锁都会选择其中一种来实现，比如读写锁就既可以基于互斥锁实现，也可以基于自旋锁实现。</strong></p><h2 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h2><p>如果项目中能明确的区分出读写场景，则可以选择读写锁。</p><p>读写锁的优势在于：当写锁未被持有时，读锁可以被多个线程并发持有，提高了资源的使用率。而当写锁被持有后，获取读锁和写锁的线程都会被阻塞。写锁是独占锁，读锁是共享锁。</p><p><strong>因此，读写锁真正发挥优势的场景，必然是读多写少的场景，否则读锁将很难并发持有。</strong></p><p>实际上，读写锁既可以倾向于读线程，又可以倾向于写线程。前者我们称为读优先锁，后者称为写优先锁。</p><p>读优先锁更强调效率，它期待锁能被更多的线程持有：当读锁被持有时，先来一个线程希望取写锁，又来一个线程希望取读锁，即使取写锁的线程先来，后续前来获取读锁的线程依然可以立刻加锁成功，因为这样两个线程在并发持有锁，效率更高。当然如果有源源不断的读线程，则会导致写线程饥饿。</p><p><img src="/img/image-20250625111249735.png" alt="image-20250625111249735"></p><p>写优先锁由于写锁的优先级更高，当有源源不断的写线程，则会导致读线程饥饿。</p><p><img src="/img/image-20250625111309693.png" alt="image-20250625111309693"></p><p>除此之外还有<code>读写公平锁，按照请求顺序排队，读线程依然可以并发持有读锁，但是无法“加队”到写线程前</code>。</p><h2 id="乐观锁"><a href="#乐观锁" class="headerlink" title="乐观锁"></a>乐观锁</h2><p>无论互斥锁、自旋锁还是读写锁，都属于悲观锁。“乐观”，就是假定冲突的概率很低，所以它采用的“加锁”方式是，<code>先修改完共享资源，再验证这段时间内有没有发生冲突</code>。如果没有其他线程在修改资源，那么操作完成。如果发现其他线程已经修改了这个资源，就放弃本次操作。</p><p>至于放弃后如何重试，则与业务场景相关，虽然<code>重试的成本很高</code>，但出现冲突的概率足够低的话，还是可以接受的。可见，<strong>乐观锁全程并没有加锁，所以它也叫无锁编程。</strong></p><p>例如在线修改文档，可以记录下获取到当前文档时的版本号，在提交时携带原始版本号，如果期间有其他用户修改，则原始版本号无法对应，就放弃本次修改。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">update</span> #&#123;<span class="hljs-keyword">table</span>&#125; <span class="hljs-keyword">set</span> version <span class="hljs-operator">=</span> #&#123;newVersion&#125; <span class="hljs-keyword">where</span> id <span class="hljs-operator">=</span> #&#123;id&#125; <span class="hljs-keyword">and</span> version <span class="hljs-operator">=</span> #&#123;oldVersion&#125;<br></code></pre></td></tr></table></figure><p><img src="/img/image-20250625110845779.png" alt="image-20250625110845779"></p><p>乐观锁除了应用在 Web 分布式场景，在数据库等单机上也有广泛的应用。只是面向多线程时，最后的验证步骤是通过 CPU 提供的 CAS 操作完成的。</p><p>乐观锁虽然去除了锁操作，但是一旦发生冲突，重试的成本非常高。所以，<strong>只有在冲突概率非常低，且加锁成本较高时，才考虑使用乐观锁。</strong></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>互斥锁能够满足各类功能性要求，特别是被锁住的代码执行时间不可控时，它通过内核执行线程切换及时释放了资源，但它的性能消耗最大。如果能够确定被锁住的代码取到锁后很快就能释放，应该使用更高效的自旋锁。</p><p>如果能区分出读写场景，读写锁是第一选择，它允许多个读线程同时持有读锁，提高了并发性。</p><p>当并发访问共享资源，冲突概率非常低的时候，可以选择无锁编程。</p><p>总之，**<code>不管使用哪种锁，锁范围内的代码都应尽量的少，执行速度要快。</code>**</p>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>性能优化</tag>
      
      <tag>锁</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之磁盘篇</title>
    <link href="/2026/04/27/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E7%A3%81%E7%9B%98%E7%AF%87/"/>
    <url>/2026/04/27/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E7%A3%81%E7%9B%98%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之磁盘篇"><a href="#系统性能优化之磁盘篇" class="headerlink" title="系统性能优化之磁盘篇"></a>系统性能优化之磁盘篇</h1><p>磁盘作为计算机中速度最慢的硬件之一，常常是系统的性能瓶颈，优化磁盘一般能得到明显的提升~</p><p>文章前部分以如何高效的传输文件来讨论针对磁盘的优化技术，如零拷贝、直接 IO、异步 IO等，后部分介绍 SSD 和 HDD 的区别，探讨如何编写对 SSD 友好的程序。</p><blockquote><p>最简单的网络传输</p></blockquote><p>最简单的方式的当然是找到文件存储的路径，创建一个固定的大小的缓冲区，每次读取缓冲区大小的文件内容，再通过网络 API 发送给客户端，重复上述流程直到把文件发送完成。</p><p>顺便一提，缓冲区的设置还是有必要的，java 有单字节的读取写入方式（FileInputStream.read()），这样会很慢，缓冲区可以有效的提升读取文件的性能。</p><p>但是这样的性能依旧不是很高，原因是这需要进行多次的内容拷贝，会导致大量的上下文切换</p><p><img src="/img/image-20250624112007687.png" alt="image-20250624112007687"></p><ul><li>每次读取发送都经历了 4 次用户态与内核态的上下文切换</li><li>每次发送的缓存区内容被拷贝了 4 次</li></ul><p>接下来就从<code>减少上下文切换次数</code>和<code>减少内存拷贝次数</code>来进行优化</p><h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h2><p>读取磁盘或者操作网卡都由操作系统内核完成。内核负责管理系统上的所有进程，它的权限最高，工作环境与用户进程完全不同。<code>只要我们的代码执行 read 或者 write 这样的系统调用，一定会发生 2 次上下文切换</code>：首先从用户态切换到内核态，当内核执行完任务后，再切换回用户态交由进程代码执行。</p><p>因此<code>如果要减少上下文切换次数，就要减少系统调用次数</code>。解决方案就是把 read 和 write 系统调用合二为一，直接在内核中完成文件的读取和发送，可以把原本的 4 次上下文切换减少为 2 次。</p><p>那如何减少内存拷贝次数呢？对于常见的文件下载场景，我们并不需要进程对文件进行处理，如添加公司信息等，那么用户缓冲区就没有存在的必要，直接由 PageCache 拷贝到 Socket 缓冲区就可以，这可以把原本的 4 次内容拷贝缩短为 3 次，</p><p><img src="/img/image-20250624112749023.png" alt="image-20250624112749023"></p><p>如果 网卡支持 DMA <a href="https://blog.csdn.net/qq_56517253/article/details/128706625"> SG-DMA（The Scatter-Gather Direct Memory Access）</a>，那么 PageCache 可以直接拷贝到网卡，可以再减少一次内存拷贝。</p><p><img src="/img/image-20250624113104692.png" alt="image-20250624113104692"></p><p>其实这就是零拷贝技术，它是操作系统提供的新函数，<code>同时接收文件描述符和 TCP socket 作为输入参数</code>，这样<code>执行时就可以完全在内核态完成内存拷贝</code>，既减少了内存拷贝次数，也降低了上下文切换次数。</p><p>零拷贝使用户无需关心 socket 缓冲区的大小（因为 socket 缓冲区是动态变化的，它既用于 TCP 滑动窗口，也用于应用缓冲区，还受到整个系统内存的影响）。综合种种优点，零拷贝可以极大的提升文件传输性能。</p><h2 id="PageCache-磁盘高速缓存"><a href="#PageCache-磁盘高速缓存" class="headerlink" title="PageCache 磁盘高速缓存"></a>PageCache 磁盘高速缓存</h2><p>正常读取文件时，是先把磁盘文件拷贝到 PageCache 上，再拷贝到进程中。原因是磁盘的读取速度是最慢的，而在内存中的 PageCache 速度就会快很多，那选择哪些数据复制到内存呢？根据<code>时间局部性</code>原理（刚被访问的数据在短时间内再次被访问的概率很高），用 PageCache 缓存最近访问的数据，当空间不足时淘汰最久未被访问的缓存（即 LRU 算法）。读磁盘时优先到 PageCache 中找一找，如果数据存在便直接返回，这便大大提升了读磁盘的性能。</p><p>对于机械硬盘来说，需要旋转磁头到数据所在的扇区，再开始顺序读取数据。其中，旋转磁头耗时很长，为了降低它的影响，PageCache 使用了<strong>预读功能</strong>。也就是即使你可能目前只读 32KB 数据，但内核会把后续的部分数据也读取到 PageCache，因为这个读取成本很低，而如果后续一段时间访问到了这些数据，带来的收益是很值得的。</p><p>PageCache 在 90% 以上场景下都会提升磁盘性能，<strong>但在某些情况下，PageCache 会不起作用，甚至由于多做了一次内存拷贝，造成性能的降低。</strong></p><p>先说结论：<code>在读取大文件时，不应使用 PageCache，进而也不应使用零拷贝技术处理。</code></p><p>当用户访问大文件时，内核就会把它们载入到 PageCache 中，这些大文件很快会把有限的 PageCache 占满。一方面这些大文件被再次访问的概率其实很低，耗费 CPU 多拷贝到 PageCache 一次；另一方面大文件占用 PageCache 会导致热点小文件无法被加载到 PageCache 中，读取的速度变慢。</p><ul><li>比如视频文件通常按时间顺序播放，播放器只会按需加载一定长度的视频数据。虽然文件本身很大，但只有少部分数据会在某一时间点内被访问到，其余的数据部分在播放过程中可能根本不会被访问到。</li><li>某些大数据库文件也是按顺序或按块读取的，而数据库的查询操作通常集中在特定区域或范围内。大文件的其他部分可能在较长时间内不会被访问，导致它们在 PageCache 中的缓存效果差。</li></ul><p>那么高并发场景下该怎么处理大文件呢？</p><h2 id="异步-IO-直接-IO"><a href="#异步-IO-直接-IO" class="headerlink" title="异步 IO + 直接 IO"></a>异步 IO + 直接 IO</h2><p>高并发场景处理大文件时，应当使用异步 IO 和直接 IO 来替换零拷贝技术。</p><p>当调用 read 方法读取文件时，实际上 read 方法会在磁盘寻址过程中阻塞等待，导致进程无法并发地处理其他任务，如下图所示：</p><p><img src="/img/image-20250624132341046.png" alt="image-20250624132341046"></p><p>异步 IO（异步 IO 既可以处理网络 IO，也可以处理磁盘 IO，这里我们只关注磁盘 IO）把读操作分为两部分，前半部分向内核发起读请求，但<strong>不等待数据就位就立刻返回</strong>，此时进程可以并发地处理其他任务。当内核将磁盘中的数据拷贝到进程缓冲区后，进程将接收到内核的通知，再去处理数据，这是异步 IO 的后半部分。如下图所示：</p><p><img src="/img/image-20250624132423219.png" alt="image-20250624132423219"></p><p>异步 IO 并没有把数据拷贝到 PageCache 中，这其实是异步 IO 实现上的缺陷。<code>经过 PageCache 的 IO 我们称为缓存 IO</code>，它与虚拟内存系统耦合太紧，导致异步 IO 从诞生起到现在都不支持缓存 IO。<code>绕过 PageCache 的 IO 是个新物种，我们把它称为直接 IO</code>。对于磁盘，异步 IO 只支持直接 IO。</p><p>直接 IO 的应用场景为：</p><ul><li>应用程序已经实现了磁盘文件的缓存，不需要 PageCache 再次缓存，引发额外的性能消耗。比如 MySQL 等数据库就使用直接 IO</li><li>高并发下传输大文件</li></ul><p>缺点为无法享受 PageCache 造成的性能提升（内核会缓存尽量多的连续IO在 PageCache 中，合并为一个更大的 IO 发给磁盘，减少磁盘的寻址操作；内核也会<strong>预读</strong>后续的 IO 放在 PageCache 中，减少磁盘操作）。</p><h2 id="SSD-vs-HDD"><a href="#SSD-vs-HDD" class="headerlink" title="SSD vs HDD"></a>SSD vs HDD</h2><p>一般情况下把程序使用的 HDD（传统机械硬盘）换为 SSD 后吞吐量可以得到明显的提升，但 SSD 并不是 “更快的 HDD”，它的结构和 HDD 有明显区别。</p><p>一个硬盘的性能最主要体现在这三个指标：IOPS，带宽 &#x2F; 吞吐率和访问延迟。<strong>IOPS</strong> (Input&#x2F;Output Per Second) ，即每秒钟系统能处理的读写请求数量。<strong>吞吐率</strong>（Throughput）或者带宽（Bandwidth），衡量的是实际数据传输速率。<strong>访问延迟</strong>，指的是从发起 IO 请求到存储系统把 IO 处理完成的时间间隔。SSD 的随机 IO 延迟比传统硬盘快百倍以上，一般在微妙级别；IO 带宽也高很多倍，可以达到每秒几个 GB；随机 IOPS 更是快了上千倍，可以达到几十万。</p><p>对于传统硬盘，当应用程序发出硬盘 IO 请求后，这个请求会进入硬盘的 IO 队列。当轮到这个 IO 来存取数据时，磁头需要机械运动到数据存放的位置，这就需要磁头寻址到相应的磁道和旋转到相应的扇区，然后才是数据的传输。</p><p>对于 <a href="https://www.bilibili.com/video/BV1Da411a7SF/">SSD</a> 而言，它有<strong>单元（Cell）</strong>、<strong>页面（Page）</strong>、<strong>块（Block）</strong>这几个核心概念，Cell 是 Page 中的存储数据的基本单位（物理层面），但 CPU 和 SSD 控制器不能单独寻址一个 Cell，因此Page 是执行编程或读写操作的最小单元，一般为 4KB&#x2F;8KB&#x2F;16KB。SSD 上没有“重写”操作，<strong>不像 HDD 可以直接对任何字节重写覆盖</strong>。一个页面一旦写入内容后就不能进行部分重写，必须和其它相邻页面一起被整体擦除重置。Block 是可以被执行擦除的最小单元，包含多个 Page。</p><h3 id="IO-和垃圾回收"><a href="#IO-和垃圾回收" class="headerlink" title="IO 和垃圾回收"></a>IO 和垃圾回收</h3><p>对 SSD 的 IO 共有三种类型：读取、写入和擦除。读取和写入以页为单位。IO 写入的延迟具体取决于磁盘的历史状态，因为如果 SSD 已经存储了许多数据，那么对页的写入就经常需要移动已有的数据。一般的读写延迟都很低，在微秒级别，远远低于 HDD。擦除是以块为单位的。擦除速度相对很慢，通常为几毫秒。</p><p>所以对同步的 IO，发出 IO 的应用程序可能会因为块的擦除，而经历很大的写入延迟。为了尽量地减少这样的场景，<code>保持空闲块的阈值</code>对于快速的写响应是很有必要的。SSD 的垃圾回收（GC）的目的就在于此。GC 可以回收用过的块，这样可以确保以后的页写入可以快速分配到一个全新的页。</p><h3 id="写入放大（Write-Amplification"><a href="#写入放大（Write-Amplification" class="headerlink" title="写入放大（Write Amplification)"></a>写入放大（Write Amplification)</h3><p>这是 SSD 相对于 HDD 的一个缺点，即实际写入 SSD 的物理数据量，有可能是应用层写入数据量的多倍。一方面，页级别的写入需要移动已有的数据来腾空页面。另一方面，GC 的操作也会移动用户数据来进行块级别的擦除。所以对 SSD 真正的写操作的数据可能比实际写的数据量大，这就是写入放大。一块 SSD 只能进行有限的擦除次数，也称为编程 &#x2F; 擦除（P&#x2F;E）周期，所以写入放大效用会缩短 SSD 的寿命。</p><h3 id="耗损平衡-（Wear-Leveling）"><a href="#耗损平衡-（Wear-Leveling）" class="headerlink" title="耗损平衡 （Wear Leveling）"></a>耗损平衡 （Wear Leveling）</h3><p>数据有冷热之分，它们存储在不同的块上，对每一个块而言，一旦达到最大操作数量，该块就会死亡。对于 SLC 块，P&#x2F;E 周期的典型数目是十万次；对于 MLC 块，P&#x2F;E 周期的数目是一万；而对于 TLC 块，则可能是几千。</p><p>为了确保 SSD 的容量和性能，我们需要在擦除次数上保持平衡，SSD 控制器具有这种“耗损平衡”机制可以实现这一目标。在损耗平衡期间，数据在各个块之间移动，以实现均衡的损耗，这种机制也会对前面讲的写入放大推波助澜。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>零拷贝技术基于 PageCache（缓存最近读的数据），合并读取和发送的系统调用，能够有效减少传输文件过程中的上下文切换次数和内存拷贝次数，同时最大程度利用 socket 缓冲区。但缺点是用户进程无法对文件做任何修改，比如压缩后再发送。当文件大小超过某个阈值后，PageCache 还可能引发副作用，因此，实践中通常会<code>设定一个文件大小阈值，针对大文件使用异步 IO 和直接 IO，而对小文件使用零拷贝</code>( 例如 Nginx 的 directio 指令)。</p><p>文件传输场景中的优化可以大概分为三个方向：</p><ul><li>减少磁盘工作量（PageCache）</li><li>减少 CPU 工作量 （直接 IO）</li><li>提高内存利用率（零拷贝）</li></ul><p>在使用 SSD 时，遵循以下准则可以或者更好的性能：</p><ul><li><strong>使用多个并发线程来执行 IO</strong>。这利用了 SSD 的内部并行性，不过多线程也可能由于操作系统的预读和回写而产生线程竞争。多 IO 线程对 HDD 则毫无益处，因为 HDD 只有一个磁头。</li><li><strong>避免长而繁重的写入</strong>。原因是 SSD 的 GC 机制会在空闲块数量下降到阈值时后台启动，不断地回收存储块以供以后使用，但如果写入的速率远超 GC 速率，则将触发前台 GC，导致较大的应用程序 IO 延迟。</li><li><strong>避免 SSD 存储太满</strong>。GC 创建空闲块时，需要对数据进行压缩整理，但平均数据量 80% 时，需要压缩 5 个块获得一个空闲块，而当平均数据量 95% 时，压缩 20 个块才能获得一个块。</li><li><strong>减少就地更新</strong>。HDD 可以重写任意字节的数据，但 SSD 以页为操作单位的特性决定了其就地更新需要经历“读取-修改-写入”（RMW）步骤，而随机更新在空间使用率不满的情况下就较少引起读取和修改步骤（即仅仅“写入”），因此速度更快。</li></ul>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>性能优化</tag>
      
      <tag>磁盘</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Netty如何高效使用内存</title>
    <link href="/2026/04/27/Netty%E5%A6%82%E4%BD%95%E9%AB%98%E6%95%88%E4%BD%BF%E7%94%A8%E5%86%85%E5%AD%98/"/>
    <url>/2026/04/27/Netty%E5%A6%82%E4%BD%95%E9%AB%98%E6%95%88%E4%BD%BF%E7%94%A8%E5%86%85%E5%AD%98/</url>
    
    <content type="html"><![CDATA[<h1 id="Netty-如何高效使用内存"><a href="#Netty-如何高效使用内存" class="headerlink" title="Netty 如何高效使用内存"></a>Netty 如何高效使用内存</h1><p><a href="https://zhuwenjie0716.github.io/2026/04/24/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E5%86%85%E5%AD%98%E7%AF%87/">系统性能优化之内存篇</a> 中提到了如何更好的使用内存，本篇以 netty 为例赏析 netty 中的内存使用技巧。</p><h2 id="基本类型-vs-包装类"><a href="#基本类型-vs-包装类" class="headerlink" title="基本类型 vs 包装类"></a>基本类型 vs 包装类</h2><p>Java 中的对象在堆内存中的存储布局可以划分为三个部分：<code>对象头（Header）、实例数据（Instance Data）和对齐填充（Padding）</code>。在64位虚拟机中对象头就需要占用64bit的空间，比一些基本类型（byte、short、int等）占用的空间都要大。</p><p>因此<code>能用基本类型就不要用包装类</code>。</p><h2 id="变量的定义"><a href="#变量的定义" class="headerlink" title="变量的定义"></a>变量的定义</h2><p>一个类在程序执行中可能会创建多个对象，如果这个变量是实例变量，则每个类对象都会有一份，而如果<code>定义为类变量，则所有实例共享</code>。</p><p>Netty 作为网络通信框架，创建的 Channel 不可估量，需要统计每一个 channel 待发送的数据（判断写缓冲区是否处于“高水位”），这个字段会被 IO 线程消费减少，同时可能被业务线程生产增加，如果使用 AtomicLong 类型，则每个 channel 都会多一份对象头的开销。Netty 定义了一个 volatile 变量和一个静态的 AtomicLongFieldUpdater，大大节省了内存。</p><p>代码参见：io&#x2F;netty&#x2F;channel&#x2F;ChannelOutboundBuffer.java</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> AtomicLongFieldUpdater&lt;ChannelOutboundBuffer&gt; TOTAL_PENDING_SIZE_UPDATER;<br><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">long</span> totalPendingSize;<br></code></pre></td></tr></table></figure><h2 id="预估内存"><a href="#预估内存" class="headerlink" title="预估内存"></a>预估内存</h2><p>定义 HashMap、List 等可能扩容的对象时指定初始容量不必多说，Netty 会根据接受到的数据动态调整（guess）下个要分配的 Buffer 大小。</p><p>代码参见：io&#x2F;netty&#x2F;channel&#x2F;AdaptiveRecvByteBufAllocator.java（省略部分代码）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">HandleImpl</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MaxMessageHandle</span> &#123;<br>    <br>    <span class="hljs-keyword">private</span> <span class="hljs-type">int</span> nextReceiveBufferSize;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">int</span> <span class="hljs-title function_">guess</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> nextReceiveBufferSize;<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">record</span><span class="hljs-params">(<span class="hljs-type">int</span> actualReadBytes)</span> &#123;<br>        <span class="hljs-keyword">if</span> (actualReadBytes &lt;= SIZE_TABLE[Math.max(<span class="hljs-number">0</span>, index - INDEX_DECREMENT - <span class="hljs-number">1</span>)]) &#123;<br>            <span class="hljs-keyword">if</span> (decreaseNow) &#123;<br>                index = Math.max(index - INDEX_DECREMENT, minIndex);<br>                nextReceiveBufferSize = SIZE_TABLE[index];<br>                decreaseNow = <span class="hljs-literal">false</span>;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                decreaseNow = <span class="hljs-literal">true</span>;<br>            &#125;<br>        &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (actualReadBytes &gt;= nextReceiveBufferSize) &#123;<br>            index = Math.min(index + INDEX_INCREMENT, maxIndex);<br>            nextReceiveBufferSize = SIZE_TABLE[index];<br>            decreaseNow = <span class="hljs-literal">false</span>;<br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><h2 id="逻辑组合代替复制"><a href="#逻辑组合代替复制" class="headerlink" title="逻辑组合代替复制"></a>逻辑组合代替复制</h2><p>当读取 channel 中的数据时，如果有新的数据来到，通过 addComponent 将新数据添加到缓冲对象里，即只是新增一个引用，无需真正复制内存。</p><p>代码参见：io&#x2F;netty&#x2F;handler&#x2F;codec&#x2F;ByteToMessageDecoder.java</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">/**</span><br><span class="hljs-comment"> * Cumulate &#123;<span class="hljs-doctag">@link</span> ByteBuf&#125;s by add them to a &#123;<span class="hljs-doctag">@link</span> CompositeByteBuf&#125; and so do no memory copy whenever possible.</span><br><span class="hljs-comment"> * Be aware that &#123;<span class="hljs-doctag">@link</span> CompositeByteBuf&#125; use a more complex indexing implementation so depending on your use-case</span><br><span class="hljs-comment"> * and the decoder implementation this may be slower then just use the &#123;<span class="hljs-doctag">@link</span> #MERGE_CUMULATOR&#125;.</span><br><span class="hljs-comment"> */</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Cumulator</span> <span class="hljs-variable">COMPOSITE_CUMULATOR</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cumulator</span>() &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> ByteBuf <span class="hljs-title function_">cumulate</span><span class="hljs-params">(ByteBufAllocator alloc, ByteBuf cumulation, ByteBuf in)</span> &#123;<br>        ByteBuf buffer;<br>        <span class="hljs-keyword">if</span> (cumulation.refCnt() &gt; <span class="hljs-number">1</span>) &#123;<br>            <span class="hljs-comment">// Expand cumulation (by replace it) when the refCnt is greater then 1 which may happen when the user</span><br>            <span class="hljs-comment">// use slice().retain() or duplicate().retain().</span><br>            <span class="hljs-comment">//</span><br>            <span class="hljs-comment">// See:</span><br>            <span class="hljs-comment">// - https://github.com/netty/netty/issues/2327</span><br>            <span class="hljs-comment">// - https://github.com/netty/netty/issues/1764</span><br>            buffer = expandCumulation(alloc, cumulation, in.readableBytes());<br>            buffer.writeBytes(in);<br>            in.release();<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            CompositeByteBuf composite;<br>            <span class="hljs-keyword">if</span> (cumulation <span class="hljs-keyword">instanceof</span> CompositeByteBuf) &#123;<br>                composite = (CompositeByteBuf) cumulation;<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                composite = alloc.compositeBuffer(Integer.MAX_VALUE);<br>                composite.addComponent(<span class="hljs-literal">true</span>, cumulation);<br>            &#125;<br>            composite.addComponent(<span class="hljs-literal">true</span>, in);<br>            buffer = composite;<br>        &#125;<br>        <span class="hljs-keyword">return</span> buffer;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h2><p>“zero-copy”既是优化内存的手段，也是优化 IO 的手段，简单来说就是 IO 读操作需要把字节从内核态拷贝到用户态的应用程序中，写操作又需要拷贝回内核的缓冲区中。如果应用程序无需对文件进行操作，那么直接在内核态进行拷贝，就可以减少拷贝次数。</p><p>Netty 在 io&#x2F;netty&#x2F;channel&#x2F;DefaultFileRegion.java 中也是使用了 JDK 原生的零拷贝（transferTo）实现</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">long</span> <span class="hljs-title function_">transferTo</span><span class="hljs-params">(WritableByteChannel target, <span class="hljs-type">long</span> position)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-type">long</span> <span class="hljs-variable">count</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.count - position;<br>    <span class="hljs-keyword">if</span> (count &lt; <span class="hljs-number">0</span> || position &lt; <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>(<br>                <span class="hljs-string">&quot;position out of range: &quot;</span> + position +<br>                <span class="hljs-string">&quot; (expected: 0 - &quot;</span> + (<span class="hljs-built_in">this</span>.count - <span class="hljs-number">1</span>) + <span class="hljs-string">&#x27;)&#x27;</span>);<br>    &#125;<br>    <span class="hljs-keyword">if</span> (count == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-number">0L</span>;<br>    &#125;<br>    <span class="hljs-keyword">if</span> (refCnt() == <span class="hljs-number">0</span>) &#123;<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalReferenceCountException</span>(<span class="hljs-number">0</span>);<br>    &#125;<br>    <span class="hljs-comment">// Call open to make sure fc is initialized. This is a no-oop if we called it before.</span><br>    open();<br><br>    <span class="hljs-type">long</span> <span class="hljs-variable">written</span> <span class="hljs-operator">=</span> file.transferTo(<span class="hljs-built_in">this</span>.position + position, count, target);<br>    <span class="hljs-keyword">if</span> (written &gt; <span class="hljs-number">0</span>) &#123;<br>        transferred += written;<br>    &#125;<br>    <span class="hljs-keyword">return</span> written;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="堆外内存"><a href="#堆外内存" class="headerlink" title="堆外内存"></a>堆外内存</h2><p>堆外内存由于不受 Java GC 的影响，更加的稳定、持久，但可能由于内存原因被操作系统杀死进程。</p><p>Netty 在池化内存池 PooledByteBufAllocator 的默认实现中设置的使用堆外内存。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">boolean</span> <span class="hljs-variable">DIRECT_BUFFER_PREFERRED</span> <span class="hljs-operator">=</span><br>        HAS_UNSAFE &amp;&amp; !SystemPropertyUtil.getBoolean(<span class="hljs-string">&quot;io.netty.noPreferDirect&quot;</span>, <span class="hljs-literal">false</span>);<br></code></pre></td></tr></table></figure><p><code>io.netty.noPreferDirect</code> 设置为 true，则使用堆内内存。</p><h2 id="对象-内存池"><a href="#对象-内存池" class="headerlink" title="对象&#x2F;内存池"></a>对象&#x2F;内存池</h2><p><code>io.netty.allocator.type</code> 是一个关键的参数，它直接决定了 Netty 内部用来管理 <code>ByteBuf</code> 内存的分配器（Allocator）类型。简单来说，它控制着 Netty 是<strong>自己高效地管理和复用内存（池化）</strong>，还是<strong>简单直接地向操作系统申请和释放内存（非池化）</strong>。非安卓平台默认是池化实现。</p><p>源码参见：io&#x2F;netty&#x2F;buffer&#x2F;ByteBufUtil.java</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-variable">allocType</span> <span class="hljs-operator">=</span> SystemPropertyUtil.get(<br>            <span class="hljs-string">&quot;io.netty.allocator.type&quot;</span>, PlatformDependent.isAndroid() ? <span class="hljs-string">&quot;unpooled&quot;</span> : <span class="hljs-string">&quot;pooled&quot;</span>);<br>    allocType = allocType.toLowerCase(Locale.US).trim();<br><br>    ByteBufAllocator alloc;<br>    <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;unpooled&quot;</span>.equals(allocType)) &#123;<br>        alloc = UnpooledByteBufAllocator.DEFAULT;<br>        logger.debug(<span class="hljs-string">&quot;-Dio.netty.allocator.type: &#123;&#125;&quot;</span>, allocType);<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-string">&quot;pooled&quot;</span>.equals(allocType)) &#123;<br>        alloc = PooledByteBufAllocator.DEFAULT;<br>        logger.debug(<span class="hljs-string">&quot;-Dio.netty.allocator.type: &#123;&#125;&quot;</span>, allocType);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        alloc = PooledByteBufAllocator.DEFAULT;<br>        logger.debug(<span class="hljs-string">&quot;-Dio.netty.allocator.type: pooled (unknown: &#123;&#125;)&quot;</span>, allocType);<br>    &#125;<br><br>    DEFAULT_ALLOCATOR = alloc;<br><br>    THREAD_LOCAL_BUFFER_SIZE = SystemPropertyUtil.getInt(<span class="hljs-string">&quot;io.netty.threadLocalDirectBufferSize&quot;</span>, <span class="hljs-number">64</span> * <span class="hljs-number">1024</span>);<br>    logger.debug(<span class="hljs-string">&quot;-Dio.netty.threadLocalDirectBufferSize: &#123;&#125;&quot;</span>, THREAD_LOCAL_BUFFER_SIZE);<br><br>    MAX_CHAR_BUFFER_SIZE = SystemPropertyUtil.getInt(<span class="hljs-string">&quot;io.netty.maxThreadLocalCharBufferSize&quot;</span>, <span class="hljs-number">16</span> * <span class="hljs-number">1024</span>);<br>    logger.debug(<span class="hljs-string">&quot;-Dio.netty.maxThreadLocalCharBufferSize: &#123;&#125;&quot;</span>, MAX_CHAR_BUFFER_SIZE);<br>&#125;<br></code></pre></td></tr></table></figure><p>可通过 ServerBootstrap.childOption(ChannelOption.ALLOCATOR, PooledByteBufAllocator.DEFAULT) 设置，在io&#x2F;netty&#x2F;buffer&#x2F;PooledDirectByteBuf.java 创建 buffer 时会直接从池中取。</p><p>当创建一个对象的成本比较大，而且创建频率高且可复用时，使用对象池可以更好的节省资源。</p><p>Netty 自行实现了轻量级（相比 Apache Commons Pool）的对象池：io&#x2F;netty&#x2F;util&#x2F;Recycler.java</p><h2 id="内存分配器"><a href="#内存分配器" class="headerlink" title="内存分配器"></a>内存分配器</h2><p> 4.1 版本开始 jemalloc 成为默认内存分配方式。</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
      <category>Netty</category>
      
    </categories>
    
    
    <tags>
      
      <tag>源码分析</tag>
      
      <tag>Netty</tag>
      
      <tag>内存</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>数字证书与HTTPS</title>
    <link href="/2026/04/26/%E6%95%B0%E5%AD%97%E8%AF%81%E4%B9%A6%E4%B8%8EHTTPS/"/>
    <url>/2026/04/26/%E6%95%B0%E5%AD%97%E8%AF%81%E4%B9%A6%E4%B8%8EHTTPS/</url>
    
    <content type="html"><![CDATA[<h1 id="数字证书与HTTPS"><a href="#数字证书与HTTPS" class="headerlink" title="数字证书与HTTPS"></a>数字证书与HTTPS</h1><p>我们知道网络包是可以被截获的，那么如果涉及到敏感信息的发送，例如各种密码，就不能明文在网络上传输了，因此需要进行加密，即在网络上传输密文，到服务端后服务端经过解密进行后续操作。</p><h2 id="加密"><a href="#加密" class="headerlink" title="加密"></a>加密</h2><p>加密分为两种</p><ul><li>对称加密，双方使用相同的秘钥或者算法来进行加密解密，例如异或操作，客户端发送 111 给服务端，使用全 1 字符串进行异或操作，得到 000，服务端收到 000 后，使用全 1 字符串进行异或解密，得到原数据 111（这里不考虑网络过程中出现的意外情况）。<ul><li>优点：简单，效率高</li><li>缺点：怎么通知对方使用同一个秘钥（即上文的全1字符串），放在公网上传输有暴露的风险，且长时间使用同一个对称秘钥容易被破解</li></ul></li><li>非对称加密，双方各生成一套公钥私钥，客户端发送数据时，使用服务端的公钥加密，服务端收到后，使用自己的私钥解密。双方都保存自己的私钥，传递消息时使用对方的公钥加密，这样即使网络包被截获，由于没有接收方的私钥，也无法被破解。<ul><li>优点：安全</li><li>缺点：效率低；存在如何获取对方的公钥且保证对方的公钥真的是对方的公钥的问题（伪造者冒充服务端，把自己的公钥发送给客户端，客户端无法验证这个公钥到底是服务端还是伪造者）</li></ul></li></ul><h2 id="数字证书"><a href="#数字证书" class="headerlink" title="数字证书"></a>数字证书</h2><p>为了解决非对称加密的获取及验证对方公钥的问题，有了数字证书。</p><p>我们想一下现实生活中如何确定对方的身份，可以要求对方拿出【公安机关】开具的【身份证明】，如果【公安机关】是可信的，且【身份证明】确实是该【公安机关】开具的，那么【身份证明】也认为是可信的。</p><ul><li>如何验证【公安机关】是可信的呢？可以去【上级公安机关】验证，依次递归，最后可以去【顶级公安机关】验证。</li><li>如何验证【身份证明】确实是该【公安机关】开具的？【身份证明】需要【公安机关】使用私钥加密，这样如果可以使用【公安机关】的公钥解密，就可以证明了。</li></ul><p>上述流程中提到的【公安机关】我们认为是【权威机构】（即<strong>CA</strong>（ <strong>Certificate Authority</strong>）），而【身份证明】称之为【证书】（<strong>Certificate</strong>）。</p><p>现在我是服务端，我希望让客户端相信我的身份，那我就需要去 CA 备案，于是我把我的公钥、域名等信息发给了 CA，CA 在核实了我的身份之后，会将我的公钥、域名等信息封装进一个证书，并用自己的私钥对其进行签名。签名后的证书相当于一个【身份证明】，证明我是该公钥的合法拥有者，相当于 CA 为我背书。</p><p>当客户端访问服务端的时候，服务端不需要把公钥给客户端，而是给客户端一个证书，证书里有 CA 的信息，客户端自己拿 CA 的公钥校验，校验通过后就确认了服务端的身份，也拿到了服务端的公钥。</p><h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p><strong>摘要（或哈希值）</strong>是指通过特定的算法<code>对数据进行处理，得到一个固定长度的唯一表示</code>。这个过程是<code>不可逆</code>的，即从摘要值无法恢复原数据。摘要在数字证书中有非常重要的作用，特别是在签名过程和数据验证中。</p><p>摘要的过程：</p><ol><li><strong>数据摘要</strong>：CA 对证书内容（如公钥、有效期、持有者信息等）进行哈希计算，生成唯一的摘要值。</li><li><strong>确保完整性</strong>：摘要算法确保数据的完整性。如果证书的任何部分被修改，摘要值就会发生变化，从而可以检测到证书是否被篡改。</li></ol><h2 id="签名"><a href="#签名" class="headerlink" title="签名"></a>签名</h2><p>在生成数字证书的过程中，<strong>签名</strong>是一个至关重要的步骤，通常是通过证书颁发机构（CA）来完成。</p><p>签名的过程：</p><ol><li><strong>摘要</strong>：CA 会对证书的内容（如公钥、证书有效期、持有者信息等）计算一个哈希值（摘要）。</li><li><strong>私钥加密</strong>：然后，CA 使用自己的私钥对这个哈希值进行加密，生成数字签名。</li><li><strong>数字签名</strong>：数字签名会附加在证书中，作为证书的一部分，客户端在验证时会使用 CA 的公钥来解密数字签名，并比对摘要值，确保证书内容没有被篡改。</li></ol><p>这里顺便提一下：为什么不对证书的内容进行签名而是对其哈希值（摘要）进行签名呢？因为数据本身内容可能会很多，如果数据量大的情况下，计算出数字签名会很耗时。</p><h2 id="Https"><a href="#Https" class="headerlink" title="Https"></a>Https</h2><p>在最开始的加密中我们提到，对称加密效率高但不安全，存在双方传递秘钥的问题，非对称加密安全但效率比较低，那么能不能对二者进行结合，利用非对称加密传输对称加密的秘钥，之后都用对称加密的秘钥来加密数据呢？</p><p>Https 就是这么设计的，参考图示（图来自于 <a href="https://time.geekbang.org/column/article/9492">趣谈网络协议-Https</a>）：</p><p><img src="/img/df1685dd308cef1db97e91493f911ab4.jpg" alt="img-df1685dd308cef1db97e91493f911ab4"></p><p>这是一个双向验证证书（客户端验证服务端证书 &amp;&amp; 服务端验证客户端证书）的流程图，流程如下：</p><ul><li>客户端会发送 Client Hello 消息到服务器，以明文传输 TLS 版本信息、加密套件候选列表、压缩算法候选列表等信息。另外，还会有一个随机数，在协商对称密钥的时候使用。</li><li>服务器返回 Server Hello 消息, 告诉客户端，服务器选择使用的协议版本、加密套件、压缩算法等，还有一个随机数，用于后续的密钥协商。</li><li>随后，服务器会给客户端一个服务器端的证书，然后说：“Server Hello Done”。</li><li>客户端会从 CA 仓库中，拿 CA 的证书里面的公钥去解密服务器的证书，这个过程可能会递归一直找到可以验证的 CA（因为 CA 是有层级的），验证通过后就进行后续步骤。</li><li>客户端计算产生随机数字 Pre-master，发送 Client Key Exchange，用证书中的公钥加密，再发送给服务器，服务器可以通过私钥解密出来。</li><li>双方此时都有了 3 个随机数，分别是自己的、对端的、Pre-master，通过这三个随机数，可以在客户端和服务器产生相同的对称密钥。</li><li>客户端发送：“Change Cipher Spec，以后都采用协商的通信密钥和加密算法进行加密通信了”。然后发送一个 Encrypted Handshake Message，将已经商定好的参数等，采用协商密钥进行加密，发送给服务器用于数据与握手验证。</li><li>同样，服务器也可以发送 Change Cipher Spec”，并且也发送 Encrypted Handshake Message 的消息试试。当双方握手结束之后，就可以通过对称密钥进行加密传输了。</li></ul><p>以上流程省略了服务端验证客户端证书的过程，在安全性要求比较高的环境里，可以采用服务端和客户端双向验证证书的方式。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><ul><li>加密分对称加密和非对称加密。对称加密效率高，但是解决不了密钥传输问题；非对称加密可以解决这个问题，但是效率不高。 </li><li>非对称加密需要通过【证书】和【权威机构】来验证公钥的合法性。</li><li>Https 是综合了对称加密和非对称加密算法的 HTTP 协议。既保证传输安全，也保证传输效率。</li></ul>]]></content>
    
    
    
    <tags>
      
      <tag>计算机网络</tag>
      
      <tag>HTTPS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之HTTP1.1篇</title>
    <link href="/2026/04/25/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BHTTP1-1%E7%AF%87/"/>
    <url>/2026/04/25/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BHTTP1-1%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之HTTP1-1篇"><a href="#系统性能优化之HTTP1-1篇" class="headerlink" title="系统性能优化之HTTP1.1篇"></a>系统性能优化之HTTP1.1篇</h1><p>由于 HTTP 头部使用 ASCII 编码方式，这造成它往往达到几 KB，而且滥用的 Cookie 头部进一步增大了体积。与此同时，REST 架构的无状态特性还要求每个请求都得重传 HTTP 头部，这就使消息的有效信息比重难以提高。</p><p>在不升级协议的情况下，有 3 种优化思路：<code>首先是通过缓存避免发送 HTTP 请求；其次，如果不得不发起请求，那么就得思考如何才能减少请求的个数；最后则是减少服务器响应的体积</code>。</p><h2 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h2><p>客户端缓存：浏览器把收到的请求和响应保存在磁盘上，关键字为 （url + 部分请求头部），这样后续发生请求时，就可以先从本地磁盘上找，查询磁盘的几十毫秒相对于慢了上百倍且不稳定的网络请求要好的多。不过当服务器的资源更新后，如何让客户端感知呢？服务端在首次响应时返回一个<code>预计过期时间</code>和<code>消息摘要</code>，当客户端发现已经过期了就会重新发送请求。</p><p><img src="/img/image-20250630175141930.png" alt="image-20250630175141930"></p><p>此外，过期缓存也是可以提升性能的，客户端发现缓存过期后，取出首次响应返回的摘要，放在 Etag 头部中，服务器获取到请求后，会将本地资源的摘要与请求中的 Etag 相比较，如果不同，那么缓存没有价值，重新发送最新资源即可；如果摘要与 Etag 相同，那么仅返回不含有包体的 304 Not Modified 响应，告知客户端缓存仍然有效即可，这就省去传递可能高达千百兆的文件资源。</p><p><img src="/img/image-20250630175614482.png" alt="image-20250630175614482"></p><p>Etag 摘要的生成与服务器的实现有关~</p><p>浏览器这样的缓存只能为当前用户使用，是私有缓存，如果能缓存在代理服务器上，就能作为共享缓存被多用户使用</p><p><img src="/img/image-20250630175840983.png" alt="image-20250630175840983"></p><h2 id="请求合并"><a href="#请求合并" class="headerlink" title="请求合并"></a>请求合并</h2><p>例如，当请求返回 302 重定向时，如果由客户端来再次请求，请求又要经过不稳定的外部网络，但如果是代理服务器来转发，就可以减少请求的路径，并且如果这个重定向被代理服务器所记忆，就能进一步减少网络消耗。</p><p><img src="/img/image-20250701085303286.png" alt="image-20250701085303286"></p><p>降低 HTTP 请求次数还可以通过合并请求的方式实现，例如一个页面的多个小图片利用 css image sprites 技术合成一张大图片，浏览器收到后根据 css 数据进行切割还原，类似地，在服务器端用 webpack 等打包工具将 Javascript、CSS 等资源合并为大文件，也能起到同样的效果。<code>优点是能减少 tcp 的连接次数，同时减少请求次数也意为着 header 的传递次数也会减少</code>，当然，这种合并请求的方式也会带来一个新问题，即，<code>当其中一个资源发生变化后，客户端必须重新下载完整的大文件</code>，这显然会带来额外的网络消耗。HTTP&#x2F;2 就不需要这个技术了。</p><h2 id="响应压缩"><a href="#响应压缩" class="headerlink" title="响应压缩"></a>响应压缩</h2><p>减少响应体积其实就是对资源进行压缩，用 cpu 换取网络带宽，这又分为无损压缩和有损压缩</p><p>支持无损压缩算法的客户端会在请求中通过 Accept-Encoding 头部明确地告诉服务器：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs html">Accept-Encoding: gzip, deflate, br<br></code></pre></td></tr></table></figure><p>服务器也会在响应中告诉客户端使用的哪种压缩算法：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs html">content-encoding: gzip<br></code></pre></td></tr></table></figure><p>虽然目前 gzip 使用最为广泛，但它的压缩效率以及执行速度其实都很一般，Google 于 2015 年推出的 Brotli 算法在这两方面表现都更优秀（也就是上文中的 br）</p><p>有损压缩通过牺牲质量来提高压缩比，主要针对的是图片和音视频。HTTP 请求可以通过 Accept 头部中的 q 质量因子，告诉服务器期望的资源质量</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs html">Accept: audio/*; q=0.2, audio/basic<br></code></pre></td></tr></table></figure><p>例如图片的 wbep 格式就比 png 要更小</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>如果一定要使用 HTTP 1.1，可以从三方面入手性能优化：</p><ul><li>缓存<ul><li>把响应缓存在本机磁盘或代理服务器，在代理服务器的缓存称为共享缓存，可以为多个用户使用。缓存离客户端越近返回越快，过期缓存也可以通过摘要值发挥作用</li></ul></li><li>减少请求次数<ul><li>代理服务器代替客户端进行重定向等</li><li>对小资源进行合并返回</li></ul></li><li>对资源进行压缩返回<ul><li>例如 js 文件就有源文件，min.js，min.zip，压缩算法 gzip、br 等</li><li>对图片,音视频进行压缩，某些资源可以使用 Base64 编码后嵌入 HTML 文件（HTML 文档本身就包含了多媒体资源的完整数据，浏览器可以直接解析并展示这些内容，而无需通过额外的 HTTP 请求获取这些资源）</li></ul></li></ul>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>计算机网络</tag>
      
      <tag>性能优化</tag>
      
      <tag>HTTP</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之内存篇</title>
    <link href="/2026/04/24/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E5%86%85%E5%AD%98%E7%AF%87/"/>
    <url>/2026/04/24/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8B%E5%86%85%E5%AD%98%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之内存篇"><a href="#系统性能优化之内存篇" class="headerlink" title="系统性能优化之内存篇"></a>系统性能优化之内存篇</h1><p>绝大部分的高级语言都是用 C 语言编写的，包括 Java，申请内存<strong>（堆外内存）</strong>必须经过 C 库，而<code>C 库会通过预分配更大的空间作为内存池</code>，来加快后续申请内存的速度，同时这种池化技术有很多优点，例如 内存池中可以利用享元模式将常用的对象一直保留着，减少重复申请导致的性能的顺耗等。但预分配更大的空间也可能导致 Java 进程的内存占用超出 Xmx 的限制。</p><h2 id="隐藏的内存池"><a href="#隐藏的内存池" class="headerlink" title="隐藏的内存池"></a>隐藏的内存池</h2><p>当代码申请内存时，首先会到应用层内存池，如果应用层内存池足够，就会反回给业务代码，否则会向更底层的 C 库内存池申请内存。Java 程序启动时通过 Xmx 指定的值就设定了JVM 堆内存池的大小。Google 的 TCMalloc 和 FaceBook 的 JEMalloc 就是 C 库内存池，当 C 库内存池也无法满足内存申请时，就会向操作系统内核申请分配内存。</p><p><img src="/img/image-20250619144007444.png" alt="image-20250619144007444"></p><p>除了 JVM 负责管理的堆内存外，<strong>Java 还拥有一些堆外内存，由于它不使用 JVM 的垃圾回收机制，且缩短了内存分配的路径，所以更稳定、持久，处理 IO 的速度也更快</strong>。这些堆外内存就会由 C 库内存池负责分配，因此 Java 程序有了堆内存池还会受到C库内存池的影响。</p><p>Linux 系统的默认 C 库内存池 Ptmalloc2 在工作时，会预分配比你申请的字节数更大的空间作为内存池。比如说，当主进程下申请 1 字节的内存时，Ptmalloc2 会预分配 132K 字节的内存（Ptmalloc2 中叫 Main Arena），应用代码再申请内存时，会从这已经申请到的 132KB 中继续分配。当我们释放这 1 字节时，Ptmalloc2 也不会把内存归还给操作系统。</p><p>这点空间其实不算很多。但是<strong>多线程和单线程的预分配策略是不同的</strong>，每个<strong>子线程预分配的内存是 64MB</strong>（Ptmalloc2 中被称为 Thread Arena，32 位系统下为 1MB，64 位系统下为 64MB）。如果有 100 个线程，就将有 6GB 的内存都会被内存池占用。当然，并不是设置了 1000 个线程，就会预分配 60GB 的内存，<code>子线程内存池最多只能到 8 倍的 CPU 核数</code>，比如在 32 核的服务器上，最多只会有 256 个子线程内存池，但这也非常夸张了，16GB（64MB * 256 &#x3D; 16GB）的内存将一直被 Ptmalloc2 占用。</p><p><code>Linux 下的 JVM 编译时默认使用了 Ptmalloc2 内存池</code>，因此每个线程都预分配了 64MB 的内存，在多数情况下，这些预分配出来的内存池，可以提升后续内存分配的性能。</p><p>当然如果接受不了这么多的对外内存，也有方式可以改变：</p><ul><li>设置 <code>MALLOC_ARENA_MAX</code> 环境变量，可以限制线程内存池的最大数量，但是注意线程内存池的数量减少后，会影响 Ptmalloc2 分配内存的速度。不过由于 Java 主要使用 JVM 内存池来管理对象，这点影响并不重要。</li><li>更换掉 Ptmalloc2 内存池，选择一个预分配内存更少的内存池，比如 Google 的 TCMalloc。</li></ul><h2 id="TCMalloc-与-Ptmalloc2"><a href="#TCMalloc-与-Ptmalloc2" class="headerlink" title="TCMalloc 与 Ptmalloc2"></a>TCMalloc 与 Ptmalloc2</h2><p>Ptmalloc2 假定，如果线程 A 申请并释放了的内存，线程 B 可能也会申请类似的内存，所以它允许内存池在线程间复用以提升性能。因此，<code>每次分配内存，Ptmalloc2 一定要加锁</code>，才能解决共享资源的互斥问题。那么当线程数多的时候，Ptmalloc2 出现锁竞争的概率也会变高，分配内存就会更慢。</p><p>TCMalloc 特意针对小内存做了优化。TCMalloc 把内存分为 3 个档次，小于等于 256KB 的称为小内存，从 256KB 到 1M 称为中等内存，大于 1MB 的叫做大内存。</p><p>总结：<strong>TCMalloc</strong> 对<code>多线程</code>下<code>小内存</code>的分配特别友好，<strong>Ptmalloc2</strong> 则更擅长<code>大内存的分配</code>，TCMalloc 对中等内存、大内存的分配速度很慢</p><p>因此，<strong>如果主要分配 256KB 以下的内存，特别是在多线程环境下，应当选择 TCMalloc；否则应使用 Ptmalloc2，它的通用性更好。</strong></p><p><strong>mysql 很多人建议把内存分配换成 tcmalloc，就是因为 mysql 要支持大量并发，适合 tcmalloc 的应用场景。</strong>性能测试参见：<a href="https://www.cnblogs.com/wangdong/p/9252668.html">MySQL5.7.18(ptmalloc VS tcmalloc VS jemalloc)性能测试</a></p><h2 id="堆内存与栈内存"><a href="#堆内存与栈内存" class="headerlink" title="堆内存与栈内存"></a>堆内存与栈内存</h2><p>如果使用的是静态语言：C&#x2F;C++&#x2F;Java ，正常不使用 new 关键字的对象就是栈内存，使用 new 或者 malloc 的则是分配在堆内存上</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 栈内存</span><br>C/C++/Java语言：<span class="hljs-type">int</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-number">10</span>;<br><br><span class="hljs-comment">// 堆内存</span><br>C语言：<span class="hljs-type">int</span> * a = (<span class="hljs-type">int</span>*) malloc(sizeof(<span class="hljs-type">int</span>));<br>C++语言：<span class="hljs-type">int</span> * a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">int</span>;<br>Java语言：<span class="hljs-type">int</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Integer</span>(<span class="hljs-number">10</span>);<br></code></pre></td></tr></table></figure><p>对于动态类型语言，无论是否使用 new 关键字，内存都是从堆中分配的。</p><p>栈内存分配更快的原因是：<code>每个线程都有独立的栈，所以分配内存时不需要加锁保护，而且栈上对象的尺寸在编译阶段就已经写入可执行文件了，执行效率更高</code>！性能至上的 Golang 语言就是按照这个逻辑设计的，即使你用 new 关键字分配了堆内存，但编译器如果认为在栈中分配不影响功能语义时，会自动改为在栈中分配。JVM 在内存分配时也有相应的考量。</p><p>但是栈内存上创建对象有以下缺点：</p><ul><li>生命周期有限：函数&#x2F;方法调用完毕生命周期结束，而堆中创建的对象存活时间较长</li><li>栈容量有限：如 CentOS 7 中是 8MB 字节，递归、对象大容易导致栈溢出错误</li></ul><p><strong>栈内存分配的速度一般是要比堆快的，我们分配内存时，如果在满足功能的情况下，可以在栈中分配的话，就选择栈。</strong></p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><ul><li><p>如果程序通常多线程分配小块内存，可以选择 TCMalloc 替换 Linux 的 Ptmalloc2 内存分配器，TCMalloc 擅长多线程下小内存分配，Ptmalloc2 比较通用。<a href="https://www.bytezonex.com/archives/O-p9zY2c.html">JVM堆外内存泄漏：从 64M 内存块问题到 JEMalloc 解决方案</a></p></li><li><p>如果有可能的话，尽量在栈中分配内存，它比内存池中的堆内存分配速度快很多</p></li></ul><p>注意：Java 语言特指堆外内存使用C库内存池，如果不使用堆外内存，则几乎不涉及。</p>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>内存</tag>
      
      <tag>性能优化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>系统性能优化之CPU篇</title>
    <link href="/2026/04/23/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BCPU%E7%AF%87/"/>
    <url>/2026/04/23/%E7%B3%BB%E7%BB%9F%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E4%B9%8BCPU%E7%AF%87/</url>
    
    <content type="html"><![CDATA[<h1 id="系统性能优化之CPU篇"><a href="#系统性能优化之CPU篇" class="headerlink" title="系统性能优化之CPU篇"></a>系统性能优化之CPU篇</h1><p>其实除了 CPU 的频率，多核架构以及多 CPU 架构对系统运行的性能也是很大影响的，那么该如何充分利用 CPU 呢？</p><h2 id="CPU-架构"><a href="#CPU-架构" class="headerlink" title="CPU 架构"></a>CPU 架构</h2><p>首先介绍一下当前主流的 CPU 架构，现在的系统基本都是多 CPU，<code>一个 CPU 处理器中一般有多个运行核心</code>，我们把<code>一个运行核心称为一个物理核</code>，<code>每个物理核都可以运行应用程序</code>。每个物理核都拥有私有的一级缓存（Level 1 cache，简称 L1 cache），包括一级指令缓存和一级数据缓存，以及私有的二级缓存（Level 2 cache，简称 L2 cache）。同时，一个 CPU 上的所有物理核共享一个三级缓存。</p><p><img src="/img/image-20250617213848204.png" alt="image-20250617213848204"></p><p>Centos 可以使用 <code>lscpu</code> 查看系统 CPU 信息</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@VM-16-11-centos zwj]# lscpu<br>Architecture:          x86_64<br>CPU op-mode(s):        32-bit, 64-bit<br>Byte Order:            Little Endian<br>CPU(s):                2<br>On-line CPU(s) list:   0,1<br>Thread(s) per core:    1<br>Core(s) per socket:    2<br>Socket(s):             1<br>NUMA node(s):          1<br>Vendor ID:             GenuineIntel<br>CPU family:            6<br>Model:                 94<br>Model name:            Intel(R) Xeon(R) Gold 6148 CPU @ 2.40GHz<br>Stepping:              3<br>CPU MHz:               2394.364<br>BogoMIPS:              4788.72<br>Hypervisor vendor:     KVM<br>Virtualization <span class="hljs-built_in">type</span>:   full<br>L1d cache:             32K<br>L1i cache:             32K<br>L2 cache:              4096K<br>L3 cache:              28160K<br>NUMA node0 CPU(s):     0,1<br></code></pre></td></tr></table></figure><p>程序在执行时，会先将内存中的数据载入三级缓存，再进入每颗核心独有的二级缓存，最后进入最快的一级缓存，之后才会被 CPU 使用。</p><p>缓存比内存的访问速度要快很多，访问内存要100个时钟周期以上，一级缓存只需要4～5个时钟周期，二级缓存大约12个时钟周期，三级缓存大约30个时钟周期。因此如果 CPU 所要操作的数据可以命中缓存，会带来一定的性能提升。</p><blockquote><p>NUMA 架构</p></blockquote><p>由于摩尔定律逐渐失效，现代处理器因频率发展受限转而向多核方向发展，以前原本在北桥芯片中的内存控制器也被集成到了处理器内核中，这样<strong>每个处理器核心所在的裸晶（DIE）都有属于自己内存管理器所管理的内存</strong>。现在的系统基本都是多 CPU（也称为多 CPU Socket），CPU 之间通过总线连接，如下图所示：</p><p><img src="/img/image-20250617214720473.png" alt="image-20250617214720473"></p><p>在多 CPU 架构上，应用程序可以在不同的 CPU 上执行，如果应用程序先在一个 Socket 上运行，并且把数据保存到了内存，然后被调度到另一个 Socket 上运行，此时，<code>应用程序再进行内存访问时，就需要访问之前 Socket 上连接的内存，这种访问属于远端内存访问</code>。和访问 Socket 直接连接的内存相比，远端内存访问会增加应用程序的延迟。</p><p>在多 CPU 架构下，一个应用程序访问所在 Socket 的本地内存和访问远端内存的延迟并不一致，所以，我们也把这个架构称为<strong>非统一内存访问架构（Non-Uniform Memory Access，NUMA 架构）</strong>。</p><p>所以 CPU 架构对应用程序的运行主要有以下影响：</p><ul><li>CPU 缓存访问速度远远大于内存，因此尽量缓存命中，多利用缓存；</li><li>NUMA 架构可能会出现远端内存访问的情况，这会直接增加应用程序的执行时间。</li></ul><h2 id="提升缓存命中率"><a href="#提升缓存命中率" class="headerlink" title="提升缓存命中率"></a>提升缓存命中率</h2><p>Centos 运行 <code>lscpu</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@VM-16-11-centos zwj]# lscpu<br>Architecture:          x86_64<br>CPU op-mode(s):        32-bit, 64-bit<br>Byte Order:            Little Endian<br>L1d cache:             32K<br>L1i cache:             32K<br>L2 cache:              4096K<br>L3 cache:              28160K<br>NUMA node0 CPU(s):     0,1<br></code></pre></td></tr></table></figure><p>可以看到 L1 cache 并不是单独的，而是分为 L1d cache 和 L1i cache，也就是 数据缓存 和 指令缓存，因此提高缓存命中率也需要分别考虑</p><blockquote><p>数据缓存</p></blockquote><p>思考这样一个场景，有一个二维数组需要进行一次遍历</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> arr[<span class="hljs-number">10</span>][<span class="hljs-number">10</span>];<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10</span>; i++) &#123;<br>        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d&quot;</span>, arr[i][j])<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> arr[<span class="hljs-number">10</span>][<span class="hljs-number">10</span>];<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; <span class="hljs-number">10</span>; i++) &#123;<br>        <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d&quot;</span>, arr[j][i])<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>不要思考这个场景是否合理，比如需要遍历一个数组把所有值加入到一个 set 中，思考这两种方式的执行速度，其实方式1是远优于方式2的（如果是 Python，会由于数组的设计效果没那么明显）。</p><p>原因是 CPU 其实有 Cache Line 的概念，CPU 在读取数据时会一次性读取 Cache Line 大小的数据到缓存中，而我们又知道数组在内存中是紧密排放的</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c">arr[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>] arr[<span class="hljs-number">0</span>][<span class="hljs-number">1</span>] arr[<span class="hljs-number">0</span>][<span class="hljs-number">2</span>]<br>arr[<span class="hljs-number">1</span>][<span class="hljs-number">0</span>]    <br>arr[<span class="hljs-number">2</span>][<span class="hljs-number">0</span>]    <br></code></pre></td></tr></table></figure><p>如果按照第一种方式遍历，就可以利用 CPU 缓存加快访问</p><p>Linux 可以通过 <code>cat /sys/devices/system/cpu/cpu0/cache/index1/coherency_line_size </code> 查看 Cache Line 大小</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@VM-16-11-centos zwj]# <span class="hljs-built_in">cat</span> /sys/devices/system/cpu/cpu0/cache/index1/coherency_line_size <br>64<br></code></pre></td></tr></table></figure><p>Cache Line 在 nginx、redis 的设计中都有所体现：</p><ul><li>nginx 使用哈希表存放域名、http头部信息，哈希表里桶的大小如 server_names_hash_bucket_size，它默认就等于 CPU Cache Line 的值。由于所存放的字符串长度不能大于桶的大小，所以当需要存放更长的字符串时，就需要修改桶大小，但 Nginx 官网上明确建议它应该是 CPU Cache Line 的整数倍。</li><li>redis 的 sds 中，embstr 的 44 字节，其实也是 CPU Cache Line 的体现，当 robj + 字符串 + ‘\0’ 的长度不超过 64 时，此时编码就是 embstr，在读取到 robj 时就直接取到了字符串数据，无需再次访问内存。</li></ul><p>执行 perf stat 可以统计出进程运行时的系统信息（通过 -e 选项指定要统计的事件，如果要查看三级缓存总的命中率，可以指定缓存未命中 cache-misses 事件，以及读取缓存次数 cache-references 事件，两者相除就是缓存的未命中率，用 1 相减就是命中率。类似的，通过 L1-dcache-load-misses 和 L1-dcache-loads 可以得到 L1 缓存的命中率）</p><blockquote><p>指令缓存</p></blockquote><p>上面只是介绍了提高数据缓存的命中率，还有指令缓存的命中率</p><p>假如有一个数组，里面是一些 0～255 的数字，需要找出其中 &lt; 128 的置为 0 并进行排序，方法1是先找到对应数据并置为0再排序，方法2是先排序再置为0。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-keyword">for</span>(i = 0; i &lt; N; i++) &#123;<br>       <span class="hljs-keyword">if</span> (array [i] &lt; 128) array[i] = 0;<br>&#125;<br><span class="hljs-built_in">sort</span>(array, array + N);<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sort</span>(array, array + N);<br><span class="hljs-keyword">for</span>(i = 0; i &lt; N; i++) &#123;<br>       <span class="hljs-keyword">if</span> (array [i] &lt; 128) array[i] = 0;<br>&#125;<br></code></pre></td></tr></table></figure><p>先排序的方法速度其实是要更快的，原因是因为循环中有大量的 if 条件分支，而 CPU<strong>含有分支预测器</strong>。当代码中出现 if、switch 等语句时，意味着此时至少可以选择跳转到两段不同的指令去执行。如果分支预测器可以预测接下来要在哪段代码执行（比如 if 还是 else 中的指令），就可以提前把这些指令放在缓存中，CPU 执行时就会很快。<code>当数组中的元素完全随机时，分支预测器无法有效工作，而当 array 数组有序时，分支预测器会动态地根据历史命中数据对未来进行预测，命中率就会非常高</code></p><h2 id="绑核"><a href="#绑核" class="headerlink" title="绑核"></a>绑核</h2><p>多 CPU 和多核心是不可避免的，为了缓解 NUMA 及应用程序重新调度后需要再次加载数据到 CPU 缓存的问题，操作系统提供了绑核指令，<code>ls cpu</code> 指令可以查看每个 CPU 对应的核心编号，可以使用 <code>taskset</code> 命令行指令在启动时将应用程序与 CPU 进行绑定，也可以使用不同语言的 API （如sched_setaffinity）通过系统调用在程序代码中设置进程的 CPU 亲和性。此外，Perf 工具也提供了 cpu-migrations 事件，它可以显示进程从不同的 CPU 核心上迁移的次数。</p><p>绑核是把双刃剑，如果你的程序有很多的后台线程，例如 redis 需要子线程生成 rdb、aof 重写、删除过期 key，就会导致子进程、后台线程和主线程竞争 CPU 资源，一旦子进程或后台线程占用 CPU 时，主线程就会被阻塞，导致 Redis 请求延迟增加。当然也有缓解的办法，比如让程序绑定一个具有多个逻辑核心的核，可以在一定程度上缓解 CPU 资源竞争。</p><p>不过 Redis 6 好像已经提供了支持 CPU 核绑定的配置操作了～</p>]]></content>
    
    
    <categories>
      
      <category>性能优化</category>
      
      <category>通用</category>
      
    </categories>
    
    
    <tags>
      
      <tag>性能优化</tag>
      
      <tag>CPU</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Oauth2的4种许可模式</title>
    <link href="/2026/04/22/Oauth2%E7%9A%844%E7%A7%8D%E8%AE%B8%E5%8F%AF%E6%A8%A1%E5%BC%8F/"/>
    <url>/2026/04/22/Oauth2%E7%9A%844%E7%A7%8D%E8%AE%B8%E5%8F%AF%E6%A8%A1%E5%BC%8F/</url>
    
    <content type="html"><![CDATA[<h1 id="Oauth2的4种许可模式"><a href="#Oauth2的4种许可模式" class="headerlink" title="Oauth2的4种许可模式"></a>Oauth2的4种许可模式</h1><p><strong>Oauth</strong> 即 Open auth ，一种开放授权协议，通过为每个第三方软件和每个用户的组合分别生成对受保护资源具有受限的访问权限的凭据，也就是访问令牌，来代替之前的用户名和密码。而生成访问令牌之前的登录操作，又是在用户跟平台之间进行的，第三方软件根本无从得知用户的任何信息，<strong>使第三方软件调用大量 API 的时候，不再传输用户名和密码，从而减少网络安全的攻击面</strong>。</p><p>假如你是一个卖家小明，在京东商城开了一个店铺， 日常运营中你要将订单打印出来以便给用户发货。但打印这事儿也挺繁琐的，之前你总是手工操作，后来发现有个叫“小兔”的第三方软件，它可以帮你高效率地处理这事。 但你想想，小兔是怎么访问到这些订单数据的呢？其实是这样，京东商城提供了开放平台， 小兔通过京东商家开放平台的 API 就能访问到用户的订单数据。只要你在软件里点击同意，小兔就可以拿到一个访问令牌，通过访问令牌来获取到你的订单数据帮你干活儿了。</p><p>在 OAuth 2.0 的体系里面有 4 种角色，按照官方的称呼它们分别是资源拥有者、客户端、授权服务和受保护资源。在上述例子中对应关系如下：资源拥有者 -&gt; 小明，客户端 -&gt; 小兔软件，授权服务 -&gt; 京东商家开放平台的授权服务，受保护资源 -&gt; 小明店铺在京东上面的订单。  </p><h2 id="授权码许可（Authorization-Code）"><a href="#授权码许可（Authorization-Code）" class="headerlink" title="授权码许可（Authorization Code）"></a>授权码许可（Authorization Code）</h2><p>在授权码许可类型中，授权服务的工作，可以划分为两大部分，一个是颁发授权码 code，一个是颁发访问令牌 access_token。</p><p>整体流程图如下：</p><p><img src="/img/image-20241205162722046.png" alt="image-20241205162722046"> </p><p>时序图如下：</p><p><img src="/img/image-20241205163038227.png" alt="image-20241205163038227"> </p><p>其中重要的是两次重定向的过程：</p><ul><li>第一次重定向，由第三方软件（客户端）发起，重定向到京东的开放平台授权页面，引导用户进行授权</li><li>第二次重定向，在用户授权完成后，授权服务生成了授权码 code，携带其重定向回三方软件</li></ul><p>为什么需要这么做：</p><ul><li>为什么不直接返回访问令牌 access_token，而先返回授权码 code</li></ul><p>如果直接返回</p><ul><li>若直接返回给小兔软件前端，相当于把非常重要的许可证暴露在了浏览器上，为了安全考虑这是不允许的</li><li>若直接返回给小兔软件后端，小兔软件此时已经可以去请求数据了，但是用户还停留在授权界面，并没有返回到小兔软件，影响用户体验。（相当于你打王者荣耀，使用微信登录，微信登录完成并授权后，一直没返回王者荣耀界面，虽然你已经登录成功了，但是还是停留在微信的授权界面）</li></ul><p>综合这样的安全性和用户体验考虑，授权码许可类型设置为了这样的流程。但是有前置条件：</p><ul><li>第三方软件首先要到授权服务处注册自己的一些信息，如回调地址，可选权限范围（王者荣耀在请求微信授权登录时可选头像、好友关系等），生成自己的 app_id 和 app_secret</li></ul><p>如果不进行注册，虽然在获取授权码的时候第三方软件携带了想要的回调地址，但授权服务发现自己根本不知道这个软件是什么，也不知道这个软件的回调地址是否安全，自然不能进行授权</p><p>这种认证方式的 grant_type 为：<code>authorization_code</code></p><h2 id="资源拥有者凭证许可（Resource-Owner-Password-Credentials）"><a href="#资源拥有者凭证许可（Resource-Owner-Password-Credentials）" class="headerlink" title="资源拥有者凭证许可（Resource  Owner Password Credentials）"></a>资源拥有者凭证许可（Resource  Owner Password Credentials）</h2><p>试想一种这样的情况，小兔此时就是京东官方出品的一款软件，小明也是京东的用户，那么小明其实是可以使用用户名和密码来直接使用小兔这款软件的。</p><p>那么，只需要在小明首次登录时，带着用户名和密码去授权模块认证一次，就可以返回 access_token 用于后续操作了。</p><p><img src="/img/image-20241206135407760.png" alt="image-20241206135407760"> </p><p>这种认证方式的 grant_type 为：<code>password</code></p><h2 id="客户端凭证许可（Client-Credentials）"><a href="#客户端凭证许可（Client-Credentials）" class="headerlink" title="客户端凭证许可（Client Credentials）"></a>客户端凭证许可（Client Credentials）</h2><p>如果<code>没有明确的资源拥有者</code>，换句话说就是，小兔软件访问了一个不需要用户小明授权的数据，比如获取京东 LOGO 的图片地址，这个 LOGO 信息不属于任何一个第三方用户，再比如其它类型的第三方软件来访问平台提供的省份信息，省份信息也不属于任何一个第 三方用户。</p><p><img src="/img/image-20241206135737527.png" alt="image-20241206135737527"></p><p>这种认证方式的 grant_type 为：<code>client_credentials</code></p><h2 id="隐式许可（Implicit）"><a href="#隐式许可（Implicit）" class="headerlink" title="隐式许可（Implicit）"></a>隐式许可（Implicit）</h2><p>隐式许可流程是<code>唯一在前端通信中要求返回 access_token 的流程</code></p><p>如果小明使用的小兔打单软件应用<code>没有后端服务</code>，就是在浏览器里面执行的，比如纯粹的 JavaScript 应用，应该如何使用 OAuth 2.0 呢？ 在这种情况下，小兔软件对于浏览器就没有任何保密的数据可以隐藏了，也不再需要应用密钥 app_secret 的值了，也不用再通过授权码 code 来换取访问令牌 access_token 的值了。</p><p>因为<code>使用授权码的目的之一，就是把浏览器和第三方软件的信息做一个隔离，确保浏览器看不到第三方软件最重要的访问令牌 access_token 的值</code>。</p><p><img src="/img/image-20241206140020043.png" alt="image-20241206140020043"> </p><h2 id="PKCE-协议"><a href="#PKCE-协议" class="headerlink" title="PKCE 协议"></a>PKCE 协议</h2><p>随着移动端应用的兴起，比如现在有一个<code>没有服务端的 APP</code>，也想要像 Web 应用那样自由的请求和响应，那它应该把 app_secret 存在哪里呢？因为 APP 会被安装到成千上万台设备上，如果 app_secret 被破解，可能造成灾难性的后果。</p><p>OAuth 2.0 里面就有这样的指导方法。这个方法就是我们将要介绍的 PKCE 协议，，全称是 Proof Key for Code Exchange by OAuth Public Clients。</p><p><img src="/img/image-20241206140806952.png" alt="image-20241206140806952"> </p><p>首先，App 自己要生成一个随机的、长度在 43~128 字符之间的，参数为 code_verifier  的字符串验证码；接着，我们再利用这个 code_verifier，来生成一个被称为“挑战码”的参数 code_challenge。</p><p>生成 code_challenge 的几种方式如下：</p><ul><li>一种 code_challenge_method&#x3D;plain，此时 code_verifier 的值就是 code_challenge  的值</li><li>另外一种 code_challenge_method&#x3D;S256，就是将 code_verifier 值进行 ASCII 编码之后再进行哈希，然后再将哈希之后的值进行 BASE64-URL 编码，代码示例如下：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">code_challenge = BASE64URL-ENCODE(SHA256(ASCII(code_verifier)))<br></code></pre></td></tr></table></figure><p>在第一步获取授权码 code 的时候，我们使用 code_challenge  和 code_challenge_method 参数也传过去，目的是让授权服务知道生成 code_challenge 值的方法是 plain 还是 S256</p><p>在第二步获取访问令牌 access_token 的时候，我们使用 code_verifier 参数和授权码 code，授权服务根据上一步 code 和 （code_challenge，code_challenge_method）的对应关系，结合 code_challenge_method 生成 code_challenge，与第一步的值进行比较，，如果相同就颁发访问令牌。</p><p>PKCE 发布的目的是为了缓解针对公开客户端的攻击，主要是解决授权码窃听的攻击。</p>]]></content>
    
    
    
    <tags>
      
      <tag>Oauth2</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Linux-JDK-NIO源码分析</title>
    <link href="/2026/04/21/Linux-JDK-NIO%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/"/>
    <url>/2026/04/21/Linux-JDK-NIO%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    
    <content type="html"><![CDATA[<h1 id="Linux-JDK-NIO-源码分析"><a href="#Linux-JDK-NIO-源码分析" class="headerlink" title="Linux JDK NIO 源码分析"></a>Linux JDK NIO 源码分析</h1><h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>多路复用是一种模型，而实现这种模型有 select、poll 和 epoll，在 linux 下 NIO 的实现默认是 epoll</p><p>当一个 Socket 成功建立，客户端向服务端发数据，内核是最先知道这个 Socket 是可读的，但是业务层也想知道这个事情，那么怎么做呢？</p><p>借鉴观察者模式的思想，当这个事情发生了，内核就开始通知这个事情的监听者，也就是 epoll 对象，epoll 对象会触发的行为已经被 linux 内核写好了，就是把这个事件放到调用阻塞方法传来的一个参数（就绪队列）上</p><p>当业务层解除阻塞返回时，传递来的这个参数（就绪队列）上就是发生的事件</p><p>这个流程在代码怎么实现呢？</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 1. 创建一个 epoll 对象</span><br><span class="hljs-comment">// fd 即 File Descriptor 文件描述符，能够定位到一个对象，epfd 即 epoll 对象的 fd</span><br><span class="hljs-type">int</span> epfd = epoll_create()<br></code></pre></td></tr></table></figure><p>当有客户端来连接，业务层收到了一个 Socket，希望 epoll 对象监听这个 Socket 的读事件</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 告诉 epoll 对象监听这个 socket 的读事件</span><br><span class="hljs-comment">// epoll 对象的 fd、操作方式(增、删、改)、socketfd、关心的事件类型</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">epoll_ctl</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-type">int</span> ops, <span class="hljs-type">int</span> fd, <span class="hljs-type">int</span> events)</span><br></code></pre></td></tr></table></figure><p>业务层调用开始阻塞</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// 等待事件,最多阻塞 timeout 时间</span><br><span class="hljs-comment">// epoll 对象的 fd、就绪队列的地址、就绪队列的长度、最大阻塞时间</span><br><span class="hljs-type">int</span> <span class="hljs-title function_">epoll_wait</span><span class="hljs-params">(<span class="hljs-type">int</span> epfd, <span class="hljs-type">long</span> pollAddress, <span class="hljs-type">int</span> numfds, <span class="hljs-type">int</span> timeout)</span><br></code></pre></td></tr></table></figure><p>首先以一段经典的 NIO 代码开始：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-type">Selector</span> <span class="hljs-variable">selector</span> <span class="hljs-operator">=</span> Selector.open();<br>    <span class="hljs-type">ServerSocketChannel</span> <span class="hljs-variable">serverSocketChannel</span> <span class="hljs-operator">=</span> ServerSocketChannel.open();<br>    serverSocketChannel.configureBlocking(<span class="hljs-literal">false</span>);<br>    serverSocketChannel.bind(<span class="hljs-keyword">new</span> <span class="hljs-title class_">InetSocketAddress</span>(<span class="hljs-string">&quot;127.0.0.1&quot;</span>, <span class="hljs-number">9097</span>));<br>    serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);<br>    <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>) &#123;<br>        selector.select();<br>        Set&lt;SelectionKey&gt; selectionKeys = selector.selectedKeys();<br>        Iterator&lt;SelectionKey&gt; iterator = selectionKeys.iterator();<br>        <span class="hljs-keyword">while</span> (iterator.hasNext()) &#123;<br>            <span class="hljs-type">SelectionKey</span> <span class="hljs-variable">selectionKey</span> <span class="hljs-operator">=</span> iterator.next();<br>            iterator.remove();<br>            <span class="hljs-type">SelectableChannel</span> <span class="hljs-variable">channel</span> <span class="hljs-operator">=</span> selectionKey.channel();<br>            <span class="hljs-keyword">if</span> (selectionKey.isAcceptable()) &#123;<br>                <span class="hljs-type">ServerSocketChannel</span> <span class="hljs-variable">serverChannel</span> <span class="hljs-operator">=</span> (ServerSocketChannel) channel;<br>                <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">accept</span> <span class="hljs-operator">=</span> serverChannel.accept();<br>                accept.configureBlocking(<span class="hljs-literal">false</span>);<br>                accept.register(selector, SelectionKey.OP_READ);<br>            &#125;<br>            <span class="hljs-keyword">if</span> (selectionKey.isReadable()) &#123;<br>                <span class="hljs-type">SocketChannel</span> <span class="hljs-variable">socketChannel</span> <span class="hljs-operator">=</span> (SocketChannel) channel;<br>                <span class="hljs-type">ByteBuffer</span> <span class="hljs-variable">byteBuffer</span> <span class="hljs-operator">=</span> ByteBuffer.allocate(<span class="hljs-number">1024</span>);<br>                <span class="hljs-type">int</span> <span class="hljs-variable">length</span> <span class="hljs-operator">=</span> socketChannel.read(byteBuffer);<br>                <span class="hljs-keyword">if</span> (length == -<span class="hljs-number">1</span>) &#123;<br>                    channel.close();<br>                &#125; <span class="hljs-keyword">else</span> &#123;<br>                    byteBuffer.flip();<br>                    <span class="hljs-type">byte</span>[] buffer = <span class="hljs-keyword">new</span> <span class="hljs-title class_">byte</span>[byteBuffer.remaining()];<br>                    byteBuffer.get(buffer);<br>                    System.out.println(<span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(buffer));<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>重点其实就是上述代码的第2、6、8行，所以接下来也分为这三个部分进行讲解</p><h2 id="Selector-open"><a href="#Selector-open" class="headerlink" title="Selector.open()"></a>Selector.open()</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Selector <span class="hljs-title function_">open</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-keyword">return</span> SelectorProvider.provider().openSelector();<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Holder</span> &#123;<br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">SelectorProvider</span> <span class="hljs-variable">INSTANCE</span> <span class="hljs-operator">=</span> provider();<br>    <span class="hljs-meta">@SuppressWarnings(&quot;removal&quot;)</span><br>    <span class="hljs-keyword">static</span> SelectorProvider <span class="hljs-title function_">provider</span><span class="hljs-params">()</span> &#123;<br>        PrivilegedAction&lt;SelectorProvider&gt; pa = () -&gt; &#123;<br>            SelectorProvider sp;<br>            <span class="hljs-keyword">if</span> ((sp = loadProviderFromProperty()) != <span class="hljs-literal">null</span>)<br>                <span class="hljs-keyword">return</span> sp;<br>            <span class="hljs-keyword">if</span> ((sp = loadProviderAsService()) != <span class="hljs-literal">null</span>)<br>                <span class="hljs-keyword">return</span> sp;<br>            <span class="hljs-keyword">return</span> sun.nio.ch.DefaultSelectorProvider.get();<br>        &#125;;<br>        <span class="hljs-keyword">return</span> AccessController.doPrivileged(pa);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>第1段代码块的第2行 SelectorProvider.provider() 其实是一个静态常量，即第2段代码块中的 INSTANCE 对象，由于是 static 代码块，所以会直接按照流程进行初始化。</p><p>第7行中首先从系统属性中读取参数，如果不存在则第9行通过 SPI 机制即 ServiceLoader 加载，如果仍然不存在，则在第11行通过默认提供的 provider 进行初始化。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">DefaultSelectorProvider</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> SelectorProviderImpl INSTANCE;<br>    <span class="hljs-keyword">static</span> &#123;<br>        PrivilegedAction&lt;SelectorProviderImpl&gt; pa = EPollSelectorProvider::<span class="hljs-keyword">new</span>;<br>        INSTANCE = AccessController.doPrivileged(pa);<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">DefaultSelectorProvider</span><span class="hljs-params">()</span> &#123; &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> SelectorProviderImpl <span class="hljs-title function_">get</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> INSTANCE;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>分析 DefaultSelectorProvider，.get() 方法就是返回了 EPollSelectorProvider 创建出的一个实例，其实就是 EPollSelectorProvider。</p><p>回到最开始的代码中，<code>SelectorProvider.provider().openSelector()</code>，接下来就是执行 openSelector() 方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">EPollSelectorProvider</span><br>    <span class="hljs-keyword">extends</span> <span class="hljs-title class_">SelectorProviderImpl</span><br>&#123;<br>    <span class="hljs-keyword">public</span> AbstractSelector <span class="hljs-title function_">openSelector</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">EPollSelectorImpl</span>(<span class="hljs-built_in">this</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码第5行创建出了我们平时在 linux 中说的 EpollSelector 实现（这里再说一下：<strong>多路复用是一个模型，select、poll、epoll 是对这个模型的实现</strong>）</p><p>接下来看一下创建 EPollSelectorImpl 都做了什么</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java">EPollSelectorImpl(SelectorProvider sp) <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-built_in">super</span>(sp);<br><br>    <span class="hljs-built_in">this</span>.epfd = EPoll.create();<br>    <span class="hljs-built_in">this</span>.pollArrayAddress = EPoll.allocatePollArray(NUM_EPOLLEVENTS);<br><br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-built_in">this</span>.eventfd = <span class="hljs-keyword">new</span> <span class="hljs-title class_">EventFD</span>();<br>        IOUtil.configureBlocking(IOUtil.newFD(eventfd.efd()), <span class="hljs-literal">false</span>);<br>    &#125; <span class="hljs-keyword">catch</span> (IOException ioe) &#123;<br>        EPoll.freePollArray(pollArrayAddress);<br>        FileDispatcherImpl.closeIntFD(epfd);<br>        <span class="hljs-keyword">throw</span> ioe;<br>    &#125;<br><br>    <span class="hljs-comment">// register the eventfd object for wakeups</span><br>    EPoll.ctl(epfd, EPOLL_CTL_ADD, eventfd.efd(), EPOLLIN);<br>&#125;<br></code></pre></td></tr></table></figure><p>第4行执行了 Epoll.create() 创建了一个 epoll 实例，epfd 即这个对象的描述符（linux万物皆文件，因此通过fd即可定位到对象），第5行分配了一部分空间并返回了空间的起始地址，这个地址在后续会用到，存放发生的事件用。</p><p>第8、9、17行创建了一个特殊的 Event，并把它的 fd 赋值给了 EPollSelectorImpl 这个实例的一个属性，其实这个 fd 是为了唤醒 epoll 使用的，我们知道在 selector.select() 过程中，如果不设置超时时间，则 epoll 会一直阻塞下去，Java NIO 设置了一个特殊的 fd，当这个 fd 发生事件时，会唤醒 epoll。</p><p>到这里  Selector.open() 的逻辑就结束了</p><h2 id="ServerSocketChannel-register"><a href="#ServerSocketChannel-register" class="headerlink" title="ServerSocketChannel.register()"></a>ServerSocketChannel.register()</h2><p>初次看到这个方法时可能会觉得奇怪，因为这是 socket 的 register 方法，还需要传一个 selector 进来，我们知道多路复用其实是一个 selector 处理多个 socket 的事件，那么为什么是调用 socket 的注册方法传 selector 而不是调用 selector 的注册方法传入 channel 呢？源码会告诉我们答案</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> SelectionKey <span class="hljs-title function_">register</span><span class="hljs-params">(Selector sel, <span class="hljs-type">int</span> ops, Object att)</span><br>    <span class="hljs-keyword">throws</span> ClosedChannelException<br>&#123;<br>    <span class="hljs-keyword">if</span> ((ops &amp; ~validOps()) != <span class="hljs-number">0</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>();<br>    <span class="hljs-keyword">if</span> (!isOpen())<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClosedChannelException</span>();<br>    <span class="hljs-keyword">synchronized</span> (regLock) &#123;<br>        <span class="hljs-keyword">if</span> (isBlocking())<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalBlockingModeException</span>();<br>        <span class="hljs-keyword">synchronized</span> (keyLock) &#123;<br>            <span class="hljs-comment">// re-check if channel has been closed</span><br>            <span class="hljs-keyword">if</span> (!isOpen())<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClosedChannelException</span>();<br>            <span class="hljs-type">SelectionKey</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> findKey(sel);<br>            <span class="hljs-keyword">if</span> (k != <span class="hljs-literal">null</span>) &#123;<br>                k.attach(att);<br>                k.interestOps(ops);<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-comment">// New registration</span><br>                k = ((AbstractSelector)sel).register(<span class="hljs-built_in">this</span>, ops, att);<br>                addKey(k);<br>            &#125;<br>            <span class="hljs-keyword">return</span> k;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>第4行到第7行其实就是做了一些校验，一般都不会有什么问题，例如 serverSocketChannel 只能监听 accept 事件。</p><p>第15行到23行是 register 的重点，因为这是第一次注册，所以 findKey 一定是找不到的，因此会执行第21行，而第21行将 selector 强转为了 AbstractSelector 并调用了注册方法，传入了 当前对象(ServerSocketChannel)，关心的事件和一个 null，这里其实解答了我们开始的问题，最终还是调用了 selector 的注册方法，在注册完成后把返回值添加到了某个容器里。</p><p>我们首先看 selector 的 register 是如何实现的</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java">   <span class="hljs-comment">// The set of keys registered with this Selector</span><br>   <span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Set&lt;SelectionKey&gt; keys;    <br><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> SelectionKey <span class="hljs-title function_">register</span><span class="hljs-params">(AbstractSelectableChannel ch,</span><br><span class="hljs-params">                                         <span class="hljs-type">int</span> ops,</span><br><span class="hljs-params">                                         Object attachment)</span><br>   &#123;<br>       <span class="hljs-keyword">if</span> (!(ch <span class="hljs-keyword">instanceof</span> SelChImpl))<br>           <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalSelectorException</span>();<br>       <span class="hljs-type">SelectionKeyImpl</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">SelectionKeyImpl</span>((SelChImpl)ch, <span class="hljs-built_in">this</span>);<br>       <span class="hljs-keyword">if</span> (attachment != <span class="hljs-literal">null</span>)<br>           k.attach(attachment);<br>       <br>       implRegister(k);<br>       <br>       keys.add(k);<br>       <span class="hljs-keyword">try</span> &#123;<br>           k.interestOps(ops);<br>       &#125; <span class="hljs-keyword">catch</span> (ClosedSelectorException e) &#123;<br>           <span class="hljs-keyword">assert</span> ch.keyFor(<span class="hljs-built_in">this</span>) == <span class="hljs-literal">null</span>;<br>           keys.remove(k);<br>           k.cancel();<br>           <span class="hljs-keyword">throw</span> e;<br>       &#125;<br>       <span class="hljs-keyword">return</span> k;<br>   &#125;<br></code></pre></td></tr></table></figure><p>第10行创建了一个对象，这个对象的构造器有两个参数，一个是 channel，另一个是 this（selector 对象本身），也就是说，**<code>这个 SelectionKeyImpl 本身就关联起了 channel 和 selector</code>**</p><p>接下里的第16行把这个关联对象加到了第2行定义的这个 Set<SelectionKey> 里，注释其实说的很清楚，这个 Set 管理了这个 selector 对象注册的所有 key。</p><p>随后的第18行，为 SelectionKeyImpl  这个关联对象注册感兴趣的事件，</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> SelectionKey <span class="hljs-title function_">interestOps</span><span class="hljs-params">(<span class="hljs-type">int</span> ops)</span> &#123;<br>    ensureValid();<br>    <span class="hljs-keyword">if</span> ((ops &amp; ~channel().validOps()) != <span class="hljs-number">0</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>();<br>    <span class="hljs-type">int</span> <span class="hljs-variable">oldOps</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) INTERESTOPS.getAndSet(<span class="hljs-built_in">this</span>, ops);<br>    <span class="hljs-keyword">if</span> (ops != oldOps) &#123;<br>        selector.setEventOps(<span class="hljs-built_in">this</span>);<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">this</span>;<br>&#125;<br><br><span class="hljs-keyword">protected</span> <span class="hljs-keyword">abstract</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setEventOps</span><span class="hljs-params">(SelectionKeyImpl ski)</span>;<br></code></pre></td></tr></table></figure><p>第7行调用了第12行的抽象函数，这个抽象函数被 EPollSelectorImpl 重写了</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">final</span> Deque&lt;SelectionKeyImpl&gt; updateKeys = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayDeque</span>&lt;&gt;();<br><span class="hljs-meta">@Override</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setEventOps</span><span class="hljs-params">(SelectionKeyImpl ski)</span> &#123;<br>    ensureOpen();<br>    <span class="hljs-keyword">synchronized</span> (updateLock) &#123;<br>        updateKeys.addLast(ski);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>分析上述代码，这个感兴趣的事件其实没有真正的注册到内核中，只是用一个队列缓存起来了~</p><p>上述这么多的代码，其实只是完成了本小节最开始那段代码中的<code>k = ((AbstractSelector)sel).register(this, ops, att);</code>，为了不用回头看，下面又复制了一份本小节最开始的那段代码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> SelectionKey <span class="hljs-title function_">register</span><span class="hljs-params">(Selector sel, <span class="hljs-type">int</span> ops, Object att)</span><br>    <span class="hljs-keyword">throws</span> ClosedChannelException<br>&#123;<br>    <span class="hljs-keyword">if</span> ((ops &amp; ~validOps()) != <span class="hljs-number">0</span>)<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalArgumentException</span>();<br>    <span class="hljs-keyword">if</span> (!isOpen())<br>        <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClosedChannelException</span>();<br>    <span class="hljs-keyword">synchronized</span> (regLock) &#123;<br>        <span class="hljs-keyword">if</span> (isBlocking())<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalBlockingModeException</span>();<br>        <span class="hljs-keyword">synchronized</span> (keyLock) &#123;<br>            <span class="hljs-comment">// re-check if channel has been closed</span><br>            <span class="hljs-keyword">if</span> (!isOpen())<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClosedChannelException</span>();<br>            <span class="hljs-type">SelectionKey</span> <span class="hljs-variable">k</span> <span class="hljs-operator">=</span> findKey(sel);<br>            <span class="hljs-keyword">if</span> (k != <span class="hljs-literal">null</span>) &#123;<br>                k.attach(att);<br>                k.interestOps(ops);<br>            &#125; <span class="hljs-keyword">else</span> &#123;<br>                <span class="hljs-comment">// New registration</span><br>                k = ((AbstractSelector)sel).register(<span class="hljs-built_in">this</span>, ops, att);<br>                addKey(k);<br>            &#125;<br>            <span class="hljs-keyword">return</span> k;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>在执行完第21行注册在 selector 中注册完成后，把返回的这个关联对象在 this(ServerSocketChannel) 中也添加起来，这里其实是添加到了一个 SelectionKey 的数组中，至于为什么在 selector 对象中用 Set 存储而在 Channel 对象中用数组存储，也是一个值得思考的问题</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> SelectionKey[] keys = <span class="hljs-literal">null</span>;<br></code></pre></td></tr></table></figure><p>至此我们走完了第19-23行 else 的逻辑，现在开始回顾第15行的逻辑</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> SelectionKey <span class="hljs-title function_">findKey</span><span class="hljs-params">(Selector sel)</span> &#123;<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(keyLock);<br>    <span class="hljs-keyword">if</span> (keys == <span class="hljs-literal">null</span>)<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; keys.length; i++)<br>        <span class="hljs-keyword">if</span> ((keys[i] != <span class="hljs-literal">null</span>) &amp;&amp; (keys[i].selector() == sel))<br>            <span class="hljs-keyword">return</span> keys[i];<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">null</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>findKey 其实就是在当前的 SelectionKey[] 数组中找当前的 selector 是否添加过，如果已经添加过了，那么第18行会根据一些逻辑判断修改其注册的事件（当然也是添加到 EPollSelectorImpl 的 updateKeys 队列里了）。</p><p>现在解答一下为什么 Channel 中存储 SelectionKey 用数组，而 selector 中存储 SelectionKey 用 Set，每一个 SocketChannel 其实一般只有一个 Selector 注册，也就只有一个 SelectionKey，少有既注册 epoll 又注册 poll 这样的，而且缓冲区中的数据最后只能读取一次，注册多个 Selector 也没用；但 Selector 中可能注册多个 Channel，当客户端关闭连接时需要删除注册的 Channel，使用 Set 以 O(1) 的速度进行操作，这也体现了 NIO 高性能的追求。</p><h2 id="selector-select"><a href="#selector-select" class="headerlink" title="selector.select()"></a>selector.select()</h2><p>这也是 NIO 中的最后一个重要的方法了</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">select</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-keyword">return</span> lockAndDoSelect(<span class="hljs-literal">null</span>, -<span class="hljs-number">1</span>);<br>&#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-title function_">lockAndDoSelect</span><span class="hljs-params">(Consumer&lt;SelectionKey&gt; action, <span class="hljs-type">long</span> timeout)</span><br>    <span class="hljs-keyword">throws</span> IOException<br>&#123;<br>    <span class="hljs-keyword">synchronized</span> (<span class="hljs-built_in">this</span>) &#123;<br>        ensureOpen();<br>        <span class="hljs-keyword">if</span> (inSelect)<br>            <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">IllegalStateException</span>(<span class="hljs-string">&quot;select in progress&quot;</span>);<br>        inSelect = <span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-keyword">synchronized</span> (publicSelectedKeys) &#123;<br>                <span class="hljs-keyword">return</span> doSelect(action, timeout);<br>            &#125;<br>        &#125; <span class="hljs-keyword">finally</span> &#123;<br>            inSelect = <span class="hljs-literal">false</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>代码的第9-13行使用二次检查防止重入（禁止在select的过程中再次select，而接下来才是真正的实现</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Override</span><br><span class="hljs-keyword">protected</span> <span class="hljs-type">int</span> <span class="hljs-title function_">doSelect</span><span class="hljs-params">(Consumer&lt;SelectionKey&gt; action, <span class="hljs-type">long</span> timeout)</span><br>    <span class="hljs-keyword">throws</span> IOException<br>&#123;<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(<span class="hljs-built_in">this</span>);<br><br>    <span class="hljs-comment">// epoll_wait timeout is int</span><br>    <span class="hljs-type">int</span> <span class="hljs-variable">to</span> <span class="hljs-operator">=</span> (<span class="hljs-type">int</span>) Math.min(timeout, Integer.MAX_VALUE);<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">blocking</span> <span class="hljs-operator">=</span> (to != <span class="hljs-number">0</span>);<br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">timedPoll</span> <span class="hljs-operator">=</span> (to &gt; <span class="hljs-number">0</span>);<br><br>    <span class="hljs-type">int</span> numEntries;<br>    processUpdateQueue();<br>    processDeregisterQueue();<br>    <span class="hljs-keyword">try</span> &#123;<br>        begin(blocking);<br><br>        <span class="hljs-keyword">do</span> &#123;<br>            <span class="hljs-type">long</span> <span class="hljs-variable">startTime</span> <span class="hljs-operator">=</span> timedPoll ? System.nanoTime() : <span class="hljs-number">0</span>;<br>            numEntries = EPoll.wait(epfd, pollArrayAddress, NUM_EPOLLEVENTS, to);<br>            <span class="hljs-keyword">if</span> (numEntries == IOStatus.INTERRUPTED &amp;&amp; timedPoll) &#123;<br>                <span class="hljs-comment">// timed poll interrupted so need to adjust timeout</span><br>                <span class="hljs-type">long</span> <span class="hljs-variable">adjust</span> <span class="hljs-operator">=</span> System.nanoTime() - startTime;<br>                to -= TimeUnit.MILLISECONDS.convert(adjust, TimeUnit.NANOSECONDS);<br>                <span class="hljs-keyword">if</span> (to &lt;= <span class="hljs-number">0</span>) &#123;<br>                    <span class="hljs-comment">// timeout expired so no retry</span><br>                    numEntries = <span class="hljs-number">0</span>;<br>                &#125;<br>            &#125;<br>        &#125; <span class="hljs-keyword">while</span> (numEntries == IOStatus.INTERRUPTED);<br>        <span class="hljs-keyword">assert</span> IOStatus.check(numEntries);<br><br>    &#125; <span class="hljs-keyword">finally</span> &#123;<br>        end(blocking);<br>    &#125;<br>    processDeregisterQueue();<br>    <span class="hljs-keyword">return</span> processEvents(numEntries, action);<br>&#125;<br></code></pre></td></tr></table></figure><p>第7-10行，由于c语言的实现阻塞时间是一个int类型，因此这里需要转换一下。</p><p>第13行 <code>processUpdateQueue</code>，在上一小节中提到，会把需要注册或修改的事件存到  EPollSelectorImpl.updateKeys 这个队列里了，这里会先处理这些待更新的事件队列</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processUpdateQueue</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(<span class="hljs-built_in">this</span>);<br>    <span class="hljs-keyword">synchronized</span> (updateLock) &#123;<br>        SelectionKeyImpl ski;<br>        <span class="hljs-keyword">while</span> ((ski = updateKeys.pollFirst()) != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">if</span> (ski.isValid()) &#123;<br>                <span class="hljs-type">int</span> <span class="hljs-variable">fd</span> <span class="hljs-operator">=</span> ski.getFDVal();<br>                <span class="hljs-comment">// add to fdToKey if needed</span><br>                <span class="hljs-type">SelectionKeyImpl</span> <span class="hljs-variable">previous</span> <span class="hljs-operator">=</span> fdToKey.putIfAbsent(fd, ski);<br>                <span class="hljs-keyword">assert</span> (previous == <span class="hljs-literal">null</span>) || (previous == ski);<br>                <span class="hljs-type">int</span> <span class="hljs-variable">newEvents</span> <span class="hljs-operator">=</span> ski.translateInterestOps();<br>                <span class="hljs-type">int</span> <span class="hljs-variable">registeredEvents</span> <span class="hljs-operator">=</span> ski.registeredEvents();<br>                <span class="hljs-keyword">if</span> (newEvents != registeredEvents) &#123;<br>                    <span class="hljs-keyword">if</span> (newEvents == <span class="hljs-number">0</span>) &#123;<br>                        <span class="hljs-comment">// remove from epoll</span><br>                        EPoll.ctl(epfd, EPOLL_CTL_DEL, fd, <span class="hljs-number">0</span>);<br>                    &#125; <span class="hljs-keyword">else</span> &#123;<br>                        <span class="hljs-keyword">if</span> (registeredEvents == <span class="hljs-number">0</span>) &#123;<br>                            <span class="hljs-comment">// add to epoll</span><br>                            EPoll.ctl(epfd, EPOLL_CTL_ADD, fd, newEvents);<br>                        &#125; <span class="hljs-keyword">else</span> &#123;<br>                            <span class="hljs-comment">// modify events</span><br>                            EPoll.ctl(epfd, EPOLL_CTL_MOD, fd, newEvents);<br>                        &#125;<br>                    &#125;<br>                    ski.registeredEvents(newEvents);<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>第9行通过一个 map 拿到 SelectionKeyImpl 这个关联对象中 channel 的 fd，如果之前没有注册过，则 previous 为 null，否则肯定和之前一样，第11行将 jdk 的事件转换为内核中的事件表示，根据 newEvents 和之前注册事件 registeredEvents 的对比，决定调用 Epoll 的哪个方法(删除、增加、修改)</p><p>在处理完待更新对象后，处理取消注册的事件也是同理</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">protected</span> <span class="hljs-keyword">final</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">processDeregisterQueue</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> IOException &#123;<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(<span class="hljs-built_in">this</span>);<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(publicSelectedKeys);<br><br>    <span class="hljs-keyword">synchronized</span> (cancelledKeys) &#123;<br>        SelectionKeyImpl ski;<br>        <span class="hljs-keyword">while</span> ((ski = cancelledKeys.pollFirst()) != <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-comment">// remove the key from the selector</span><br>            implDereg(ski);<br><br>            selectedKeys.remove(ski);<br>            keys.remove(ski);<br><br>            <span class="hljs-comment">// remove from channel&#x27;s key set</span><br>            deregister(ski);<br><br>            <span class="hljs-type">SelectableChannel</span> <span class="hljs-variable">ch</span> <span class="hljs-operator">=</span> ski.channel();<br>            <span class="hljs-keyword">if</span> (!ch.isOpen() &amp;&amp; !ch.isRegistered())<br>                ((SelChImpl)ch).kill();<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>注意第11、12行都调用了 Remove 方法，这也解释了上小节中为什么 SelectorImpl 存放 Selection 对象用 Set，因为可能需要频繁查找或移除，第15行从 channel 的数组中也移除。</p><p>在处理完变动的事件后，继续分析接下来的逻辑</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    begin(blocking);<br><br>    <span class="hljs-keyword">do</span> &#123;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">startTime</span> <span class="hljs-operator">=</span> timedPoll ? System.nanoTime() : <span class="hljs-number">0</span>;<br>        numEntries = EPoll.wait(epfd, pollArrayAddress, NUM_EPOLLEVENTS, to);<br>        <span class="hljs-keyword">if</span> (numEntries == IOStatus.INTERRUPTED &amp;&amp; timedPoll) &#123;<br>            <span class="hljs-comment">// timed poll interrupted so need to adjust timeout</span><br>            <span class="hljs-type">long</span> <span class="hljs-variable">adjust</span> <span class="hljs-operator">=</span> System.nanoTime() - startTime;<br>            to -= TimeUnit.MILLISECONDS.convert(adjust, TimeUnit.NANOSECONDS);<br>            <span class="hljs-keyword">if</span> (to &lt;= <span class="hljs-number">0</span>) &#123;<br>                <span class="hljs-comment">// timeout expired so no retry</span><br>                numEntries = <span class="hljs-number">0</span>;<br>            &#125;<br>        &#125;<br>    &#125; <span class="hljs-keyword">while</span> (numEntries == IOStatus.INTERRUPTED);<br>    <span class="hljs-keyword">assert</span> IOStatus.check(numEntries);<br><br>&#125; <span class="hljs-keyword">finally</span> &#123;<br>    end(blocking);<br>&#125;<br>processDeregisterQueue();<br><span class="hljs-keyword">return</span> processEvents(numEntries, action);<br></code></pre></td></tr></table></figure><p>第4-17行的这个循环，其实是在第一小节中创建 epoll 时 JDK 注册了一个唤醒 epoll 用的事件，这里就是判断如果是被这个事件打断了（第16行），需要根据阻塞时间重新计算 timeout 的时间（第7到13行）</p><p>正常情况下发送了其他事件，就会退出循环，再次处理取消注册队列中的 SelectionKey，numEntries 就是返回发生事件数，接下来开始处理这些事件</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">int</span> <span class="hljs-title function_">processEvents</span><span class="hljs-params">(<span class="hljs-type">int</span> numEntries, Consumer&lt;SelectionKey&gt; action)</span><br>    <span class="hljs-keyword">throws</span> IOException<br>&#123;<br>    <span class="hljs-keyword">assert</span> Thread.holdsLock(<span class="hljs-built_in">this</span>);<br><br>    <span class="hljs-type">boolean</span> <span class="hljs-variable">interrupted</span> <span class="hljs-operator">=</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-type">int</span> <span class="hljs-variable">numKeysUpdated</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>; i&lt;numEntries; i++) &#123;<br>        <span class="hljs-type">long</span> <span class="hljs-variable">event</span> <span class="hljs-operator">=</span> EPoll.getEvent(pollArrayAddress, i);<br>        <span class="hljs-type">int</span> <span class="hljs-variable">fd</span> <span class="hljs-operator">=</span> EPoll.getDescriptor(event);<br>        <span class="hljs-keyword">if</span> (fd == eventfd.efd()) &#123;<br>            interrupted = <span class="hljs-literal">true</span>;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-type">SelectionKeyImpl</span> <span class="hljs-variable">ski</span> <span class="hljs-operator">=</span> fdToKey.get(fd);<br>            <span class="hljs-keyword">if</span> (ski != <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-type">int</span> <span class="hljs-variable">rOps</span> <span class="hljs-operator">=</span> EPoll.getEvents(event);<br>                numKeysUpdated += processReadyEvents(rOps, ski, action);<br>            &#125;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (interrupted) &#123;<br>        clearInterrupt();<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> numKeysUpdated;<br>&#125;<br></code></pre></td></tr></table></figure><p>第9行的 pollArrayAddress 其实在第一小节创建 selector 时候介绍过，是 epoll 实例用来存储 epoll_wait 函数返回事件的容器起始地址，因此可以通过这个起始地址和事件序获取到这个事件对象。</p><p>如果这个事件对象的描述符是我们在创建 epoll 对象时注册的那个中断 fd，则标记位 interrupted 修改为 true，否则从 Map&lt;fd,SelectionKeyImpl&gt; 这个 map 中拿到一个具体的关联对象（在本节处理 updateKeys 的队列时存放进去的）。</p><p>拿到关联对象意味着拿到了注册的事件，第17行<code>processReadyEvents</code>会比对真正发生的事件和注册的感兴趣的事件，如果满足某些条件才认为是应用层真正感兴趣的事件，存放到 selectedKeys 容器中，最后返回应用层真正感兴趣的事件数。</p><p>业务层在解除阻塞后调用 <code>            Set&lt;SelectionKey&gt; selectionKeys = selector.selectedKeys();</code> 其实就是返回的这些内容。因此在处理完成需要调用迭代器的 Remove 方法移除，因为 NIO 只告诉应用层这些关联对象中的 Channel 有事件，但并不知道应用层是否处理完成，需要主动移除。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>到这里 Java NIO 的源码就梳理结束了，以上内容均基于 jdk-17.0.17 的 linux 版本，如果是 Windows 则根本不会选 Epoll 的实现，版本不一致也可能会有小的 API 差异。</p><p><img src="/img/image-20260123170610411.png" alt="image-20260123170610411"></p><p>这是源码中几个重要的类的 uml 图，新手可能会很懵，什么 selector、SelectionKey、SelectorImpl 什么的，尤其是成员变量有的还一致~</p>]]></content>
    
    
    <categories>
      
      <category>源码分析</category>
      
    </categories>
    
    
    <tags>
      
      <tag>IO模型</tag>
      
      <tag>源码分析</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MybatisPlus配置联合主键</title>
    <link href="/2026/04/21/MybatisPlus%E9%85%8D%E7%BD%AE%E8%81%94%E5%90%88%E4%B8%BB%E9%94%AE/"/>
    <url>/2026/04/21/MybatisPlus%E9%85%8D%E7%BD%AE%E8%81%94%E5%90%88%E4%B8%BB%E9%94%AE/</url>
    
    <content type="html"><![CDATA[<h1 id="MybatisPlus配置联合主键"><a href="#MybatisPlus配置联合主键" class="headerlink" title="MybatisPlus配置联合主键"></a>MybatisPlus配置联合主键</h1><p>背景是这样的，最近着手修改一个老项目，平时其他项目用的都是 JPA、倒不是不会用 Mybatis，只是这个项目是用来解析 xml 为对象并进行存储的，而且有的类字段特别多，觉得写 mapper 和 sql 太麻烦了，于是想到了 mybatis-plus（以下简称mp）。</p><p>本来以为很好解决的事情，没想到默认的 mp 不支持联合主键，于是查询了一些资料，有一个 mybatisplus-plus（以下简称mpp） 的工具，因此总结一个 demo 出来，包括整合过程和其中遇到的问题和解决方式。</p><p>1、引入依赖</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">properties</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">mybatis-plus.version</span>&gt;</span>3.4.0<span class="hljs-tag">&lt;/<span class="hljs-name">mybatis-plus.version</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">mybatis-plus-plus.version</span>&gt;</span>1.5.1-RELEASE<span class="hljs-tag">&lt;/<span class="hljs-name">mybatis-plus-plus.version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">properties</span>&gt;</span><br></code></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.baomidou<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>mybatis-plus-boot-starter<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>$&#123;mybatis-plus.version&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">dependency</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>com.github.jeffreyning<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>mybatisplus-plus<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>$&#123;mybatis-plus-plus.version&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">dependency</span>&gt;</span><br></code></pre></td></tr></table></figure><p>注意事项：</p><ul><li>mp 和 mpp 的版本号对应关系是有要求的，版本不匹配会出现意想不到的错误</li></ul><p>2、实体类定义</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.pojo;<br><br><span class="hljs-keyword">import</span> com.baomidou.mybatisplus.annotation.TableField;<br><span class="hljs-keyword">import</span> com.baomidou.mybatisplus.annotation.TableName;<br><span class="hljs-keyword">import</span> com.github.jeffreyning.mybatisplus.anno.MppMultiId;<br><span class="hljs-keyword">import</span> lombok.Data;<br><br><span class="hljs-meta">@Data</span><br><span class="hljs-meta">@TableName(value = &quot;MP_USER&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">User</span> &#123;<br><br>    <span class="hljs-meta">@MppMultiId</span><br>    <span class="hljs-meta">@TableField(value = &quot;id&quot;)</span><br>    <span class="hljs-keyword">private</span> String uuid;<br><br>    <span class="hljs-meta">@MppMultiId</span><br>    <span class="hljs-meta">@TableField(value = &quot;xh&quot;)</span><br>    <span class="hljs-keyword">private</span> Integer xh;<br><br>    <span class="hljs-keyword">private</span> String name;<br><br>    <span class="hljs-keyword">private</span> Integer age;<br><br>    <span class="hljs-keyword">private</span> String email;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>我在项目中日常遇到的联合主键就是 uuid + xh 来解决，当然用 uuid 做主键，再用另一个字段来关联其他表也可以，而且也没有联合主键这个问题。</p><p>注意事项：</p><ul><li>使用的是 <code>@MppMultiId</code> 而不是 <code>@TableId</code></li><li>虽然数据库字段叫 id, 但我在类中的定义却是 uuid</li></ul><p>3、Mapper（dao）定义</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.mapper;<br><br><span class="hljs-keyword">import</span> com.github.jeffreyning.mybatisplus.base.MppBaseMapper;<br><span class="hljs-keyword">import</span> com.xsdl.pojo.User;<br><span class="hljs-keyword">import</span> org.apache.ibatis.annotations.Mapper;<br><br><span class="hljs-meta">@Mapper</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">UserMapper</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MppBaseMapper</span>&lt;User&gt; &#123;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>注意事项：</p><ul><li>继承的是 MppBaseMapper，而不是 BaseMapper</li></ul><p>4、简单使用</p><p>如果你只是想要使用它进行简单的持久化操作，现在已经足够了，例如下面的用法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.controller;<br><br><span class="hljs-keyword">import</span> com.xsdl.mapper.UserMapper;<br><span class="hljs-keyword">import</span> com.xsdl.pojo.User;<br><span class="hljs-keyword">import</span> lombok.extern.slf4j.Slf4j;<br><span class="hljs-keyword">import</span> org.springframework.beans.factory.annotation.Autowired;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.GetMapping;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.RestController;<br><br><span class="hljs-keyword">import</span> java.util.List;<br><span class="hljs-keyword">import</span> java.util.stream.Collectors;<br><span class="hljs-keyword">import</span> java.util.stream.IntStream;<br><br><span class="hljs-meta">@RestController</span><br><span class="hljs-meta">@RequestMapping(&quot;/user&quot;)</span><br><span class="hljs-meta">@Slf4j</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserController</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> UserMapper userMapper;<br><br>    <span class="hljs-meta">@GetMapping(&quot;/list&quot;)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">list</span><span class="hljs-params">()</span> &#123;<br>        List&lt;User&gt; users = userMapper.selectList(<span class="hljs-literal">null</span>);<br>        <span class="hljs-keyword">for</span> (User user : users) &#123;<br>            log.info(<span class="hljs-string">&quot;user:&#123;&#125;&quot;</span>, user);<br>        &#125;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; users.size(); i++) &#123;<br>            <span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> users.get(i);<br>            user.setName(<span class="hljs-string">&quot;xsdl&quot;</span> + i);<br>            userMapper.updateByMultiId(user);<br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>注意事项：</p><ul><li>这里的 update 操作，我用的是 updateByMultiId，不能用 updateById</li></ul><p>但是我都用联合主键了，说明我大概率是想要批量保存的，而 MppBaseMapper 并不支持这样的操作，于是需要引入 Service</p><p>5、Service 定义</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.service;<br><br><span class="hljs-keyword">import</span> com.github.jeffreyning.mybatisplus.service.IMppService;<br><span class="hljs-keyword">import</span> com.xsdl.pojo.User;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">UserService</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">IMppService</span>&lt;User&gt; &#123;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>注意事项：</p><ul><li>继承了 IMppService</li></ul><p>6、ServiceImpl 定义</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.service;<br><br><span class="hljs-keyword">import</span> com.github.jeffreyning.mybatisplus.service.MppServiceImpl;<br><span class="hljs-keyword">import</span> com.xsdl.mapper.UserMapper;<br><span class="hljs-keyword">import</span> com.xsdl.pojo.User;<br><span class="hljs-keyword">import</span> org.springframework.stereotype.Service;<br><br><span class="hljs-meta">@Service(&quot;userService&quot;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserServiceImpl</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">MppServiceImpl</span>&lt;UserMapper, User&gt; <span class="hljs-keyword">implements</span> <span class="hljs-title class_">UserService</span> &#123;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>注意事项：</p><ul><li>先继承 MppServiceImpl，并把对应的 Mapper 传入再实现对应的 Service</li></ul><p>7、使用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.xsdl.controller;<br><br><span class="hljs-keyword">import</span> com.xsdl.mapper.UserMapper;<br><span class="hljs-keyword">import</span> com.xsdl.pojo.User;<br><span class="hljs-keyword">import</span> com.xsdl.service.UserService;<br><span class="hljs-keyword">import</span> lombok.extern.slf4j.Slf4j;<br><span class="hljs-keyword">import</span> org.springframework.beans.factory.annotation.Autowired;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.GetMapping;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;<br><span class="hljs-keyword">import</span> org.springframework.web.bind.annotation.RestController;<br><br><span class="hljs-keyword">import</span> java.util.List;<br><span class="hljs-keyword">import</span> java.util.stream.Collectors;<br><span class="hljs-keyword">import</span> java.util.stream.IntStream;<br><br><span class="hljs-meta">@RestController</span><br><span class="hljs-meta">@RequestMapping(&quot;/user&quot;)</span><br><span class="hljs-meta">@Slf4j</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserController</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> UserMapper userMapper;<br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> UserService userService;<br><br>    <span class="hljs-meta">@GetMapping(&quot;/list&quot;)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">list</span><span class="hljs-params">()</span> &#123;<br>        List&lt;User&gt; users = userMapper.selectList(<span class="hljs-literal">null</span>);<br>        <span class="hljs-keyword">for</span> (User user : users) &#123;<br>            log.info(<span class="hljs-string">&quot;user:&#123;&#125;&quot;</span>, user);<br>        &#125;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> <span class="hljs-variable">i</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>; i &lt; users.size(); i++) &#123;<br>            <span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> users.get(i);<br>            user.setName(<span class="hljs-string">&quot;zss&quot;</span> + i);<br>            userMapper.updateByMultiId(user);<br>        &#125;<br><br>        List&lt;User&gt; userList = IntStream.rangeClosed(<span class="hljs-number">1</span>, <span class="hljs-number">10</span>).mapToObj(i -&gt; &#123;<br>            <span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">User</span>();<br>            user.setUuid(i + <span class="hljs-string">&quot;&quot;</span> + i);<br>            user.setXh(i);<br>            user.setName(<span class="hljs-string">&quot;zss&quot;</span> + i);<br>            user.setAge(i);<br>            user.setEmail(<span class="hljs-string">&quot;zss&quot;</span> + i + <span class="hljs-string">&quot;@qq.com&quot;</span>);<br>            <span class="hljs-keyword">return</span> user;<br>        &#125;).collect(Collectors.toList());<br>        userService.saveBatch(userList);<br><br>        List&lt;User&gt; list = userService.list();<br>        <span class="hljs-keyword">for</span> (User user : list) &#123;<br>            log.info(<span class="hljs-string">&quot;user:&#123;&#125;&quot;</span>, user);<br>        &#125;<br>    &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>可能出现的问题:</p><ol><li>我在第二步 实体类定义 这里强调说，虽然数据库字段名是 id，但我的属性名却是 uuid，如果设置为 id，启动项目会出现这样的错误 org.springframework.beans.factory.UnsatisfiedDependencyException: Error creating bean with name ‘userController’: Unsatisfied dependency expressed through field ‘userMapper’; nested exception is org.springframework.beans.factory.BeanCreationException: Error creating bean with name ‘userMapper’ defined in file Invocation of init method failed; nested exception is java.lang.IllegalArgumentException: java.lang.RuntimeException: not found column for id</li></ol><p>大概意思就是说创建 UserMapper 失败，原因是找不到 id 列，我没详细去解决这个错误，推测原因可能是 mp 里把类里的 id 属性自动解释为主键，解决方式就是像我一样，给它起个新的名字，然后使用  <code>@TableField(value = &quot;id&quot;)</code> 标记一下</p><ol start="2"><li>因为我接手的这个项目早期是 mybatis 嘛，我虽然引入了 mp，但不敢删除之前的 mybatis 的依赖，于是二者共存了，有机会遇到这样的错误 Error creating bean with name ‘com.github.jeffreyning.mybatisplus.conf.PlusConfig’: Invocation of init method failed; nested exception is java.lang.NoSuchFieldException: CLASS_RESOLVER</li></ol><p>这个错误就通俗易懂多了，说创建这个 bean 失败，原因是根本就没有一个 CLASS_RESOLVER 的字段，进入 PlusConfig 查看源码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.github.jeffreyning.mybatisplus.conf;<br><br><span class="hljs-meta">@Import(&#123;PlusACUtils.class, MppSqlInjector.class&#125;)</span><br><span class="hljs-meta">@Configuration</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PlusConfig</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> LoggerFactory.getLogger(PlusConfig.class);<br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> Environment env;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-title function_">PlusConfig</span><span class="hljs-params">()</span> &#123;<br>    &#125;<br><br>    <span class="hljs-meta">@PostConstruct</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">initRM</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">NhOgnlClassResolver</span> <span class="hljs-variable">resolver</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">NhOgnlClassResolver</span>();<br>        resolver.baseList.add(<span class="hljs-string">&quot;com.github.jeffreyning.mybatisplus.check&quot;</span>);<br>        LambdaUtil.setValue(OgnlCache.class, <span class="hljs-string">&quot;CLASS_RESOLVER&quot;</span>, resolver);<br>        <span class="hljs-type">String</span> <span class="hljs-variable">utilBasePaths</span> <span class="hljs-operator">=</span> <span class="hljs-built_in">this</span>.env.getProperty(<span class="hljs-string">&quot;mpp.utilBasePath&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>17 行通过工具类向 OgnlCache 的 CLASS_RESOLVER 设置值，但是这个字段在 OgnlCache 类不存在，这个其实是 mybatis 和 mpp  版本问题，mybatis 的早期版本中，OgnlCache 没有这个字段，因此修改下 mybatis 的版本就可以了，我这里修改到了 3.5.5</p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java">&lt;dependency&gt;<br>&lt;groupId&gt;org.mybatis&lt;/groupId&gt;<br>&lt;artifactId&gt;mybatis&lt;/artifactId&gt;<br>&lt;version&gt;<span class="hljs-number">3.5</span><span class="hljs-number">.5</span>&lt;/version&gt;<br>&lt;/dependency&gt;<br></code></pre></td></tr></table></figure><p>不过我后来发现 mp 和 springboot 的依赖中其实已经引入了 mybatis 的依赖，似乎把老的删了就行，总之这个问题跟 mybatis 的版本有关系</p>]]></content>
    
    
    
    <tags>
      
      <tag>MybatisPlus</tag>
      
      <tag>配置</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>云原生部署常见服务</title>
    <link href="/2026/04/20/%E4%BA%91%E5%8E%9F%E7%94%9F%E9%83%A8%E7%BD%B2%E5%B8%B8%E8%A7%81%E6%9C%8D%E5%8A%A1/"/>
    <url>/2026/04/20/%E4%BA%91%E5%8E%9F%E7%94%9F%E9%83%A8%E7%BD%B2%E5%B8%B8%E8%A7%81%E6%9C%8D%E5%8A%A1/</url>
    
    <content type="html"><![CDATA[<h1 id="云原生部署常见服务"><a href="#云原生部署常见服务" class="headerlink" title="云原生部署常见服务"></a>云原生部署常见服务</h1><p>在开发过程中，经常需要自己进行一些小小的测试，亦或是简单的验证一些功能，简单功能使用公司的业务库还好，若是公司的业务库小众，或者测试的功能有危险情况，一般都希望能在隔离环境测试，虚拟机对于此场景太重了，用 Docker 轻量的启动一些服务非常方便。</p><p>该博客并非科普向，因此不再赘述 Docker、容器、Kubernetes 基础知识。</p><h2 id="镜像拉取"><a href="#镜像拉取" class="headerlink" title="镜像拉取"></a>镜像拉取</h2><p>内网环境一般公司会自行搭建 Harbor 镜像仓库，在拉取时配置 secret 秘钥即可。公网环境由于网络等限制，一般需要配置镜像加速，阿里云中提供了方法：<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors">阿里云配置镜像加速</a></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">mkdir</span> -p /etc/docker<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tee</span> /etc/docker/daemon.json &lt;&lt;-<span class="hljs-string">&#x27;EOF&#x27;</span><br>&#123;<br>  <span class="hljs-string">&quot;registry-mirrors&quot;</span>: [<span class="hljs-string">&quot;https://quype1o7.mirror.aliyuncs.com&quot;</span>]<br>&#125;<br>EOF<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl daemon-reload<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> systemctl restart docker<br></code></pre></td></tr></table></figure><p>有的时候，即使配置了这个也还是拉不下来，我一般都会去三方仓库（如：<a href="https://docker.aityp.com/">渡渡鸟镜像同步站</a>）拉，找到自己想要的镜像后使用 docker pull 国内镜像下载:</p><p><img src="/img/image-20260420163935539.png" alt="image-20260420163935539"></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker pull swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/fnproject/fn-java-fdk:jre11-1.0.211<br></code></pre></td></tr></table></figure><p>拉下来后这个 tag 太长了，一般我会改成比较简单的 tag：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker tag swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/fnproject/fn-java-fdk:jre11-1.0.211 jre:11<br></code></pre></td></tr></table></figure><p>有时我们内网下载不下镜像，希望在外网下载后导入到内网使用，使用如下命令导出为 tar 文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker save jre:11 -o jre11.tar<br></code></pre></td></tr></table></figure><p>注意我用的 <code>jre:11</code> 如果这里使用镜像ID，在内网导入后会丢失 tag 信息，内网导入：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker load -i jre11.tar<br></code></pre></td></tr></table></figure><p>其实部署服务时镜像的选择有很多知识，如果是想要绝对安全的环境（内部 curl、vim等指令均不存在），则使用只包含基础工具的镜像，像 Java 程序运行时其实只依赖 jre，如果使用 jdk 会导致最后的镜像体积很大，当然这也是一种权衡，有了 jdk 后可以定位一些问题。</p><h2 id="Docker-运行-Mysql"><a href="#Docker-运行-Mysql" class="headerlink" title="Docker 运行 Mysql"></a>Docker 运行 Mysql</h2><p>数据库想必是开发中最常用的工具了，启动指令也非常简单：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run --name mysql -e MYSQL_ROOT_PASSWORD=root -p 3306:3306 -d mysql:5.7<br></code></pre></td></tr></table></figure><p>这是个用完即丢的示例，因为这个指令并没有保存 mysql 的数据，非常适合测试，如果想要保存数据，则命令如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run --name mysql -e MYSQL_ROOT_PASSWORD=root -p 3306:3306 -v /data/mysql:/var/lib/mysql -d mysql:5.7<br></code></pre></td></tr></table></figure><p>其中 <code>-v /data/mysql:/var/lib/mysql</code> 即把容器外的 &#x2F;data&#x2F;mysql 目录挂载到 容器内的 &#x2F;var&#x2F;lib&#x2F;mysql 目录</p><p>注：不同版本的镜像数据目录、环境变量未必完全相同，使用时注意看文档。</p><h2 id="Docker-运行-Redis"><a href="#Docker-运行-Redis" class="headerlink" title="Docker 运行 Redis"></a>Docker 运行 Redis</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker run --name redis -d -p 6379:6379 redis:5.0.8<br></code></pre></td></tr></table></figure><p>依然是用完即丢的示例，并且没有设置账号和密码，这种都倾向于用完即弃，生产环境千万不要这样</p><hr><p>像一些常见的还有 consul、kafka 等….</p><h2 id="Kubernetes-部署-Redis-Cluster-集群"><a href="#Kubernetes-部署-Redis-Cluster-集群" class="headerlink" title="Kubernetes 部署 Redis-Cluster 集群"></a>Kubernetes 部署 Redis-Cluster 集群</h2><p>Redis-Cluster 和 主从哨兵集群的区别与优劣就不在此比对了，本文是以部署为主。</p><ol><li>从三方仓库拉取 Redis7 镜像：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker pull docker.1ms.run/library/redis:7<br></code></pre></td></tr></table></figure><ol start="2"><li>换一个简单的 tag</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker tag docker.1ms.run/library/redis:7 redis:7<br></code></pre></td></tr></table></figure><ol start="3"><li>为了让业务更可控（对于陌生的镜像我们连 redis.conf 在哪都不清楚），基于这个镜像改造我们自己的镜像，首先去 <a href="https://github.com/redis/redis/releases">github-redis-release</a> 中找到对应的版本，接着找到里面的 redis.conf 文件</li><li>在有 docker 环境的机器上组成如下的结构（dockerfile 自行创建）：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">└── redis<br>    ├── dockerfile<br>    └── redis.conf<br></code></pre></td></tr></table></figure><ol start="5"><li>编写 dockerfile</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 基于刚刚下载的镜像</span><br>FROM redis:7 as base<br><br><span class="hljs-comment"># 创建配置目录</span><br>RUN <span class="hljs-built_in">mkdir</span> -p /usr/local/etc<br><br><span class="hljs-comment"># 复制本地的redis.conf到容器中</span><br>COPY redis.conf /usr/local/etc/redis.conf<br><br><span class="hljs-comment"># 可选：设置配置文件权限（如果需要）</span><br>RUN <span class="hljs-built_in">chmod</span> 644 /usr/local/etc/redis.conf<br><br><span class="hljs-comment"># 暴露Redis默认端口</span><br>EXPOSE 6379<br><br><span class="hljs-comment"># 设置启动命令</span><br>CMD [<span class="hljs-string">&quot;redis-server&quot;</span>, <span class="hljs-string">&quot;/usr/local/etc/redis.conf&quot;</span>]<br></code></pre></td></tr></table></figure><p>将 dockerfile 构建成镜像：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">docker build -t redis:7.4 -f dockerfile .<br></code></pre></td></tr></table></figure><ol start="6"><li>写 Kubernetes 声明文件，注意事项如下：</li></ol><ul><li>第4行 redis-cluster 为命名空间的名字,不推荐修改,如果修改则需修改所有 namespace: redis-cluster 的地方</li><li>第9行 redis-cluster-config 是 redis.conf 中的一些属性,通过 74-79 行挂载进了容器的 redis.conf 文件中，requirepass 是连该节点的密码, masterauth 是主备间的密码</li><li>第31行与93行应保持一致</li><li>第85行对应的字符串应取 “kubectl get sc” 查询返回结果中的第一列(Name)值，sc 是用来真正帮忙创建存储目录的，有些公司可能有固态和机械等多种盘，会根据应用情况选择合适的 sc</li><li>第88行指定了 redis 存储的大小，根据使用量情况自行决定(redis的aof、rdb持久化机制)，12行指定了 redis 工作目录为容器内的&#x2F;data，82行指定了通过sc创建出来的路径的别名，64、65行将这个别名挂载到了容器的&#x2F;data目录，不推荐修改这个目录，因为还涉及到15、18行等地方</li></ul><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span><br><span class="hljs-attr">kind:</span> <span class="hljs-string">Namespace</span><br><span class="hljs-attr">metadata:</span><br>  <span class="hljs-attr">name:</span> <span class="hljs-string">redis-cluster</span><br><span class="hljs-meta">---</span><br><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span><br><span class="hljs-attr">kind:</span> <span class="hljs-string">ConfigMap</span><br><span class="hljs-attr">metadata:</span><br>  <span class="hljs-attr">name:</span> <span class="hljs-string">redis-cluster-config</span><br>  <span class="hljs-attr">namespace:</span> <span class="hljs-string">redis-cluster</span><br><span class="hljs-attr">data:</span><br>  <span class="hljs-attr">redis-config:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    appendonly yes</span><br><span class="hljs-string">    protected-mode no</span><br><span class="hljs-string">    dir /data</span><br><span class="hljs-string">    port 6379</span><br><span class="hljs-string">    cluster-enabled yes</span><br><span class="hljs-string">    cluster-config-file /data/nodes.conf</span><br><span class="hljs-string">    cluster-node-timeout 5000</span><br><span class="hljs-string">    masterauth Fusion@123</span><br><span class="hljs-string">    requirepass Fusion@123</span><br><span class="hljs-string"></span><span class="hljs-meta">---</span><br><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">apps/v1</span><br><span class="hljs-attr">kind:</span> <span class="hljs-string">StatefulSet</span><br><span class="hljs-attr">metadata:</span><br>  <span class="hljs-attr">name:</span> <span class="hljs-string">redis-cluster</span><br>  <span class="hljs-attr">namespace:</span> <span class="hljs-string">redis-cluster</span><br>  <span class="hljs-attr">labels:</span><br>    <span class="hljs-attr">app.kubernetes.io/name:</span> <span class="hljs-string">redis-cluster</span><br><span class="hljs-attr">spec:</span><br>  <span class="hljs-attr">serviceName:</span> <span class="hljs-string">redis-headless</span><br>  <span class="hljs-attr">replicas:</span> <span class="hljs-number">6</span><br>  <span class="hljs-attr">selector:</span><br>    <span class="hljs-attr">matchLabels:</span><br>      <span class="hljs-attr">app.kubernetes.io/name:</span> <span class="hljs-string">redis-cluster</span><br>  <span class="hljs-attr">template:</span><br>    <span class="hljs-attr">metadata:</span><br>      <span class="hljs-attr">labels:</span><br>        <span class="hljs-attr">app.kubernetes.io/name:</span> <span class="hljs-string">redis-cluster</span><br>    <span class="hljs-attr">spec:</span><br>      <span class="hljs-attr">containers:</span><br>        <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">redis</span><br>          <span class="hljs-attr">image:</span> <span class="hljs-string">&#x27;redis:7.4&#x27;</span><br>          <span class="hljs-attr">command:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;redis-server&quot;</span><br>          <span class="hljs-attr">args:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;/usr/local/etc/redis.conf&quot;</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;--protected-mode&quot;</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;no&quot;</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;--cluster-announce-ip&quot;</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-string">&quot;$(POD_IP)&quot;</span><br>          <span class="hljs-attr">env:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">POD_IP</span><br>              <span class="hljs-attr">valueFrom:</span><br>                <span class="hljs-attr">fieldRef:</span><br>                  <span class="hljs-attr">fieldPath:</span> <span class="hljs-string">status.podIP</span><br>          <span class="hljs-attr">ports:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">redis-6379</span><br>              <span class="hljs-attr">containerPort:</span> <span class="hljs-number">6379</span><br>              <span class="hljs-attr">protocol:</span> <span class="hljs-string">TCP</span><br>          <span class="hljs-attr">volumeMounts:</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">config</span><br>              <span class="hljs-attr">mountPath:</span> <span class="hljs-string">/usr/local/etc</span><br>            <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">pvc</span><br>              <span class="hljs-attr">mountPath:</span> <span class="hljs-string">/data</span><br>          <span class="hljs-attr">resources:</span><br>            <span class="hljs-attr">limits:</span><br>              <span class="hljs-attr">cpu:</span> <span class="hljs-string">&#x27;2&#x27;</span><br>              <span class="hljs-attr">memory:</span> <span class="hljs-string">8Gi</span><br>            <span class="hljs-attr">requests:</span><br>              <span class="hljs-attr">cpu:</span> <span class="hljs-string">50m</span><br>              <span class="hljs-attr">memory:</span> <span class="hljs-string">500Mi</span><br>      <span class="hljs-attr">volumes:</span><br>        <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">config</span><br>          <span class="hljs-attr">configMap:</span><br>            <span class="hljs-attr">name:</span> <span class="hljs-string">redis-cluster-config</span><br>            <span class="hljs-attr">items:</span><br>              <span class="hljs-bullet">-</span> <span class="hljs-attr">key:</span> <span class="hljs-string">redis-config</span><br>                <span class="hljs-attr">path:</span> <span class="hljs-string">redis.conf</span><br>  <span class="hljs-attr">volumeClaimTemplates:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-attr">metadata:</span><br>        <span class="hljs-attr">name:</span> <span class="hljs-string">pvc</span><br>      <span class="hljs-attr">spec:</span><br>        <span class="hljs-attr">accessModes:</span> [ <span class="hljs-string">&quot;ReadWriteOnce&quot;</span> ]<br>        <span class="hljs-attr">storageClassName:</span> <span class="hljs-string">&quot;managed-nfs-storage&quot;</span><br>        <span class="hljs-attr">resources:</span><br>          <span class="hljs-attr">requests:</span><br>            <span class="hljs-attr">storage:</span> <span class="hljs-string">3Gi</span><br><span class="hljs-meta">---</span><br><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span><br><span class="hljs-attr">kind:</span> <span class="hljs-string">Service</span><br><span class="hljs-attr">metadata:</span><br>  <span class="hljs-attr">name:</span> <span class="hljs-string">redis-headless</span><br>  <span class="hljs-attr">namespace:</span> <span class="hljs-string">redis-cluster</span><br>  <span class="hljs-attr">labels:</span><br>    <span class="hljs-attr">app.kubernetes.io/name:</span> <span class="hljs-string">redis-cluster</span><br><span class="hljs-attr">spec:</span><br>  <span class="hljs-attr">ports:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">redis-6379</span><br>      <span class="hljs-attr">protocol:</span> <span class="hljs-string">TCP</span><br>      <span class="hljs-attr">port:</span> <span class="hljs-number">6379</span><br>      <span class="hljs-attr">targetPort:</span> <span class="hljs-number">6379</span><br>  <span class="hljs-attr">selector:</span><br>    <span class="hljs-attr">app.kubernetes.io/name:</span> <span class="hljs-string">redis-cluster</span><br>  <span class="hljs-attr">clusterIP:</span> <span class="hljs-string">None</span><br>  <span class="hljs-attr">type:</span> <span class="hljs-string">ClusterIP</span><br></code></pre></td></tr></table></figure><p>对于这个声明文件你可能会有疑问，上一步建自己的镜像时我说为了安全，能自己控制 redis.conf，但我却没有暴露 redis.conf 反而直接在这个声明文件的 13-21 行之间配置了，其实我是把它声明为了 configmap，修改时觉得还好，就这样部署下来了。</p><ol start="7"><li>接下来在 kubernetes 环境运行就好啦，其中 redis-cluster.yaml 是我自己的 yaml 文件名，注意要先把第 6 步创建出的镜像加载到 k8s 集群的各个节点上！</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl apply -f redis-cluster.yaml<br></code></pre></td></tr></table></figure><ol start="8"><li>此时系统中运行了 6 个 redis，但它们之间并没有组成集群，需要通过下方指令，注意事项如下：</li></ol><ul><li>redis-cluster-0 创建出来的1个pod的名字(命名规则是文件1第26行 + 副本序号,例如6副本则0-5)</li><li>-n redis-cluster 命名空间,如果文件1的第4行修改了这里也需要修改,注意这段中(-n redis-cluster)出现了2次，其实就是筛选这个命名空间下有特定标签(app.kubernetes.io&#x2F;name&#x3D;redis-cluster，文件1的第29行定义)的 pod 共有多少</li><li>-a Fusion@123  文件1 中第 21 行指定的密码</li><li>–cluster-replicas 1 副本数1,6 节点恰好 3 主 3 备</li><li>后面的 podIP、6379等都不推荐修改,在文件1中通过环境变量等已经配置好了,而且命名空间、容器互相隔离,相互直接使用相同的端口啥的毫无影响</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">kubectl <span class="hljs-built_in">exec</span> -it redis-cluster-0 -n redis-cluster -- redis-cli -a Fusion@123 --cluster create --cluster-replicas 1 $(kubectl get pods -n redis-cluster -l app.kubernetes.io/name=redis-cluster -o jsonpath=<span class="hljs-string">&#x27;&#123;range.items[*]&#125;&#123;.status.podIP&#125;:6379 &#123;end&#125;&#x27;</span>)<br></code></pre></td></tr></table></figure><p>执行后可能会让输入 yes，输入完成后会输出集群创建成功啥的，并展示每个节点分配的槽数（槽是redis-cluster集群中的概念，默认是均分，也有某节点性能特别好需要特殊划分的情况），可以通过创建出来的目录 (&#x2F;mnt&#x2F;nfs 之类的)进入后查看 node.conf（文件1第18行配置） 查看</p><ol start="9"><li>那系统中该如何使用呢，以 Java 为例：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash">spring:<br>  redis:<br>    cluster:<br>      nodes: redis-cluster-0.redis-headless.redis-cluster.svc.cluster.local:6379,redis-cluster-1.redis-headless.redis-cluster.svc.cluster.local:6379,redis-cluster-2.redis-headless.redis-cluster.svc.cluster.local:6379,redis-cluster-3.redis-headless.redis-cluster.svc.cluster.local:6379,redis-cluster-4.redis-headless.redis-cluster.svc.cluster.local:6379,redis-cluster-5.redis-headless.redis-cluster.svc.cluster.local:6379<br>    lettuce:<br>      pool:<br>        min-idle: 30<br>        max-idle: 100<br>    password: Fusion@123<br></code></pre></td></tr></table></figure><p>注意需要部署的 Java 应用和 Redis 在同一 Kubernetes 集群里哦，如果不在则需要通过其它手段（例如 NodePort） 来访问了~</p><p>这里只是以部署 Redis-Cluster 为例，其实部署有状态集群如数据库等也是一样的，需要理解的是 Service、StorageClass 等~</p>]]></content>
    
    
    <categories>
      
      <category>云原生</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Kubernetes</tag>
      
      <tag>Redis</tag>
      
      <tag>Docker</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Kubernetes查漏补缺</title>
    <link href="/2026/04/19/Kubernetes%E8%BF%90%E7%BB%B4%E6%9F%A5%E6%BC%8F%E8%A1%A5%E7%BC%BA/"/>
    <url>/2026/04/19/Kubernetes%E8%BF%90%E7%BB%B4%E6%9F%A5%E6%BC%8F%E8%A1%A5%E7%BC%BA/</url>
    
    <content type="html"><![CDATA[<h1 id="Kubernetes查漏补缺"><a href="#Kubernetes查漏补缺" class="headerlink" title="Kubernetes查漏补缺"></a>Kubernetes查漏补缺</h1><p>在【超聚变】的日子里天天跟 Kubernetes 打交道，即使如此对它的认识依旧浅显，只是停留在部署服务、定位问题上，虽然我还曾基于 CRD 设计出了服务插件化接入，但对深层的控制和网络等依旧知之甚浅。</p><p>一部分原因是当时忙于业务和毕设，没有太多精力去深入，另一方面毕竟 Kubernetes 建立在 <a href="https://research.google/pubs/pub43438">Google 大规模运行生产工作负载十几年经验</a> 的基础上，结合了社区中最优秀的想法和实践，难度也确实有些大。</p><p>最近看了一些相关的书，发现了一些之前未曾注意到的知识，在这里整理了下，由于不是科普向且查漏补缺，所以本篇的逻辑性与关联性可能不甚清晰。</p><h2 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h2><p>kubelet 有两个参数可以控制镜像删除的行为，一个是 image-gc-high-threshold，另一个是 image-gc-low-threshold，它们的值默认是 85% 和 80%，这意味着当磁盘使用率达到 85% 的时候自动执行清理，并将磁盘使用率降到 80%。不过一般不用担心，由于镜像层次之间的复用性，并不是每次拉取镜像都会额外占用一份镜像空间大小。</p><h2 id="Pod-与控制器"><a href="#Pod-与控制器" class="headerlink" title="Pod 与控制器"></a>Pod 与控制器</h2><p>Pod 不具备自动恢复能力，所以需要 ReplicaSet，ReplicaSet 只在副本数发生变动时有感知，所以需要更上层的 Deployment&#x2F;StatefulSet，Deployment 的滚动更新是通过 ReplicaSet 实现的：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@k8s-master-01 ~]# k -n &lt;ns&gt; describe deploy &lt;deploy&gt; | grep Replica<br>Replicas:               2 desired | 2 updated | 2 total | 2 available | 0 unavailable<br>  Progressing    True    NewReplicaSetAvailable<br>  Available      True    MinimumReplicasAvailable<br>OldReplicaSets:  &lt;none&gt;<br>NewReplicaSet:   &lt;deploy-name&gt;-65d6674947 (2/2 replicas created)<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@k8s-master-01 ~]# k -n &lt;ns&gt; get replicaset<br>NAME                             DESIRED   CURRENT   READY   AGE<br>&lt;deploy-name&gt;-65d6674947         2         2         2       2d19h<br></code></pre></td></tr></table></figure><p>当发生更新时，会创建新的 replicaset，旧的 replicaset 期望数会变为 0。</p><p>Deployment 在更新 Pod 时默认会采用 <code>RollingUpdate</code> 也就是滚动更新的方式，它不会造成业务停机</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@k8s-master-01 ~]# k -n &lt;ns&gt; describe deploy &lt;deploy&gt; | grep Strategy<br>StrategyType:           RollingUpdate<br>RollingUpdateStrategy:  25% max unavailable, 25% max surge<br></code></pre></td></tr></table></figure><p>max surge 用来指定最大超出期望  Pod 的个数，max unavailable 是允许 Pod 不可用的数量，以期望 8 个副本数的工作负载为例，max unavailable 的值为 25% ，也就是 2，max surge 的值为 25%，也是 2，那么在滚动更新时，更新策略是： </p><ul><li>更新期间最多会有 10 个 Pod（8 个所需的 Pod + 2 个 max surge Pod）处于运行状态； </li><li>更新期间至少会有 6 个 Pod（8 个所需的 Pod - 2 个 max unavailable Pod）处于运行状态。</li></ul><p>当 kubernetes 决定要驱逐 Pod 的时候，按照 未配置资源配额、Request 小于 Limit、Request 等于 Limit 的顺序驱逐，对于一些基础核心组件，例如中间件或者核心服务，可以将 Request 预估为一个 较高的合理水平，并且将 Limit 配置为相同的值。</p><p>HPA 需要安装 Metrics Server 和为工作负载配置资源  Request 才能工作。</p><p>对于启动非常慢的大型应用，随意配置 Readiness 和  Liveness 探针，Pod 将因为探针失败而永远无法启动。修改初始检测时间等字段虽然有效但降低了 kubernetes 检测 Pod 健康状态的频率，这会延迟 kubernetes 感知故障的速度。可以用 StartupProbe 探针来解决这个问题。</p><ul><li>Readiness 就绪探针可以让 kubernetes 感知到业务的 Ready 就绪状态，以此来控制什么时候将 Pod 加入到 EndPoints 列表中接收外部流量；</li><li>Liveness 存活探针当感知到 Pod 不健康时，会重启 Pod；</li><li>StartupProbe 探针主要用来解决服务启动慢的问题。</li></ul><h2 id="Service"><a href="#Service" class="headerlink" title="Service"></a>Service</h2><p>Pod 每次创建都会分配一个新的 IP，无法进行固定访问，Service 将一组来自同一个 ReplicaSet 创建的 Pod 组合在一起，并提供 DNS 的访问能力。</p><p>Service 在集群内拥有唯一的 IP 地址，并且这个 IP 是稳定的（但是删了重新建会获得一个新的 IP）。此外，Service 本身并没有直接提供服务发现的能力，它需要借助 Endpoints 来实现。Endpoints 记录了一组 Pod 的 IP 地址，Service 只需要查看自身所对应的 Endpoints 便能够找到具体的 Pod。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">[root@k8s-master-01 ~]# k -n &lt;ns&gt; get endpoints<br>Warning: v1 Endpoints is deprecated <span class="hljs-keyword">in</span> v1.33+; use discovery.k8s.io/v1 EndpointSlice<br>NAME                  ENDPOINTS                              AGE<br>&lt;deploy-name&gt;         10.244.128.22:80,10.244.16.128:80      83d<br></code></pre></td></tr></table></figure><p>Service 通过 selector 选择关联的 Pod，通过 metadata.name 关联 endpoints，如下的示例就会关联 <code>app.kubernetes.io/name=redis-cluster</code> 的 Pod，port 字段代表 Service 监听端口，targetPort 字段代表将请求转发到 Pod 时的目标端口。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs bash">apiVersion: v1<br>kind: Service<br>metadata:<br>  name: redis-headless<br>  namespace: redis-cluster<br>  labels:<br>    app.kubernetes.io/name: redis-cluster<br>spec:<br>  ports:<br>    - name: redis-6379<br>      protocol: TCP<br>      port: 6379<br>      targetPort: 6379<br>  selector:<br>    app.kubernetes.io/name: redis-cluster<br>  clusterIP: None<br>  <span class="hljs-built_in">type</span>: ClusterIP<br></code></pre></td></tr></table></figure><p>Service 只有在创建后才会分配得到 IP，且删除后重建还会分配新的 IP，所以也需要一个 IP 无关的唯一标识：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">&#123;<span class="hljs-variable">$service_name</span>&#125;.&#123;<span class="hljs-variable">$namespace</span>&#125;.svc.cluster.local<br></code></pre></td></tr></table></figure><p>日常可以省略 .svc.cluster.local，同一命名空间下 namespace 也可以省略，即同一命名空间下可以这么访问：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">&#123;<span class="hljs-variable">$service_name</span>&#125;<br></code></pre></td></tr></table></figure><p>所以在 A Pod 访问其它服务的 B Pod 时的流程其实是：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">Pod-A -----&gt; service-B -------&gt; Endpoints-B --------&gt; Pod-B<br></code></pre></td></tr></table></figure><p>ExternalName 类型的 Service 可以将 Service 和另外一个命名空间的 Service 关联起来</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span><br><span class="hljs-attr">kind:</span> <span class="hljs-string">Service</span><br><span class="hljs-attr">metadata:</span><br><span class="hljs-attr">name:</span> <span class="hljs-string">backend-service</span><br><span class="hljs-attr">namespace:</span> <span class="hljs-string">default</span><br><span class="hljs-attr">spec:</span><br><span class="hljs-attr">type:</span> <span class="hljs-string">ExternalName</span><br><span class="hljs-attr">externalName:</span> <span class="hljs-string">backend-service.example.svc.cluster.local</span><br></code></pre></td></tr></table></figure><p>为了统一访问，还可以创建 Service 和 EP 绑定外部服务地址，如下示例展示了如何在当前集群内通过 service 访问外部的 mysql 服务：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span> <br><span class="hljs-attr">kind:</span> <span class="hljs-string">Service</span> <br><span class="hljs-attr">metadata:</span> <br>  <span class="hljs-attr">name:</span> <span class="hljs-string">db</span> <br>  <span class="hljs-attr">namespace:</span> <span class="hljs-string">example</span> <br><span class="hljs-attr">spec:</span> <br>  <span class="hljs-attr">ports:</span> <br>    <span class="hljs-bullet">-</span> <span class="hljs-attr">port:</span> <span class="hljs-number">3306</span> <br>      <span class="hljs-attr">targetPort:</span> <span class="hljs-number">3306</span> <br><span class="hljs-meta">--- </span><br><span class="hljs-attr">apiVersion:</span> <span class="hljs-string">v1</span> <br><span class="hljs-attr">kind:</span> <span class="hljs-string">Endpoints</span> <br><span class="hljs-attr">metadata:</span> <br>  <span class="hljs-attr">name:</span> <span class="hljs-string">db</span> <br>  <span class="hljs-attr">namespace:</span> <span class="hljs-string">example</span> <br><span class="hljs-attr">subsets:</span> <br>  <span class="hljs-bullet">-</span> <span class="hljs-attr">addresses:</span> <br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">ip:</span> <span class="hljs-number">10.244</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span> <br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">ip:</span> <span class="hljs-number">10.244</span><span class="hljs-number">.0</span><span class="hljs-number">.2</span> <br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">ip:</span> <span class="hljs-number">10.244</span><span class="hljs-number">.0</span><span class="hljs-number">.3</span> <br>    <span class="hljs-attr">ports:</span> <br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">port:</span> <span class="hljs-number">3306</span> <br>        <span class="hljs-attr">protocol:</span> <span class="hljs-string">TCP</span><br></code></pre></td></tr></table></figure><h2 id="相关工程实践"><a href="#相关工程实践" class="headerlink" title="相关工程实践"></a>相关工程实践</h2><ol><li><a href="https://developer.aliyun.com/article/1696821">Kubernetes v1.30.6 环境下 HPA 自动扩缩容完整实践指南</a></li><li><a href="https://eastondev.com/blog/zh/posts/dev/20260419-docker-multistage-build/">多阶段构建降低生产环境镜像体积</a></li></ol>]]></content>
    
    
    <categories>
      
      <category>云原生</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Kubernetes</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>谈谈我的软考经历</title>
    <link href="/2026/04/17/%E8%B0%88%E8%B0%88%E6%88%91%E7%9A%84%E8%BD%AF%E8%80%83%E7%BB%8F%E5%8E%86/"/>
    <url>/2026/04/17/%E8%B0%88%E8%B0%88%E6%88%91%E7%9A%84%E8%BD%AF%E8%80%83%E7%BB%8F%E5%8E%86/</url>
    
    <content type="html"><![CDATA[<h1 id="谈谈我的软考经历"><a href="#谈谈我的软考经历" class="headerlink" title="谈谈我的软考经历"></a>谈谈我的软考经历</h1><p>我 2020 年高考进入大学，软件工程专业，去年（24年7月）毕业开始工作。我实习是在一家云计算公司，公司内部对软考的证书没有什么激励或补助之类的，我也一直认为计算机嘛，“talk is cheap，show me the code”，能力在代码水平上是能体现出来的，综合这些，一直没有去准备相关考试。</p><p>正式工作后，公司对这个有激励标准，加上当年考过后第二年可凭证书免当年最高 3600 的税，至于那些大城市的什么积分落户，我没有详细了解过也不觉得自己能留在大城市。除此之外，我刚毕业来到新的城市嘛，本身也不是很忙，刚好有点空闲时间，就开始看这个软考，2024 年 11 月过了软考中级（软件设计师），今天 25 年 5 月的考试成绩发布，过了系统架构设计师，就想记录一下我考试的经历和一些过程。</p><p><img src="/img/dc60dfe97a3894d16aec5650a0126248.png" alt="image-20250626165025209"></p><p><img src="/img/e3bc65304aed9a5bf985eed7286485dd.png" alt="image-20250626164917140"></p><p>说真的分不算很高，两次选择题基本上都是压着线过的，论文也是。</p><p>先来说说软件设计师，这个准备的时间比架构师要久，原因一是当时刚工作，没什么事情，公司电脑打开个 pdf 软件做历年真题领导都不在意，二是当时初来乍到，也确实没有太多事情。</p><blockquote><p> 软件设计师我是如何准备的</p></blockquote><p>上午题我本来在 b 站搜索一些视频（视频还是挺多的），但是里面讲的大部分知识，其实我在上大学时候已经知道了，大部分都是数据结构、计算机网络、操作系统、计算机组成原理、编译原理、数据库里的内容，但是不要觉得说只有把这些全都学会、学好了才能过，原因一方面是<code>这些东西虽然多，考的杂，但考的不是很深</code>，另一方面<code>这是考试，又不是真正工作遇到那种完全摸不着头脑的问题，考试就有重点、历年真题规律、每一部分占的分值等，按照分值比重去重点复习</code>就行了。此外还会有 5 分的全英语完形填空（说实话这 5 分全选一个就行，如果你其他部分准备的很好，这 5 分真的不是很在意，我就全选的 A）。这个部分**<code>一定要做题</code>**，看视频和文档容易睡着，我刷题用的是微信小程序，叫小鸡软考，我看同事也有用希赛之类的。因为我设计师没怎么看上午题的视频，这里就不胡乱推荐了。</p><p>下午题我看的是 <a href="https://space.bilibili.com/91286799?spm_id_from=333.337.0.0">B站zst(炸薯条)老师</a> 的，从普通人的角度讲述题该怎么做，下午题我个人觉得比上午题好过，原因是题型固定，我那年刚好算法题还出的很简单（正常做往年题算法题15分我都做好了一分不要的准备，但那次我应该拿了9分附近），但是需要掌握 java 或者 c++ 的一些语法规则（二选一），这会有15分，虽然这题官方考的叫设计模式，但我说实话懂语法就够了，当然设计模式也很重要，尤其是创建型那几个，选择题经常考到。<code>up 动态置顶有网友，路人朋友总结的笔记，历年真题，是一个云盘链接，真的啥都准备齐全了，下午题跟就行了。而且每次出成绩，up 的视频下面都会有很多朋友留言，过了，充电支持之类的</code>，虽然他已经很久不更新了，留下的视频还是足够应对考试的。当然学习前可以先问问上次考试的朋友，对比下题型变化之类的，万一改版很大就不好了~</p><p><img src="/img/7129785e7a7803eb900893aa322b7185.png" alt="image-20250626171441632"></p><p>这是我写文字的时候刚看到的评论~</p><blockquote><p>系统架构师我是怎么准备的</p></blockquote><p>我先说我是很有自知之明的，我知道这个过了我离架构师也很遥远，这里顺便一提，这个和系分据说是高级里比较难的，如果没有必须要求，考那个管理师也是可以的，比如我们公司就只要是高级就行，补贴待遇一致。</p><p>这个的上午题我本来想着跟设计师差不多，无脑做题去了，一看一脸懵，什么质量属性，什么敏感点权衡点，这个是真得看视频了，于是我几经搜罗，找到了一个很不错的视频 <a href="https://www.bilibili.com/video/BV1G2ApeQEm7/?p=42">系统架构设计师</a>，看了两三遍质量属性，架构风格相关的章节，视频老师讲的真好，我刚刚去搜，视频已经被拿下了，所以我也没办法推荐了，讲师是王建平老师，视频里好像有慕课，不知道是不是慕课平台的。讲的真的很好，我一直分不清那么多开发模型，什么瀑布，V 模型，极限水晶什么什么的，这个老师解释完还给了总结。</p><p>案例分析题也相对固定，比如有一题考一直在考 Redis ，比如 redis 的主从复制，主节点 fork 子线程创建 rdb，传给从节点同步，同步期间使用扇形缓冲区记录新的操作，rdb 同步完成后再把这段时间的操作发过去，细节其实是很多的，包括 rdb，aof 的优缺点，怎么保持缓存和数据库的一致性，我恰好在极客时间买了 redis 的一个专栏，所以这部分我答的还算可以。共 5 题，第一题必做，剩下 4 选 2，我记得第一题是质量属性的必选，剩下的数据库，嵌入式，web和一个什么（记不大清了，但是我肯定不会），我能选的基本上是固定的，数据库和 web，web 是跟架构有关系的，比如某几个软件是一个层，让你把这个层对应的字母选上，最后还会有一些问答题，总之一定要学好 **<code>架构风格和质量属性</code>**。</p><p>论文题是四选一，网上有很详细的分析，我也看了往年的题，基本上每年都只会其中一两个，而且不是说你知道这个东西就能写，论文里会有三个子标题，比如我这次考试有个负载均衡，我知道负载均衡这个概念，但当时我真不知道负载均衡的 4 种策略（子标题之一），我只知道个加权的。还好有个事件驱动的，最后我写了事件驱动，以 spring 里的事件为例讲的，结合消息队列。这都是有格式的，本人是xxx架构师，参与了项目，耗时 N 个月，B 站找几个模板就行，但是事前一定要写几篇，确定要写哪个项目，另外<code>这个对知识的广度真的很大，分布式理论，负载均衡，消息队列，事件驱动，一致性哈希，共识算法.......所以架构师也确实是比软件设计师难度大的</code>。</p><p><img src="/img/9ad765bf503f983810b8c8216cf11d41.png" alt="image-20250626173848830"></p><p>当然最后我想说，考试<code>肯定也是有运气成分的，要有赌的勇气，所以不要因为没有完全准备好就不考</code>，要用于尝试~</p><p>这个考试，报名费也不是很贵，而且有些公司报销报名费，过了给奖金，每月有补贴，性价比还是很高的。</p><p>考了这个架构师虽然说对我直接编码能力提升不大，但是知道了很多东西，soa，ejb，webservice，rpc 和很多相关的东西，对 rpc、多路复用的理解也更深刻了~</p><p>后续补：所谓的抵扣个税 3600 并不是指能抵扣 3600 的税款，而是比如超出 6w 的部分需要交税，而恰好年收入是 63600，那么抵扣 3600 就不需要交税了。</p><p>注：刚开始写在其它平台，所以文中的日期可能已不大符合，原文发布于：2025-06-26。</p>]]></content>
    
    
    
    <tags>
      
      <tag>证书</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>AI 时代的无力感</title>
    <link href="/2026/04/16/AI%E6%97%B6%E4%BB%A3%E7%9A%84%E6%97%A0%E5%8A%9B%E6%84%9F/"/>
    <url>/2026/04/16/AI%E6%97%B6%E4%BB%A3%E7%9A%84%E6%97%A0%E5%8A%9B%E6%84%9F/</url>
    
    <content type="html"><![CDATA[<h1 id="AI时代的无力感"><a href="#AI时代的无力感" class="headerlink" title="AI时代的无力感"></a>AI时代的无力感</h1><p>我在哔哩哔哩一直有几个非常喜欢的 up 主，内容比较深刻又由浅入深，一个是 <a href="https://space.bilibili.com/7968519">学java的生生</a>，一个是 <a href="https://space.bilibili.com/352224540">编程不良人</a>，当然除这些外还有很多优秀的 up 主，通俗易懂讲解 AI 技术的 <a href="https://space.bilibili.com/1815948385">马克的技术工作坊</a>、一直在分享一些视频的尚硅谷、黑马、马士兵等（不管这些教学的深度亦或者引流的目的，确实让普通人可以免费对很多技术栈至少可以入门）……</p><p>在前天（2026-04-14），上述中的一位发了一篇动态：<a href="https://www.bilibili.com/opus/1191180746919772167?spm_id_from=333.1365.0.0"><strong>告别，Java：一个全栈老兵的“退场白”与“入场券”</strong></a>，引用其中的一段话：</p><blockquote><p>但说实话，现在的技术圈，真的入冬了。这种冷，不是说 Java 没人用了（毕竟 JDK 26 都发了），而是<strong>单纯堆砌代码、研究 CRUD 框架的价值，正在断崖式下跌。</strong> 以前我们引以为傲的“全栈能力”，在 AI 面前显得如此笨拙。曾经要写三天三夜的复杂业务逻辑、调优半个月的性能参数，现在的 AI 只需要你给出正确的提示词，几秒钟就能生成出比人工更严谨的代码。</p><p><strong>当“写代码”这件事本身变得廉价，我们这些“码农”的护城河又在哪里？ 这种无力感，是我决定转身的第一层原因。</strong></p></blockquote><p>我印象里 20 年 gpt 横空出世的时候，被大部分人当做搜索引擎使用，甚至经常答非所问；23 年的时候优化某个函数、分析函数意图已经很不错了，但依然是文本交互为主；如今 AI 百花齐放，智能补全代码、修复代码 bug、知识库检索……，Tools、Skills、MCP 极大的提高了 Agent 的可操作范围；而面对未来，我贫瘠的想象力已经展望不出未来的场景了。</p><p>我并非一个不愿意学习与接触新事务的人，从下面哔哩哔哩的收藏和某平台发布的文章应该也能看出来：</p><p><img src="/img/image-20260416084925747.png" alt="image-20260416084925747"></p><p><img src="/img/image-20260416085216168.png" alt="image-20260416085216168"></p><p>对于大多数人而言，以前大家可能看中间件的掌握深度，代码的优雅规范性来评价一个人的技术，但在 AI 的发展下，AI 理解业务需求后会自动帮你管理好事务的传播，Redis 数据结构的选型，使用多种设计模式提高代码的拓展性。突然觉得有点理解“阿尔法狗”出现后的部分围棋人。</p><p>当从事的工作门槛大幅度降低，写代码不再是程序员的专利，产品经理也可以在 AI 工具的辅助下，跨越原型阶段，直接发布很多有创意的产品。</p><p>如果所有人的编码趋同，那么个人的独特价值该如何体现？出现这样的无力感也是情理之中，但我并不想表达太悲观的想法。</p><p>软件工程并没有因为大模型的出现就变得简单（毕竟编码不等于软件工程）。需求本身就是零散的，目标也是模糊的，在没有全局视图的情况下，架构自然就是有局限性，所以需要不断迭代变更。每个迭代，能拿到的信息仅仅是宏大视图中的小小一角，根本没有全貌，大模型对此也是无能为力的。</p><p>最重要的一点是：<strong>大模型基于过往，而人类创造未来</strong>。</p><p>注：以上不过是一个不足两年工作经验的我的思考，也许被说浅薄（毕竟未来无法预料，三体里也说：弱小和无知不是生存的障碍，傲慢才是），也许被说清醒，都留给未来的我见证吧~</p>]]></content>
    
    
    
    <tags>
      
      <tag>Agent</tag>
      
      <tag>思考</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>分布式系统中的谁先谁后</title>
    <link href="/2026/04/15/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E4%B8%AD%E7%9A%84%E8%B0%81%E5%85%88%E8%B0%81%E5%90%8E/"/>
    <url>/2026/04/15/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E4%B8%AD%E7%9A%84%E8%B0%81%E5%85%88%E8%B0%81%E5%90%8E/</url>
    
    <content type="html"><![CDATA[<h1 id="分布式系统中的谁先谁后"><a href="#分布式系统中的谁先谁后" class="headerlink" title="分布式系统中的谁先谁后"></a>分布式系统中的谁先谁后</h1><h2 id="情景描述"><a href="#情景描述" class="headerlink" title="情景描述"></a>情景描述</h2><p>前端会定时的更新 redis 中用户的学习时长（增量而非累加值），后端有定时任务定时同步 redis 中的值到业务数据库中。后来为了提高后端性能，又在其他节点部署了一份服务，通过 nginx 做了负载均衡，但是忘了修改定时任务为 xxl-job 实现（即每个节点可能都会执行）。</p><p>思考这样的执行逻辑：</p><ol><li>后端服务1从 redis 中得知需要修改用户学习时长为原始值 + 3</li><li>后端服务1从业务库中查询得到现在用户学习时长为 0，于是修改 PO 中该值为 3，在调用 dao.save(po) 前发生 gc</li><li>redis 更新用户时长为 4</li><li>后端服务2从 redis 中得知需要修改用户学习时长为原始值 + 4</li><li>后端服务2从业务库中查询得到现在用户学习时长为 0，于是修改 PO 中该值为 4，调用 dao.save(po) 保存成功</li><li>后端服务1从 gc 中恢复，调用 dao.save(po) 保存值成功</li></ol><p>此时数据库中的值是 3 而非 4，业务逻辑出现问题。这个问题并不是使用没有 gc 的语言就可以避免的，即使没有 gc、也可能会有网络延迟、主机缓冲区阻塞等情况。</p><p>如果给每次更新都带上一个绝对时间是否可行呢？在数据库服务收到后端服务1的更新请求时，发现服务2的更新请求更新时间晚于服务1，因此拒绝服务1的更新请求。</p><p>不过在自然界中，无论采用怎样的时间同步机制，服务器的时钟并不能时刻保持一致，而且这样的绝对时间精度总有上限的。两次数据变更，间隔时间可能非常小，比如就是来源于邻近两行代码的执行而已，这样的时间间隔，即便是最精密的物理时钟，可能都无法感知。</p><p>携带绝对时间的想法，源于我们想对系统中的事件进行全局的排序。</p><h2 id="逻辑时钟"><a href="#逻辑时钟" class="headerlink" title="逻辑时钟"></a>逻辑时钟</h2><p>Lamport 在他的论文中提出了逻辑时钟的概念，<code>我们为每个进程 𝑃𝑖 分别定义一个时钟 𝐶𝑖，它为进程中的任一事件 𝑎 分配一个值 𝐶𝑖 ⟨𝑎⟩。整个系统的时钟由函数 𝐶 表示，它为任一事件 𝑏 分配数值 𝐶⟨𝑏⟩，其中，若 𝑏 是进程 𝑃𝑗 中的一个事件，则 𝐶⟨𝑏⟩ = 𝐶𝑗 ⟨𝑏⟩。</code>注意此时定义的时钟和物理时间毫无关系，可以认为这就是一个逻辑时钟。</p><p>接下来介绍一下事件的“先于发生”（由于没有物理时钟，不能再依靠绝对时间来判断）关系（此处有些像多线程中的 happened before）：</p><p>定义：系统中事件集合上的关系“→”是满足以下三个条件的最小关系： </p><p>（1）若 𝑎 和 𝑏 是同一进程中的事件，且 𝑎 早于 𝑏 发生，则 𝑎 → 𝑏。 </p><p>（2）若 𝑎 是一个进程的消息发送事件，𝑏 是另一进程接收同一消息的事件，则 𝑎 → 𝑏。 </p><p>（3）若 𝑎 → 𝑏 且 𝑏 → 𝑐，则 𝑎 → 𝑐。 </p><p>对两个不同的事件 𝑎 和 𝑏，若 𝑎 ↛ 𝑏 且 𝑏 ↛ 𝑎，则称 𝑎 和 𝑏 是并发的（concurrent）。 我们假设对任意事件 𝑎，𝑎 ↛ 𝑎（事件先于自身发生似乎没有实际意义）。这意味着 → 是系统中所有事件的集合上的反自反（irreflexive）的偏序关系。</p><p><img src="/img/image-20260415132350063.png" alt="image-20260415132350063"></p><p>在这张图里 𝑝3 和 𝑞3 就是并发的，即使图中暗示了 𝑞3 发生的物理时间会更早（图中只能证明：𝑞1 → 𝑝2 → 𝑝3）。</p><p>我们希望事件满足这样的时钟条件：<strong>对于任意事件 𝑎，𝑏，若 𝑎 → 𝑏，则 𝐶⟨𝑎⟩ &lt; 𝐶⟨𝑏⟩。</strong>注意它的逆命题并不成立， 𝐶⟨𝑎⟩ &lt; 𝐶⟨𝑏⟩ 也可能二者可能不存在 → 关系而无法比较。</p><p>满足以下的条件也就满足了时钟条件：</p><ul><li>C1 若 𝑎 和 𝑏 都是进程 𝑃𝑖 中的事件，且 𝑎 先于 𝑏 发生，则 𝐶𝑖 ⟨𝑎⟩ &lt; 𝐶𝑖 ⟨𝑏⟩。 </li><li>C2 若 𝑎 是进程 𝑃𝑖 发送消息的事件，𝑏 是进程 𝑃𝑗 接收到该消息的事件，则 𝐶𝑖 ⟨𝑎⟩ &lt; 𝐶𝑗 ⟨𝑏⟩。</li></ul><h2 id="全序关系"><a href="#全序关系" class="headerlink" title="全序关系"></a>全序关系</h2><p>为了保证时钟系统满足时钟条件，我们需确保其满足条件 C1 和 C2。条件 C1 很简单；进程仅需遵循以下的实现规则：</p><ul><li>IR1 每个进程 𝑃𝑖 在任意两个相继的事件之间增加 𝐶𝑖。</li></ul><p>为了满足条件 C2，我们要求每个消息 𝑚 包含一个时间戳 𝑇𝑚，该时间戳等于发送消息的时刻。收到时间戳为 𝑇𝑚 消息后，进程必须推动其时钟，使其晚于 𝑇𝑚。更确切地说，我们有以下规则。</p><ul><li>IR2 (a) 若 𝑎 是进程 𝑃𝑖 发送消息 𝑚 这一事件，则消息 𝑚 需包含时间戳 𝑇𝑚 &#x3D; 𝐶𝑖 ⟨𝑎⟩；</li><li>IR2 (b) 一旦收到消息 𝑚，进程 𝑃𝑗 就将其时钟 𝐶𝑗 设置为不小于当前值，且大于 𝑇𝑚 的值。</li></ul><p>这样可以满足偏序关系，但对于并发的事件 𝑎 和 𝑏 完全有可能 𝐶𝑖 ⟨𝑎⟩ &#x3D; 𝐶𝑖 ⟨𝑏⟩，此时需要借助一个任意的全序关系来打破僵局，例如进程 id。更准确地说，我们<code>定义关系 ⇒ 如下： 若 𝑎 是进程 𝑃𝑖 的事件，𝑏 是进程 𝑃𝑗 的事件，则 𝑎 ⇒ 𝑏 当且仅当 </code></p><ul><li><p><code>(i) 𝐶𝑖 ⟨𝑎⟩ &lt; 𝐶𝑗 ⟨𝑏⟩ ；或</code></p></li><li><p><code> (ii) 𝐶𝑖 ⟨𝑎⟩ = 𝐶𝑗 ⟨𝑏⟩ 且 𝑃𝑖 ≺ 𝑃𝑗。</code></p></li></ul><p>≺ 为任意的全序关系，这定义了全序，且时钟条件意味着，若 𝑎 → 𝑏，则 𝑎 ⇒ 𝑏。</p><p><strong>能将事件全局排序对实现分布系统是非常有用的。实际上，实现正确的逻辑时钟系统就是为了获得全序。</strong></p><h2 id="分布式互斥算法"><a href="#分布式互斥算法" class="headerlink" title="分布式互斥算法"></a>分布式互斥算法</h2><p>文中基于此提供了一个分布式互斥算法，用于在多个进程之间安全地共享一个资源。在系统中有多个进程（例如 P1,P2,…,Pn），它们共享一个资源，满足以下条件：</p><ul><li>同一时刻<strong>只能有一个进程</strong>使用该资源（条件1：互斥）</li><li>资源请求必须<strong>按照发出请求的先后顺序</strong>被满足（条件2）</li><li>只要每个获得资源的进程最终都会释放资源，那么每个请求<strong>最终都会被满足</strong>（条件3：无饥饿）</li></ul><p>只看背景可能会认为这是一个分布式锁的问题，但分布式锁并不需要满足条件2，条件2要求系统能对<strong>并发请求</strong>也确定一个全局顺序。但分布式系统中，不同进程发出的请求可能没有直接的因果顺序（即并发的），因此需要人为构造一个全序。</p><blockquote><p>算法依赖的机制</p></blockquote><ul><li><strong>逻辑时钟</strong>：每个进程 Pi 维护一个逻辑时钟 Ci ，按照规则 IR1（事件间递增）和 IR2（消息携带时间戳，接收时调整）运行。由此得到所有事件的偏序 →，再通过比较 (Ci(a),进程ID) 扩展为全序 ⇒。</li><li><strong>消息假设</strong>：<ul><li>任意两个进程之间，消息按发送顺序被接收（FIFO 通道）。</li><li>消息最终都会被收到（无无限延迟或丢失）。</li><li>每个进程可以直接向其他所有进程发送消息。</li></ul></li></ul><hr><blockquote><p>算法的数据结构</p></blockquote><p>每个进程 Pi 维护一个<strong>请求队列</strong>（本地，不与其他进程共享）。队列中存放形如 <code>(T, P_j)</code> 的消息，表示进程 Pj 在逻辑时间 T 发起的资源请求。初始时，队列中只有一条消息 <code>(T0, P0)</code>，其中 P0 是初始拥有资源的进程，T0 小于所有时钟的初始值。</p><hr><blockquote><p>算法规则</p></blockquote><p>规则中的每个动作都视为一个<strong>原子事件</strong>。</p><ol><li><p><strong>请求资源</strong><br>当 Pi 需要资源时：</p><ul><li>生成当前逻辑时间戳 Tm&#x3D;Ci。</li><li>向<strong>所有其他进程</strong>发送消息 <code>&quot;请求资源 (T_m, P_i)&quot;</code>。</li><li>同时将这条消息放入<strong>自己的</strong>请求队列。</li></ul></li><li><p><strong>接收请求消息</strong><br>任何进程 Pj（包括 Pi 自己）收到 <code>&quot;请求资源 (T_m, P_i)&quot;</code> 后：</p><ul><li>将消息插入自己的请求队列（按 ⇒⇒ 全序排序，即先按时间戳，再按进程 ID）。</li><li>向 Pi 回复一个带时间戳的<strong>确认消息</strong>（确认的目的是让 Pi 知道对方已收到请求，规则 5 会用到）。</li></ul></li><li><p><strong>释放资源</strong><br>当 Pi 使用完资源后：</p><ul><li>从自己的请求队列中删除 <code>(T_m, P_i)</code> 这一条目（即自己的请求）。</li><li>向<strong>所有其他进程</strong>发送 <code>&quot;释放资源 (P_i)&quot;</code> 消息。</li></ul></li><li><p><strong>接收释放消息</strong><br>收到 <code>&quot;释放资源 (P_i)&quot;</code> 后，进程 Pj 从自己的请求队列中删除所有来自 Pi 的请求消息（即删除 <code>(T_m, P_i)</code> 条目）。</p></li><li><p><strong>获得资源</strong><br>进程 Pi <strong>持续检查</strong>以下两个条件是否同时满足：</p><ul><li>(i) 自己的请求队列中，排在<strong>最前面</strong>（按全序 ⇒⇒ 最小）的消息就是它自己发出的 <code>(T_m, P_i)</code>。</li><li>(ii) 它已经收到了<strong>所有其他进程</strong>发来的消息，且这些消息的时间戳<strong>都大于</strong> Tm。<br>这里“所有其他进程发来的消息”是指：每个其他进程 Pj 都已经向 Pi 发送过一条时间戳大于 Tm的消息。实际上，这可以通过规则 2 中的<strong>确认消息</strong>来满足：只要 Pi 收到了来自 Pj 的确认，且该确认的时间戳 &gt; Tm，就说明 Pj 已经知道了 Pi 的请求，并且其队列中任何比 Tm 更早的请求都已经在 Pi 之前处理了。</li></ul><p>当这两个条件都成立时，Pi 就可以安全地获得资源。</p></li></ol><blockquote><p> 算法为什么正确</p></blockquote><ul><li><strong>条件 I（互斥）</strong>：<br>如果两个进程同时认为自己获得了资源，那么它们各自的请求必定都排在各自队列的首位。但由于全序 ⇒⇒ 对所有进程的队列是<strong>一致的</strong>（因为所有进程都收到同样的请求消息，并按相同规则排序），因此全序中只能有一个最小请求。所以只有一个进程能同时满足条件 (i)。</li><li><strong>条件 II（按请求顺序满足）</strong>：<br>假设 Pa 的请求 Ra 在因果顺序上先于 Pb 的请求 Rb（即 Ra→Rb​）。由于逻辑时钟满足 C(Ra)&lt;C(Rb)，因此 Ra 的全序一定小于 Rb，所以 Ra 会在所有队列中排在 Rb 前面，先获得资源。</li><li><strong>条件 III（无饥饿）</strong>：<br>对于任何请求 R，最终所有其他进程都会收到它（消息最终到达），并通过确认告知请求者。当排在 R 之前的所有请求都被释放后，R 就会成为队列首位，并且条件 (ii) 也会满足（因为所有进程都已经发送了大于 R 时间戳的确认），因此 R 最终会被授予。</li></ul><blockquote><p>算法的局限性与意义</p></blockquote><ul><li><strong>通信开销大</strong>：每次请求和释放都需要向<strong>所有</strong>其他进程发送消息，复杂度 O(N)，不适合大规模系统。</li><li><strong>对故障敏感</strong>：如果某个进程崩溃，整个系统可能阻塞（因为没有收到它的确认或释放消息）。算法没有处理故障，需要额外的机制（如超时）。</li><li><strong>历史地位</strong>：该算法是<strong>复制状态机（Replicated State Machine）</strong>思想的雏形。Lamport 后来明确指出，这篇论文的真正贡献是：通过给所有事件一个全局排序，可以将分布式系统建模为一个确定性状态机，每个进程独立重放相同的命令序列，从而实现任意分布式服务。互斥算法只是一个演示的例子。</li></ul><blockquote><p>简单的直观理解</p></blockquote><p>可以把算法想象成“<strong>投票加排队</strong>”：</p><ul><li>每个进程想用资源时，就向所有人广播“我想用，这是我的票号（时间戳）”。</li><li>每个人都把收到的请求按票号顺序放在自己的小本本上。</li><li>只有当一个人的票号排在最前面，并且确认了所有其他人<strong>都已经知道他的票号</strong>（即别人没有更早的未处理请求）时，他才能进门。</li><li>用完资源后，广播“我出来了”，所有人就把他的票号从小本本上划掉，下一个票号的人就能进去。</li></ul><p>这个机制保证了全局唯一的顺序，从而解决了分布式互斥问题。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ol><li><p><a href="https://lamport.azurewebsites.net/pubs/time-clocks.pdf">lamport-Time, Clocks, and the Ordering of Events in a Distributed System</a></p></li><li><p><a href="https://ying-zhang.cn/time/1978-lamport-cn.pdf">译文-Time, Clocks, and the Ordering of Events in a Distributed System</a></p></li><li><p><a href="https://cloud.tencent.cn/developer/article/2051777?from_column=20421&from=20421">从物理时钟到逻辑时钟</a></p></li><li><p><a href="https://mp.weixin.qq.com/s/FZnJLPeTh-bV0amLO5CnoQ">分布式领域最重要的一篇论文，到底讲了什么？</a></p></li></ol>]]></content>
    
    
    <categories>
      
      <category>分布式</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Happens-Before</tag>
      
      <tag>分布式</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>分布式系统的本质是什么</title>
    <link href="/2026/04/10/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E7%9A%84%E6%9C%AC%E8%B4%A8%E6%98%AF%E4%BB%80%E4%B9%88/"/>
    <url>/2026/04/10/%E5%88%86%E5%B8%83%E5%BC%8F%E7%B3%BB%E7%BB%9F%E7%9A%84%E6%9C%AC%E8%B4%A8%E6%98%AF%E4%BB%80%E4%B9%88/</url>
    
    <content type="html"><![CDATA[<h1 id="分布式系统的本质是什么"><a href="#分布式系统的本质是什么" class="headerlink" title="分布式系统的本质是什么"></a>分布式系统的本质是什么</h1><p>在众多计算机技术当中，分布式技术无疑是最璀璨的明珠之一。</p><p>在分布式系统出现之前，机构会尽其所能购买最大的单个大型机，因为在当时的定律是：计算机性能随着成本的平方而增加。</p><p>后来摩尔定律指出：集中式系统的运算能力每隔一段时间才能提升一倍。至于这里的一段时间究竟是18个月还是2年倒不用过于纠结，因为这意味着：<strong>如果系统在这段时间对所承载数据计算力的增长速度比不过计算机性能提升的速度，那么在未来的某一个时间点，集中式系统将无法承载所需的计算量。</strong></p><p>同时在经济因素的影响下，人们发现用廉价机器的集合组成的分布式系统，除了可以获得超过 CPU 发展速度的性能外，花费更低，具有更好的性价比。</p><p>分布式系统的价值，是建立在分布式系统的“分治”和“冗余”之上的。</p><p>有的定义说：分布式系统是计算机程序的集合，这些程序利用跨多个独立计算节点的计算资源来实现共同的目标。</p><blockquote><p>学习过软件架构的话，可能我们还听说过 SOA、ESB、微服务这些名词，分布式系统等价于这些吗？</p></blockquote><p>所谓“分治”，就是 分解 -&gt; 治理 -&gt; 归并，把问题拆解为独立的小问题，只要解决了这些小问题，问题也就解决了。但是<strong>不同分支上的子问题，不能相互依赖，需要各自独立</strong>。因为一旦包含了依赖关系，子问题和父问题之间就失去了可以被“归并”的意义。在软件开发领域，对应的概念称为“<strong>耦合度”和“内聚度”</strong>。</p><p>SOA、ESB、微服务是服务化的模式，服务化就像企业当中将相同岗位的人员划分到同一个部门管理，以此来收敛特定的工作入口，再进行二次分配，以提高人员利用率和劳动成果的复用度。服务化的本质就是“分治”。这时，高内聚、低耦合的思想在拆分过程中起到了一个非常重要的作用，因为这可以尽可能地降低拆分后不同组件间进行协作的复杂度。所以重要的是“怎么拆“，还有如何循序渐进地拆，而这个过程中你究竟是采用了何种服务化模式（比如 SOA、ESB、微服务等）并不是关键。</p><p>拆分为如下的工作模式就是高内聚、低耦合的，因为一个服务只与另一个服务产生了关联，并且仅有一次。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">serviceA  ---&gt;  ServiceB ---&gt; ServiceC<br></code></pre></td></tr></table></figure><p>服务化体现了“分治”的效果，这也是分布式系统的核心思想，因此从“分治”这个本质上来看，服务化的确是分布式系统，但分布式系统不仅仅停留在那些服务化的模式上。</p><p>在我们平时的编程中，会把相关的部分收敛到一个独立的逻辑体中，这个逻辑体可以是函数、类以及命名空间等等。所以，从这个角度来说“分治”的问题其实早就存在我们的工作中，就看我们是否有去关注它了。因此，这并不只是我们在进行服务化时才需要考虑的问题。</p><p><strong>分治可以简化解题的难度，通过高内聚、低耦合的协作关系达到更好“性能与经济比”，来承载更大的流量。</strong></p><blockquote><p>用了某某MQ、某某 RPC 框架就是分布式系统吗？</p></blockquote><p>中间件的作用是什么？</p><p>中间件起到的是标准化的作用。中间件只是承载这些标准化想法的介质、工具，可以起到引导和约束的效果，以此起到大大降低系统复杂度和协作成本的作用，使开发者避免将精力过多地花费在某个子功能下众多差异不大的选项中。</p><p>所以，<strong>虽然分布式系统中会运用中间件，但分布式系统却不仅仅停留在用了什么中间件上</strong>。</p><blockquote><p>那么，分布式系统到底是什么？</p></blockquote><p>其实很多小项目同样可以理解为分布式系统，其中遇到的问题很多同样也存在于成熟的分布式系统中。</p><p>以常见的一个系统而言，需要配置数据库，使用 Redis 做缓存（即使不引入 Redis 下面的结论也成立）。</p><p>在关于如何保持 Redis 和数据库数据一致性上，要考虑多种情况，原因是什么？</p><p>因为 Redis 和数据库是不同的服务，由不同的进程运行。程序的本质是一套代码，而代码只是一段文字，除了提供文字所表述的信息之外，本身无法“动”起来，需要进程来执行文字表述的信息。</p><p>所以，小项目为什么也是分布式系统这个问题就很明白了。因为我们所编写的程序运行时所在的进程，和程序中使用到的数据库所在的进程，和中间件执行操作的进程，并不是同一个。也因此导致了，让这两个进程（系统）完成各自的部分，而后最终完成一件完整的事，变得不再像由单个个体独自完成这件事那么简单。</p><p><strong>所以，我们可以这么理解，涉及多个进程协作才能提供一个完整功能的系统就是“分布式系统”。</strong></p><blockquote><p>分布式系统的冗余能带来什么价值？</p></blockquote><p>“冗余”为系统带来了可以 7*24 小时不间断运作的希望。</p><p>我们可以将冗余的节点部署在一个独立的环境中。这个独立的环境，可能是处于同一个局域网内的不同主机，也可能是在不同的局域网，还可能是在不同的机房。当主体出现问题时，冗余节点可以迅速接替主体的工作，使系统不受影响。</p><p>这种单纯地为了备用而做的冗余，最大的弊端是，如果没有出现故障，那么冗余的这部分资源就白白浪费了，不能发挥任何作用。所以，我们才提出了诸如双主多活、读写分离之类的概念，以提高资源利用率。</p><p>不过也很显然，当故障影响范围大于冗余的容量时，系统依然会挂。所以，既然无法预知故障的发生情况，那么做冗余的时候需要平衡的另一端就是成本。相比更多的冗余，追求更好的性价比更合理一些。</p><blockquote><p>小结</p></blockquote><p>“横看成岭侧成峰，远近高低各不同”。分布式系统的本质就是多个进程协作共同提供完整功能的系统，其价值建立在“分治”和“冗余”上。</p><p>分治和冗余讲究的都是分散化，最终形成一个完整的系统还需要将它们“连接”起来。</p><p>天下没有免费的午餐，获得分布式系统价值的同时，这个“再连接”的过程就是我们相比集中式系统要做的额外工作。</p>]]></content>
    
    
    <categories>
      
      <category>分布式</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式</tag>
      
      <tag>微服务</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2026/04/08/hello-world/"/>
    <url>/2026/04/08/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
